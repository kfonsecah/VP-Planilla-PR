import { TimeWindow } from '../../../services/timeWindowService';

export function classifyByTimeWindow(time: string, windows: TimeWindow[]) {
  // basic stub
  return { type: 'IN', confidence: 'HIGH', windowName: 'A_WINDOW' };
}
