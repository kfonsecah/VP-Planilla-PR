{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "scope-enum": [
      2,
      "always",
      ["api", "ui", "db", "root", "dx", "docs"]
    ],
    "scope-empty": [
      2,
      "never"
    ]
  }
}
