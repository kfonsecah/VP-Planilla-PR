---
phase: 38-tests-unitarios-verificacion-integracion
plan: 02
type: execute
wave: 2
depends_on:
  - 38-01-PLAN.md
files_modified:
  - src/backend/__tests__/unit/services/ClockLogEffectiveService.test.ts
  - src/backend/__tests__/unit/controller/ClockLogsController.test.ts
  - src/frontend/jest.setup.js
  - src/frontend/__tests__/clock-logs/page.test.tsx
autonomous: true
requirements:
  - QUAL-03

must_haves:
  truths:
    - "Backend mock synchronization fixed"
    - "Frontend polyfills configured"
    - "All critical test failures resolved"
  artifacts:
    - path: "src/backend/__tests__"
      provides: "Backend test files with fixed mocks"
    - path: "src/frontend/jest.setup.js"
      provides: "Frontend test polyfills configuration"
  key_links:
    - from: "src/backend/__tests__/unit/services/ClockLogEffectiveService.test.ts"
      to: "ClockLogEffectiveService"
      via: "mock sync fix"
    - from: "src/frontend/jest.setup.js"
      to: "Jest environment"
      via: "polyfill configuration"
---

<objective>
Fix the identified test failures based on categorization from Plan 01.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/templates/summary.md

# Research context - Key Fixes Needed

## Backend Fixes (16 failures -> target 0)
1. **ClockLogEffectiveService.Paginated.test.ts** (3 failures)
   - Fix: Rename mock method from `getClockLogsPaginated` to `getPaginatedEffectiveMarks`
   - Match actual service method signature and return type

2. **ClockLogsController.test.ts** (8 failures)
   - Fix: Update expected status codes from 400 to 500 for invalid dates
   - Controller now throws 500 for server errors

## Frontend Fixes (11 failures -> target 0)
1. **clock-logs/page.test.tsx** (11 failures)
   - Fix 1: Add fetch polyfill to jest.setup.js
   - Fix 2: Add IntersectionObserver mock to jest.setup.js
   - Reference: Research sections "Fixing Fetch Not Defined" and "Fixing IntersectionObserver"

2. **ClockLogDetailModal.test.tsx** (warnings only)
   - Fix: Use waitFor() for async state updates to eliminate act() warnings
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/38-tests-unitarios-verificacion-integracion/38-RESEARCH.md

# Phase 38 Goal
**Goal:** Ejecutar y verificar la suite de pruebas unitarias y de integración para asegurar la estabilidad de las nuevas funcionalidades de la v1.5.

# Output from Plan 01
- test-failures-categorized.md contains full failure breakdown
- Priority: Fix backend mocks first, then frontend polyfills
</context>

<tasks>

<task type="auto">
  <name>Fix backend ClockLogEffectiveService mocks</name>
  <files>src/backend/__tests__/unit/services/ClockLogEffectiveService.test.ts</files>
  <read_first>
    - src/backend/src/services/ClockLogEffectiveService.ts (actual service methods)
    - src/backend/__tests__/unit/services/ClockLogEffectiveService.test.ts (current broken test)
  </read_first>
  <action>
Fix the mock synchronization issue:
1. Read ClockLogEffectiveService.ts to identify actual method names and signatures
2. Update test mocks to match:
   - Rename `getClockLogsPaginated` to `getPaginatedEffectiveMarks`
   - Match return type: { data: [], total: number, page: number, pageSize: number }
3. Ensure all mock methods have correct signatures matching production
4. Run tests to verify: npm test -- --testPathPattern=ClockLogEffectiveService
  </action>
  <verify>
    <automated>cd src/backend && npm test -- --testPathPattern=ClockLogEffectiveService 2>&1 | tail -20</automated>
  </verify>
  <done>ClockLogEffectiveService tests pass (3 failures -> 0)</done>
</task>

<task type="auto">
  <name>Fix backend ClockLogsController error handling expectations</name>
  <files>src/backend/__tests__/unit/controller/ClockLogsController.test.ts</files>
  <read_first>
    - src/backend/src/controllers/ClockLogsController.ts (actual controller)
    - src/backend/__tests__/unit/controller/ClockLogsController.test.ts (current broken test)
  </read_first>
  <action>
Fix controller test error status expectations:
1. Read ClockLogsController to see actual error handling (400 vs 500)
2. Update test expectations:
   - Invalid date params should expect 500 (server error) not 400
   - Update mock to return proper error responses
3. Run tests to verify: npm test -- --testPathPattern=ClockLogsController
  </action>
  <verify>
    <automated>cd src/backend && npm test -- --testPathPattern=ClockLogsController 2>&1 | tail -20</automated>
  </verify>
  <done>ClockLogsController tests pass (8 failures -> 0)</done>
</task>

<task type="auto">
  <name>Add frontend test polyfills (fetch + IntersectionObserver)</name>
  <files>src/frontend/jest.setup.js</files>
  <read_first>
    - src/frontend/jest.setup.js (current setup file)
    - .planning/phases/38-tests-unitarios-verificacion-integracion/38-RESEARCH.md (code examples)
  </read_first>
  <action>
Add missing polyfills to jest.setup.js:
1. Add fetch polyfill:
   ```javascript
   import 'whatwg-fetch';
   ```

2. Add IntersectionObserver mock:
   ```javascript
   class MockIntersectionObserver {
     constructor() {}
     observe() {}
     unobserve() {}
     disconnect() {}
   }
   global.IntersectionObserver = MockIntersectionObserver;
   ```

3. Verify the setup file is imported in jest.config.js
4. Run frontend tests to verify: npm test -- --testPathPattern=clock-logs
  </action>
  <verify>
    <automated>cd src/frontend && npm test -- --testPathPattern=clock-logs 2>&1 | tail -30</automated>
  </verify>
  <done>Frontend clock-logs tests run without fetch/IntersectionObserver errors (11 failures -> 0)</done>
</task>

</tasks>

<verification>
- [ ] All 16 backend test failures resolved
- [ ] All 11 frontend test failures resolved
- [ ] Full test suite passes (506+ tests)
- [ ] No critical test failures remain
- [ ] Warnings reduced (act() warnings addressed)
</verification>

<success_criteria>
All test failures fixed:
- Backend: ClockLogEffectiveService + ClockLogsController tests pass
- Frontend: clock-logs page tests pass with polyfills
- Full suite: npm test in both backend and frontend returns 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/38-tests-unitarios-verificacion-integracion/38-02-SUMMARY.md`
</output>