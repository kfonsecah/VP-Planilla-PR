# Test Failures Categorized Report

**Generated:** 2026-04-17
**Phase:** 38-01 (Test Execution Baseline)
**Context:** Full test suite execution for v1.5 stability verification

---

## Executive Summary

| Metric | Count |
|--------|-------|
| **Total Tests** | 533 |
| **Passing** | 506 |
| **Failing** | 27 (16 backend + 11 frontend) |
| **Suites Failed** | 5 (3 backend + 2 frontend) |

---

## Test Results by Category

### Backend Test Suite

```
Test Suites: 3 failed, 26 passed, 29 total
Tests:       16 failed, 457 passed, 473 total
Time:        10.334 s
```

### Frontend Test Suite

```
Test Suites: 2 failed, 8 passed, 10 total
Tests:       11 failed, 49 passed, 60 total
Time:        3.984 s
```

---

## Failure Categorization by Research Pitfalls

### Pitfall 1: Test Mocks Out of Sync with Production Code

**Root Cause:** Test mocks use method names that no longer exist in production code.

#### Examples:

| File | Test Name | Issue | Fix Required |
|------|----------|-------|--------------|
| `ClockLogEffectiveService.Paginated.test.ts` | getPaginatedEffectiveMarks tests | Mock calls `getClockLogsPaginated` but production method is `getPaginatedEffectiveMarks` | Update mock method name |
| `ClockLogsController.test.ts` | getClockLogsPaginated tests | Controller method `instance.getClockLogsPaginated` is undefined | Update mock to match new service structure |

**Affected Tests:** 8
**Priority:** CRITICAL
**Estimated Fix:** 30 minutes

---

### Pitfall 2: Missing Global Fetch Polyfill in JSDOM

**Root Cause:** Frontend services use `fetch` which is not available in JSDOM test environment.

#### Examples:

| File | Test Name | Error |
|------|----------|-------|
| `clock-logs/page.test.tsx` | All 11 tests | `ReferenceError: fetch is not defined` |

**Affected Tests:** 11
**Priority:** CRITICAL
**Estimated Fix:** 15 minutes (add polyfill to jest.setup.js)

---

### Pitfall 3: IntersectionObserver Not Defined

**Root Cause:** Page component uses IntersectionObserver for infinite scroll, not available in JSDOM.

#### Examples:

| File | Test Name | Error |
|------|----------|-------|
| `clock-logs/page.test.tsx` | All 11 tests | `ReferenceError: IntersectionObserver is not defined` |

**Affected Tests:** 11
**Priority:** CRITICAL
**Estimated Fix:** 15 minutes (add mock to jest.setup.js)

---

### Pitfall 4: Missing act() for Async State Updates

**Root Cause:** Tests pass but with warnings about state updates not wrapped in act().

#### Examples:

| File | Test Name | Warning Count |
|------|----------|-------------|
| `ClockLogDetailModal.test.tsx` | Multiple tests | 15 warnings |

**Affected Tests:** Passing with warnings
**Priority:** COSMETIC
**Estimated Fix:** 45 minutes

---

### Pitfall 5: Controller Error Handling Changed

**Root Cause:** Controller changed to return 500 for invalid dates instead of 400.

#### Examples:

| File | Test Name | Expected | Received |
|------|----------|----------|----------|
| `ClockLogsController.test.ts` | getOrphans invalid dates | 400 | 500 |
| `ClockLogsController.test.ts` | getAnomalies invalid dates | 400 | 500 |

**Affected Tests:** 6
**Priority:** CRITICAL
**Estimated Fix:** 20 minutes (update test expectations)

---

## Prioritization

### Critical (Must Fix)

| Priority | Pitfall | Tests Affected | Reason |
|----------|---------|---------------|--------|
| 1 | Pitfall 2 (fetch polyfill) | 11 | Tests cannot run, blocking all frontend clock-logs tests |
| 2 | Pitfall 3 (IntersectionObserver) | 11 | Tests cannot run, blocking all frontend clock-logs tests |
| 3 | Pitfall 1 (mock sync) | 8 | Core backend tests failing, affects coverage |
| 4 | Pitfall 5 (error codes) | 6 | Tests expecting wrong status codes |

**Total Critical:** 27 tests

### Cosmetic (Nice to Fix)

| Priority | Pitfall | Tests Affected | Reason |
|----------|---------|---------------|--------|
| 5 | Pitfall 4 (act() warnings) | 15 warnings | Tests pass, just noisy warnings |

---

## Fix Recommendations

### Phase 38-02 (Plan 2) Recommended Actions:

1. **Add Fetch Polyfill** → Add to `src/frontend/jest.setup.js`
2. **Add IntersectionObserver Mock** → Add to `src/frontend/jest.setup.js`
3. **Sync Mocks** → Update mock method names in backend tests
4. **Update Error Expectations** → Change 400 → 500 in controller tests

---

## Verification Commands

```bash
# Backend
cd src/backend && npm test

# Frontend  
cd src/frontend && npm test

# Combined
cd src/backend && npm test && cd ../frontend && npm test
```

---

*Generated by GSD executor during Phase 38-01*