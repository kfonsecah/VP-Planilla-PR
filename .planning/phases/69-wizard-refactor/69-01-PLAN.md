---
phase: 69-wizard-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/frontend/src/hooks/usePayrollWizard.ts, src/frontend/src/app/pages/payroll/wizard/steps/Step1Period.tsx, src/frontend/src/app/pages/payroll/wizard/page.tsx]
autonomous: true
requirements: [69-01]
must_haves:
  truths:
    - "Step 1 UI is extracted into a dedicated component"
    - "Period selection state is managed via usePayrollWizard hook"
    - "Wizard navigation from Step 1 to Step 2 remains functional"
  artifacts:
    - path: "src/frontend/src/app/pages/payroll/wizard/steps/Step1Period.tsx"
      provides: "Period selection step component"
---

<objective>
Decompose Step 1 (Period Selection) from the monolithic Wizard page.tsx into a modular component and migrate its state to the usePayrollWizard hook to ensure persistence and clean orchestration.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/frontend/src/app/pages/payroll/wizard/page.tsx
@src/frontend/src/hooks/usePayrollWizard.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Update usePayrollWizard hook for Step 1 state</name>
  <files>src/frontend/src/hooks/usePayrollWizard.ts</files>
  <action>
    Add the following state and handlers to the hook (migrated from page.tsx):
    - dateStart (string)
    - dateEnd (string)
    - selectedMonth (string)
    - selectedQuincena (1 | 2 | null)
    - Handlers: applyQuincenaPreset, applyMonthPreset, setDateStart, setDateEnd.
    Ensure 'reset' also clears these new states.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p src/frontend/tsconfig.json</automated>
  </verify>
  <done>Hook updated with Step 1 state management</done>
</task>

<task type="auto">
  <name>Task 1.2: Create Step1Period component</name>
  <files>src/frontend/src/app/pages/payroll/wizard/steps/Step1Period.tsx</files>
  <action>
    Extract the Step 1 JSX and related sub-components (CalendarIcon, etc.) from page.tsx.
    - Component should consume state/handlers from usePayrollWizard.
    - Use common UI classes (CARD_CLASSES, INPUT_CLASSES, etc.) which can be exported from a shared constants file or temporarily duplicated if not yet shared.
    - Implement validation: "Siguiente" button disabled if !dateStart or !dateEnd.
  </action>
  <verify>
    <automated>test -f src/frontend/src/app/pages/payroll/wizard/steps/Step1Period.tsx</automated>
  </verify>
  <done>Step1Period.tsx created with extracted logic</done>
</task>

<task type="auto">
  <name>Task 1.3: Wire Step1Period into page.tsx</name>
  <files>src/frontend/src/app/pages/payroll/wizard/page.tsx</files>
  <action>
    - Import Step1Period from ./steps/Step1Period.
    - Remove local Step 1 state and handlers.
    - Replace Step 1 rendering block with <Step1Period />.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p src/frontend/tsconfig.json</automated>
  </verify>
  <done>page.tsx refactored to use Step1Period</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client-side state | Period selection state managed in hook; untrusted until validated on server in later steps. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | Tampering | Period Selection | mitigate | Client-side validation for non-empty dates; server-side validation during calculation (Phase 70). |
</threat_model>

<verification>
Check that the Wizard still loads and shows the period selection correctly. Verify presets work.
</verification>

<success_criteria>
- Step 1 is rendered by the new component.
- Presets (quincena/mes) correctly update start/end dates.
- Type checking passes.
</success_criteria>

<output>
After completion, create .planning/phases/69-wizard-refactor/69-01-SUMMARY.md
</output>
