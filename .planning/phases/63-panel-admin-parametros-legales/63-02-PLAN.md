---
wave: 2
depends_on: [63-01]
files_modified:
  - src/frontend/src/app/pages/configuracion/parametros-legales/page.tsx
  - src/frontend/src/components/LegalParamCard.tsx
  - src/frontend/src/services/legalParamService.ts
autonomous: true
---

# Phase 63 Plan 02 - Página base, Acordeones y LegalParamCard

## Contexto
Crear la estructura principal del panel de administración de parámetros legales en `/pages/configuracion/parametros-legales`. Se cargan los datos desde el nuevo endpoint `/active` y se muestran en un layout de acordeones usando el componente `LegalParamCard`.

## Tareas

<task>
<id>63-02-01</id>
<title>Actualizar legalParamService</title>
<requirements>PAY-28</requirements>
<read_first>
- src/frontend/src/services/legalParamService.ts
- src/frontend/src/types/legalParam.ts
</read_first>
<action>
Añadir los endpoints en `LegalParamService` (en `src/frontend/src/services/legalParamService.ts`):
```typescript
  getActiveParams: async (): Promise<{ success: boolean; data?: LegalParam[] }> => {
    return http.get(`/legal-params/active`);
  },
  
  getParamHistory: async (key: string): Promise<{ success: boolean; data?: LegalParam[] }> => {
    return http.get(`/legal-params/history/${key}`);
  },
  
  bulkUpsertMinWages: async (data: { updates: { key: string; value: number }[]; validFrom: string; source_decree: string; confirmationPassword?: string }): Promise<{ success: boolean }> => {
    return http.post(`/legal-params/min-wages/bulk`, data);
  },
```
</action>
<acceptance_criteria>
- `legalParamService.ts` exports `getActiveParams`, `getParamHistory`, and `bulkUpsertMinWages`
- `npx tsc --noEmit` exits 0
</acceptance_criteria>
<verification_command>npx tsc --noEmit</verification_command>
</task>

<task>
<id>63-02-02</id>
<title>Crear Componente LegalParamCard</title>
<requirements>PAY-28</requirements>
<read_first>
- .planning/phases/63-panel-admin-parametros-legales/63-UI-SPEC.md
</read_first>
<action>
Crear `src/frontend/src/components/LegalParamCard.tsx`.
- Propiedades: `param: LegalParam`, `onEdit: (param: LegalParam) => void`, `onHistory: (param: LegalParam) => void`, `readOnly: boolean`.
- Estructura UI: Tarjeta de color `#FCF1D5` / `zinc-900`.
- Mostrar `param.description` como subtítulo.
- Mostrar el `param.key` y un ícono de candado (`LockClosedIcon` de heroicons) si `param.isCritical` es true.
- Mostrar el valor grande formateado (dependiendo de si es un factor, tasa o dinero).
- Mostrar metadatos: `validFrom` (formateado) y `source_decree`.
- Renderizar botones ghost/iconos para "Editar" y "Ver Historial" (a menos que `readOnly` sea true).
- Usar Tailwind 4 (zinc-950) y los colores especificados en UI-SPEC.
</action>
<acceptance_criteria>
- `LegalParamCard.tsx` exported correctly
- Uses Zinc-950 colors and Heroicons
- Displays lock icon for `isCritical`
- `npx tsc --noEmit` exits 0
</acceptance_criteria>
<verification_command>npx tsc --noEmit</verification_command>
</task>

<task>
<id>63-02-03</id>
<title>Crear Página Principal de Parámetros Legales</title>
<requirements>PAY-28</requirements>
<read_first>
- src/frontend/src/components/LegalParamCard.tsx
- src/frontend/src/services/legalParamService.ts
</read_first>
<action>
Crear la página `src/frontend/src/app/pages/configuracion/parametros-legales/page.tsx`:
- Es un `"use client"`.
- Usar un hook que traiga los datos on mount llamando a `LegalParamService.getActiveParams()`.
- Usar el rol del usuario (del context o localStorage) para bloquear no-admins y hacer redirect a `/pages/main`, o poner la página en `readOnly` si el rol es solo `payroll_manager`. (Si el auth store no existe, asumir validación simple y redirect).
- Agrupar los parámetros por `category` (Jornada Laboral, Horas Extraordinarias, CCSS, Salarios Mínimos, Configuración).
- Mapear cada categoría a una sección/acordeón.
- Dentro de cada sección, renderizar un grid de `LegalParamCard`.
- Manejar estados de loading y error.
</action>
<acceptance_criteria>
- `page.tsx` created and loads data via `getActiveParams`
- Parameters are grouped by category and displayed via `LegalParamCard`
- `npx tsc --noEmit` exits 0
</acceptance_criteria>
<verification_command>npx tsc --noEmit</verification_command>
</task>

<threat_model>
- Validar rol de administrador en frontend para evitar exposición indebida, aunque la API ya lo rechace.
</threat_model>

## Verification
- Ejecutar `npx tsc --noEmit` y `npx next lint`.
