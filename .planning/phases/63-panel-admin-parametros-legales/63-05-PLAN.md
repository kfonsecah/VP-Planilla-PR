---
wave: 5
depends_on: [63-04]
files_modified:
  - src/frontend/src/components/FeatureFlagToggle.tsx
  - src/frontend/src/app/pages/configuracion/parametros-legales/page.tsx
  - src/frontend/src/components/layout/Sidebar.tsx
autonomous: true
---

# Phase 63 Plan 05 - Feature Flags y Navegación

## Contexto
Implementar la visualización y edición específica para parámetros de tipo booleano o toggles de sistema (ej. `MIN_WAGE_CHECK_ENABLED`). Además, se debe agregar el enlace de navegación para acceder a la página de parámetros legales desde el Sidebar.

## Tareas

<task>
<id>63-05-01</id>
<title>Crear Componente FeatureFlagToggle</title>
<requirements>PAY-28</requirements>
<read_first>
- src/frontend/src/services/legalParamService.ts
- src/frontend/src/components/PasswordConfirmModal.tsx
</read_first>
<action>
Crear `src/frontend/src/components/FeatureFlagToggle.tsx`.
- Props: `param: LegalParam`, `onChange: () => void`, `readOnly?: boolean`.
- Es una tarjeta con el layout similar a `LegalParamCard` pero en lugar de mostrar un valor numérico, muestra un control Toggle (Switch animado).
- Convertir `param.value` a booleano (si `'1'`, `1`, o `true` => on; de lo contrario => off).
- Si está en "OFF", mostrar un badge rojo con el texto "DESACTIVADO".
- Al interactuar con el toggle, si `isCritical` es true, disparar flujo de `usePasswordConfirmation`. Al confirmar, llamar a `patchParam` (enviando "1" o "0").
</action>
<acceptance_criteria>
- `FeatureFlagToggle.tsx` renders a toggle switch.
- Triggers password confirmation if parameter is critical.
- Displays "DESACTIVADO" badge in red when value is off.
- `npx tsc --noEmit` exits 0
</acceptance_criteria>
<verification_command>npx tsc --noEmit</verification_command>
</task>

<task>
<id>63-05-02</id>
<title>Integrar FeatureFlagToggle en la Categoría Configuración</title>
<requirements>PAY-28</requirements>
<read_first>
- src/frontend/src/app/pages/configuracion/parametros-legales/page.tsx
</read_first>
<action>
En `page.tsx`:
- En la sección donde se renderiza la categoría `FEATURE_FLAG` (Configuración), renderizar `<FeatureFlagToggle />` en lugar de `<LegalParamCard />`.
- Pasar el `onChange` (refetch) al componente.
</action>
<acceptance_criteria>
- `FEATURE_FLAG` category renders the `FeatureFlagToggle` component.
- `npx tsc --noEmit` exits 0
</acceptance_criteria>
<verification_command>npx tsc --noEmit</verification_command>
</task>

<task>
<id>63-05-03</id>
<title>Añadir Enlace en Menú Sidebar</title>
<requirements>PAY-28</requirements>
<read_first>
- src/frontend/src/components/layout/Sidebar.tsx
</read_first>
<action>
En `src/frontend/src/components/layout/Sidebar.tsx` (o archivo similar de navegación principal si está en otra ruta):
- Añadir un enlace a `/pages/configuracion/parametros-legales`.
- Título: "Parámetros Legales".
- Restringir la visibilidad de este enlace para que solo los roles con permiso adecuado (ej. `admin` o `payroll_manager`) puedan verlo. Asumir `role === 'admin' || role === 'payroll_manager'` según la lógica existente.
</action>
<acceptance_criteria>
- Navigation link for "Parámetros Legales" added.
- Only visible to allowed roles.
- `npx tsc --noEmit` exits 0
</acceptance_criteria>
<verification_command>npx tsc --noEmit</verification_command>
</task>

## Verification
- `npx tsc --noEmit` y `npx next lint`.
