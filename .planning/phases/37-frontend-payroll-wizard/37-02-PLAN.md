---
phase: 37-frontend-payroll-wizard
plan: "02"
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - src/frontend/src/components/PayrollWizardStep2.tsx
  - src/frontend/src/components/PayrollResults.tsx (reuse)
autonomous: true
requirements:
  - PLANILLA-03

must_haves:
  truths:
    - User can review calculation summary by employee
    - Employee rows are expandable to show details
    - Warning displayed for employees with inconsistencies
  artifacts:
    - path: src/frontend/src/components/PayrollWizardStep2.tsx
      provides: Step 2 wrapper with PayrollResults integration
      min_lines: 60
  key_links:
    - from: src/frontend/src/components/PayrollWizardStep2.tsx
      to: src/frontend/src/components/PayrollResults.tsx
      via: Component import
      pattern: import.*PayrollResults
    - from: src/frontend/src/components/PayrollWizardStep2.tsx
      to: src/frontend/src/hooks/usePayrollWizard.ts
      via: usePayrollWizard state
      pattern: calculationData
---

<objective>
**Phase 37 Plan 02:** Step 2 — Review Calculation — wrap existing PayrollResults component with wizard integration.

Purpose: Enable users to review the calculation breakdown by employee before approval. Reuses existing PayrollResults.

Output:
- PayrollWizardStep2.tsx (Step 2 wrapper)
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-frontend-payroll-wizard/37-RESEARCH.md
@.planning/phases/37-frontend-payroll-wizard/37-UI-SPEC.md

<interfaces>
<!-- From 37-RESEARCH.md Pattern 3 -->
```typescript
interface WizardStep2Props {
  data: CalculationResult;
  onBack: () => void;
  onNext: () => void;
  onConfirm: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PayrollWizardStep2 wrapper</name>
  <files>src/frontend/src/components/PayrollWizardStep2.tsx</files>
  <read_first>src/frontend/src/components/PayrollResults.tsx (existing component to reuse)</read_first>
  <action>
Create PayrollWizardStep2.tsx with:
- Props: data (CalculationResult), onBack, onNext, onConfirm
- Navigation: "Cambiar período" back button → Step 1, primary CTA "Revisar y continuar" → Step 3
- Warning banner (yellow bg) if data.employees has inconsistencies
- Wraps existing PayrollResults component
- Passes data.results to PayrollResults
- Passes onConfirm as the trigger for Step 3

Per UI-SPEC.md: "Warning banner (yellow) if any employee has inconsistencies"
  </action>
  <verify>
grep -l "PayrollWizardStep2" src/frontend/src/components/PayrollWizardStep2.tsx && \
grep -l "PayrollResults" src/frontend/src/components/PayrollWizardStep2.tsx
  </verify>
  <acceptance_criteria>
- File exists at src/frontend/src/components/PayrollWizardStep2.tsx
- Imports and uses PayrollResults component
- Shows "Cambiar período" back button
- Shows "Revisar y continuar" forward CTA
- Yellow warning banner when employee inconsistencies exist
  </acceptance_criteria>
  <done>PayrollWizardStep2 component with warning display</done>
</task>

</tasks>

<verification>
Check that PayrollWizardStep2 imports PayrollResults and handles navigation.
</verification>

<success_criteria>
- [ ] PayrollWizardStep2.tsx wraps existing PayrollResults
- [ ] Displays inconsistency warnings per employee
- [ ] Back navigation returns to Step 1
- [ ] Forward navigation to Step 3
</success_criteria>

<output>
After completion, create `.planning/phases/37-frontend-payroll-wizard/37-02-SUMMARY.md`
</output>