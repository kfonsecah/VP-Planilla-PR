---
phase: 40-fix-15-remaining-test-failures-from-phase-38
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/__tests__/unit/services/ClockLogEffectiveService.Paginated.test.ts
  - src/backend/__tests__/unit/NomineeService.test.ts
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "All backend tests pass with 0 failures"
    - "TypeScript error in ClockLogEffectiveService.Paginated.test.ts is fixed"
    - "NomineeService.test.ts 5 pre-existing failures are resolved"
  artifacts:
    - path: "src/backend/__tests__/unit/services/ClockLogEffectiveService.Paginated.test.ts"
      provides: "Test compilation and execution"
    - path: "src/backend/__tests__/unit/NomineeService.test.ts"
      provides: "Test pass for NomineeService"
  key_links:
    - from: "src/backend/__tests__/unit/services/ClockLogEffectiveService.Paginated.test.ts"
      to: "src/backend/src/service/ClockLogEffectiveService.ts"
      via: "mock synchronization"
    - from: "src/backend/__tests__/unit/NomineeService.test.ts"
      to: "src/backend/src/service/NomineeService.ts"
      via: "service method calls"
---

<objective>
Fix the 6 backend test failures (1 TypeScript error + 5 pre-existing in NomineeService).
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/40-fix-15-remaining-test-failures-from-phase-38/40-CONTEXT.md
@.planning/phases/38-tests-unitarios-verificacion-integracion/38-VERIFICATION.md

**Gap analysis from Phase 38:**
1. ClockLogEffectiveService.Paginated.test.ts: TypeScript error - mock uses `employee_is_active` field that doesn't exist in Prisma type
2. NomineeService.test.ts: 5 pre-existing failures - tests reference outdated service methods or assertions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix TypeScript error in ClockLogEffectiveService.Paginated.test.ts</name>
  <files>src/backend/__tests__/unit/services/ClockLogEffectiveService.Paginated.test.ts</files>
  <action>
Read the test file and identify where `employee_is_active` is used in the mock. Update the mock to use only fields that exist in the Prisma type (check src/backend/node_modules/.prisma/client or schema.prisma for available fields). If the field is not needed, remove it from the mock. If it's a real field needed for tests, verify it exists in the schema.
  </action>
  <verify>
    <automated>cd src/backend && npx tsc --noEmit __tests__/unit/services/ClockLogEffectiveService.Paginated.test.ts 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compilation passes for the test file</done>
</task>

<task type="auto">
  <name>Task 2: Fix 5 pre-existing failures in NomineeService.test.ts</name>
  <files>src/backend/__tests__/unit/NomineeService.test.ts</files>
  <action>
Run the test file to see the specific failures:
  cd src/backend && npm test -- --testPathPattern="NomineeService.test.ts" 2>&1
  
Analyze each failure and fix:
- Check if service methods being tested still exist with the same signatures
- Update test assertions to match current service behavior
- Update mock setups to match current implementation

Focus on fixing the actual test code, not skipping tests.
  </action>
  <verify>
    <automated>cd src/backend && npm test -- --testPathPattern="NomineeService.test.ts" 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 previously failing tests in NomineeService.test.ts now pass</done>
</task>

<task type="auto">
  <name>Task 3: Verify all backend tests pass</name>
  <files>src/backend</files>
  <action>
Run full backend test suite to confirm all tests pass:
  cd src/backend && npm test
  </action>
  <verify>
    <automated>cd src/backend && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>Backend test suite shows 0 failures</done>
</task>

</tasks>

<verification>
Run: cd src/backend && npm test
Expected: All tests pass, 0 failures
</verification>

<success_criteria>
- [ ] ClockLogEffectiveService.Paginated.test.ts compiles without TypeScript errors
- [ ] NomineeService.test.ts passes all tests (was 5 failures, now 0)
- [ ] Full backend test suite passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/40-fix-15-remaining-test-failures-from-phase-38/40-01-SUMMARY.md`
</output>