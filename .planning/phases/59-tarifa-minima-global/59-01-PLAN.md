---
phase: 59
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/prisma/seed.ts
autonomous: true
requirements:
  - PAY-24
must_haves:
  truths:
    - "System contains reference minimum wage parameters for 2024 and 2025."
  artifacts:
    - path: "src/backend/prisma/seed.ts"
      provides: "Seed data for GLOBAL_MIN_WAGE_RATE"
      contains: "GLOBAL_MIN_WAGE_RATE"
  key_links:
    - from: "src/backend/prisma/seed.ts"
      to: "vpg_legal_params"
      via: "prisma.vpgLegalParam.upsert"
      pattern: "key: 'GLOBAL_MIN_WAGE_RATE'"
---

<objective>
Update the database seed script to include the `GLOBAL_MIN_WAGE_RATE` legal parameter.

Purpose: Simplifies administration by providing a single global reference rate for payroll warnings based on the MTSS decree.
Output: Modified `seed.ts` containing 2024 and 2025 values for the minimum wage rate.
</objective>

<context>
@.planning/phases/59-tarifa-minima-global/59-CONTEXT.md
@.planning/phases/59-tarifa-minima-global/59-RESEARCH.md
@.planning/phases/59-tarifa-minima-global/59-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GLOBAL_MIN_WAGE_RATE to seed data</name>
  <files>src/backend/prisma/seed.ts</files>
  <read_first>
    - src/backend/prisma/seed.ts
  </read_first>
  <action>
    Follow the "Seeding pattern" and "Upsert loop pattern" from 59-PATTERNS.md for `src/backend/prisma/seed.ts`.
    
    Add the `GLOBAL_MIN_WAGE_RATE` parameter to the `legalParams` array (or equivalent structure) in `src/backend/prisma/seed.ts`.
    Include two records based on the MTSS decree (as outlined in Phase 59 RESEARCH):
    
    1. 2024 Rate:
       key: 'GLOBAL_MIN_WAGE_RATE', value: 1494.20, description: 'Tarifa mínima por hora (Referencia MTSS 2024)', category: 'MIN_WAGE', validFrom: new Date('2024-01-01T00:00:00.000Z'), isCritical: true
       
    2. 2025 Rate:
       key: 'GLOBAL_MIN_WAGE_RATE', value: 1529.62, description: 'Tarifa mínima por hora (Referencia MTSS 2025)', category: 'MIN_WAGE', validFrom: new Date('2025-01-01T00:00:00.000Z'), isCritical: true
       
    The existing upsert loop will automatically handle insertion. Ensure you conform exactly to the analog file structure.
  </action>
  <acceptance_criteria>
    - src/backend/prisma/seed.ts contains 'GLOBAL_MIN_WAGE_RATE'
    - src/backend/prisma/seed.ts contains 1494.20
    - src/backend/prisma/seed.ts contains 1529.62
    - Command `cd src/backend && npx tsc --noEmit && npx prisma db seed` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd src/backend && npx tsc --noEmit && npx prisma db seed</automated>
  </verify>
  <done>Seed file type-checks successfully, contains the new rates following established analog patterns, and is verified by running the seed command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| System → DB | System seeds default trusted legal parameters into the DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01-01 | Tampering | seed.ts | accept | Only administrators execute seeds; parameter is locked in code. |
</threat_model>

<verification>
- `src/backend/prisma/seed.ts` contains the new parameter logic.
- Project type-checks correctly.
</verification>

<success_criteria>
- The parameter `GLOBAL_MIN_WAGE_RATE` is added to the seed script for 2024 and 2025.
</success_criteria>

<output>
After completion, create `.planning/phases/59-tarifa-minima-global/59-01-SUMMARY.md`
</output>