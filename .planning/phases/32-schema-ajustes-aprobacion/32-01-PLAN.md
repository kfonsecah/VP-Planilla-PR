---
phase: 32
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/backend/prisma/schema.prisma]
autonomous: true
requirements: [MARCAS-02, MARCAS-03, MARCAS-04, PLANILLA-04, PLANILLA-06]
must_haves:
  truths:
    - "The database schema includes vpg_clock_log_adjustments table"
    - "The database schema includes vpg_payroll_recalculations table"
    - "vpg_payrolls.payrolls_status uses a type-safe Enum"
    - "vpg_clock_logs.clock_logs_source includes 'device' and 'manual' options"
  artifacts:
    - path: "src/backend/prisma/schema.prisma"
      provides: "Updated database model"
    - path: "src/backend/prisma/migrations/"
      provides: "SQL migration files"
  key_links:
    - from: "vpg_clock_log_adjustments"
      to: "vpg_clock_logs"
      via: "adjustment_clock_log_id"
    - from: "vpg_payroll_recalculations"
      to: "vpg_payrolls"
      via: "recalc_payroll_id"
---

<objective>
Define and apply the database schema changes required for the Adjustment Layer and Payroll State Machine.

Purpose: Provide the structural foundation for non-destructive clock log edits and payroll lifecycle management.
Output: Updated Prisma schema and applied database migration.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-schema-ajustes-aprobacion/32-RESEARCH.md
@src/backend/prisma/schema.prisma

<interfaces>
From src/backend/prisma/schema.prisma:
```prisma
enum ClockLogType {
  IN
  OUT
}

enum ClockLogStatus {
  pending
  valid
  anomaly
  corrected
  orphan
}

enum ClockLogSource {
  java_import
  excel_import
  manual
}

model vpg_clock_logs {
  clock_logs_id                Int                       @id @default(autoincrement())
  clock_logs_employee_id       Int
  clock_logs_timestamp         DateTime                  @db.Timestamp(6)
  clock_logs_log_type          ClockLogType
  clock_logs_status            ClockLogStatus            @default(pending)
  clock_logs_source            ClockLogSource            @default(manual)
  ...
}

model vpg_payrolls {
  payrolls_id              Int                       @id @default(autoincrement())
  payrolls_status          String                    @db.VarChar(20)
  ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update schema.prisma with new models and enums</name>
  <files>src/backend/prisma/schema.prisma</files>
  <action>
    Modify the Prisma schema to include:
    1. Update `ClockLogSource` enum: Add `device` (keep existing ones).
    2. Add `ClockLogAdjustmentType` enum: `CORRECTION`, `ANULATION`.
    3. Add `ClockLogAdjustmentStatus` enum: `ACTIVE`, `INACTIVE`.
    4. Add `PayrollStatus` enum: `BORRADOR`, `APROBADA`, `PAGADA`.
    5. Add `vpg_clock_log_adjustments` model per Research recommendation:
       - `adjustment_id` (PK, autoinc)
       - `adjustment_clock_log_id` (FK to vpg_clock_logs)
       - `adjustment_type` (Enum)
       - `adjustment_adjusted_timestamp` (DateTime? @db.Timestamp(6))
       - `adjustment_adjusted_log_type` (ClockLogType? - reused from existing enum)
       - `adjustment_reason` (String @db.Text)
       - `adjustment_status` (Enum @default(ACTIVE))
       - `adjustment_created_at` (DateTime @default(now()))
       - `adjustment_created_by` (FK to vpg_users)
    6. Add `vpg_payroll_recalculations` model per Research recommendation:
       - `recalc_id` (PK, autoinc)
       - `recalc_payroll_id` (FK to vpg_payrolls)
       - `recalc_reason` (String @db.Text)
       - `recalc_timestamp` (DateTime @default(now()))
       - `recalc_created_by` (FK to vpg_users)
       - `recalc_data_snapshot` (Json)
    7. Update `vpg_payrolls`: Change `payrolls_status` from `String` to `PayrollStatus` enum.
    
    Ensure naming follows the `{table}_{column}` pattern where applicable (e.g., `adjustment_id`, `recalc_id`).
  </action>
  <verify>
    <automated>npx prisma validate --schema=src/backend/prisma/schema.prisma</automated>
  </verify>
  <done>schema.prisma updated and passes validation</done>
</task>

<task type="auto">
  <name>Task 2: Apply migration and generate client</name>
  <files>src/backend/prisma/migrations/</files>
  <action>
    Run the migration process:
    1. Run `npx prisma migrate dev --name phase_32_schema_refinement --schema=src/backend/prisma/schema.prisma`.
    2. If Prisma warns about data loss (converting String to Enum), provide a migration SQL that maps current values:
       - 'PENDIENTE' -> 'BORRADOR'
       - 'APROBADA' -> 'APROBADA'
       - 'PAGADA' -> 'PAGADA'
       (Note: If the executor is running in a clean environment, it might just create the tables).
    3. Ensure `npx prisma generate` runs successfully.
  </action>
  <verify>
    <automated>npx prisma generate --schema=src/backend/prisma/schema.prisma</automated>
  </verify>
  <done>Database migrated and Prisma Client generated with new types</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database Layer | Enforces referential integrity for adjustments and recalculations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | `vpg_clock_log_adjustments` | mitigate | Use Foreign Key constraints to ensure adjustments always point to valid logs and users. |
| T-32-02 | Tampering | `vpg_payrolls` | mitigate | Use Enum type for `payrolls_status` to prevent invalid states at the DB level. |
</threat_model>

<verification>
Check generated Prisma Client for existence of:
- `vpg_clock_log_adjustments` model
- `vpg_payroll_recalculations` model
- `PayrollStatus` enum with `BORRADOR`, `APROBADA`, `PAGADA`
</verification>

<success_criteria>
- Prisma schema is valid.
- Migration is successfully applied to the database.
- Prisma Client includes the new models and enums.
</success_criteria>

<output>
After completion, create `.planning/phases/32-schema-ajustes-aprobacion/32-01-SUMMARY.md`
</output>
