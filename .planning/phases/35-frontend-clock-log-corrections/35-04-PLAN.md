---
phase: 35-frontend-clock-log-corrections
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/src/components/AuditTimeline.tsx
  - src/frontend/src/components/DailyRow.tsx
autonomous: true
requirements:
  - MARCAS-05
context_references:
  - .planning/phases/35-frontend-clock-log-corrections/35-CONTEXT.md
  - .planning/phases/35-frontend-clock-log-corrections/35-UI-SPEC.md

must_haves:
  truths:
    - User can see audit indicator badge on day rows
    - User can expand to see full audit timeline per mark
    - Timeline shows: usuario, fecha, cambio, justificación
  artifacts:
    - path: src/frontend/src/components/AuditTimeline.tsx
      provides: Expandable timeline component showing change history
      min_lines: 80
  key_links:
    - from: DailyRow
      to: auditLogsService.getLogsForMark
      via: onClick handler for audit indicator
      pattern: onClick.*getLogsForMark
    - from: AuditTimeline
      to: auditLogsService
      via: useEffect fetching
      pattern: useEffect.*getLogsForMark
---

<objective>
Create Audit Timeline component with badge indicator and expandable history per day.
</objective>

<interfaces>
From auditLogsService (existing):
```typescript
interface AuditLog {
  id: string;
  entityType: 'CLOCK_LOG';
  entityId: string;
  action: 'ADD' | 'EDIT' | 'VOID';
  previousValue?: string;
  newValue?: string;
  justification: string;
  performedBy: string;
  performedAt: string;
}

function getLogsForMark(clockLogId: string): Promise<AuditLog[]>
```
</interfaces>

<read_first>
- src/frontend/src/services/auditLogsService.ts — Existing audit fetching
- src/frontend/src/components/DailyRow.tsx — From Phase 34, shows day card
- src/frontend/src/components/ClockLogDetailModal.tsx — Has audit section pattern
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Create AuditTimeline Component</name>
  <files>src/frontend/src/components/AuditTimeline.tsx</files>
  <action>
Create expandable audit timeline component:

**Props:**
```typescript
interface AuditTimelineProps {
  clockLogId: string; // Fetch logs for this mark
  compact?: boolean; // Show summary only vs full list
}
```

**Behavior:**
- On mount: fetch logs via `auditLogsService.getLogsForMark(clockLogId)`
- Loading state while fetching
- Show entries in chronological order (oldest first)

**Display (full mode):**
- Each entry shows:
  - Icon: + (green) for ADD, pencil (blue) for EDIT, trash (red) for VOID
  - "Usuario: {performedBy}"
  - "Fecha: {performedAt formatted}"
  - "Cambio: {action}" + previous → new if applicable
  - "Justificación: {justification}"
- Use timeline connecting line between entries

**Display (compact mode - tooltip):**
- Badge with count: "(3 cambios)"
- Tooltip on hover: summary of recent changes

**Styling:**
- Zinc-950 palette
- Green for ADD, blue for EDIT, red for VOID icons
- Animation: expand/collapse via motion.div
  </action>
  <verify>
    <automated>test -f src/frontend/src/components/AuditTimeline.tsx && echo "File exists"</automated>
  </verify>
  <done>Component fetches and displays audit logs for a mark with expand/collapse</done>
</task>

<task type="auto">
  <name>Task 2: Add AuditBadge to DailyRow</name>
  <files>src/frontend/src/components/DailyRow.tsx</files>
  <action>
Integrate audit indicator into DailyRow component from Phase 34:

**Add to existing DailyRow:**
1. Fetch audit count on mount:
   - Call `auditLogsService.getLogsForMark(entry.id)`
   - Store count in local state

2. Show audit indicator (D-06):
   - If count > 0: Show badge "(N cambios)" with blue color
   - Badge positioned next to day/date

3. Add expandable section:
   - Below day info, before close button
   - Renders `<AuditTimeline clockLogId={entry.id} />`
   - Click badge toggles expand

**Note:** Only fetch once per entry ID, cache results to avoid duplicate calls.

**Styling:**
- Blue badge matching correction indicator from UI-SPEC
- Expand below day row content
  </action>
  <verify>
    <automated>grep -l "AuditTimeline\|auditLogs" src/frontend/src/components/DailyRow.tsx</automated>
  </verify>
  <done>DailyRow shows audit badge when changes exist, expands to show timeline on click</done>
</task>

</tasks>

<verification>
- [ ] AuditTimeline fetches logs via auditLogsService
- [ ] Shows icon + user + date + change + justification for each entry
- [ ] DailyRow shows audit badge when changes exist
- [ ] Badge click expands timeline within day card
</verification>

<success_criteria>
User can see audit history badge on day rows, expand to see full timeline with usuario, fecha, cambio, justificación.
</success_criteria>

<output>
After completion, create `.planning/phases/35-frontend-clock-log-corrections/35-04-SUMMARY.md`
</output>