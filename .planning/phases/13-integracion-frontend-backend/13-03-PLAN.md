---
phase: 13-integracion-frontend-backend
plan: 03
type: execute
wave: 2
depends_on: ["13-02"]
files_modified:
  - src/frontend/src/components/ui/Table.tsx
  - src/frontend/src/components/ui/FormModal.tsx
  - src/frontend/src/app/pages/employee/list/page.tsx
  - src/frontend/src/app/pages/payroll/page.tsx
  - src/frontend/src/app/pages/payroll/create/page.tsx
  - src/frontend/src/app/pages/payroll/results/page.tsx
  - src/frontend/src/app/pages/deductions/page.tsx
  - src/frontend/src/app/pages/positions/page.tsx
  - src/frontend/src/app/pages/branches/page.tsx
  - src/frontend/src/app/pages/clocklogs/page.tsx
  - src/frontend/src/app/pages/bonuses/page.tsx
  - src/frontend/src/app/pages/vacations/page.tsx
  - src/frontend/src/app/pages/attendance/page.tsx
  - src/frontend/src/app/pages/labor-events/page.tsx
  - src/frontend/src/app/pages/payroll-types/page.tsx
  - src/frontend/src/app/pages/employee-deductions/page.tsx
  - src/frontend/src/app/pages/audit-logs/page.tsx
  - src/frontend/src/app/pages/users/page.tsx
  - src/frontend/src/app/pages/reports/page.tsx
autonomous: true
requirements: [INTEG-03]

must_haves:
  truths:
    - "Todos los hooks de datos muestran un spinner o skeleton mientras cargan"
    - "Todos los hooks muestran un mensaje de error descriptivo si la petición falla"
    - "Ninguna vista queda en blanco indefinidamente — siempre muestra loading, error, datos, o estado vacío"
  artifacts:
    - path: "src/frontend/src/components/ui/Table.tsx"
      provides: "Loading skeleton and error state rendering"
      contains: "isLoading skeleton rows"
    - path: "src/frontend/src/components/ui/FormModal.tsx"
      provides: "Form loading state and error display"
      contains: "isLoading spinner"
  key_links:
    - from: "Page components"
      to: "ui/Table.tsx"
      via: "isLoading and error props passed from hooks"
      pattern: "isLoading.*Table|error.*Table"
    - from: "Hooks (usePositions, useDeductions, etc.)"
      to: "Page components"
      via: "hook return { data, isLoading, error } consumed by pages"
      pattern: "const.*=.*use(Deductions|Positions|Branches)"
---

<objective>
Ensure all data hooks expose isLoading and error with UI that reflects them (spinners, empty states, error messages)

Purpose: No view should ever be blank — every data-driven page must show loading state, error state, empty state, or data. Users always know what's happening.
Output: Updated Table component with loading skeletons, updated FormModal with loading states, updated pages that wire hook loading/error states to UI
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 12 summary (UI components already dark-mode styled)
@.planning/phases/12-tablas-formularios-modales/12-SUMMARY.md

# UI components
@src/frontend/src/components/ui/Table.tsx
@src/frontend/src/components/ui/FormModal.tsx
@src/frontend/src/components/ui/Modal.tsx

# Hooks that expose isLoading/error (already have the shape, need UI wiring)
@src/frontend/src/hooks/usePositions.ts
@src/frontend/src/hooks/useDeductions.ts
@src/frontend/src/hooks/useBranches.ts
@src/frontend/src/hooks/useBonuses.ts
@src/frontend/src/hooks/useVacations.ts
@src/frontend/src/hooks/usePayrollTypes.ts
@src/frontend/src/hooks/usePayroll.ts
@src/frontend/src/hooks/useNominee.ts
@src/frontend/src/hooks/useLaborEvents.ts
@src/frontend/src/hooks/useEmployeeDeductions.ts
@src/frontend/src/hooks/usePayrollEmployees.ts
@src/frontend/src/hooks/useAuditLogs.ts
@src/frontend/src/hooks/useEmployeeList.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add loading skeletons and error states to Table component</name>
  <files>src/frontend/src/components/ui/Table.tsx</files>
  <action>
    Enhance the Table component to accept and render loading and error states:

    1. Add optional props: `isLoading?: boolean`, `error?: string | null`, `emptyMessage?: string`
    2. When `isLoading` is true: render skeleton rows (3-5 rows with animated shimmer effect using framer-motion or CSS animation). Each skeleton row should have placeholder cells with `bg-zinc-700 dark:bg-zinc-800` and `animate-pulse` class. The number of skeleton rows should match a `skeletonRows` prop (default 5).
    3. When `error` is non-null: render an error banner within the table area showing the error message with a red/dark-red background and a refresh icon. Include a "Reintentar" button that calls an optional `onRetry` callback.
    4. When data is empty and not loading and no error: render the existing empty state (already has one from Phase 12) with the provided `emptyMessage` or a default "No hay datos disponibles"
    5. Preserve all existing Table props and behavior — this is additive only

    The skeleton should match the dark mode styling from Phase 12 (zinc color scale). Use CSS `animate-pulse` from Tailwind — do not add new animation libraries.
  </action>
  <verify>
    <automated>npx tsc --noEmit passes in src/frontend/</automated>
  </verify>
  <done>Table component accepts isLoading, error, emptyMessage, skeletonRows, and onRetry props; renders skeleton rows when loading, error banner when error, empty state when no data, and normal data otherwise</done>
</task>

<task type="auto">
  <name>Task 2: Wire loading/error states to all data pages</name>
  <files>
    src/frontend/src/app/pages/employee/list/page.tsx
    src/frontend/src/app/pages/payroll/page.tsx
    src/frontend/src/app/pages/payroll/create/page.tsx
    src/frontend/src/app/pages/payroll/results/page.tsx
    src/frontend/src/app/pages/deductions/page.tsx
    src/frontend/src/app/pages/positions/page.tsx
    src/frontend/src/app/pages/branches/page.tsx
    src/frontend/src/app/pages/clocklogs/page.tsx
    src/frontend/src/app/pages/bonuses/page.tsx
    src/frontend/src/app/pages/vacations/page.tsx
    src/frontend/src/app/pages/attendance/page.tsx
    src/frontend/src/app/pages/labor-events/page.tsx
    src/frontend/src/app/pages/payroll-types/page.tsx
    src/frontend/src/app/pages/employee-deductions/page.tsx
    src/frontend/src/app/pages/audit-logs/page.tsx
    src/frontend/src/app/pages/users/page.tsx
    src/frontend/src/app/pages/reports/page.tsx
  </files>
  <action>
    Update all data pages to pass isLoading and error from their hooks to the Table component and other UI components:

    For each page, the pattern is:
    1. The page already destructures { data, isLoading, error } from its hook
    2. Pass `isLoading={isLoading}` to the Table component
    3. Pass `error={error}` to the Table component
    4. Pass `onRetry={() => refetch?.()}` if the hook exposes a refetch function
    5. For pages with forms/modals: pass `isLoading` to FormModal so it shows a submit spinner during API calls
    6. For pages with multiple data sources (e.g., employee list + positions dropdown): pass loading state for the primary data table

    Specific pages to update:
    - **employee/list**: Already uses useEmployeeList which has isLoadingEmployee for edit loading. Add isLoading for the main list to Table.
    - **payroll**: usePayroll — pass isLoading/error to payroll results table
    - **payroll/create**: usePayroll + usePositions — show loading while fetching payroll types and positions
    - **payroll/results**: useNominee — show loading skeleton while calculating payroll
    - **deductions**: useDeductions — pass isLoading/error to Table
    - **positions**: usePositions — pass isLoading/error to Table
    - **branches**: useBranches — pass isLoading/error to Table
    - **clocklogs**: useClockLogs (if exists) or direct service call — add loading/error handling
    - **bonuses**: useBonuses — pass isLoading/error to Table
    - **vacations**: useVacations — pass isLoading/error to Table
    - **attendance**: useEmployeeList or attendance-specific hook — pass isLoading/error. Note: has pre-existing TypeScript error (skipped_count) — do NOT fix that, just wire loading/error
    - **labor-events**: useLaborEvents — pass isLoading/error to Table
    - **payroll-types**: usePayrollTypes — pass isLoading/error to Table
    - **employee-deductions**: useEmployeeDeductions — pass isLoading/error to Table
    - **audit-logs**: useAuditLogs — pass isLoading/error to Table
    - **users**: Check if there's a useUsers hook or direct service — add loading/error handling
    - **reports**: Check if there's a useReports hook or direct service — add loading/error handling

    Do NOT change the data fetching logic, hook implementations, or page layouts — only add the isLoading/error props to existing Table and FormModal components.
  </action>
  <verify>
    <automated>npx tsc --noEmit passes in src/frontend/</automated>
  </verify>
  <done>All 17 data pages pass isLoading and error props from their hooks to Table/FormModal components; no page renders a blank view during loading or error states; npx next lint passes</done>
</task>

</tasks>

<verification>
- Table component renders skeleton rows when isLoading=true, error banner when error is set, empty state when no data
- All 17 data pages wire isLoading and error from hooks to Table component
- FormModal shows submit spinner during API calls
- npx tsc --noEmit passes in frontend (pre-existing attendance skipped_count error is acceptable)
- npx next lint passes in frontend
- No page shows a blank screen during loading or after error
</verification>

<success_criteria>
- Every data page shows a visible loading indicator (skeleton rows) while fetching data
- Every data page shows a descriptive error message with retry option when API calls fail
- Every data page shows an empty state message when there are no records
- No page is ever blank — always shows one of: loading, error, empty, or data
- Form modals show a submit spinner during API calls
</success_criteria>

<output>
After completion, create `.planning/phases/13-integracion-frontend-backend/13-03-SUMMARY.md`
</output>
