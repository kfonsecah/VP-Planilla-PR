---
phase: 13-integracion-frontend-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/src/services/employeeService.ts
  - src/frontend/src/services/payrollService.ts
  - src/frontend/src/services/deductionsService.ts
  - src/frontend/src/services/positionsService.ts
  - src/frontend/src/services/branchService.ts
  - src/frontend/src/services/clockLogsService.ts
  - src/frontend/src/services/bonusesService.ts
  - src/frontend/src/services/vacationsService.ts
  - src/frontend/src/services/laborEventsService.ts
  - src/frontend/src/services/nomineeService.ts
  - src/frontend/src/services/payrollTypesService.ts
  - src/frontend/src/services/employeeDeductionsService.ts
  - src/frontend/src/services/payrollEmployeesService.ts
  - src/frontend/src/services/userService.ts
  - src/frontend/src/services/auditLogsService.ts
  - src/frontend/src/services/reportsService.ts
  - src/frontend/CONTRACT_AUDIT.md
autonomous: true
requirements: [INTEG-01]

must_haves:
  truths:
    - "Existe un documento de auditoría que mapea cada payload del frontend contra su schema Zod del backend"
    - "Los campos enviados por cada formulario coinciden exactamente con los schemas del backend — sin mismatches"
    - "Los servicios frontend normalizan los datos al formato exacto que espera el backend"
  artifacts:
    - path: "src/frontend/CONTRACT_AUDIT.md"
      provides: "Contrato de auditoría frontend-backend"
      min_lines: 100
    - path: "src/frontend/src/services/employeeService.ts"
      provides: "Payloads normalizados al schema backend"
      exports: ["createEmployee", "updateEmployee"]
  key_links:
    - from: "src/frontend/src/services/employeeService.ts"
      to: "src/backend/src/schemas/EmployeeSchema.ts"
      via: "payload fields matching Zod schema keys"
      pattern: "employee_first_name|employee_last_name|employee_email"
---

<objective>
Audit all frontend service payloads against backend Zod schemas and fix field mismatches

Purpose: Ensure every form submission and API call from the frontend sends exactly what the backend Zod schemas expect — no missing fields, no wrong field names, no type mismatches
Output: CONTRACT_AUDIT.md document + fixed service payloads
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Backend Zod schemas
@src/backend/src/schemas/EmployeeSchema.ts
@src/backend/src/schemas/PayrollSchema.ts
@src/backend/src/schemas/DeductionSchema.ts
@src/backend/src/schemas/ClockLogSchema.ts
@src/backend/src/schemas/UserSchema.ts

# Frontend services
@src/frontend/src/services/employeeService.ts
@src/frontend/src/services/payrollService.ts
@src/frontend/src/services/deductionsService.ts
@src/frontend/src/services/positionsService.ts
@src/frontend/src/services/branchService.ts
@src/frontend/src/services/clockLogsService.ts
@src/frontend/src/services/bonusesService.ts
@src/frontend/src/services/vacationsService.ts
@src/frontend/src/services/laborEventsService.ts
@src/frontend/src/services/nomineeService.ts
@src/frontend/src/services/payrollTypesService.ts
@src/frontend/src/services/employeeDeductionsService.ts
@src/frontend/src/services/payrollEmployeesService.ts
@src/frontend/src/services/userService.ts
@src/frontend/src/services/auditLogsService.ts
@src/frontend/src/services/reportsService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive contract audit document</name>
  <files>src/frontend/CONTRACT_AUDIT.md</files>
  <action>
    Create CONTRACT_AUDIT.md at the frontend root that audits every frontend service against its corresponding backend schema. For each domain (employee, payroll, deductions, positions, branches, clocklogs, bonuses, vacations, labor events, nominee, payroll types, employee deductions, payroll employees, users, audit logs, reports):

    1. Read the backend Zod schema in src/backend/src/schemas/ (for domains that have one — Employee, Payroll, Deduction, ClockLog, User)
    2. Read the backend route file in src/backend/src/routes/ to identify all endpoints and their expected payloads
    3. Read the frontend service file in src/frontend/src/services/ to identify what payload fields are sent
    4. Document matches and mismatches in a table format:
       - Domain name
       - Endpoint (method + path)
       - Frontend payload fields (list all)
       - Backend expected fields (from Zod schema or route handler)
       - Status: ✅ MATCH or ❌ MISMATCH
       - Mismatch detail: what's wrong (missing field, wrong name, wrong type)
    5. Include a summary section at the top with total matches vs mismatches
    6. For domains without explicit Zod schemas (positions, branches, bonuses, vacations, labor events, payroll types, employee deductions, payroll employees, audit logs, reports), audit against the backend route/controller to determine what fields the endpoint actually reads from req.body

    Known mismatches to verify and document:
    - employeeService.updateEmployee: sends fields WITHOUT employee_ prefix (name, last_name, etc.) but backend updateEmployeeSchema accepts both prefixed and non-prefixed — verify this is working correctly
    - employeeService.deleteEmployee: sends { fired: true, exit_date: ... } via PUT — verify backend handles this
    - payrollEmployeesService and employeeDeductionsService: verify payload shapes match backend expectations
  </action>
  <verify>
    <automated>File exists at src/frontend/CONTRACT_AUDIT.md with at least 100 lines covering all 16 service domains</automated>
  </verify>
  <done>CONTRACT_AUDIT.md exists with complete audit of all frontend services against backend schemas/routes, with clear match/mismatch status for each endpoint</done>
</task>

<task type="auto">
  <name>Task 2: Fix payload mismatches in frontend services</name>
  <files>
    src/frontend/src/services/employeeService.ts
    src/frontend/src/services/payrollService.ts
    src/frontend/src/services/deductionsService.ts
    src/frontend/src/services/positionsService.ts
    src/frontend/src/services/branchService.ts
    src/frontend/src/services/clockLogsService.ts
    src/frontend/src/services/bonusesService.ts
    src/frontend/src/services/vacationsService.ts
    src/frontend/src/services/laborEventsService.ts
    src/frontend/src/services/nomineeService.ts
    src/frontend/src/services/payrollTypesService.ts
    src/frontend/src/services/employeeDeductionsService.ts
    src/frontend/src/services/payrollEmployeesService.ts
    src/frontend/src/services/userService.ts
    src/frontend/src/services/auditLogsService.ts
    src/frontend/src/services/reportsService.ts
  </files>
  <action>
    Based on the CONTRACT_AUDIT.md findings, fix all payload mismatches in frontend services:

    1. **employeeService.ts**: 
       - createEmployee: verify payload matches createEmployeeSchema exactly (employee_first_name, employee_last_name, employee_middle_name, employee_national_id, employee_social_code, employee_email, employee_position_id, employee_hire_date, employee_required_hours_biweekly, employee_status)
       - updateEmployee: verify payload fields match backend expectations (currently sends without prefix — backend accepts both, but standardize on one approach)
       - deleteEmployee/fireEmployee: verify { fired, exit_date } payload is correct

    2. **payrollService.ts**: verify createPayroll payload matches backend PayrollSchema
    3. **deductionsService.ts**: verify create/update deduction payloads match backend DeductionSchema
    4. **positionsService.ts**: verify create/update position payloads match backend route expectations
    5. **branchService.ts**: verify create/update branch payloads match backend route expectations
    6. **clockLogsService.ts**: verify clock log registration payloads match backend ClockLogSchema
    7. **bonusesService.ts**: verify bonus payloads match backend route expectations
    8. **vacationsService.ts**: verify vacation payloads match backend route expectations
    9. **laborEventsService.ts**: verify labor event payloads match backend route expectations
    10. **nomineeService.ts**: verify calculatePayrollForPeriod and other method payloads match backend expectations
    11. **payrollTypesService.ts**: verify payroll type payloads match backend route expectations
    12. **employeeDeductionsService.ts**: verify employee deduction assignment payloads match backend expectations
    13. **payrollEmployeesService.ts**: verify payroll-employee linking payloads match backend expectations
    14. **userService.ts**: verify user payloads match backend UserSchema
    15. **auditLogsService.ts**: verify audit log query payloads match backend expectations
    16. **reportsService.ts**: verify report generation payloads match backend expectations

    For each fix: ensure field names, types, and required/optional status match the backend. Add comments explaining the mapping where field names differ between frontend and backend conventions.
  </action>
  <verify>
    <automated>npx tsc --noEmit passes in src/frontend/</automated>
  </verify>
  <done>All mismatches from CONTRACT_AUDIT.md are resolved, TypeScript compiles without errors, and all service payloads match backend schema expectations</done>
</task>

</tasks>

<verification>
- CONTRACT_AUDIT.md exists at src/frontend/CONTRACT_AUDIT.md with comprehensive audit of all 16 service domains
- All payload mismatches identified in the audit are fixed in the corresponding service files
- npx tsc --noEmit passes in both frontend and backend
- No new TypeScript errors introduced
</verification>

<success_criteria>
- Every frontend service sends payloads that match backend Zod schema field names, types, and required/optional status
- CONTRACT_AUDIT.md documents the complete contract map between frontend and backend
- No field name mismatches remain that could cause silent data loss or validation failures
</success_criteria>

<output>
After completion, create `.planning/phases/13-integracion-frontend-backend/13-01-SUMMARY.md`
</output>
