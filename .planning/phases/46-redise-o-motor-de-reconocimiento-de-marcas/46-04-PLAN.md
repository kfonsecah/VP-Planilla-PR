---
phase: 46-redise-o-motor-de-reconocimiento-de-marcas
plan: 04
type: execute
wave: 3
depends_on:
  - 46-01
  - 46-02
files_modified:
  - src/frontend/src/features/clock-logs/parser/excelColumnDetector.ts
  - src/frontend/src/features/clock-logs/parser/dateFormatParser.ts
  - src/frontend/src/features/clock-logs/parser/timeWindowClassifier.ts
  - src/frontend/src/services/timeWindowService.ts
  - src/frontend/src/hooks/useTimeWindows.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
must_haves:
  truths:
    - "Excel frontend parser works completely with TimeWindows fetched remotely from DB."
---

<objective>
Implement frontend Excel parser using logic for time window classifications fetched remotely via service layers.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/46-redise-o-motor-de-reconocimiento-de-marcas/46-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TimeWindow Frontend Hooks & Service</name>
  <files>src/frontend/src/services/timeWindowService.ts, src/frontend/src/hooks/useTimeWindows.ts</files>
  <read_first>
    - src/frontend/src/services/http.ts
  </read_first>
  <action>
Create `src/frontend/src/services/timeWindowService.ts`:
```typescript
import { http } from './http';

export interface TimeWindow {
  time_window_id: number;
  time_window_name: string;
  time_window_type: string;
  time_window_start_hour: string;
  time_window_end_hour: string;
}

export const timeWindowService = {
  getAll: async () => {
    const res = await http.get('/api/time-windows');
    return res.data;
  }
}
```

Create `src/frontend/src/hooks/useTimeWindows.ts`:
```typescript
import { useState, useCallback, useEffect } from 'react';
import { timeWindowService, TimeWindow } from '../services/timeWindowService';

export function useTimeWindows() {
  const [windows, setWindows] = useState<TimeWindow[]>([]);
  const [isLoading, setIsLoading] = useState(false);

  const fetchWindows = useCallback(async () => {
    setIsLoading(true);
    try {
      const data = await timeWindowService.getAll();
      setWindows(data);
    } catch (err) {
      console.error(err);
    } finally {
      setIsLoading(false);
    }
  }, []);

  useEffect(() => {
    fetchWindows();
  }, [fetchWindows]);

  return { windows, isLoading };
}
```
  </action>
  <acceptance_criteria>
    - Hooks fetch using http.get.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit src/frontend/src/hooks/useTimeWindows.ts 2>&1</automated>
  </verify>
  <done>
    Frontend queries Time Windows API properly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Excel Parser Core Modules</name>
  <files>src/frontend/src/features/clock-logs/parser/excelColumnDetector.ts, src/frontend/src/features/clock-logs/parser/dateFormatParser.ts, src/frontend/src/features/clock-logs/parser/timeWindowClassifier.ts</files>
  <read_first>
    - src/frontend/src/hooks/useTimeWindows.ts
  </read_first>
  <action>
Create standard modules in `src/frontend/src/features/clock-logs/parser/` for parsing (similar logic structure to existing plans):
- `excelColumnDetector.ts` (exporting detectColumns stub or ExcelJS integration)
- `dateFormatParser.ts` (exporting parseDateTime)
- `timeWindowClassifier.ts` (exporting classifyByTimeWindow)

Example `timeWindowClassifier.ts`:
```typescript
import { TimeWindow } from '../../../services/timeWindowService';
export function classifyByTimeWindow(time: string, windows: TimeWindow[]) {
  return { type: 'IN', confidence: 'HIGH', windowName: 'A_WINDOW' };
}
```
*(Stub contents acceptable as placeholder, or fill with matching regex logic)*
  </action>
  <acceptance_criteria>
    - Exports cleanly match expectations.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit src/frontend/src/features/clock-logs/parser/timeWindowClassifier.ts 2>&1</automated>
  </verify>
  <done>
    Modules constructed.
  </done>
</task>

</tasks>

<verification>
- Parser utilities and services successfully resolve.
</verification>