---
phase: 46-redise-o-motor-de-reconocimiento-de-marcas
plan: 06
type: execute
wave: 5
depends_on:
  - 46-04
  - 46-05
files_modified:
  - src/frontend/src/components/ClockImportModal.tsx
  - src/frontend/src/hooks/useClockAudit.ts
  - src/frontend/src/components/ImportPreviewTable.tsx
  - src/frontend/src/app/pages/clock-logs/page.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07

must_haves:
  truths:
    - "File upload triggers auto-parse and column detection"
    - "Preview table shows detected data before import"
    - "Skipped rows reported to user"
    - "User confirms to complete import"
  artifacts:
    - path: "src/frontend/src/components/ClockImportModal.tsx"
      provides: "Import modal with file upload, preview, confirmation"
      exports: ["ClockImportModal"]
    - path: "src/frontend/src/components/ImportPreviewTable.tsx"
      provides: "Preview table showing parsed data"
      exports: ["ImportPreviewTable"]
---

<objective>
Create ClockImportModal with file upload, preview, and confirmation flow (D-01 to D-04).

Purpose: The main import UI integrates parser, shows preview, allows user to confirm before sending to backend.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/46-redise-o-motor-de-reconocimiento-de-marcas/46-CONTEXT.md
@.planning/phases/46-redise-o-motor-de-reconocimiento-de-marcas/46-RESEARCH.md
@.planning/phases/46-redise-o-motor-de-reconocimiento-de-marcas/46-UI-SPEC.md

# Dependencies:
# - Plan 04: Excel parser modules (excelColumnDetector, dateFormatParser, timeWindowClassifier)
# - Plan 05: UI components (MarkConfidenceBadge)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ImportPreviewTable component</name>
  <files>src/frontend/src/components/ImportPreviewTable.tsx</files>
  <action>
Create ImportPreviewTable.tsx:

```tsx
import React from 'react';

interface ParsedRow {
  id: number;
  employeeName: string;
  date: string;
  time: string;
  type: 'IN' | 'OUT' | 'UNDETERMINED';
  confidence: 'HIGH' | 'MEDIUM' | 'LOW';
  rowNumber: number;
}

interface ImportPreviewTableProps {
  rows: ParsedRow[];
  skippedRows: { rowNumber: number; content: string }[];
}

export function ImportPreviewTable({ rows, skippedRows }: ImportPreviewTableProps) {
  return (
    <div className="space-y-4">
      {/* Parsed data table */}
      <div className="border border-zinc-200 rounded-lg overflow-hidden">
        <table className="w-full">
          <thead className="bg-zinc-50 border-b border-zinc-200">
            <tr>
              <th className="px-3 py-2 text-left text-xs font-medium text-zinc-500 uppercase">Fila</th>
              <th className="px-3 py-2 text-left text-xs font-medium text-zinc-500 uppercase">Empleado</th>
              <th className="px-3 py-2 text-left text-xs font-medium text-zinc-500 uppercase">Fecha</th>
              <th className="px-3 py-2 text-left text-xs font-medium text-zinc-500 uppercase">Hora</th>
              <th className="px-3 py-2 text-left text-xs font-medium text-zinc-500 uppercase">Tipo</th>
              <th className="px-3 py-2 text-left text-xs font-medium text-zinc-500 uppercase">Confianza</th>
            </tr>
          </thead>
          <tbody className="divide-y divide-zinc-200">
            {rows.slice(0, 50).map((row) => (
              <tr key={row.id} className="hover:bg-zinc-50">
                <td className="px-3 py-2 text-sm text-zinc-500">{row.rowNumber}</td>
                <td className="px-3 py-2 text-sm font-medium text-zinc-800">{row.employeeName}</td>
                <td className="px-3 py-2 text-sm text-zinc-600">{row.date}</td>
                <td className="px-3 py-2 text-sm text-zinc-600 font-mono">{row.time}</td>
                <td className="px-3 py-2">
                  <span className={`px-2 py-0.5 rounded text-xs font-medium ${
                    row.type === 'UNDETERMINED' ? 'bg-red-100 text-red-700' :
                    row.type === 'IN' ? 'bg-green-100 text-green-700' :
                    'bg-blue-100 text-blue-700'
                  }`}>
                    {row.type === 'UNDETERMINED' ? '?' : row.type === 'IN' ? 'ENTRADA' : 'SALIDA'}
                  </span>
                </td>
                <td className="px-3 py-2">
                  <span className={`w-2 h-2 rounded-full inline-block ${
                    row.confidence === 'HIGH' ? 'bg-green-500' :
                    row.confidence === 'MEDIUM' ? 'bg-yellow-400' :
                    'bg-red-500'
                  }`} />
                </td>
              </tr>
            ))}
          </tbody>
        </table>
        {rows.length > 50 && (
          <div className="px-3 py-2 bg-zinc-50 text-sm text-zinc-500 text-center">
            Mostrando 50 de {rows.length} filas
          </div>
        )}
      </div>

      {/* Skipped rows report (D-03) */}
      {skippedRows.length > 0 && (
        <div className="border border-yellow-200 bg-yellow-50 rounded-lg p-4">
          <div className="flex items-center gap-2 mb-2">
            <ExclamationTriangleIcon className="w-5 h-5 text-yellow-600" />
            <span className="font-medium text-yellow-800">
              {skippedRows.length} filas omitidas
            </span>
          </div>
          <ul className="text-sm text-yellow-700 space-y-1">
            {skippedRows.slice(0, 10).map((row) => (
              <li key={row.rowNumber} className="font-mono">
                Fila {row.rowNumber}: {row.content}
              </li>
            ))}
            {skippedRows.length > 10 && (
              <li className="text-zinc-500">
                ...y {skippedRows.length - 10} más
              </li>
            ))}
          </ul>
        </div>
      )}
    </div>
  );
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit src/frontend/src/components/ImportPreviewTable.tsx 2>&1</automated>
  </verify>
  <done>
    Preview table displays parsed data with confidence colors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ClockImportModal main component</name>
  <files>src/frontend/src/components/ClockImportModal.tsx</files>
  <action>
Create ClockImportModal.tsx following existing modal patterns:

```tsx
import React, { useState, useCallback } from 'react';
import { motion, AnimatePresence } from 'framer-motion';
import { detectColumns, DetectedColumns } from '../features/clock-logs/parser/excelColumnDetector';
import { parseDateTime } from '../features/clock-logs/parser/dateFormatParser';
import { classifyByTimeWindow } from '../features/clock-logs/parser/timeWindowClassifier';
import { useTimeWindows } from '../hooks/useTimeWindows';
import { ImportPreviewTable } from './ImportPreviewTable';
import { useClockLogsImport } from '../hooks/useClockLogsImport';

interface ParsedRow {
  employeeName: string;
  date: string;
  time: string;
  type: 'IN' | 'OUT';
  confidence: 'HIGH' | 'MEDIUM' | 'LOW';
  rowNumber: number;
}

export function ClockImportModal({ isOpen, onClose }: { isOpen: boolean; onClose: () => void }) {
  const [step, setStep] = useState<'upload' | 'preview' | 'importing' | 'done'>('upload');
  const [file, setFile] = useState<File | null>(null);
  const [parsedRows, setParsedRows] = useState<ParsedRow[]>([]);
  const [skippedRows, setSkippedRows] = useState<{ rowNumber: number; content: string }[]>([]);
  const [error, setError] = useState<string | null>(null);
  
  const { windows, isLoading: loadingWindows } = useTimeWindows();
  const { importMarks, isImporting } = useClockLogsImport();

  const handleFileSelect = useCallback(async (e: React.ChangeEvent<HTMLInputElement>) => {
    const selectedFile = e.target.files?.[0];
    if (!selectedFile) return;
    
    setFile(selectedFile);
    setError(null);
    
    try {
      const buffer = await selectedFile.arrayBuffer();
      const { columns, skippedRows: skipped } = await detectColumns(buffer);
      
      // Parse rows using detected columns
      // (actual parsing logic omitted for brevity - see Plan 04)
      
      setParsedRows(/* parsed data */);
      setSkippedRows(skipped);
      setStep('preview');
    } catch (err) {
      setError('Error al leer el archivo. Asegúrese que es un archivo Excel válido.');
    }
  }, []);

  const handleImport = useCallback(async () => {
    if (parsedRows.length === 0) return;
    
    setStep('importing');
    try {
      await importMarks(parsedRows);
      setStep('done');
    } catch (err) {
      setError('Error al importar marcas. Intente de nuevo.');
      setStep('preview');
    }
  }, [parsedRows, importMarks]);

  return (
    <AnimatePresence>
      {isOpen && (
        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          exit={{ opacity: 0 }}
          className="fixed inset-0 z-50 flex items-center justify-center bg-black/50"
        >
          <motion.div
            initial={{ scale: 0.95, opacity: 0 }}
            animate={{ scale: 1, opacity: 1 }}
            exit={{ scale: 0.95, opacity: 0 }}
            className="bg-white rounded-xl shadow-xl w-full max-w-4xl max-h-[90vh] overflow-hidden"
          >
            {/* Header */}
            <div className="flex items-center justify-between px-6 py-4 border-b border-zinc-200">
              <h2 className="text-xl font-bold text-zinc-800">Importar Marcas</h2>
              <button onClick={onClose} className="p-2 hover:bg-zinc-100 rounded-lg">
                <XMarkIcon className="w-5 h-5" />
              </button>
            </div>

            {/* Content */}
            <div className="p-6 overflow-y-auto max-h-[calc(90vh-140px)]">
              {step === 'upload' && (
                <div className="space-y-4">
                  {/* File drop zone */}
                  <div className="border-2 border-dashed border-zinc-300 rounded-lg p-8 text-center hover:border-blue-500 transition-colors">
                    <input
                      type="file"
                      accept=".xlsx,.xls"
                      onChange={handleFileSelect}
                      className="hidden"
                      id="clock-import-file"
                    />
                    <label htmlFor="clock-import-file" className="cursor-pointer">
                      <ArrowUpTrayIcon className="w-12 h-12 text-zinc-400 mx-auto mb-4" />
                      <p className="text-zinc-600">
                        Arrastre un archivo Excel o haga clic para seleccionar
                      </p>
                      <p className="text-sm text-zinc-400 mt-1">
                        Soporta archivos .xlsx y .xls
                      </p>
                    </label>
                  </div>
                  
                  {error && (
                    <div className="p-3 bg-red-50 text-red-700 rounded-lg text-sm">
                      {error}
                    </div>
                  )}
                </div>
              )}

              {step === 'preview' && (
                <div className="space-y-4">
                  <div className="flex items-center justify-between">
                    <p className="text-zinc-600">
                      {parsedRows.length} marcas detectadas ready for import
                    </p>
                    <button
                      onClick={() => setStep('upload')}
                      className="text-sm text-blue-600 hover:underline"
                    >
                      Cambiar archivo
                    </button>
                  </div>
                  
                  <ImportPreviewTable rows={parsedRows} skippedRows={skippedRows} />
                </div>
              )}

              {step === 'importing' && (
                <div className="text-center py-12">
                  <div className="w-12 h-12 border-4 border-blue-500 border-t-transparent rounded-full animate-spin mx-auto mb-4" />
                  <p className="text-zinc-600">Importando marcas...</p>
                </div>
              )}

              {step === 'done' && (
                <div className="text-center py-12">
                  <CheckCircleIcon className="w-12 h-12 text-green-500 mx-auto mb-4" />
                  <p className="text-lg font-medium text-zinc-800">Importación completada</p>
                  <p className="text-zinc-500 mt-1">
                    {parsedRows.length} marcas importadas correctamente
                  </p>
                </div>
              )}
            </div>

            {/* Footer */}
            <div className="flex items-center justify-end gap-3 px-6 py-4 border-t border-zinc-200 bg-zinc-50">
              <button
                onClick={onClose}
                className="px-4 py-2 text-zinc-600 hover:bg-zinc-100 rounded-lg transition-colors"
              >
                {step === 'done' ? 'Cerrar' : 'Cancelar'}
              </button>
              
              {step === 'preview' && (
                <button
                  onClick={handleImport}
                  disabled={parsedRows.length === 0}
                  className="px-4 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600 disabled:opacity-50 transition-colors"
                >
                  Importar {parsedRows.length} marcas
                </button>
              )}
              
              {step === 'done' && (
                <button
                  onClick={() => {
                    setStep('upload');
                    setFile(null);
                    setParsedRows([]);
                    setSkippedRows([]);
                    onClose();
                  }}
                  className="px-4 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600 transition-colors"
                >
                  Importar más
                </button>
              )}
            </div>
          </motion.div>
        </motion.div>
      )}
    </AnimatePresence>
  );
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit src/frontend/src/components/ClockImportModal.tsx 2>&1</automated>
  </verify>
  <done>
    ClockImportModal with upload → preview → confirm flow
  </done>
</task>

</tasks>

<verification>
- [ ] File upload triggers parsing
- [ ] Preview table shows with confidence colors
- [ ] Skipped rows reported
- [ ] Import confirmation sends JSON to backend
</verification>

<success_criteria>
End-to-end import flow functional
</success_criteria>

<output>
After completion, add to `.planning/phases/46-redise-o-motor-de-reconocimiento-de-marcas/46-06-SUMMARY.md`
</output>