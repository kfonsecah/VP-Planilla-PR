---
phase: 46-redise-o-motor-de-reconocimiento-de-marcas
plan: 03
type: execute
wave: 2
depends_on:
  - 46-01
  - 46-02
files_modified:
  - src/backend/src/service/MarkSuggestionService.ts
  - src/backend/src/controller/MarkSuggestionController.ts
  - src/backend/src/routes/MarkSuggestionRoute.ts
  - src/backend/src/routes/index.ts
autonomous: true
requirements:
  - D-12
must_haves:
  truths:
    - "Backend suggests missing marks accurately using controllers and routers properly registered."
---

<objective>
Implement suggestion functionality for missing marks (D-12) properly wiring service, controller and route.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/46-redise-o-motor-de-reconocimiento-de-marcas/46-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MarkSuggestionService</name>
  <files>src/backend/src/service/MarkSuggestionService.ts</files>
  <read_first>
    - src/backend/src/service/DayConfirmationService.ts
  </read_first>
  <action>
Create `src/backend/src/service/MarkSuggestionService.ts` containing the following snippet:
```typescript
import { prisma } from '../lib/prisma';

export class MarkSuggestionService {
  static async suggestMissingMark(employeeId: number, date: string, existingMarks: any[]) {
    // Basic baseline dummy inference logic; a real version would deeply query history.
    const dayMarks = existingMarks || [];
    const hasIn = dayMarks.some(m => m.type === 'IN');
    const hasOut = dayMarks.some(m => m.type === 'OUT');
    
    if (hasIn && !hasOut) {
      return { suggestedType: 'OUT', suggestedTime: '17:00', confidence: 'MEDIUM', reason: 'Falta marca de salida.' };
    }
    if (!hasIn && hasOut) {
      return { suggestedType: 'IN', suggestedTime: '08:00', confidence: 'MEDIUM', reason: 'Falta marca de entrada.' };
    }
    return null;
  }
}
```
  </action>
  <acceptance_criteria>
    - Compiles successfully.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit src/backend/src/service/MarkSuggestionService.ts 2>&1</automated>
  </verify>
  <done>
    Done.
  </done>
</task>

<task type="auto">
  <name>Task 2: Suggestion Controller & Route</name>
  <files>src/backend/src/controller/MarkSuggestionController.ts, src/backend/src/routes/MarkSuggestionRoute.ts, src/backend/src/routes/index.ts</files>
  <read_first>
    - src/backend/src/routes/index.ts
  </read_first>
  <action>
Create `src/backend/src/controller/MarkSuggestionController.ts`:
```typescript
import { Request, Response } from 'express';
import { MarkSuggestionService } from '../service/MarkSuggestionService';

export class MarkSuggestionController {
  static async suggest(req: Request, res: Response) {
    const { employeeId, date, existingMarks } = req.body;
    const suggestion = await MarkSuggestionService.suggestMissingMark(employeeId, date, existingMarks);
    res.json({ success: true, data: suggestion });
  }
}
```

Create `src/backend/src/routes/MarkSuggestionRoute.ts`:
```typescript
import { Router } from 'express';
import { asyncHandler } from '../utils/asyncHandler';
import { AuthMiddleware } from '../middleware/AuthMiddleware';
import { MarkSuggestionController } from '../controller/MarkSuggestionController';

const router = Router();
router.post('/missing-mark', AuthMiddleware.verifyToken, asyncHandler(MarkSuggestionController.suggest));
export default router;
```

Append in `src/backend/src/routes/index.ts`:
```typescript
import markSuggestionRoutes from './MarkSuggestionRoute';
// ...
app.use('/api/suggestions', markSuggestionRoutes);
```
  </action>
  <acceptance_criteria>
    - Controller calls service cleanly.
    - Router forwards POST /api/suggestions/missing-mark to controller.
    - index.ts actively registers /api/suggestions
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit src/backend/src/routes/index.ts 2>&1</automated>
  </verify>
  <done>
    Done.
  </done>
</task>

</tasks>

<verification>
- Backend APIs for mark suggestions are up and correctly routed.
</verification>