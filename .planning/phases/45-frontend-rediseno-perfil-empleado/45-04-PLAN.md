# Phase 45, Plan 04: Conexión con Tabla Principal (`EmployeeTable.tsx`)

## Objetivo
Actualizar la tabla global de empleados (`pages/employee/list`) para que el usuario pueda entrar al nuevo perfil.

## Tareas

1. **Modificar `EmployeeTable.tsx` / Actions Card**:
   - Cambiar el botón o link de "Ver Perfil" / "Detalles" que actualmente abría un drawer o solo modo edición, para que sea una navegación Next.js (`<Link href={'/pages/employee/' + id}>`).

2. **Clean-up en ListView**:
   - Remover código obsoleto en la vista de lista si el edit y dismiss modal ya no necesitan vivir ahí para la vista detallada (O dejarlo para edición rápida inline en la tabla y *también* tenerlo en la vista profunda). 
   - El estándar indica que la acción rápida de editar puede quedarse en la lista, pero "Ver perfil" será el nuevo Action principal.

## Verificación
- Clicar el ojito / botón Ver desde la tabla lleva a URL `/pages/employee/N`.
- Botón Atrás (Back) de navegador funciona correctamente.
