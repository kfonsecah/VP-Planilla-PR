---
gsd_plan_version: 1.0
phase: 39
plan: 2
title: Ensure Data Synchronization
status: [ ] Planned
must_haves:
  - usePositions refetch correctly invalidates cache
  - AddEmployeeModal/EditEmployeeModal re-render when positions data changes
---

<objective>
Ensure that updates made to positions (via the Positions management modal) are immediately and correctly reflected in all employee creation and editing forms.
</objective>

<context>
@.planning/phases/39-frontend-fix-employee-edit-position-selector/39-RESEARCH.md
@src/frontend/src/hooks/usePositions.ts
@src/frontend/src/app/pages/employee/list/page.tsx
@src/frontend/src/hooks/useEmployeeList.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify cache invalidation in usePositions</name>
  <files>src/frontend/src/hooks/usePositions.ts</files>
  <read_first>
    - src/frontend/src/hooks/usePositions.ts (lines 14-36 for fetchAll logic)
  </read_first>
  <action>
    Modify `fetchAll` in `usePositions.ts` to support an optional `ignoreCache` flag, or ensure that `invalidateCache` is called before calling `refetch` in critical UI paths.
    Actually, looking at `usePositions.ts`, `create`, `update`, and `remove` already call `invalidateCache(CACHE_KEY)`.
    I will add a manual `invalidateCache` to the `refetch` function exported by the hook to ensure that any manual "Recargar" also bypasses the cache.
  </action>
  <verify>
    Verify that `refetch` calls `invalidateCache` before calling `PositionsService.getAllPositions()`.
  </verify>
  <done>
    `refetch` correctly bypasses the cache.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ensure modal reactivity in EmployeeListPage</name>
  <files>src/frontend/src/hooks/useEmployeeList.ts</files>
  <read_first>
    - src/frontend/src/hooks/useEmployeeList.ts (lines 100-115 for usePositions usage)
  </read_first>
  <action>
    Ensure that `positions` data is correctly passed through and that components that consume it (like `AddEmployeeModal` and `EditEmployeeModal`) react correctly to its updates.
    Since `useEmployeeList` already consumes `usePositions`, the `positions` data is already reactive.
    I will add a `refreshPositions` call inside the `refreshEmployees` function of `useEmployeeList` to ensure both datasets are refreshed together when requested.
  </action>
  <verify>
    Verify that `refreshEmployees` now also calls `refreshPositions`.
  </verify>
  <done>
    Employee and position data are refreshed in sync.
  </done>
</task>

</tasks>
