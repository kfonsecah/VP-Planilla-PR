---
plan: 48-01
phase: 48
title: "Limpieza de artefactos generados, logs y MDs obsoletos"
wave: 1
depends_on: []
autonomous: true
files_modified:
  - src/backend/test_log.txt
  - src/backend/test_output.txt
  - src/backend/jest_html_reporters.html
  - src/backend/dist/ (directorio completo)
  - src/backend/coverage/ (directorio completo)
  - src/backend/jest-html-reporters-attach/ (directorio completo)
  - src/backend/PAYMENT_RECEIPTS_API.md
  - src/backend/TEST_DOCUMENTATION.md
  - src/backend/PAYROLL_SAVE_EXAMPLE.md
  - FLUJO_GUARDADO_PLANILLA.md
  - WORKFLOW.md
  - tracked_files.txt
  - src/frontend/tsconfig.tsbuildinfo
  - graphify-out/ (directorio completo)
must_haves:
  - "Los artefactos generados por herramientas (dist, coverage, build) son eliminados del repo"
  - "Los MDs de notas de desarrollo (flujos old, workflows manuales) son eliminados"
  - "Los logs de tests no se rastrean en el repositorio"
  - "graphify-out generado automáticamente no persiste en el repo"
  - ".gitignore actualizado para prevenir reingreso de estos artefactos"
---

# Plan 48-01: Limpieza de artefactos generados, logs y MDs obsoletos

## Objetivo

Eliminar del repositorio todos los archivos que son generados automáticamente por herramientas (build, test, coverage), logs de ejecución, y documentos markdown que quedaron como notas de desarrollo en etapas anteriores y ya no tienen valor activo.

---

## Inventario de archivos a eliminar

### Logs de tests y outputs temporales

| Archivo | Motivo de eliminación |
|---|---|
| `src/backend/test_log.txt` | Output de ejecución de tests. Se regenera con `npm test`. Nunca debe estar en el repo. |
| `src/backend/test_output.txt` | Ídem — copia redundante del output de tests. |
| `src/backend/jest_html_reporters.html` | Reporte HTML generado por Jest reporters. Artefacto de ejecución. |
| `src/backend/jest-html-reporters-attach/` | Directorio generado por jest-html-reporters. Artefacto de ejecución. |

### Build artifacts

| Archivo/Directorio | Motivo de eliminación |
|---|---|
| `src/backend/dist/` | Output de compilación TypeScript. Regenerable con `tsc`. Ya en `.gitignore` de backend pero presente. |
| `src/frontend/tsconfig.tsbuildinfo` | Cache incremental de TypeScript. Generado automáticamente por `tsc`. |

### Coverage

| Directorio | Motivo de eliminación |
|---|---|
| `src/backend/coverage/` | Generado por `npm run test:coverage`. Artefacto de herramienta. |

### graphify-out

| Directorio | Motivo de eliminación |
|---|---|
| `graphify-out/` | Generado por `/gsd-graphify`. Contiene `graph.json` (2.8MB), `graph.html` (1.8MB), `GRAPH_REPORT.md` (62KB). Artefacto de herramienta — se regenera cuando se necesita. |

### Documentos Markdown obsoletos

| Archivo | Motivo de eliminación |
|---|---|
| `FLUJO_GUARDADO_PLANILLA.md` | Nota de desarrollo de una feature ya implementada (Fase 37). Información absorbida por el código. No es documentación viva. |
| `WORKFLOW.md` | Documento de workflow desactualizado con comandos `/gsd:` (sintaxis vieja). El verdadero workflow vive en `.planning/` y `GEMINI.md`. |
| `src/backend/PAYMENT_RECEIPTS_API.md` | Documentación de API ad-hoc de fase antigua. Información duplicada en el código. No se mantiene. |
| `src/backend/TEST_DOCUMENTATION.md` | Notas de testing de fases tempranas (PayrollService v1). El estado real de los tests vive en `__tests__/`. |
| `src/backend/PAYROLL_SAVE_EXAMPLE.md` | Ejemplos de payload de fases pasadas. Ya superado por implementación actual. |
| `tracked_files.txt` | 134KB de inventario generado automáticamente. Artefacto de herramienta, no debe persistir. |

---

## Tareas

### Tarea 1: Eliminar logs de tests y HTMLs generados

<read_first>
- src/backend/test_log.txt
- src/backend/test_output.txt
- src/backend/jest_html_reporters.html
- src/backend/.gitignore
</read_first>

<action>
Eliminar los siguientes archivos usando comandos de sistema:

```powershell
Remove-Item "src\backend\test_log.txt" -Force
Remove-Item "src\backend\test_output.txt" -Force
Remove-Item "src\backend\jest_html_reporters.html" -Force
Remove-Item "src\backend\jest-html-reporters-attach" -Recurse -Force
```

Luego verificar que `.gitignore` en `src/backend/` incluye entradas para prevenir reingreso:
- Si `test_log.txt` no está en `.gitignore`, añadirlo.
- Si `test_output.txt` no está en `.gitignore`, añadirlo.
- Si `jest_html_reporters.html` no está en `.gitignore`, añadirlo.
- Si `jest-html-reporters-attach/` no está en `.gitignore`, añadirlo.
</action>

<acceptance_criteria>
- `src/backend/test_log.txt` no existe: `Test-Path "src\backend\test_log.txt"` retorna `False`
- `src/backend/test_output.txt` no existe: `Test-Path "src\backend\test_output.txt"` retorna `False`
- `src/backend/jest_html_reporters.html` no existe: `Test-Path "src\backend\jest_html_reporters.html"` retorna `False`
- `src/backend/jest-html-reporters-attach/` no existe: `Test-Path "src\backend\jest-html-reporters-attach"` retorna `False`
- `src/backend/.gitignore` contiene `test_log.txt`
- `src/backend/.gitignore` contiene `test_output.txt`
- `src/backend/.gitignore` contiene `jest_html_reporters.html`
- `src/backend/.gitignore` contiene `jest-html-reporters-attach`
</acceptance_criteria>

---

### Tarea 2: Eliminar build artifacts y coverage

<read_first>
- src/backend/.gitignore
- src/frontend/tsconfig.json
</read_first>

<action>
Eliminar los directorios de build y coverage:

```powershell
Remove-Item "src\backend\dist" -Recurse -Force -ErrorAction SilentlyContinue
Remove-Item "src\backend\coverage" -Recurse -Force
Remove-Item "src\frontend\tsconfig.tsbuildinfo" -Force
```

Verificar que el root `.gitignore` o el `.gitignore` de backend incluye:
- `dist/`
- `coverage/`
- `tsconfig.tsbuildinfo`

Si alguna entrada falta, añadirla al `.gitignore` raíz del proyecto.
</action>

<acceptance_criteria>
- `src/backend/coverage/` no existe: `Test-Path "src\backend\coverage"` retorna `False`
- `src/frontend/tsconfig.tsbuildinfo` no existe: `Test-Path "src\frontend\tsconfig.tsbuildinfo"` retorna `False`
- El `.gitignore` raíz contiene `tsconfig.tsbuildinfo`
- El `.gitignore` raíz contiene `coverage/` o `src/backend/coverage`
</acceptance_criteria>

---

### Tarea 3: Eliminar graphify-out

<read_first>
- graphify-out/ (listar contenido)
- .gitignore (raíz)
</read_first>

<action>
Eliminar el directorio `graphify-out/` completo:

```powershell
Remove-Item "graphify-out" -Recurse -Force
```

Añadir al `.gitignore` raíz la entrada `graphify-out/` si no está presente, para evitar que se agregue accidentalmente al repositorio en el futuro.
</action>

<acceptance_criteria>
- `graphify-out/` no existe: `Test-Path "graphify-out"` retorna `False`
- El `.gitignore` raíz contiene `graphify-out/`
</acceptance_criteria>

---

### Tarea 4: Eliminar MDs obsoletos del backend

<read_first>
- src/backend/PAYMENT_RECEIPTS_API.md
- src/backend/TEST_DOCUMENTATION.md
- src/backend/PAYROLL_SAVE_EXAMPLE.md
</read_first>

<action>
Eliminar los documentos markdown de desarrollo que ya no son relevantes:

```powershell
Remove-Item "src\backend\PAYMENT_RECEIPTS_API.md" -Force
Remove-Item "src\backend\TEST_DOCUMENTATION.md" -Force
Remove-Item "src\backend\PAYROLL_SAVE_EXAMPLE.md" -Force
```

Estos documentos fueron notas de trabajo de fases tempranas (pre-v1.3). La información de APIs vive en el código. La documentación de tests vive en los propios archivos de test.
</action>

<acceptance_criteria>
- `src/backend/PAYMENT_RECEIPTS_API.md` no existe: `Test-Path "src\backend\PAYMENT_RECEIPTS_API.md"` retorna `False`
- `src/backend/TEST_DOCUMENTATION.md` no existe: `Test-Path "src\backend\TEST_DOCUMENTATION.md"` retorna `False`
- `src/backend/PAYROLL_SAVE_EXAMPLE.md` no existe: `Test-Path "src\backend\PAYROLL_SAVE_EXAMPLE.md"` retorna `False`
</acceptance_criteria>

---

### Tarea 5: Eliminar MDs obsoletos del root y tracked_files.txt

<read_first>
- FLUJO_GUARDADO_PLANILLA.md
- WORKFLOW.md
- tracked_files.txt
- GEMINI.md (para confirmar que contiene info equivalente a WORKFLOW.md)
</read_first>

<action>
Eliminar los documentos de notas del root del repositorio:

```powershell
Remove-Item "FLUJO_GUARDADO_PLANILLA.md" -Force
Remove-Item "WORKFLOW.md" -Force
Remove-Item "tracked_files.txt" -Force
```

Verificar antes de eliminar WORKFLOW.md que GEMINI.md contiene información equivalente de comandos de trabajo. Confirmar que el archivo existe y tiene contenido antes de eliminar.
</action>

<acceptance_criteria>
- `FLUJO_GUARDADO_PLANILLA.md` no existe: `Test-Path "FLUJO_GUARDADO_PLANILLA.md"` retorna `False`
- `WORKFLOW.md` no existe: `Test-Path "WORKFLOW.md"` retorna `False`
- `tracked_files.txt` no existe: `Test-Path "tracked_files.txt"` retorna `False`
- `GEMINI.md` existe: `Test-Path "GEMINI.md"` retorna `True`
</acceptance_criteria>

---

### Tarea 6: Actualizar .gitignore raíz para prevenir reingreso

<read_first>
- .gitignore (raíz del proyecto)
</read_first>

<action>
Leer el `.gitignore` raíz actual y añadir las entradas faltantes al final del archivo. Las entradas a añadir (solo si no existen ya):

```
# Artefactos generados por herramientas
graphify-out/
tracked_files.txt
tsconfig.tsbuildinfo

# Logs de tests
test_log.txt
test_output.txt
jest_html_reporters.html
jest-html-reporters-attach/

# Coverage reports
coverage/
```

No duplicar entradas que ya existan en el archivo.
</action>

<acceptance_criteria>
- `cat .gitignore | Select-String "graphify-out"` retorna resultado no vacío
- `cat .gitignore | Select-String "tracked_files"` retorna resultado no vacío
- `cat .gitignore | Select-String "tsconfig.tsbuildinfo"` retorna resultado no vacío
- `cat .gitignore | Select-String "test_log"` retorna resultado no vacío
</acceptance_criteria>

---

## Verificación del plan

```bash
# Verificar que todos los archivos fueron eliminados
$eliminados = @(
  "src\backend\test_log.txt",
  "src\backend\test_output.txt",
  "src\backend\jest_html_reporters.html",
  "src\backend\jest-html-reporters-attach",
  "src\backend\coverage",
  "src\backend\PAYMENT_RECEIPTS_API.md",
  "src\backend\TEST_DOCUMENTATION.md",
  "src\backend\PAYROLL_SAVE_EXAMPLE.md",
  "src\frontend\tsconfig.tsbuildinfo",
  "graphify-out",
  "FLUJO_GUARDADO_PLANILLA.md",
  "WORKFLOW.md",
  "tracked_files.txt"
)
$eliminados | ForEach-Object { 
  if (Test-Path $_) { Write-Host "FALTA ELIMINAR: $_" -ForegroundColor Red }
  else { Write-Host "OK eliminado: $_" -ForegroundColor Green }
}

# Verificar que el servidor de tests sigue funcionando
cd src/backend && npm test -- --passWithNoTests
```

## UAT

- [ ] Los archivos listados ya no existen en el repositorio
- [ ] `npm test` en backend sigue pasando correctamente (0 errores)
- [ ] `.gitignore` raíz previene reingreso de artefactos eliminados
- [ ] `git status` no muestra archivos sin rastrear relacionados con los eliminados
