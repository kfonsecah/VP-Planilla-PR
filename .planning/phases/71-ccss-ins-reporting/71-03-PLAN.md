---
phase: "71"
plan: "03"
type: execute
wave: 3
depends_on: ["71-02"]
files_modified: [
  "src/frontend/src/app/pages/reports/page.tsx",
  "src/frontend/src/services/reportsService.ts"
]
autonomous: true
requirements: ["REP-71-03"]
must_haves:
  truths:
    - "Reports page includes an 'Institutional Exports' section"
    - "Users can trigger CCSS and INS CSV downloads for a selected payroll"
    - "The UI handles loading states during report generation"
  artifacts:
    - path: "src/frontend/src/app/pages/reports/page.tsx"
      provides: "UI for institutional report downloads"
  key_links:
    - from: "Reports page"
      to: "reportsService.downloadCCSSReport"
      via: "Button click"
---

<objective>
Update the Reports page in the frontend to support the new institutional exports for CCSS and INS.

Purpose: Provide a user interface for HR/Administrators to export regulatory data.
Output: Updated Reports page with download buttons and institutional export service methods.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-ccss-ins-reporting/71-VALIDATION.md
@.planning/phases/71-ccss-ins-reporting/71-02-SUMMARY.md
@src/frontend/src/app/pages/reports/page.tsx
@src/frontend/src/services/reportsService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Frontend Reports Service</name>
  <files>src/frontend/src/services/reportsService.ts</files>
  <action>
    1. Add `downloadCCSSReport(payrollId: number)` method.
    2. Add `downloadINSReport(payrollId: number)` method.
    3. Use the central `http.ts` client.
    4. Implement logic to handle the blob response and trigger the browser download (standard pattern in the codebase).
  </action>
  <verify>
    <automated>cd src/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Frontend service can handle institutional report downloads.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Institutional Exports UI</name>
  <files>src/frontend/src/app/pages/reports/page.tsx</files>
  <action>
    1. Add a new section "Exportaciones Institucionales" to the Reports page.
    2. Include two new actions/buttons: "Descargar Reporte CCSS (SICERE)" and "Descargar Reporte INS (Riesgos)".
    3. Ensure buttons are only enabled when a payroll is selected and its status is "Closed" or "Processed".
    4. Add a loading spinner or state indicator while the download is in progress.
    5. Follow the UI design rules in PHASE_CONTRACT.md (bg-zinc-950, text-zinc-100, etc.).
  </action>
  <verify>
    <automated>cd src/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Reports page UI supports institutional exports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> API | Download request for sensitive CSV |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-03-01 | Information Disclosure | Downloaded File | mitigate | Ensure the frontend only shows the institutional report options to users with appropriate roles (Admin/HR) |
</threat_model>

<verification>
1. Follow validation steps in `.planning/phases/71-ccss-ins-reporting/71-VALIDATION.md` section "Integration".
2. Navigate to the Reports page, select a valid payroll, and click "Descargar Reporte CCSS".
</verification>

<success_criteria>
- Users can download CCSS and INS reports via the UI.
- Downloads are correctly triggered and filenames are descriptive.
- UI follows established patterns and design language.
</success_criteria>

<output>
After completion, create `.planning/phases/71-ccss-ins-reporting/71-03-SUMMARY.md`
</output>
