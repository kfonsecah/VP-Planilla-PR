---
phase: "71"
plan: "02"
type: execute
wave: 2
depends_on: ["71-01"]
files_modified: [
  "src/backend/src/service/ReportsService.ts",
  "src/backend/src/controller/ReportsController.ts",
  "src/backend/src/routes/ReportsRoute.ts"
]
autonomous: true
requirements: ["REP-71-02"]
must_haves:
  truths:
    - "CCSS report generation produces a valid CSV for SICERE"
    - "INS report generation produces a valid CSV with correct risk metadata"
    - "Endpoints are secure and return correct Content-Type for downloads"
  artifacts:
    - path: "src/backend/src/service/ReportsService.ts"
      provides: "Institutional CSV generation logic"
  key_links:
    - from: "ReportsController"
      to: "ReportsService"
      via: "Method calls"
---

<objective>
Implement the backend logic for generating CCSS (SICERE) and INS (Riesgos del Trabajo) consolidated CSV reports.

Purpose: Provide institutional reporting capabilities as required by Costa Rican authorities.
Output: New reporting methods in ReportsService and corresponding API endpoints.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-ccss-ins-reporting/RESEARCH.md
@.planning/phases/71-ccss-ins-reporting/71-VALIDATION.md
@.planning/phases/71-ccss-ins-reporting/71-01-SUMMARY.md
@src/backend/src/service/ReportsService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement CSV Generation in ReportsService</name>
  <files>src/backend/src/service/ReportsService.ts</files>
  <action>
    1. Implement `generateCCSSReportCSV(payrollId: number): Promise<{ filename: string, content: string }>`:
       - Fetch payroll records, including employee salaries and the new `worked_days` field.
       - Format data following SICERE requirements (CSV/fixed-width).
    2. Implement `generateINSReportCSV(payrollId: number): Promise<{ filename: string, content: string }>`:
       - Fetch payroll records and associated position metadata (`position_occupation_code`, `position_risk_class`).
       - Format as CSV for INS Riesgos del Trabajo.
    3. Use a centralized CSV utility or helper within the service for formatting.
    4. Add mandatory JSDoc to both methods.
  </action>
  <verify>
    <automated>cd src/backend && npx tsc --noEmit</automated>
  </verify>
  <done>ReportsService has logic to produce institutional CSV files.</done>
</task>

<task type="auto">
  <name>Task 2: Expose Reporting Endpoints</name>
  <files>src/backend/src/controller/ReportsController.ts, src/backend/src/routes/ReportsRoute.ts</files>
  <action>
    1. Update `ReportsController.ts`:
       - Add `downloadCCSSReport` and `downloadINSReport` methods.
       - Methods should call `ReportsService`, set `Content-Type: text/csv` and appropriate `Content-Disposition`.
    2. Update `ReportsRoute.ts`:
       - Register GET routes for these reports (e.g., `/reports/institutional/ccss/:id`, `/reports/institutional/ins/:id`).
       - Apply `AuthMiddleware` for security.
  </action>
  <verify>
    <automated>cd src/backend && npx tsc --noEmit</automated>
  </verify>
  <done>API endpoints for institutional reports are functional and secure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> Response Stream | CSV data streamed to client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-02-01 | Information Disclosure | CSV Content | mitigate | Enforce strict role-based access to reporting endpoints; CSVs contain PII |
| T-71-02-02 | Denial of Service | CSV Generation | mitigate | Implement basic timeout/rate limiting if generation proves resource-intensive |
</threat_model>

<verification>
1. Follow validation steps in `.planning/phases/71-ccss-ins-reporting/71-VALIDATION.md` section "Format Integrity".
2. Use `curl` or Postman to request the CCSS report for a completed payroll; verify headers.
</verification>

<success_criteria>
- ReportsService successfully generates valid CSV strings.
- API endpoints return the CSV as a downloadable file with correct headers.
- Data in CSV matches database state (salaries, worked days, position_occupation_code, position_risk_class).
</success_criteria>

<output>
After completion, create `.planning/phases/71-ccss-ins-reporting/71-02-SUMMARY.md`
</output>
