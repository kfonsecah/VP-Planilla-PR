---
phase: 54-rediseno-flujo-planilla
plan: 05
type: execute
wave: 4
depends_on:
  - 54-03
  - 54-04
files_modified:
  - src/frontend/src/app/pages/payroll/calculate/page.tsx
  - src/frontend/src/components/ui/Sidebar.tsx
autonomous: true
requirements:
  - PAY-13

must_haves:
  truths:
    - "/pages/payroll/calculate redirige a /pages/payroll/wizard (preserva compatibilidad)"
    - "Sidebar muestra enlace a /pages/payroll/wizard con label 'Nueva Planilla'"
    - "npx tsc --noEmit pasa en src/frontend/ sin errores"
  artifacts:
    - path: "src/frontend/src/app/pages/payroll/calculate/page.tsx"
      provides: "Redirect component to /pages/payroll/wizard"
      contains: "redirect"
    - path: "src/frontend/src/components/ui/Sidebar.tsx"
      provides: "Updated nav link pointing to /pages/payroll/wizard"
      contains: "/pages/payroll/wizard"
  key_links:
    - from: "Sidebar link"
      to: "/pages/payroll/wizard"
      via: "Next.js Link component"
      pattern: "/pages/payroll/wizard"
---

<objective>
Integration wiring for Phase 54 frontend: (1) redirect old `/calculate` page to new `/wizard` for backward compatibility, (2) update Sidebar navigation to point to the new wizard. This is the final integration step that makes the new wizard the default payroll creation flow.

Output: Old `/calculate` redirects; Sidebar updated; TypeScript compiles clean.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/54-rediseno-flujo-planilla/54-CONTEXT.md

<interfaces>
<!-- Next.js redirect in page component (app router): -->
```typescript
// Server component with redirect
import { redirect } from 'next/navigation';

export default function PayrollCalculateRedirectPage() {
  redirect('/pages/payroll/wizard');
}
```

<!-- Sidebar.tsx current payroll link (read to find exact nav entry): -->
<!-- Look for href containing "/payroll/calculate" or label containing "Planilla" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redirect /pages/payroll/calculate to /pages/payroll/wizard</name>
  <files>
    src/frontend/src/app/pages/payroll/calculate/page.tsx
  </files>
  <read_first>
    - src/frontend/src/app/pages/payroll/calculate/page.tsx (lines 1-5 — check if "use client" is present)
  </read_first>
  <action>
Replace the **entire content** of `src/frontend/src/app/pages/payroll/calculate/page.tsx` with a simple redirect.

Since this is an App Router page, use a **server component** with `redirect()` (do NOT keep "use client"):

```typescript
import { redirect } from 'next/navigation';

export default function PayrollCalculateRedirectPage() {
  redirect('/pages/payroll/wizard');
}
```

This preserves any bookmarks or sidebar links pointing to `/calculate` while routing users to the new wizard.

**Important:** Confirm the current file uses `"use client"` — if so, it must be removed for `redirect()` to work in a server component. The entire old page content is replaced.
  </action>
  <verify>
    <automated>cd "C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/src/frontend" && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "redirect" src/frontend/src/app/pages/payroll/calculate/page.tsx` — found
    - `grep "use client" src/frontend/src/app/pages/payroll/calculate/page.tsx` — NOT found (server component)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>/pages/payroll/calculate redirects to /pages/payroll/wizard; server component; TypeScript clean</done>
</task>

<task type="auto">
  <name>Task 2: Update Sidebar to link to /pages/payroll/wizard</name>
  <files>
    src/frontend/src/components/ui/Sidebar.tsx
  </files>
  <read_first>
    - src/frontend/src/components/ui/Sidebar.tsx (full file — find the nav entry for payroll/calculate)
  </read_first>
  <action>
Read the full `Sidebar.tsx`. Find the nav entry that links to `/pages/payroll/calculate` (or similar payroll calculation link).

Change the `href` from `/pages/payroll/calculate` to `/pages/payroll/wizard`.

If the label says something like "Calcular Planilla" or "Cálculo", update it to "Nueva Planilla" for clarity.

Do NOT change any other nav entries, styles, or logic. This is a minimal targeted change.

If there is no existing `/payroll/calculate` link in the sidebar (i.e., the payroll section only has `/payroll/list`), then **add** a new nav item for "Nueva Planilla" pointing to `/pages/payroll/wizard`, following the exact same pattern as the existing nav items (same className, same icon usage style).
  </action>
  <verify>
    <automated>cd "C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/src/frontend" && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "/pages/payroll/wizard" src/frontend/src/components/ui/Sidebar.tsx` — at least 1 line
    - `npx tsc --noEmit` exits 0 in src/frontend/
  </acceptance_criteria>
  <done>Sidebar has link to /pages/payroll/wizard; TypeScript clean</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-14 | Spoofing | Redirect target URL | accept | Redirect is hardcoded to internal route; no user input involved |
</threat_model>

<verification>
1. Navigate to `/pages/payroll/calculate` in browser → automatically redirected to `/pages/payroll/wizard`
2. Sidebar shows "Nueva Planilla" link pointing to `/pages/payroll/wizard`
3. Clicking Sidebar link opens the 4-step wizard page correctly
4. `npx tsc --noEmit` in src/frontend/ exits 0
5. `npm run build` in src/frontend/ succeeds (optional — only if explicitly requested)
</verification>

<success_criteria>
- /calculate redirects to /wizard (backward compatible)
- Sidebar navigation updated
- TypeScript compiles clean across entire frontend
</success_criteria>

<output>
After completion, create `.planning/phases/54-rediseno-flujo-planilla/54-05-SUMMARY.md` with:
- Files modified
- Redirect working (confirmed via browser or server component check)
- Sidebar entry added/updated
- Final `npx tsc --noEmit` result
- Any deviations from plan

Also update ROADMAP.md: change Phase 54 status from "Not Started" to "In Progress".
</output>
