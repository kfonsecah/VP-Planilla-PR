---
phase: 27
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/frontend/package.json, src/frontend/eslint.config.mjs, src/backend/package.json, src/backend/eslint.config.mjs, .planning/phases/27-monolith-decomposition-and-maintainability/27-AUDIT-REPORT.md]
autonomous: true
requirements: [MOD-01]

must_haves:
  truths:
    - "Existen reportes de complejidad para frontend y backend"
    - "Se han identificado los 5 archivos con mayor complejidad cognitiva"
  artifacts:
    - path: ".planning/phases/27-monolith-decomposition-and-maintainability/27-AUDIT-REPORT.md"
      provides: "Lista priorizada de candidatos para refactor"
    - path: "src/backend/eslint.config.mjs"
      provides: "Configuración de ESLint 9 para el backend"
---

<objective>
Instalar herramientas de análisis de complejidad (eslint-plugin-sonarjs) y realizar una auditoría empírica para confirmar los objetivos de refactorización.

Propósito: Tener métricas objetivas que justifiquen el refactor y guíen la descomposición de archivos.
Salida: Configuración de ESLint con reglas de complejidad y un reporte de auditoría.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-monolith-decomposition-and-maintainability/27-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Configurar ESLint 9 y SonarJS en Frontend</name>
  <files>src/frontend/package.json, src/frontend/eslint.config.mjs</files>
  <action>
    1. Instalar `eslint-plugin-sonarjs@^1.0.0` en el directorio frontend.
    2. Actualizar `src/frontend/eslint.config.mjs` para importar el plugin y usar `sonarjs.configs.recommended`.
    3. Configurar la regla `sonarjs/cognitive-complexity` con un umbral de 15.
    4. Ejecutar `npm run lint` para verificar que la configuración sea válida (aunque falle por complejidad).
  </action>
  <verify>
    <automated>cd src/frontend && npx eslint . --max-warnings 1000</automated>
  </verify>
  <done>Frontend tiene ESLint con SonarJS configurado y funcional.</done>
</task>

<task type="auto">
  <name>Configurar ESLint 9 y SonarJS en Backend</name>
  <files>src/backend/package.json, src/backend/eslint.config.mjs</files>
  <action>
    1. Instalar dependencias en el directorio backend: `eslint@^9.0.0`, `eslint-plugin-sonarjs@^1.0.0`, `@typescript-eslint/parser`, `@typescript-eslint/eslint-plugin`, `globals`.
    2. Crear `src/backend/eslint.config.mjs` con soporte para TypeScript y SonarJS (Flat Config).
    3. Configurar la regla `sonarjs/cognitive-complexity` con un umbral de 15.
    4. Añadir script `"lint": "eslint ."` al `package.json` del backend.
  </action>
  <verify>
    <automated>cd src/backend && npx eslint . --max-warnings 1000</automated>
  </verify>
  <done>Backend tiene ESLint con SonarJS configurado y funcional.</done>
</task>

<task type="auto">
  <name>Generar Reporte de Auditoría de Complejidad</name>
  <files>.planning/phases/27-monolith-decomposition-and-maintainability/27-AUDIT-REPORT.md</files>
  <action>
    1. Ejecutar lint en ambos proyectos capturando la salida de complejidad.
    2. Identificar los archivos que superan el umbral de 15 en complejidad cognitiva.
    3. Crear el archivo `27-AUDIT-REPORT.md` listando el "Top 5 Monolitos" con su score de complejidad.
    4. Confirmar que `ClockLogsDashboardPage` (frontend) y `ClockLogsController` (backend) están en la lista (o justificar su inclusión si los scores son bajos pero el tamaño es grande).
  </action>
  <verify>
    <automated>test -f ".planning/phases/27-monolith-decomposition-and-maintainability/27-AUDIT-REPORT.md"</automated>
  </verify>
  <done>Reporte de auditoría generado con candidatos claros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Toolchain | External plugins (sonarjs) added to devDependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | ESLint Config | mitigate | Lock versions in package.json and verify config integrity. |
</threat_model>

<verification>
Verificar que `npm run lint` (o comando equivalente) reporte errores de complejidad en los archivos identificados como monolitos.
</verification>

<success_criteria>
- ESLint 9 con SonarJS configurado en frontend y backend.
- Reporte 27-AUDIT-REPORT.md creado con métricas reales.
</success_criteria>

<output>
After completion, create `.planning/phases/27-monolith-decomposition-and-maintainability/27-01-SUMMARY.md`
</output>
