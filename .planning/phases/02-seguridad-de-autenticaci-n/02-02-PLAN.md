---
phase: 02-seguridad-de-autenticaci-n
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/src/index.ts
  - src/backend/src/controller/AuthController.ts
  - src/backend/src/service/PayrollService.ts
  - .gitignore
autonomous: true
requirements: [2.3, 2.4, 2.5, 2.6]

must_haves:
  truths:
    - "Server refuses to start when JWT_SECRET is not set in environment"
    - "Login endpoint only reads credentials from req.body, never from query params"
    - "PayrollService.updatePayroll throws a real Error object, not undefined"
    - "No temporary debug scripts exist in the repository"
  artifacts:
    - path: "src/backend/src/index.ts"
      provides: "JWT_SECRET startup assertion"
      contains: "process.exit(1)"
    - path: "src/backend/src/controller/AuthController.ts"
      provides: "Login credential parsing from body only"
    - path: "src/backend/src/service/PayrollService.ts"
      provides: "Correct error throw in updatePayroll"
      contains: "throw new Error"
  key_links:
    - from: "src/backend/src/index.ts"
      to: "process.env.JWT_SECRET"
      via: "startup assertion before route mounting"
      pattern: "if \\(!process\\.env\\.JWT_SECRET\\)"
    - from: "src/backend/src/service/PayrollService.ts"
      to: "Error class"
      via: "throw new Error"
      pattern: "throw new Error\\("
---

<objective>
Fix four surgical security and correctness issues: JWT startup assertion, login query-param removal, PayrollService throw-undefined bug, and temp file cleanup.

Purpose: Ensure the server cannot run with a hardcoded JWT secret, credentials cannot leak via URL query strings, PayrollService does not throw undefined, and debug scripts are removed from the repo.
Output: 4 files modified + 5 temp files deleted + .gitignore updated.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-seguridad-de-autenticaci-n/02-RESEARCH.md

<interfaces>
<!-- src/backend/src/index.ts — dotenv.config() is on line 22, routes mount after -->
<!-- src/backend/src/controller/AuthController.ts — lines 12-15 have the query param fallback -->
<!-- src/backend/src/service/PayrollService.ts — line 3 has bad import, line 132 has throw error() -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JWT_SECRET startup assertion and remove login query-param fallback</name>
  <files>
    src/backend/src/index.ts,
    src/backend/src/controller/AuthController.ts
  </files>
  <read_first>
    CLAUDE.md,
    src/backend/src/index.ts,
    src/backend/src/controller/AuthController.ts
  </read_first>
  <action>
    **Part A — JWT_SECRET startup assertion (REQ 2.3):**

    In `src/backend/src/index.ts`, add the following 4 lines IMMEDIATELY AFTER the `dotenv.config();` call (which is around line 22), BEFORE any route or middleware setup:

    ```typescript
    if (!process.env.JWT_SECRET) {
      console.error('FATAL: JWT_SECRET environment variable is not set. Server will not start.');
      process.exit(1);
    }
    ```

    Do NOT modify any other part of index.ts. Do NOT remove the fallback strings in AuthService.ts — that is out of scope for Phase 2.

    **Part B — Remove login query-param fallback (REQ 2.4):**

    In `src/backend/src/controller/AuthController.ts`, find lines 12-15 which currently read:

    ```typescript
    const credentials: LoginCredentials = {
      username: req.body.username || req.query.username as string,
      password: req.body.password || req.query.password as string
    };
    ```

    Change them to:

    ```typescript
    const credentials: LoginCredentials = {
      username: req.body.username,
      password: req.body.password
    };
    ```

    Remove ONLY the `|| req.query.username as string` and `|| req.query.password as string` parts. Do not touch anything else in this file. The existing validation logic on lines 23-38 already handles missing/empty credentials.
  </action>
  <verify>
    <automated>cd "C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/src/backend" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "process.exit(1)" src/backend/src/index.ts` returns at least 1
    - `grep -c "JWT_SECRET" src/backend/src/index.ts` returns at least 1
    - `grep "req.query" src/backend/src/controller/AuthController.ts` returns 0 results
    - `grep "req.body.username" src/backend/src/controller/AuthController.ts` returns at least 1 result
    - `cd src/backend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Server exits at startup if JWT_SECRET is missing. Login only reads credentials from req.body. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Fix PayrollService throw-undefined bug and delete temp files</name>
  <files>
    src/backend/src/service/PayrollService.ts,
    .gitignore,
    parse_tmp.js,
    temp_script.py,
    test_hours.js,
    src/backend/check_employee.ts,
    src/backend/query_emp.mjs
  </files>
  <read_first>
    CLAUDE.md,
    src/backend/src/service/PayrollService.ts,
    .gitignore
  </read_first>
  <action>
    **Part A — Fix PayrollService throw-undefined (REQ 2.6):**

    In `src/backend/src/service/PayrollService.ts`:

    1. DELETE line 3 entirely — the line that reads:
       `import { error } from "console";`

    2. Find the line (around line 132 after deletion shifts lines) that reads:
       `if (!updatedPayroll) throw error("Payroll not found");`
       Change it to:
       `if (!updatedPayroll) throw new Error('Payroll not found');`

    No other changes to this file.

    **Part B — Delete 5 temp files (REQ 2.5):**

    Delete these 5 files using `rm` (or git rm):
    - `parse_tmp.js` (project root)
    - `temp_script.py` (project root)
    - `test_hours.js` (project root)
    - `src/backend/check_employee.ts` (src/backend/)
    - `src/backend/query_emp.mjs` (src/backend/)

    **Part C — Update .gitignore:**

    Add the following block to the end of `.gitignore`:

    ```
    # Temp/debug scripts — do not commit
    parse_tmp.js
    temp_script.py
    test_hours.js
    check_employee.ts
    query_emp.mjs
    *.tmp.js
    *.tmp.ts
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/src/backend" && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import { error }" src/backend/src/service/PayrollService.ts` returns 0 results
    - `grep "throw new Error" src/backend/src/service/PayrollService.ts` returns at least 1 result
    - `ls "C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/parse_tmp.js" 2>&1` returns "No such file"
    - `ls "C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/temp_script.py" 2>&1` returns "No such file"
    - `ls "C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/test_hours.js" 2>&1` returns "No such file"
    - `ls "C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/src/backend/check_employee.ts" 2>&1` returns "No such file"
    - `ls "C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/src/backend/query_emp.mjs" 2>&1` returns "No such file"
    - `grep "parse_tmp" .gitignore` returns at least 1 result
    - `cd src/backend && npx tsc --noEmit` exits 0
    - `cd src/backend && npm test` passes
  </acceptance_criteria>
  <done>PayrollService throws real Error objects. All 5 temp files deleted. .gitignore updated to prevent re-committing debug scripts. TypeScript compiles and tests pass.</done>
</task>

</tasks>

<verification>
1. `grep "import { error }" src/backend/src/service/PayrollService.ts` — must return empty
2. `grep "throw new Error" src/backend/src/service/PayrollService.ts` — must return a match
3. `grep "process.exit" src/backend/src/index.ts` — must return a match
4. `grep "req.query" src/backend/src/controller/AuthController.ts` — must return empty
5. 5 temp files no longer exist on disk
6. `cd src/backend && npx tsc --noEmit` — 0 errors
7. `cd src/backend && npm test` — all tests pass
</verification>

<success_criteria>
- JWT_SECRET assertion prevents server startup without the env var
- Login reads credentials only from req.body
- PayrollService.updatePayroll throws `new Error('Payroll not found')` instead of `throw undefined`
- 5 temp files deleted from repo
- .gitignore prevents re-committing debug scripts
- TypeScript compiles cleanly, existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-seguridad-de-autenticaci-n/02-02-SUMMARY.md`
</output>
