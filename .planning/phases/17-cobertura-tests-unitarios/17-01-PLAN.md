---
phase: 17-cobertura-tests-unitarios
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/src/__tests__/unit/services/VacationService.test.ts
  - src/backend/src/__tests__/unit/services/PayrollTypeService.test.ts
  - src/backend/src/__tests__/unit/services/PositionService.test.ts
  - src/backend/src/__tests__/unit/services/BonusesService.test.ts
  - src/backend/src/__tests__/unit/services/EmployeeDeductionsService.test.ts
autonomous: true
requirements:
  - COV-01

must_haves:
  truths:
    - "All 5 new test files exist and pass with 0 failures"
    - "Existing 104 tests continue to pass after adding new test files"
    - "Statement coverage is measurably higher after this plan (~44% from baseline ~32%)"
  artifacts:
    - path: "src/backend/src/__tests__/unit/services/VacationService.test.ts"
      provides: "Tests for all 5 VacationService methods including P2021 swallowing branch"
    - path: "src/backend/src/__tests__/unit/services/PayrollTypeService.test.ts"
      provides: "Tests for all 4 PayrollTypeService methods including $disconnect finally block"
    - path: "src/backend/src/__tests__/unit/services/PositionService.test.ts"
      provides: "Tests for all 5 PositionService methods including Decimal mock chain"
    - path: "src/backend/src/__tests__/unit/services/BonusesService.test.ts"
      provides: "Tests for all 4 BonusesService methods including null-guard paths"
    - path: "src/backend/src/__tests__/unit/services/EmployeeDeductionsService.test.ts"
      provides: "Tests for assignDeductionToEmployee and removeDeductionFromEmployee"
  key_links:
    - from: "test files"
      to: "src/backend/src/lib/prisma"
      via: "jest.mock inline pattern — NOT prisma-mock.ts"
      pattern: "jest\\.mock\\('\\.\\.\\.lib/prisma'"
---

<objective>
Create 5 unit test files for the simplest CRUD services. Each test file follows the established inline mock pattern from EmployeeService.test.ts and DeductionsService.test.ts exactly.

Purpose: Raise statement coverage from ~32% to ~44% by covering the five lowest-complexity, highest-uncovered-statement services.
Output: 5 new test files, ~40-55 new passing tests.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.planning/ROADMAP.md

Canonical test pattern (from EmployeeService.test.ts and DeductionsService.test.ts):

```typescript
import { PrismaClient } from '@prisma/client';
import { mockDeep } from 'jest-mock-extended';
import { XyzService } from '../../../service/XyzService';

jest.mock('../../../lib/prisma', () => {
  const mock = mockDeep<PrismaClient>();
  return { prisma: mock };
});

const { prisma } = require('../../../lib/prisma');

// mock Prisma row (snake_case, vpg_tablename_fieldname columns)
const mockPrismaXyz = { ... };

// domain model factory
function makeXyz(input = {}) { return { ...defaults, ...input }; }

beforeEach(() => {
  jest.clearAllMocks();
  prisma.vpg_table.findUnique.mockResolvedValue(null);
  prisma.vpg_table.findMany.mockResolvedValue([]);
  // ...
});

describe('XyzService', () => {
  describe('methodName', () => {
    it('should ...', async () => { ... });
  });
});
```

Key rules:
- NEVER import from `../../../__tests__/setup/prisma-mock.ts` — use inline jest.mock
- `jest.clearAllMocks()` in beforeEach (not mockReset)
- Prisma error code P2025 is simulated via `(error as any).code = 'P2025'`
- `expect.objectContaining({...})` for partial create/update data assertions
</context>

<tasks>

<task type="auto">
  <name>Task 1.1 — Create VacationService.test.ts and PayrollTypeService.test.ts</name>
  <files>
    src/backend/src/__tests__/unit/services/VacationService.test.ts
    src/backend/src/__tests__/unit/services/PayrollTypeService.test.ts
  </files>
  <action>
Read `src/backend/src/service/VacationService.ts` and `src/backend/src/service/PayrollTypeService.ts` to confirm method signatures before writing tests.

**VacationService.test.ts** — mock table: `vpg_vacations`. Prisma row fields:
`vacations_id`, `vacations_employee_id`, `vacations_start_date`, `vacations_end_date`,
`vacations_total_days`, `vacations_paid`, `vacations_status`, `vacations_version`.

Test cases to write (10 total):
1. `getVacationById` — found: returns mapped Vacation object with correct field mapping
2. `getVacationById` — not found: returns null
3. `getVacationById` — DB fails: propagates error
4. `getAllVacations` — returns mapped array
5. `getAllVacations` — empty table: returns []
6. `getAllVacations` — error message contains 'does not exist': silently returns [] (special branch)
7. `getAllVacations` — error message contains 'p2021': silently returns []
8. `getAllVacations` — other DB error: propagates (does NOT swallow)
9. `createVacation` — creates and returns mapped vacation
10. `updateVacation` — updates and returns mapped vacation with incremented version
11. `deleteVacation` — happy path: returns true
12. `deleteVacation` — DB error: propagates

For `beforeEach`, set: `prisma.vpg_vacations.findUnique.mockResolvedValue(null)`, `prisma.vpg_vacations.findMany.mockResolvedValue([])`, `prisma.vpg_vacations.create.mockResolvedValue(mockPrismaVacation)`, `prisma.vpg_vacations.update.mockResolvedValue(mockPrismaVacation)`, `prisma.vpg_vacations.delete.mockResolvedValue(mockPrismaVacation)`.

---

**PayrollTypeService.test.ts** — mock table: `vpg_payroll_types`. Prisma row fields:
`payroll_types_id`, `payroll_types_name`, `payroll_types_description`, `payroll_types_version`.

CRITICAL: `createPayrollType` has a `finally { await prisma.$disconnect() }` block. The mock MUST include `prisma.$disconnect.mockResolvedValue(undefined)` in `beforeEach`, otherwise Jest will throw "not a function".

Test cases to write (9 total):
1. `createPayrollType` — happy path: creates and returns PayrollType with correct field mapping
2. `createPayrollType` — DB fails: wraps error in "Failed to create payroll type" message (the service catches and re-throws with that message)
3. `createPayrollType` — $disconnect is called in finally even on success
4. `getAllPayrollTypes` — returns mapped array
5. `getAllPayrollTypes` — empty: returns []
6. `getAllPayrollTypes` — DB fails: propagates
7. `getPayrollTypeById` — found: returns PayrollType
8. `getPayrollTypeById` — not found: returns null
9. `updatePayrollType` — updates and returns PayrollType with version = (data.version ?? 0) + 1

Run after writing both files:
```
cd "C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/src/backend" && npx jest --testPathPattern="VacationService|PayrollTypeService" --no-coverage 2>&1 | tail -20
```
  </action>
  <verify>
    <automated>cd "C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/src/backend" && npx jest --testPathPattern="VacationService|PayrollTypeService" --no-coverage 2>&1 | tail -15</automated>
  </verify>
  <done>Both test files pass with 0 failures. VacationService.test.ts has 12 tests covering all 5 methods including the 'does not exist' / 'p2021' swallow branch. PayrollTypeService.test.ts has 9 tests and verifies $disconnect is called.</done>
</task>

<task type="auto">
  <name>Task 1.2 — Create PositionService.test.ts, BonusesService.test.ts, and EmployeeDeductionsService.test.ts</name>
  <files>
    src/backend/src/__tests__/unit/services/PositionService.test.ts
    src/backend/src/__tests__/unit/services/BonusesService.test.ts
    src/backend/src/__tests__/unit/services/EmployeeDeductionsService.test.ts
  </files>
  <action>
Read `src/backend/src/service/PositionService.ts`, `src/backend/src/service/BonusesService.ts`, and `src/backend/src/service/EmployeeDeductions.ts` before writing tests.

---

**PositionService.test.ts** — mock table: `vpg_positions` (findUnique, findMany, create, updateMany, deleteMany).

CRITICAL: `position_base_salary` is a Prisma `Decimal`. The mock row must return an object that supports the `.toDecimalPlaces(2).toNumber()` call chain that PositionService uses. Use this mock:
```typescript
const mockDecimal = { toDecimalPlaces: (_: number) => ({ toNumber: () => 1500.00 }) };
```
Include `position_base_salary: mockDecimal` in the mock Prisma row.

`updatePosition` calls `updateMany` (returns `{ count: N }`) then calls `this.getPositionById` (findUnique). Set up `findUnique` to return the updated mock row in `beforeEach` so the internal `getPositionById` call resolves.

`deletePosition` uses `deleteMany` and returns `prismaPosition.count > 0`.

Test cases to write (11 total):
1. `getPositionById` — found: returns Position with base_salary as number (1500.00)
2. `getPositionById` — not found: returns null
3. `getPositionById` — DB fails: propagates
4. `getAllPositions` — returns mapped array with Decimal converted
5. `getAllPositions` — empty: returns []
6. `createPosition` — creates and returns Position
7. `updatePosition` — updateMany returns count=1, then getPositionById returns updated: result not null
8. `updatePosition` — updateMany returns count=0 (version mismatch): returns null
9. `updatePosition` — DB fails: propagates
10. `deletePosition` — deleteMany returns count=1: returns true
11. `deletePosition` — deleteMany returns count=0: returns false

---

**BonusesService.test.ts** — mock table: `vpg_bonuses`. Prisma row fields:
`bonuses_id`, `bonuses_employee_id`, `bonuses_payroll_id`, `bonuses_year`, `bonuses_month`,
`bonuses_description`, `bonuses_amount`, `bonuses_granted_at`, `bonuses_version`.

`bonuses_amount` in the mock: use a plain `number` (e.g., `1500`). The service casts it with `Number(prismaBonus.bonuses_amount)` which is a no-op for a number.

`updateBonus` does a `findUnique` first, then `update` — both must be mocked. If `findUnique` returns null, `updateBonus` returns null immediately without calling `update`.
`deleteBonus` also does `findUnique` first, then `delete`.

Test cases to write (10 total):
1. `createBonus` — creates and returns Bonus with correct field mapping including amount as number
2. `createBonus` — DB fails: propagates
3. `getBonusById` — found: returns Bonus
4. `getBonusById` — not found (findUnique returns null): returns null
5. `updateBonus` — bonus exists (findUnique returns row): updates and returns Bonus
6. `updateBonus` — bonus not found (findUnique returns null): returns null without calling update
7. `updateBonus` — DB fails on findUnique: propagates
8. `deleteBonus` — bonus exists (findUnique returns row): deletes and returns Bonus
9. `deleteBonus` — bonus not found (findUnique returns null): returns null without calling delete
10. `deleteBonus` — DB fails on delete: propagates

---

**EmployeeDeductionsService.test.ts** — mock table: `vpg_deductions_per_employee`.

Note: the service class is in `src/backend/src/service/EmployeeDeductions.ts` (no 'Service' suffix in filename). Import path in test: `'../../../service/EmployeeDeductions'`. The class is still named `EmployeeDeductionsService`.

Prisma row fields: `deductions_per_employee_employee_id`, `deductions_per_employee_deduction_id`, `deductions_per_employee_version`.

`removeDeductionFromEmployee` uses the composite unique key:
```typescript
where: {
  deductions_per_employee_employee_id_deductions_per_employee_deduction_id: {
    deductions_per_employee_employee_id: employeeId,
    deductions_per_employee_deduction_id: deductionId,
  }
}
```

Test cases to write (6 total):
1. `assignDeductionToEmployee` — creates record and returns DeductionsPerEmployee with correct field mapping
2. `assignDeductionToEmployee` — DB fails: propagates
3. `removeDeductionFromEmployee` — deletes record successfully: resolves without error
4. `removeDeductionFromEmployee` — DB fails: propagates
5. `assignDeductionToEmployee` — verifies create called with correct composite data (employee_id and deduction_id)
6. `removeDeductionFromEmployee` — verifies delete called with correct composite where key

Run after writing all three files:
```
cd "C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/src/backend" && npx jest --testPathPattern="PositionService|BonusesService|EmployeeDeductionsService" --no-coverage 2>&1 | tail -15
```

Then run the full suite to confirm no regressions:
```
cd "C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/src/backend" && npm test -- --no-coverage 2>&1 | tail -20
```
  </action>
  <verify>
    <automated>cd "C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/src/backend" && npm test -- --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 new test files pass with 0 failures. Full suite (104 + new tests) passes with 0 failures. PositionService.test.ts correctly exercises the Decimal mock chain. BonusesService.test.ts covers both null-guard paths in updateBonus and deleteBonus. EmployeeDeductionsService.test.ts imports from the correct 'EmployeeDeductions' filename.</done>
</task>

</tasks>

<verification>
- [ ] `npm test -- --no-coverage` in `src/backend/` passes with 0 failures
- [ ] 5 new test files exist in `src/backend/src/__tests__/unit/services/`
- [ ] `npx tsc --noEmit` in `src/backend/` passes
- [ ] `npx jest --coverage 2>&1 | grep "Statements"` shows improvement over 31.59% baseline
</verification>

<success_criteria>
All 5 test files created and passing. Full test suite has 0 failures. Statement coverage is measurably above the 31.59% baseline (expected ~44%). No service source files modified.
</success_criteria>

<output>
After completion, create `.planning/phases/17-cobertura-tests-unitarios/17-01-SUMMARY.md`
</output>
