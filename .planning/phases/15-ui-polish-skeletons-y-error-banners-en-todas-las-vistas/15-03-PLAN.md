---
phase: 15-ui-polish-skeletons-y-error-banners-en-todas-las-vistas
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/src/app/pages/employee-deductions/list/page.tsx
  - src/frontend/src/app/pages/notifications/page.tsx
autonomous: true
requirements: [UI-POLISH-02]
gap_closure: true
must_haves:
  truths:
    - "Error banner on employee-deductions shows only on actual fetch error, not during loading"
    - "Notifications page renders exactly one error banner when error state is set"
  artifacts:
    - path: "src/frontend/src/app/pages/employee-deductions/list/page.tsx"
      provides: "Fixed error banner with correct error state condition and retry button"
      contains: "fetchError state variable"
    - path: "src/frontend/src/app/pages/notifications/page.tsx"
      provides: "Single error banner block (duplicate removed)"
      contains: "One {error && (...) block"
  key_links:
    - from: "employee-deductions error banner"
      to: "fetchEmployeeDeductions(selectedEmployeeId)"
      via: "retry button onClick handler"
      pattern: "onClick.*fetchEmployeeDeductions"
    - from: "notifications error banner"
      to: "fetchNotifications(page, limit)"
      via: "retry button onClick handler"
      pattern: "onClick.*fetchNotifications"
must_haves:
  truths:
    - "Error banner on employee-deductions shows only on actual fetch error, not during loading"
    - "Notifications page renders exactly one error banner when error state is set"
  artifacts:
    - path: "src/frontend/src/app/pages/employee-deductions/list/page.tsx"
      provides: "Fixed error banner with correct error state condition and retry button"
      contains: "fetchError state variable"
    - path: "src/frontend/src/app/pages/notifications/page.tsx"
      provides: "Single error banner block (duplicate removed)"
      contains: "One {error && (...) block"
  key_links:
    - from: "employee-deductions error banner"
      to: "fetchEmployeeDeductions(selectedEmployeeId)"
      via: "retry button onClick handler"
      pattern: "onClick.*fetchEmployeeDeductions"
    - from: "notifications error banner"
      to: "fetchNotifications(page, limit)"
      via: "retry button onClick handler"
      pattern: "onClick.*fetchNotifications"
---

<objective>
Fix broken error banner implementations on 2 pages with bugs identified in verification
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-ui-polish-skeletons-y-error-banners-en-todas-las-vistas/15-VERIFICATION.md
@src/frontend/src/app/pages/employee-deductions/list/page.tsx
@src/frontend/src/app/pages/notifications/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Fix employee-deductions error banner condition and add retry button</name>
  <files>src/frontend/src/app/pages/employee-deductions/list/page.tsx</files>
  <action>
    Current issue (line 107): Error banner condition uses `loadingDeductions && loadingEmpDeductions` which shows error-styled banner during loading, not on actual error.

    Fix:
    1. Add error state variables: `const [fetchError, setFetchError] = useState<string | null>(null);`
    2. Create useEffect to track errors from both hooks:
       - Monitor `loadingDeductions` and `loadingEmpDeductions` transitions
       - Set `fetchError` when loading completes but data is missing/failed
    3. Replace the broken condition `{loadingDeductions && loadingEmpDeductions && (...)}` with `{fetchError && (...)}`
    4. Add retry button inside error banner that calls `fetchEmployeeDeductions(selectedEmployeeId)` if selectedEmployeeId exists, otherwise calls `window.location.reload()`
    5. Clear fetchError on successful data load
  </action>
  <verify>
    <automated>npx tsc --noEmit in src/frontend/ passes</automated>
  </verify>
  <done>Error banner shows only when fetchError state is populated (not during loading), includes Reintentar button that retries the failed fetch</done>
</task>

<task type="auto">
  <name>Remove duplicate error banner from notifications page</name>
  <files>src/frontend/src/app/pages/notifications/page.tsx</files>
  <action>
    Current issue: Identical error banner blocks at lines 106-124 and 126-144. Both render when `error` state is set, causing duplicate banners.

    Fix:
    1. Delete lines 126-144 (the second duplicate error banner block)
    2. Keep lines 106-124 (the first error banner block) as it already has correct implementation with retry button
  </action>
  <verify>
    <automated>npx tsc --noEmit in src/frontend/ passes</automated>
  </verify>
  <done>Only one error banner renders when error state is set, no duplicate blocks in the JSX</done>
</task>

</tasks>

<verification>
- Error banner on employee-deductions shows only on actual error, not during loading
- Notifications page has single error banner, not duplicates
- TypeScript compilation passes
</verification>

<success_criteria>
- 2 files fixed with zero new TypeScript errors
- Error banner condition uses actual error state, not loading state
- No duplicate JSX blocks in notifications page
</success_criteria>

<output>
After completion, create `.planning/phases/15-ui-polish-skeletons-y-error-banners-en-todas-las-vistas/15-03-SUMMARY.md`
</output>