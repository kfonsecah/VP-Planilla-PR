---
phase: 15-ui-polish-skeletons-y-error-banners-en-todas-las-vistas
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/src/app/pages/payroll/[id]/page.tsx
  - src/frontend/src/app/pages/attendance/page.tsx
  - src/frontend/src/app/pages/clocklogs/list/page.tsx
  - src/frontend/src/app/pages/reports/page.tsx
autonomous: true
requirements: [UI-POLISH-02]
gap_closure: true
must_haves:
  truths:
    - "Payroll detail page shows persistent error banner with retry when API fails"
    - "Attendance page shows error banner with retry when fetch fails"
    - "Clocklogs page shows error banner with retry when fetch fails"
    - "Reports page shows error banner with retry when dashboard/dataset fetch fails"
  artifacts:
    - path: "src/frontend/src/app/pages/payroll/[id]/page.tsx"
      provides: "Error state + banner replacing toast-only error handling"
      contains: "useState.*error.*setError"
    - path: "src/frontend/src/app/pages/attendance/page.tsx"
      provides: "Error banner with retry for attendance data fetch"
      contains: "ExclamationTriangleIcon"
    - path: "src/frontend/src/app/pages/clocklogs/list/page.tsx"
      provides: "Error banner with retry for attendance summary fetch"
      contains: "ExclamationTriangleIcon"
    - path: "src/frontend/src/app/pages/reports/page.tsx"
      provides: "Error banner with retry for dashboard/dataset fetch"
      contains: "ExclamationTriangleIcon"
  key_links:
    - from: "payroll/[id]/page.tsx error banner"
      to: "loadPayrollDetails(payrollId)"
      via: "retry button onClick"
      pattern: "onClick.*loadPayrollDetails"
    - from: "attendance/page.tsx error banner"
      to: "fetch function"
      via: "retry button onClick"
      pattern: "onClick.*handleFetch|fetchAttendance"
    - from: "clocklogs/list/page.tsx error banner"
      to: "handleFetch()"
      via: "retry button onClick"
      pattern: "onClick.*handleFetch"
    - from: "reports/page.tsx error banner"
      to: "loadDashboard()"
      via: "retry button onClick"
      pattern: "onClick.*loadDashboard"
---

<objective>
Add missing error banners with retry to 4 pages that currently have no error feedback on fetch failures
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-ui-polish-skeletons-y-error-banners-en-todas-las-vistas/15-VERIFICATION.md
@src/frontend/src/app/pages/payroll/[id]/page.tsx
@src/frontend/src/app/pages/attendance/page.tsx
@src/frontend/src/app/pages/clocklogs/list/page.tsx
@src/frontend/src/app/pages/reports/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Add error state and banner to payroll detail page</name>
  <files>src/frontend/src/app/pages/payroll/[id]/page.tsx</files>
  <action>
    Current issue: payroll/[id]/page.tsx catches errors but only shows toast.error (line 55), no persistent error banner with retry.

    Fix:
    1. Add error state: `const [error, setError] = useState<string | null>(null);`
    2. In loadPayrollDetails catch block: replace `toast.error(message)` with `setError(message)`
    3. Add error banner component (before the `if (isLoading)` check) using the established pattern:
       - Red border + bg-red-50/dark:bg-red-950/50
       - ExclamationTriangleIcon from @heroicons/react/24/outline
       - Error message display
       - "Reintentar" button that calls `loadPayrollDetails(payrollId)`
    4. Clear error state on successful load: `setError(null)` at start of loadPayrollDetails
    5. Show error banner when `error` is not null, regardless of loading state
  </action>
  <verify>
    <automated>npx tsc --noEmit in src/frontend/ passes</automated>
  </verify>
  <done>Payroll detail page shows persistent error banner with retry button when API call fails, instead of transient toast</done>
</task>

<task type="auto">
  <name>Add error banners to attendance, clocklogs, and reports pages</name>
  <files>
    src/frontend/src/app/pages/attendance/page.tsx
    src/frontend/src/app/pages/clocklogs/list/page.tsx
    src/frontend/src/app/pages/reports/page.tsx
  </files>
  <action>
    For each page, add error state and banner using the established pattern from Phase 15:

    **attendance/page.tsx:**
    - Add `const [error, setError] = useState<string | null>(null);`
    - In fetch catch blocks: `setError(err instanceof Error ? err.message : 'Error al cargar asistencia')`
    - Add error banner before loading skeleton, after page header
    - Retry button calls the fetch function
    - Clear error on successful fetch

    **clocklogs/list/page.tsx:**
    - Add `const [error, setError] = useState<string | null>(null);`
    - In handleFetch catch block: add `setError(err instanceof Error ? err.message : 'Error al obtener registros')`
    - Add error banner before the date filter section
    - Retry button calls `handleFetch()`
    - Clear error at start of handleFetch

    **reports/page.tsx:**
    - Add `const [error, setError] = useState<string | null>(null);`
    - In loadDashboard catch block: `setError(err instanceof Error ? err.message : 'Error al cargar dashboard')`
    - In loadDataset catch blocks: `setError(err instanceof Error ? err.message : 'Error al cargar datos')`
    - Add error banner before the main content area
    - Retry button calls `loadDashboard()`
    - Clear error on successful loads

    Error banner pattern (consistent across all pages):
    ```tsx
    {error && (
      <div className="mb-4 overflow-auto rounded-lg border border-red-200 dark:border-red-800">
        <div className="bg-red-50 dark:bg-red-950/50 p-6 text-center">
          <ExclamationTriangleIcon className="w-10 h-10 mx-auto mb-3 text-red-500 dark:text-red-400" />
          <p className="text-sm font-medium text-red-800 dark:text-red-200 mb-1">Error al cargar datos</p>
          <p className="text-xs text-red-600 dark:text-red-400 mb-4">{error}</p>
          <button
            onClick={retryFunction}
            className="flex items-center gap-2 mx-auto px-4 py-2 bg-red-600 hover:bg-red-700 text-white rounded-lg text-sm font-medium transition-colors"
          >
            <ArrowPathIcon className="w-4 h-4" />
            Reintentar
          </button>
        </div>
      </div>
    )}
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit in src/frontend/ passes</automated>
  </verify>
  <done>All 3 pages show error banners with retry buttons on fetch failures, no more silent console.error or modal-only error handling</done>
</task>

</tasks>

<verification>
- All 4 pages have error banners with ExclamationTriangleIcon + Reintentar button
- Error banners appear on API failures, not just during loading
- TypeScript compilation passes with no new errors
- Error state is cleared on successful data loads
</verification>

<success_criteria>
- 4 files modified with consistent error banner pattern
- Zero new TypeScript errors
- All error banners include retry functionality
- No more toast-only or console.error-only error handling for data fetches
</success_criteria>

<output>
After completion, create `.planning/phases/15-ui-polish-skeletons-y-error-banners-en-todas-las-vistas/15-04-SUMMARY.md`
</output>