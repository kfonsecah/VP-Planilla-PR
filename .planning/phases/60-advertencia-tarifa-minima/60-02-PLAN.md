---
phase: 60
plan: 02
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - "src/frontend/src/hooks/usePayrollWizard.ts"
  - "src/frontend/src/app/pages/payroll/wizard/page.tsx"
autonomous: true
requirements:
  - PAY-25
must_haves:
  truths:
    - "El Payroll Wizard muestra una advertencia visual si el salario de un empleado es menor a la tarifa mínima global."
    - "La advertencia no bloquea la selección de empleados ni el cálculo."
  artifacts:
    - path: "src/frontend/src/app/pages/payroll/wizard/page.tsx"
      provides: "UI icon (Tooltip/Badge) para advertir salario bajo."
    - path: "src/frontend/src/hooks/usePayrollWizard.ts"
      provides: "Lógica de auto-fetch de parámetros legales al montar el hook."
  key_links:
    - from: "src/frontend/src/hooks/usePayrollWizard.ts"
      to: "src/frontend/src/services/legalParamService.ts"
      via: "useEffect en el montaje del hook."
---

<objective>
Implementar la advertencia visual no bloqueante en el Payroll Wizard para notificar al usuario cuando el salario configurado de un empleado es inferior a la tarifa mínima global estipulada.

Purpose: Dar cumplimiento a PAY-25 brindando visibilidad de posibles infracciones de salario mínimo sin impedir el flujo normal de cálculo.
Output: Hook usePayrollWizard con auto-fetch y UI del Wizard (Paso 2) actualizada con íconos de advertencia.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/60-advertencia-tarifa-minima/60-CONTEXT.md
@.planning/phases/60-advertencia-tarifa-minima/60-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch de parámetros en usePayrollWizard</name>
  <read_first>
    - src/frontend/src/hooks/usePayrollWizard.ts
    - src/frontend/src/services/legalParamService.ts
  </read_first>
  <files>src/frontend/src/hooks/usePayrollWizard.ts</files>
  <action>
    Modificar `usePayrollWizard.ts` para incluir un `useEffect` que se ejecute una sola vez al montar el hook.
    Dentro del `useEffect`, realizar las llamadas:
    - `LegalParamService.getParam('MIN_WAGE_CHECK_ENABLED')`
    - `LegalParamService.getParam('GLOBAL_MIN_WAGE_RATE')`
    
    Actualizar los estados correspondientes (`minWageCheckEnabled` y `globalMinWageRate`) con los valores obtenidos.
    Implementar manejo de errores silencioso (catch) que deje los valores en su estado inicial (null/default) para no interrumpir el flujo del usuario si la API falla.
  </action>
  <acceptance_criteria>
    - El hook `usePayrollWizard` dispara los requests al montarse.
    - Los estados se actualizan correctamente tras la respuesta de la API.
    - Fallos de red no bloquean el wizard.
  </acceptance_criteria>
  <verify>
    <automated>cd src/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>La lógica de obtención de parámetros está centralizada en el hook y ocurre automáticamente al iniciar el wizard.</done>
</task>

<task type="auto">
  <name>Task 2: Renderizar advertencia en Paso 2</name>
  <read_first>
    - src/frontend/src/app/pages/payroll/wizard/page.tsx
  </read_first>
  <files>src/frontend/src/app/pages/payroll/wizard/page.tsx</files>
  <action>
    En el renderizado de la lista de empleados del Paso 2 de `page.tsx` (donde se itera `employees.map(emp => ...)`):
    Extraer `minWageCheckEnabled` y `globalMinWageRate` del hook `usePayrollWizard`.
    
    Evaluar la condición: `minWageCheckEnabled === 1 && Number(emp.salary) < Number(globalMinWageRate)`.
    Si la condición se cumple, renderizar un ícono de advertencia (⚠️ o `ExclamationTriangleIcon` de Heroicons en color `text-yellow-500`) junto al nombre o salario del empleado.
    Envolver el ícono en un Tooltip (usando `@/components/ui/Tooltip` o un div con `group-hover` si no existe componente) que muestre el texto:
    `"Salario inferior a la tarifa mínima global (₡X.XX)"` (reemplazando X.XX por el valor de `globalMinWageRate`).
  </action>
  <acceptance_criteria>
    - El ícono de advertencia se muestra condicionalmente según el salario y el toggle global.
    - Se utiliza `Number()` para evitar bugs de comparación léxica entre strings.
    - La interfaz no bloquea la selección del checkbox del empleado.
  </acceptance_criteria>
  <verify>
    <automated>cd src/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Los usuarios ven advertencias claras y con el monto de referencia cuando el salario es bajo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | Safe read of configuration. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-02 | Tampering | UI Warning | accept | Informational warning only; user can proceed regardless. Logic cannot be maliciously altered to cause business impact. |
</threat_model>

<verification>
- Compilación limpia con `npx tsc --noEmit`.
</verification>

<success_criteria>
- El Wizard muestra la advertencia si se cumplen las condiciones y el toggle global está activo.
</success_criteria>

<output>
After completion, create `.planning/phases/60-advertencia-tarifa-minima/60-02-SUMMARY.md`
</output>
