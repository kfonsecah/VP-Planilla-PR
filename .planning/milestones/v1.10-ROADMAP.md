# Milestone v1.10: Production Hardening & Developer Experience

**Status:** ✅ SHIPPED 2026-05-22
**Phases:** 74-76
**Total Plans:** 4

## Overview

Milestone v1.10 hardened the production stack with full-stack error observability (Sentry), HTTP Parameter Pollution protection, global query normalization for Express 5 safety, automated Conventional Commits enforcement via Husky, and auto-generated database documentation from Prisma.

## Phases

### Phase 74: Standards & Git Hygiene

**Goal**: Establecer estándares de repositorio y documentación de base de datos automatizada.
**Depends on**: Nothing (start of milestone)
**Plans**: 2 plans

- [x] 74-01: Configure Husky and Commitlint at root
- [x] 74-02: Integrate prisma-markdown in backend

**Details:**
- 74-01: Added root `package.json`, initialized Husky, configured `commit-msg` hook → `commitlint`. Non-conventional commits are now rejected at commit time.
- 74-02: Integrated `prisma-markdown@3.0.1` as a Prisma generator. `npm run dbml` regenerates `src/backend/prisma/dbml/schema.md` automatically.

---

### Phase 75: Security & API Hardening

**Goal**: Asegurar la integridad de la API contra ataques de polución de parámetros y asegurar estabilidad en Express 5 mediante normalización global.
**Depends on**: Phase 74
**Plans**: 1 plan

- [x] 75-01: HPP middleware + global req.query normalization

**Details:**
- Created `src/backend/src/middleware/queryNormalizer.ts` exporting `normalizeQuery` (Object.defineProperty shadow for Express 5 safety) and `preventParameterPollution` (hpp with whitelist).
- Applied `querySecurityMiddleware = [normalizeQuery, preventParameterPollution]` globally in `index.ts` after body parsers, before all routes.

---

### Phase 76: Error Observability & Tracing

**Goal**: Visibilidad total de errores en producción vinculando el comportamiento del cliente con el servidor.
**Depends on**: Phase 75
**Plans**: 1 plan

- [x] 76-01: Sentry full-stack integration (backend + frontend + distributed tracing)

**Details:**
- Backend: `src/backend/instrument.js` loaded via `--import ./instrument.js` in dev/start scripts. `Sentry.setupExpressErrorHandler(app)` registered after all routes.
- Frontend: `src/frontend/src/instrumentation.ts` + `sentry.{server,client,edge}.config.ts`. `withSentryConfig` in `next.config.ts` with `tunnelRoute: "/monitoring-tunnel"`.
- Distributed tracing: `tracesSampleRate: 1.0` on both sides sharing DSN.

---

## Milestone Summary

**Key Decisions:**
- Used `Object.defineProperty` to shadow `req.query` getter in Express 5 instead of mutating it directly — required for Express 5 compatibility.
- Swapped `prisma-dbml-generator` for `prisma-markdown` due to Prisma 6 compatibility issues.
- Used `--import ./instrument.js` (Node.js ESM loader) for Sentry backend instrumentation before any module loads.
- Added `tunnelRoute: '/monitoring-tunnel'` to prevent ad-blockers from dropping Sentry client events.

**Issues Resolved:**
- HPP vulnerability: duplicate query params now normalized (last-value-wins) before reaching any controller.
- Sentry deprecation warnings (`disableLogger`, `automaticVercelMonitors`) cleaned up from `next.config.ts`.

**Issues Deferred:**
- `260404-o3p-implementar-sessionstorage-cache-en-hook` — quick task carried since v1.2, acknowledged at milestone close.
- Zod validation for query params (full refactor deferred as tech debt).
- DX-03: Semantic Release auto-versioning — explicitly deferred to future milestone.
- OBS-04: Sentry Session Replay — explicitly deferred to future milestone.

**Technical Debt Incurred:**
- No VERIFICATION.md generated for phases 74, 75, 76 (workflow gap).
- REQUIREMENTS.md checkboxes not updated after Phase 75/76 execution (stale).
- `SENTRY_DSN` / `NEXT_PUBLIC_SENTRY_DSN` env vars not documented in `.env.example`.
- STATE.md and ROADMAP.md progress table not updated after Phase 76 execution until close.
- Nyquist VALIDATION.md missing for all 3 v1.10 phases.

---

_For current project status, see .planning/ROADMAP.md_
