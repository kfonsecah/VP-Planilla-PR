---
phase: 73-data-integrity-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/src/__tests__/unit/service/IntegrityService.test.ts
  - src/backend/src/service/IntegrityService.ts
  - src/backend/src/controller/IntegrityController.ts
  - src/backend/src/routes/IntegrityRoute.ts
  - src/backend/src/index.ts
  - src/frontend/src/services/integrityService.ts
  - src/frontend/src/hooks/useIntegrityDashboard.ts
  - src/frontend/src/components/integrity/IntegrityHealthScore.tsx
  - src/frontend/src/components/integrity/IntegrityAlertList.tsx
  - src/frontend/src/app/pages/configuracion/integridad/page.tsx
  - src/frontend/src/app/pages/configuracion/page.tsx
autonomous: true
requirements: [INT-73-01, INT-73-02]
must_haves:
  truths:
    - "User can view a health score representing overall data quality"
    - "User can see a list of specific integrity alerts grouped by category"
    - "User can trigger a manual integrity audit from the dashboard"
    - "The system detects missing IDs, invalid formats, and calculation drifts"
    - "IntegrityService has >90% test coverage for its 7 core rules"
  artifacts:
    - path: "src/backend/src/service/IntegrityService.ts"
      provides: "Core rule engine for 7 data checks"
    - path: "src/backend/src/__tests__/unit/service/IntegrityService.test.ts"
      provides: "Unit tests for integrity rules"
    - path: "src/frontend/src/app/pages/configuracion/integridad/page.tsx"
      provides: "Dashboard UI for data integrity"
  key_links:
    - from: "src/frontend/src/services/integrityService.ts"
      to: "/api/integrity/dashboard"
      via: "http.get"
    - from: "src/backend/src/controller/IntegrityController.ts"
      to: "IntegrityService.ts"
      via: "static method calls"
---

<objective>
Implement the Data Integrity Dashboard and Backend Rule Engine with full test coverage.
Purpose: Detect and visualize data debt (missing IDs, incomplete metadata) and logical debt (calculation drift, orphan marks) to ensure compliance and auditability.
Output: Backend integrity engine (7 rules), unit tests, specialized API (/api/integrity/dashboard), and interactive frontend dashboard.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/73-data-integrity-analytics/73-RESEARCH.md
@src/backend/prisma/schema.prisma
@src/backend/src/index.ts
@src/frontend/src/app/pages/configuracion/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend Integrity Engine & Unit Tests (TDD)</name>
  <files>src/backend/src/service/IntegrityService.ts, src/backend/src/__tests__/unit/service/IntegrityService.test.ts</files>
  <behavior>
    - EMP-001: Detects missing employee_national_id.
    - EMP-002: Detects invalid ID format (9 digits for physical, 10 for legal/DIMEX).
    - POS-001: Detects missing position_occupation_code or position_risk_class.
    - PAY-001: Calculation Drift (compare gross salary vs components with 0.01 tolerance).
    - PAY-002: Approved payrolls missing snapshots.
    - CLK-001: Detects orphan status logs.
    - CLK-002: Detects open sessions (> 16 hours).
  </behavior>
  <action>
    1. Create `src/backend/src/__tests__/unit/service/IntegrityService.test.ts`:
       - Write tests for each of the 7 rules using mocked Prisma.
       - Ensure edge cases like 0.01 drift tolerance and 16-hour session thresholds are tested.
    2. Implement `src/backend/src/service/IntegrityService.ts`:
       - Implement static method `runAudit()` that executes all 7 rules.
       - Each rule should return an `IntegrityAlert` object as defined in RESEARCH.md.
       - Implement `getDashboardStatus()` returning summary stats and a health score (0-100).
  </action>
  <verify>
    <automated>cd src/backend && npm test src/__tests__/unit/service/IntegrityService.test.ts</automated>
  </verify>
  <done>Integrity service implemented with 7 rules and passing unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Backend API & Controller</name>
  <files>src/backend/src/controller/IntegrityController.ts, src/backend/src/routes/IntegrityRoute.ts, src/backend/src/index.ts</files>
  <action>
    1. Create `src/backend/src/controller/IntegrityController.ts`:
       - `getDashboardStatus`: calls `IntegrityService.getDashboardStatus`.
       - `runAudit`: calls `IntegrityService.runAudit` and returns fresh results.
       - Wrap responses in success wrapper.
    2. Create `src/backend/src/routes/IntegrityRoute.ts`:
       - GET `/api/integrity/dashboard` -> `getDashboardStatus`.
       - POST `/api/integrity/audit` -> `runAudit`.
       - Use `AuthMiddleware` to restrict to ADMIN/HR_MANAGER.
    3. Register route in `src/backend/src/index.ts` under `/api/integrity`.
  </action>
  <verify>
    <automated>cd src/backend && npx tsc --noEmit</automated>
  </verify>
  <done>API endpoints registered and type-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend Integrity Dashboard UI</name>
  <files>src/frontend/src/services/integrityService.ts, src/frontend/src/hooks/useIntegrityDashboard.ts, src/frontend/src/components/integrity/IntegrityHealthScore.tsx, src/frontend/src/components/integrity/IntegrityAlertList.tsx, src/frontend/src/app/pages/configuracion/integridad/page.tsx, src/frontend/src/app/pages/configuracion/page.tsx</files>
  <action>
    1. Create `src/frontend/src/services/integrityService.ts`:
       - Wrap `GET /integrity/dashboard` and `POST /integrity/audit` using `http.ts`.
    2. Create `src/frontend/src/hooks/useIntegrityDashboard.ts`:
       - Return `{ data, isLoading, isAuditing, runAudit, error }`.
    3. Create `src/frontend/src/components/integrity/IntegrityHealthScore.tsx`:
       - Circular score visualization with dynamic colors.
    4. Create `src/frontend/src/components/integrity/IntegrityAlertList.tsx`:
       - Grouped list of alerts with severity badges.
    5. Create `src/frontend/src/app/pages/configuracion/integridad/page.tsx`:
       - Main dashboard with score, alerts, and "Run Audit" button.
    6. Link the new page from `src/frontend/src/app/pages/configuracion/page.tsx`.
  </action>
  <verify>
    <automated>cd src/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Interactive dashboard accessible and functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted requests to trigger audits or view data quality stats. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Tampering | Audit Queries | mitigate | Ensure audit queries use Prisma parameterized inputs to prevent injection. |
| T-73-02 | Information Disclosure | Integrity Dashboard | mitigate | Restrict endpoints to ADMIN/HR_MANAGER via AuthMiddleware. |
| T-73-03 | Denial of Service | Audit Engine | mitigate | Optimize queries with aggregates (count) instead of full record fetching. |
</threat_model>

<verification>
1. `cd src/backend && npm test` passes.
2. `npx tsc --noEmit` passes in both stacks.
3. Visit `/configuracion/integridad`, run audit, and verify alerts.
</verification>

<success_criteria>
- Backend `IntegrityService` passes all unit tests for the 7 rules.
- API responds at `/api/integrity/dashboard`.
- Frontend Dashboard shows correct health score and grouped alert list.
</success_criteria>

<output>
After completion, create `.planning/phases/73-data-integrity-analytics/73-01-SUMMARY.md`
</output>
