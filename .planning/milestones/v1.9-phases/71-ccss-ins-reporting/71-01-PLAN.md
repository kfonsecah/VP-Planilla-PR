---
phase: "71"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/backend/prisma/schema.prisma",
  "src/backend/src/types/payroll.types.ts",
  "src/backend/src/model/position.ts",
  "src/backend/src/service/PositionService.ts",
  "src/backend/src/service/NomineeService.ts",
  "src/frontend/src/app/pages/positions/list/page.tsx"
]
autonomous: true
requirements: ["REP-71-01"]
must_haves:
  truths:
    - "Positions can store INS Occupation Code and Risk Class"
    - "Worked days are calculated and stored in vpg_payroll_employee during payroll processing"
    - "Worked days count includes both physical clock logs and approved vacation overlaps (per legal requirement)"
    - "Position management UI allows editing institutional metadata fields"
  artifacts:
    - path: "src/backend/prisma/schema.prisma"
      provides: "Schema with institutional reporting fields"
    - path: "src/backend/src/service/NomineeService.ts"
      provides: "Persistence of worked days with refined legal logic"
  key_links:
    - from: "src/backend/src/service/NomineeService.ts"
      to: "vpg_payroll_employee"
      via: "prisma.create/update"
      pattern: "payroll_employee_worked_days"
---

<objective>
Update the database schema and services to handle institutional metadata (INS codes, risk class) and track worked days for CCSS reporting.

Purpose: Regulatory compliance with CCSS (SICERE) and INS requires specific fields and precise calculation of "worked/paid days" including vacations.
Output: Updated DB schema, migration, and metadata persistence logic.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-ccss-ins-reporting/RESEARCH.md
@.planning/phases/71-ccss-ins-reporting/71-VALIDATION.md
@src/backend/prisma/schema.prisma
@src/backend/src/service/NomineeService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema Update and Type Definitions</name>
  <files>src/backend/prisma/schema.prisma, src/backend/src/model/position.ts, src/backend/src/types/payroll.types.ts</files>
  <action>
    1. Update `src/backend/prisma/schema.prisma`:
       - Add `position_occupation_code String? @db.VarChar(20)` and `position_risk_class String? @db.VarChar(10)` to `vpg_positions`.
       - Add `payroll_employee_worked_days Int @default(0)` to `vpg_payroll_employee`.
    2. Run migration in `src/backend/`: `npx prisma migrate dev --name add_ins_metadata_and_worked_days`.
    3. Update `src/backend/src/model/position.ts` interface to include the two new position fields.
    4. Update `src/backend/src/types/payroll.types.ts`:
       - Add `workedDays: number` to `EmployeePayroll` interface.
  </action>
  <verify>
    <automated>cd src/backend && npx prisma validate && npx tsc --noEmit</automated>
  </verify>
  <done>Database schema and TS types reflect the new institutional reporting requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Refined Worked Days Logic in NomineeService</name>
  <files>src/backend/src/service/NomineeService.ts, src/backend/src/service/PositionService.ts</files>
  <action>
    1. Update `NomineeService.ts`:
       - In `calculateEmployeePayroll`, implement logic to count `workedDays`.
       - Definition of "Worked Day": Any day within the period where the employee either has recorded hours (> 0) OR an approved vacation (isVacation is true). 
       - Legally, vacations count as worked/paid days for CCSS.
       - Store the result in the `EmployeePayroll` object.
    2. Update `savePayrollEmployees` in `NomineeService.ts`:
       - Persist `employee.workedDays` to `vpg_payroll_employee.payroll_employee_worked_days`.
    3. Update `PositionService.ts`:
       - Update CRUD operations to handle `position_occupation_code` and `position_risk_class`.
  </action>
  <verify>
    <automated>cd src/backend && npm test src/backend/src/service/NomineeService.test.ts</automated>
  </verify>
  <done>Worked days logic implemented following CCSS legal standards; position metadata persistence updated.</done>
</task>

<task type="auto">
  <name>Task 3: Position Management UI Update</name>
  <files>src/frontend/src/app/pages/positions/list/page.tsx</files>
  <action>
    1. Modify the Position management page and its form modal.
    2. Add input fields for "Código de Ocupación (INS)" and "Clase de Riesgo".
    3. Update `react-hook-form` and Zod schema in the component to include these fields.
    4. Ensure the UI follows `PHASE_CONTRACT.md` design rules (zinc color palette).
  </action>
  <verify>
    <automated>cd src/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>UI allows administrators to manage institutional metadata for positions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> Position UI | Metadata input for insurance reporting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01-01 | Tampering | Position Data | mitigate | Validate inputs with Zod schemas both on frontend and backend |
| T-71-01-02 | Information Disclosure | Metadata Fields | accept | These fields are business-standard codes, not PII |
</threat_model>

<verification>
1. Follow validation steps in `.planning/phases/71-ccss-ins-reporting/71-VALIDATION.md` section "Data Accuracy".
2. Run a payroll calculation for an employee with both work logs and vacations; verify `vpg_payroll_employee` count matches expectations.
</verification>

<success_criteria>
- Schema migration completed without errors.
- `vpg_payroll_employee` correctly stores the count of worked days (logs + vacations).
- Position management UI handles new institutional fields correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/71-ccss-ins-reporting/71-01-SUMMARY.md`
</output>
