---
phase: 72-hacienda-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/backend/package.json, src/backend/src/service/ReportsService.ts, src/backend/src/controller/ReportsController.ts, src/backend/src/routes/ReportsRoute.ts, src/frontend/src/services/reportsService.ts, src/frontend/src/app/pages/reports/components/ReportsTab.tsx, src/frontend/src/app/pages/reports/hooks/useOfficialReports.ts]
autonomous: true
requirements: [REP-01, REP-03]
must_haves:
  truths:
    - "D-151 CSV contains correct headers per TRIBU-CR specs"
    - "D-151 report aggregates annual payments per employee correctly"
    - "UI button for D-151 download exists and functions"
  artifacts:
    - path: "src/backend/src/service/ReportsService.ts"
      provides: "generateHaciendaD151CSV method"
    - path: "src/frontend/src/app/pages/reports/components/ReportsTab.tsx"
      provides: "D-151 download button"
  key_links:
    - from: "src/frontend/src/app/pages/reports/components/ReportsTab.tsx"
      to: "/api/reports/hacienda/d151/:year"
      via: "reportsService.downloadD151Report"
---

<objective>
Implement D-151 CSV generation logic and provide a UI trigger in the Reports page.

Purpose: Comply with Hacienda informative declaration requirements for professional services/contractors.
Output: Backend service methods, API endpoints, and updated frontend components.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-hacienda-reporting/72-RESEARCH.md
@PHASE_CONTRACT.md
@src/backend/src/service/ReportsService.ts
@src/backend/src/controller/ReportsController.ts
@src/backend/src/routes/ReportsRoute.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install backend reporting dependencies</name>
  <files>src/backend/package.json</files>
  <action>Install `exceljs`, `xmlbuilder2`, and `csv-writer` in the backend to support Hacienda reporting formats as recommended in 72-RESEARCH.md.</action>
  <verify>
    <automated>cd src/backend && npm install exceljs xmlbuilder2 csv-writer && npm list exceljs csv-writer</automated>
  </verify>
  <done>Dependencies added to package.json and installed in node_modules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement D-151 CSV generation in ReportsService</name>
  <files>src/backend/src/service/ReportsService.ts</files>
  <behavior>
    - Test: `generateHaciendaD151CSV(year)` aggregates gross salary by employee for the specified year.
    - Test: Headers match TRIBU-CR specs (Tipo Identificación, Identificación, Nombre / Razón Social, Monto Acumulado, Código de Operación).
    - Test: IDs have no hyphens.
  </behavior>
  <action>
    Add `static async generateHaciendaD151CSV(year: number): Promise<{ filename: string; content: string }>` to `ReportsService`.
    - Query `vpg_payroll_employee` grouped by `employee_id` for the given year.
    - Map `employee_national_id` to Hacienda code (1 for physical, 2 for legal - default to 1 for employees).
    - Use `csv-writer` or existing `generateCSV` helper (if enough) to produce the content.
    - Ensure UTF-8 encoding.
  </action>
  <verify>
    <automated>npm test src/backend/tests/service/ReportsService.test.ts</automated>
  </verify>
  <done>Method implemented and passing unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Wire D-151 endpoint and frontend UI</name>
  <files>src/backend/src/routes/ReportsRoute.ts, src/backend/src/controller/ReportsController.ts, src/frontend/src/services/reportsService.ts, src/frontend/src/app/pages/reports/components/ReportsTab.tsx, src/frontend/src/app/pages/reports/hooks/useOfficialReports.ts</files>
  <action>
    - Add `downloadD151Report` to `ReportsController.ts`.
    - Register `GET /reports/hacienda/d151/:year` in `ReportsRoute.ts`.
    - Add `downloadD151Report(year)` to `reportsService.ts` (frontend).
    - Update `useOfficialReports.ts` hook to include `downloadD151`.
    - Add a button for D-151 in `ReportsTab.tsx` under the Institutional Exports section.
  </action>
  <verify>
    <automated>cd src/backend && npx tsc --noEmit && cd ../frontend && npx tsc --noEmit</automated>
  </verify>
  <done>API endpoint functional and UI button triggers download.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | User requests D-151 for a specific year. `year` is an untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | Tampering | ReportsController | mitigate | Validate `year` parameter with Zod to be a 4-digit number. |
| T-72-02 | Info Disclosure | ReportsService | mitigate | Ensure user has 'ADMIN' or 'ACCOUNTANT' role via AuthMiddleware before allowing export. |
</threat_model>

<verification>
Check D-151 CSV format against Hacienda TRIBU-CR template (Tipo ID, ID, Nombre, Monto, Código).
</verification>

<success_criteria>
CSV file downloads with correct headers and aggregated annual data.
</success_criteria>

<output>
After completion, create `.planning/phases/72-hacienda-reporting/72-01-SUMMARY.md`
</output>
