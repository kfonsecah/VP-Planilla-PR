---
phase: 75-security-hardening
plan: 01
type: execute
wave: 1
depends_on: ["74-01", "74-02"]
files_modified: ["src/backend/package.json", "src/backend/src/middleware/queryNormalizer.ts", "src/backend/src/index.ts"]
autonomous: true
requirements: ["SEC-01", "SEC-02"]
must_haves:
  truths:
    - "HPP is configured with an explicit whitelist."
    - "Express req.query is normalized without mutating the original getter."
  artifacts:
    - path: "src/backend/src/middleware/queryNormalizer.ts"
      provides: "Query normalization and HPP middleware"
    - path: "src/backend/src/index.ts"
      provides: "Global application of security middleware"
---

<objective>
Configure HPP with an explicit whitelist and implement safe query normalization for Express 5.

Purpose: Prevent HTTP Parameter Pollution attacks and bypass the Express 5 getter restriction safely.
Output: Updated package.json, new queryNormalizer middleware, and updated index.ts.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@PHASE_CONTRACT.md
@TODO.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies</name>
  <files>src/backend/package.json</files>
  <action>
    Install `hpp` and `@types/hpp` as dependencies.
  </action>
  <verify>
    <automated>grep "hpp" src/backend/package.json</automated>
  </verify>
  <done>Dependencies installed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement queryNormalizer middleware</name>
  <files>src/backend/src/middleware/queryNormalizer.ts</files>
  <action>
    Create a middleware that safely copies and redefines `req.query` using `Object.defineProperty` to bypass the Express 5 getter, and configure `hpp` with a whitelist of valid array parameters (`employeeId`, `status`, `type`, etc.).
  </action>
  <verify>
    <automated>test -f src/backend/src/middleware/queryNormalizer.ts</automated>
  </verify>
  <done>Middleware created.</done>
</task>

<task type="auto">
  <name>Task 3: Apply middleware in index.ts</name>
  <files>src/backend/src/index.ts</files>
  <action>
    Import and apply `querySecurityMiddleware` globally in `src/backend/src/index.ts`, right after `express.json()`.
  </action>
  <verify>
    <automated>grep "querySecurityMiddleware" src/backend/src/index.ts</automated>
  </verify>
  <done>Middleware applied.</done>
</task>

</tasks>

<success_criteria>
- HPP is active and configured correctly.
- `req.query` is normalized safely.
- Typecheck and tests pass.
</success_criteria>
