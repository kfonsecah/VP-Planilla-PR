---
phase: 76-error-observability
plan: 01
type: execute
wave: 1
depends_on: ["75-01"]
files_modified: [
  "src/backend/package.json", 
  "src/backend/instrument.js", 
  "src/backend/src/index.ts",
  "src/frontend/package.json",
  "src/frontend/next.config.ts",
  "src/frontend/src/instrumentation.ts",
  "src/frontend/sentry.server.config.ts",
  "src/frontend/sentry.client.config.ts",
  "src/frontend/sentry.edge.config.ts"
]
autonomous: true
requirements: ["OBS-01", "OBS-02", "OBS-04"]
must_haves:
  truths:
    - "Sentry is initialized in both Backend and Frontend."
    - "next.config.ts uses tunnelRoute to prevent ad-blockers from dropping events."
    - "instrumentation.ts is kept lightweight without circular dependencies."
    - "Distributed Tracing works across both layers (same DSN)."
  artifacts:
    - path: "src/backend/instrument.js"
      provides: "Backend Sentry initialization"
    - path: "src/frontend/src/instrumentation.ts"
      provides: "Frontend Sentry registration"
---

<objective>
Integrate Sentry into both the Express backend and the Next.js frontend to enable comprehensive error observability and distributed tracing.

Purpose: Detect and diagnose bugs in production with full cross-layer context.
Output: Sentry SDK installed and configured in both applications.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@PHASE_CONTRACT.md
@TODO.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Sentry dependencies</name>
  <files>src/backend/package.json, src/frontend/package.json</files>
  <action>
    1. In `src/backend`, install `@sentry/node` and `@sentry/profiling-node`.
    2. In `src/frontend`, install `@sentry/nextjs`.
  </action>
  <verify>
    <automated>grep "@sentry/node" src/backend/package.json && grep "@sentry/nextjs" src/frontend/package.json</automated>
  </verify>
  <done>Sentry dependencies installed in both layers.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Sentry for Backend</name>
  <files>src/backend/instrument.js, src/backend/src/index.ts, src/backend/package.json</files>
  <action>
    1. Create `src/backend/instrument.js` to initialize Sentry (`Sentry.init()`) with tracing and profiling enabled. Use `process.env.SENTRY_DSN`.
    2. Import `./instrument.js` at the very top of `src/backend/src/index.ts` or add `--import ./instrument.js` to the `dev` and `start` scripts in `src/backend/package.json`. *Using `--import ./instrument.js` is the standard for OpenTelemetry based Sentry in Node.*
    3. Add `Sentry.setupExpressErrorHandler(app)` right before the error handling middlewares (if any) or at the end of routes in `index.ts`.
  </action>
  <verify>
    <automated>test -f src/backend/instrument.js && grep "Sentry.setupExpressErrorHandler" src/backend/src/index.ts</automated>
  </verify>
  <done>Backend Sentry configured via Node --import.</done>
</task>

<task type="auto">
  <name>Task 3: Configure Sentry for Frontend</name>
  <files>src/frontend/src/instrumentation.ts, src/frontend/sentry.server.config.ts, src/frontend/sentry.client.config.ts, src/frontend/sentry.edge.config.ts</files>
  <action>
    1. Create `src/frontend/sentry.*.config.ts` (client, server, edge) to initialize Sentry.
    2. Create `src/frontend/src/instrumentation.ts` and register Sentry (`export function register() { ... }`) as per Next.js App Router guidelines. Ensure it only imports what is necessary.
  </action>
  <verify>
    <automated>test -f src/frontend/src/instrumentation.ts && test -f src/frontend/sentry.client.config.ts</automated>
  </verify>
  <done>Frontend Sentry configs and instrumentation created.</done>
</task>

<task type="auto">
  <name>Task 4: Configure Next.js Sentry Plugin and tunnelRoute</name>
  <files>src/frontend/next.config.ts</files>
  <action>
    1. Wrap the existing Next.js config with `withSentryConfig` in `src/frontend/next.config.ts`.
    2. Ensure `tunnelRoute: "/monitoring-tunnel"` is added to the plugin options to bypass ad-blockers.
  </action>
  <verify>
    <automated>grep "withSentryConfig" src/frontend/next.config.ts && grep "monitoring-tunnel" src/frontend/next.config.ts</automated>
  </verify>
  <done>Next.js config updated with Sentry plugin and tunnelRoute.</done>
</task>

</tasks>

<success_criteria>
- Backend is instrumented via `--import`.
- Frontend has `instrumentation.ts` and Sentry config files.
- `next.config.ts` wraps the config with `tunnelRoute`.
- Both sides use the `SENTRY_DSN` env variable.
</success_criteria>
