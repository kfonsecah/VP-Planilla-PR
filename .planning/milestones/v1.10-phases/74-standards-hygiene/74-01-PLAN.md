---
phase: 74-standards-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["package.json", ".commitlintrc.json", ".husky/commit-msg"]
autonomous: true
requirements: ["DX-01"]
must_haves:
  truths:
    - "Non-conventional commit messages are rejected by Git hooks."
    - "Husky is initialized in the root directory."
    - "Commitlint is configured with conventional standards."
  artifacts:
    - path: "package.json"
      provides: "Root dependency management and husky setup"
    - path: ".commitlintrc.json"
      provides: "Commitlint configuration"
    - path: ".husky/commit-msg"
      provides: "Git hook for commit message validation"
  key_links:
    - from: "git commit"
      to: ".husky/commit-msg"
      via: "Husky hook"
    - from: ".husky/commit-msg"
      to: "npx commitlint"
      via: "Shell command execution"
---

<objective>
Configure Husky and Commitlint to enforce Conventional Commits standards across the repository.

Purpose: Improve Git history legibility and enable automated changelog generation in the future.
Output: Root package.json, Commitlint config, and Husky commit-msg hook.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@PHASE_CONTRACT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize root package.json and install dependencies</name>
  <files>package.json</files>
  <action>
    Create a minimal package.json in the root directory (if not exists) and install husky, @commitlint/cli, and @commitlint/config-conventional as devDependencies.
    Use `npm init -y` to create it if it doesn't exist.
    Run: `npm install --save-dev husky @commitlint/cli @commitlint/config-conventional`
  </action>
  <verify>
    <automated>test -f package.json && grep "husky" package.json && grep "commitlint" package.json</automated>
  </verify>
  <done>package.json exists with required devDependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Commitlint and Husky</name>
  <files>.commitlintrc.json, .husky/commit-msg</files>
  <action>
    1. Create `.commitlintrc.json` extending `@commitlint/config-conventional`.
    2. Initialize husky with `npx husky init`.
    3. Modify `.husky/commit-msg` to run `npx --no -- commitlint --edit "$1"`.
  </action>
  <verify>
    <automated>test -f .commitlintrc.json && test -f .husky/commit-msg && grep "commitlint" .husky/commit-msg</automated>
  </verify>
  <done>Commitlint and Husky are configured to validate commit messages.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Enforcement of Conventional Commits using Husky and Commitlint.</what-built>
  <how-to-verify>
    1. Try to commit with a non-conventional message:
       `git commit -m "bad commit message" --allow-empty`
       Expected: The commit should be rejected by the hook.
    2. Try to commit with a conventional message:
       `git commit -m "chore(74-01): test conventional commit" --allow-empty`
       Expected: The commit should succeed.
    3. Clean up the test commit:
       `git reset --soft HEAD~1` (if it succeeded)
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → Git | Hook execution happens on local dev machine before commit is accepted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | Tampering | .husky/commit-msg | accept | Hooks can be bypassed with --no-verify, but this is an accepted risk for developers. |
</threat_model>

<success_criteria>
- Commitlint is installed and configured.
- Husky is active and blocks non-conventional commits.
</success_criteria>

<output>
After completion, create `.planning/phases/74-standards-hygiene/74-01-SUMMARY.md`
</output>
