---
phase: 74-standards-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: ["src/backend/package.json", "src/backend/prisma/schema.prisma", "src/backend/prisma/dbml/schema.dbml"]
autonomous: true
requirements: ["DX-02"]
must_haves:
  truths:
    - "The DBML file is generated automatically when running 'npx prisma generate'."
    - "The schema.prisma includes the dbml generator block."
    - "prisma-dbml-generator is installed in the backend dev dependencies."
  artifacts:
    - path: "src/backend/prisma/dbml/schema.dbml"
      provides: "Database documentation in DBML format"
    - path: "src/backend/prisma/schema.prisma"
      provides: "Generator configuration"
  key_links:
    - from: "npx prisma generate"
      to: "src/backend/prisma/dbml/schema.dbml"
      via: "prisma-dbml-generator"
---

<objective>
Integrate prisma-dbml-generator to automatically generate database documentation (ER diagrams in DBML format) whenever the Prisma client is generated.

Purpose: Maintain up-to-date database documentation without manual effort.
Output: Updated schema.prisma and generated schema.dbml.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/backend/prisma/schema.prisma
@PHASE_CONTRACT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install prisma-dbml-generator and update schema.prisma</name>
  <files>src/backend/package.json, src/backend/prisma/schema.prisma</files>
  <action>
    1. Navigate to `src/backend` and install `prisma-dbml-generator` as a devDependency.
       `npm install --save-dev prisma-dbml-generator`
    2. Add the following generator block to `src/backend/prisma/schema.prisma`:
       ```prisma
       generator dbml {
         provider = "prisma-dbml-generator"
       }
       ```
    Note: Ensure it's added after the `client` generator.
  </action>
  <verify>
    <automated>cd src/backend && grep "prisma-dbml-generator" package.json && grep "generator dbml" prisma/schema.prisma</automated>
  </verify>
  <done>Generator is installed and configured in schema.prisma.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and verify DBML file</name>
  <files>src/backend/prisma/dbml/schema.dbml</files>
  <action>
    Run `npx prisma generate` in `src/backend` to trigger the DBML generation.
    Verify that the file `src/backend/prisma/dbml/schema.dbml` is created and contains the model definitions (e.g., `Table vpg_users`).
  </action>
  <verify>
    <automated>test -f src/backend/prisma/dbml/schema.dbml && grep "Table vpg_users" src/backend/prisma/dbml/schema.dbml</automated>
  </verify>
  <done>DBML file is successfully generated and verified.</done>
</task>

<task type="auto">
  <name>Task 3: Add npm script for explicit DBML generation</name>
  <files>src/backend/package.json</files>
  <action>
    Add a script `"dbml": "prisma generate"` to `src/backend/package.json` to allow generating the documentation explicitly via `npm run dbml`.
  </action>
  <verify>
    <automated>cd src/backend && npm run dbml</automated>
  </verify>
  <done>NPM script "dbml" is available and working.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → Schema | Documentation is generated from the local schema.prisma. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-02 | Information Disclosure | schema.dbml | accept | The DBML file describes the database structure, which is already visible in schema.prisma. No sensitive data (credentials) is included. |
</threat_model>

<success_criteria>
- Running `npx prisma generate` updates `src/backend/prisma/dbml/schema.dbml`.
- The DBML file correctly reflects the current state of the database models.
</success_criteria>

<output>
After completion, create `.planning/phases/74-standards-hygiene/74-02-SUMMARY.md`
</output>
