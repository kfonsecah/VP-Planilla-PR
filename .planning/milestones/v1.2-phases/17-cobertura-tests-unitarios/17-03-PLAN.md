---
phase: 17-cobertura-tests-unitarios
plan: 03
type: execute
wave: 3
depends_on: [17-02]
files_modified:
  - src/backend/src/__tests__/unit/payrollUtils.test.ts
  - src/backend/src/__tests__/unit/services/AuditLogsService.test.ts
autonomous: true
requirements:
  - COV-01

must_haves:
  truths:
    - "payrollUtils.test.ts extended with all remaining uncovered functions — no source changes"
    - "AuditLogsService.test.ts created covering all 3 methods including the 6-filter where builder"
    - "Full test suite passes with 0 failures"
    - "Statement coverage reaches ~74% — provides buffer above the 60% target"
  artifacts:
    - path: "src/backend/src/__tests__/unit/payrollUtils.test.ts"
      provides: "Extended with ~24 additional functions covering all branches listed in RESEARCH.md"
    - path: "src/backend/src/__tests__/unit/services/AuditLogsService.test.ts"
      provides: "Tests for getAuditLogs (with filters), createAuditLog, getAuditLogById"
  key_links:
    - from: "payrollUtils.test.ts"
      to: "src/backend/src/utils/payrollUtils.ts"
      via: "named import — NEVER modify the source file"
      pattern: "import \\{.*\\} from '../../utils/payrollUtils'"
    - from: "AuditLogsService.test.ts"
      to: "vpg_audit_logs.findMany with include vpg_users"
      via: "Promise.all([findMany, count])"
      pattern: "Promise\\.all"
---

<objective>
Extend payrollUtils.test.ts with the remaining ~24 pure utility functions and create AuditLogsService.test.ts for the filter-heavy audit log service. This plan provides a coverage buffer above the 60% phase target.

Purpose: Push coverage from ~62% (after Plan 2) to ~74% by covering the bulk of payrollUtils.ts pure functions (no Prisma needed) and the AuditLogsService's complex filter builder.
Output: 1 extended file + 1 new file, ~80-100 new passing tests.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.planning/ROADMAP.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.planning/phases/17-cobertura-tests-unitarios/17-02-SUMMARY.md

payrollUtils.ts contains pure functions — no Prisma, no mocking required. Tests import named exports directly:
```typescript
import { calculateHoursBetween, isDateInRange, ... } from '../../utils/payrollUtils';
```

The existing payrollUtils.test.ts covers: `isCRHoliday`, `getCRHolidays`, `countWorkingDaysInPeriod`.
All new describe blocks must be APPENDED to the existing file — do not replace or truncate.

AuditLogsService uses the standard inline Prisma mock pattern (same as other services in Plan 1 and 2) but the test file lives in `src/__tests__/unit/services/` so the import path is `'../../../lib/prisma'`.
</context>

<tasks>

<task type="auto">
  <name>Task 3.1 — Extend payrollUtils.test.ts with remaining pure functions</name>
  <files>
    src/backend/src/__tests__/unit/payrollUtils.test.ts
  </files>
  <action>
Read `src/backend/src/utils/payrollUtils.ts` in full before writing tests — confirm each function's signature and return type. Read the existing `src/backend/src/__tests__/unit/payrollUtils.test.ts` in full before modifying — do NOT overwrite or truncate existing content.

CRITICAL: NEVER modify `src/backend/src/utils/payrollUtils.ts`. Tests only.

Append the following new describe blocks AFTER the last existing describe block. Group related functions together.

**Group 1: Date/time utilities**

`calculateHoursBetween(start: Date, end: Date): number`
- Same day, 8h apart → 8
- end before start → 0 or negative (verify actual behavior)
- Same timestamp → 0

`isDateInRange(date: Date, start: Date, end: Date): boolean`
- Date on start boundary → true
- Date on end boundary → true
- Date before start → false
- Date after end → false
- Date strictly between → true

`formatDateString(date: Date): string`
- Known date → expected 'YYYY-MM-DD' formatted string (use a UTC-safe date like `new Date('2026-03-15T12:00:00Z')`)

`parseDateString(dateStr: string): Date | null`
- Valid date string '2026-03-15' → returns Date object (not null, not NaN)
- Invalid string 'not-a-date' → returns null (verify the function checks isNaN)

`generateDateRange(start: Date, end: Date): Date[]`
- Same day start and end → array with 1 element
- 3-day range → array with 3 elements
- End before start → empty array or single element (verify behavior)

**Group 2: Numeric utilities**

`roundToMoney(value: number): number`
- 1500.567 → rounds to 2 decimal places
- 0 → 0
- Negative value → rounds correctly (no sign error)

`applyPercentageDeduction(gross: number, pct: number): number`
- 1000, 10 → 100
- 1000, 0 → 0
- 1000, 100 → 1000

`calculateNetSalary(gross: number, totalDeductions: number): number`
- gross > deductions → gross - deductions
- deductions > gross → 0 (Math.max(0,...) prevents negative)

`averageOfSalaries(salaries: number[]): number`
- Empty array → 0
- [12000] → 1000 (12000/12)
- [24000] → 2000

`hasAYear(hireDate: Date, checkDate: Date): boolean`
- hireDate 1 year before checkDate → true
- hireDate less than 1 year before → false
- Same date → false

**Group 3: Payroll validation**

`validateClockLogPairs(logs: Array<{timestamp: Date, log_type: string}>): { valid: boolean, error?: string }`
- Empty array → valid (no logs is acceptable or returns specific result — verify)
- Single log (odd count) → not valid
- Two logs IN/OUT in correct order → valid
- Two logs OUT/IN (wrong type order for first) → not valid
- Two logs IN/IN (both same type) → not valid
- IN timestamp equals OUT timestamp (out <= in boundary) → not valid

`validatePayrollPeriod(start: Date, end: Date): { valid: boolean, error?: string }`
- Valid period (start < end, reasonable range) → valid: true
- start equals end → not valid
- start after end → not valid
- Period too long (verify what the max is by testing) → not valid

**Group 4: Hours calculation**

`calculateRegularHours(totalHours: number): number`
- 0 → 0
- 8 → 8
- 10 → 8 (regular capped at 8h/day)

`calculateOvertimeHours(totalHours: number): number`
- 0 → 0
- 8 → 0
- 10 → 2 (2 overtime hours above 8)
- 12 → 4

`calculateOvertimeHoursBiweekly(workedHours: number, requiredHours: number): number`
- worked < required → 0
- worked > required → worked - required

`calculateScheduledHours(start: Date, end: Date): number`
- Short period (e.g., Mon-Fri one week) → positive number based on working days

`calculateWeeklyRestHours(restDays: number, dailyHours: number): number`
- Known input → expected output (formula check, e.g., 1 rest day × 8h/day)

**Group 5: Pay calculation**

`calculateOvertimePay(overtimeHours: number, hourlyRate: number): number`
- 0 hours → 0
- 2 hours at 1000/h → expected overtime pay (1.5× for first 2h above 8 up to 10, 2× above 10 — check function signature)

`calculateWeeklyRestPay(restDays: number, dailyRate: number): number`
- 0 rest days → 0
- 1 rest day at 8000/day → 4000 (0.5× formula)

`calculateGrossSalary(regularHours, regularRate, overtimeHours, overtimeRate, restPay, bonuses): number`
- All zeros → 0
- Full scenario with known inputs → expected sum

**Group 6: Pair helpers**

`calculateTotalHoursFromPairs(pairs: Array<{in: Date, out: Date}>): number`
- Empty array → 0
- One pair 8h apart → 8

`hasOverlappingPairs(pairs: Array<{in: Date, out: Date}>): boolean`
- Non-overlapping pairs → false
- Overlapping pairs (pair2.in < pair1.out) → true

`getSundaysInPeriod(start: Date, end: Date): Date[]`
- Period with no Sundays → empty array
- Period with 1 Sunday → array with 1 element

`getWeeklyRestDays(start: Date, end: Date, vacationDates: Date[], sundayDates: Date[]): Date[]`
- No Sundays, no vacations → empty or minimal (depends on implementation)
- Sunday not in vacation → included in rest days

After appending all describe blocks, run:
```
cd "C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/src/backend" && npx jest --testPathPattern="payrollUtils" --no-coverage 2>&1 | tail -20
```

If any test fails because the actual function behavior differs from the expected value above, adjust the expected value to match the real behavior — do NOT modify the source function. Read the function body again to understand the exact behavior and fix the test expectation.
  </action>
  <verify>
    <automated>cd "C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/src/backend" && npx jest --testPathPattern="payrollUtils" --no-coverage 2>&1 | tail -15</automated>
  </verify>
  <done>payrollUtils.test.ts extended with all groups. All payrollUtils tests pass with 0 failures. No source file modified. Existing 3 describe blocks (isCRHoliday, getCRHolidays, countWorkingDaysInPeriod) still pass.</done>
</task>

<task type="auto">
  <name>Task 3.2 — Create AuditLogsService.test.ts and verify final coverage</name>
  <files>
    src/backend/src/__tests__/unit/services/AuditLogsService.test.ts
  </files>
  <action>
Read `src/backend/src/service/AuditLogsService.ts` to confirm method signatures and the `AuditLogFilters` interface before writing.

**AuditLogsService.test.ts** — mock tables: `vpg_audit_logs` (findMany, count, create, findUnique).

`getAuditLogs` returns `{ data, total, limit, offset }` using `Promise.all([findMany, count])`. The `findMany` result must include a nested `vpg_users` object because the service accesses `log.vpg_users.user_username` and `log.vpg_users.user_email`.

Mock audit log row:
```typescript
const mockAuditLogRow = {
  audit_logs_id: 1,
  audit_logs_user_id: 1,
  audit_logs_action: 'CREATE',
  audit_logs_entity: 'vpg_employees',
  audit_logs_entity_id: 5,
  audit_logs_timestamp: new Date('2026-01-01'),
  audit_logs_details: '{"field":"value"}',
  vpg_users: {
    user_id: 1,
    user_username: 'admin',
    user_email: 'admin@vp.com',
  },
};
```

In `beforeEach`, set:
```typescript
prisma.vpg_audit_logs.findMany.mockResolvedValue([]);
prisma.vpg_audit_logs.count.mockResolvedValue(0);
prisma.vpg_audit_logs.create.mockResolvedValue({ audit_logs_id: 1 } as any);
prisma.vpg_audit_logs.findUnique.mockResolvedValue(null);
```

Test cases to write (16 total):

`getAuditLogs` — 10 tests:
1. No filters: returns { data: [], total: 0, limit: 100, offset: 0 }
2. With data: maps rows to { id, user_id, username, user_email, action, entity, entity_id, timestamp, details }
3. Filter by userId: where clause includes `audit_logs_user_id`
4. Filter by action: where includes `audit_logs_action: { contains: ..., mode: 'insensitive' }`
5. Filter by entity: where includes `audit_logs_entity: { contains: ..., mode: 'insensitive' }`
6. Filter by startDate only: where includes `audit_logs_timestamp: { gte: startDate }`
7. Filter by endDate only: where includes `audit_logs_timestamp: { lte: endDate }`
8. Filter by both startDate and endDate: where includes both gte and lte
9. Custom limit and offset: findMany called with `take: limit, skip: offset`
10. DB fails: propagates

`createAuditLog` — 2 tests:
11. Creates audit log with correct fields (userId, action, entity, entityId, details)
12. Without details: `audit_logs_details` is null

`getAuditLogById` — 4 tests:
13. Found: returns mapped audit log object with username and user_email from vpg_users
14. Not found (findUnique returns null): returns null
15. DB fails: propagates
16. Verifies findUnique called with `include: { vpg_users: { select: { user_id, user_username, user_email } } }`

For filter tests (3-10), verify the `where` argument passed to `findMany` using:
```typescript
const call = prisma.vpg_audit_logs.findMany.mock.lastCall;
expect(call[0].where).toEqual(expect.objectContaining({ ... }));
```

After creating the file, run:
```
cd "C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/src/backend" && npx jest --testPathPattern="AuditLogsService" --no-coverage 2>&1 | tail -20
```

Then run the full suite with coverage to see final numbers:
```
cd "C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/src/backend" && npm test -- --coverage 2>&1 | tail -30
```
  </action>
  <verify>
    <automated>cd "C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/src/backend" && npm test -- --coverage 2>&1 | tail -30</automated>
  </verify>
  <done>AuditLogsService.test.ts (16 tests) passes with 0 failures. Full suite passes with 0 failures. Coverage output shows Statements >= 60% (target achieved). All filter branches in getAuditLogs are covered.</done>
</task>

</tasks>

<verification>
- [ ] `npm test -- --no-coverage` in `src/backend/` passes with 0 failures
- [ ] `npm test -- --coverage` shows Statements >= 60%
- [ ] payrollUtils.test.ts still contains all original 3 describe blocks
- [ ] `src/backend/src/utils/payrollUtils.ts` is unchanged (git diff shows no modifications)
- [ ] `npx tsc --noEmit` in `src/backend/` passes
</verification>

<success_criteria>
payrollUtils.test.ts extended with all remaining function groups. AuditLogsService.test.ts created with full filter coverage. Full test suite has 0 failures. Statement coverage >= 60% (expected ~74% as buffer above target). No source files modified — tests only.
</success_criteria>

<output>
After completion, create `.planning/phases/17-cobertura-tests-unitarios/17-03-SUMMARY.md`
</output>
