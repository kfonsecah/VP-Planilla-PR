---
phase: 68-recovery-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/backend/package-lock.json, src/frontend/package-lock.json]
autonomous: true
requirements: [ENV-01]

must_haves:
  truths:
    - "Backend node_modules are complete and include express, prisma, and zod"
    - "Frontend node_modules are complete and support Next.js 15"
    - "TypeScript checks pass in both layers"
  artifacts:
    - path: "src/backend/node_modules"
      provides: "Backend runtime dependencies"
    - path: "src/frontend/node_modules"
      provides: "Frontend runtime dependencies"
  key_links:
    - from: "src/backend/package.json"
      to: "src/backend/node_modules"
      via: "npm install"
    - from: "src/frontend/package.json"
      to: "src/frontend/node_modules"
      via: "npm install"
---

<objective>
Perform a clean recovery of the development environment by purging and reinstalling all dependencies in both backend and frontend layers. This resolves issues with corrupted or incomplete node_modules identified in Phase 68 research.

Purpose: Ensure a stable foundation for Milestone v1.8 and future phases.
Output: Verified node_modules and updated package-lock.json files.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-recovery-sync/68-RESEARCH.md
@PHASE_CONTRACT.md
@GEMINI.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend Environment Recovery</name>
  <files>src/backend/package-lock.json</files>
  <action>
    Navigate to src/backend.
    Remove node_modules and package-lock.json.
    Run 'npm install' to perform a clean installation.
    Verify that key dependencies (express, prisma, zod) are present in node_modules.
  </action>
  <verify>
    <automated>cd src/backend && if (Test-Path node_modules/express) { echo "Success" } else { throw "Express missing" }</automated>
  </verify>
  <done>Backend node_modules restored and package-lock.json regenerated.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend Environment Recovery</name>
  <files>src/frontend/package-lock.json</files>
  <action>
    Navigate to src/frontend.
    Remove node_modules and package-lock.json.
    Run 'npm install' to perform a clean installation.
    Verify that Next.js and React dependencies are correctly installed.
  </action>
  <verify>
    <automated>cd src/frontend && if (Test-Path node_modules/next) { echo "Success" } else { throw "Next.js missing" }</automated>
  </verify>
  <done>Frontend node_modules restored and package-lock.json regenerated.</done>
</task>

<task type="auto">
  <name>Task 3: Base State Verification</name>
  <files></files>
  <action>
    Run TypeScript checks in both layers to ensure the environment is fully operational.
    Per PHASE_CONTRACT.md Gate 1.
  </action>
  <verify>
    <automated>cd src/backend && npx tsc --noEmit; cd ../frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Environment verified as stable with zero TypeScript errors in relevant files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm Registry | External dependency source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01-01 | T | package-lock.json | mitigate | Use 'npm install' which respects integrity hashes in package-lock.json (once regenerated). |
| T-68-01-02 | S | node_modules | mitigate | Ensure no malicious packages are introduced by sticking to pinned versions in package.json. |
</threat_model>

<verification>
Check that both src/backend/node_modules and src/frontend/node_modules exist and that TSC runs successfully in both.
</verification>

<success_criteria>
Clean install completed in both layers and TSC passes without errors.
</success_criteria>

<output>
After completion, create .planning/phases/68-recovery-sync/68-01-SUMMARY.md
</output>
