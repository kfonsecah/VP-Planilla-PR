---
phase: 69-wizard-refactor
plan: 02
type: execute
wave: 2
depends_on: ["69-01"]
files_modified: [src/frontend/src/hooks/usePayrollWizard.ts, src/frontend/src/app/pages/payroll/wizard/steps/Step2Employees.tsx, src/frontend/src/app/pages/payroll/wizard/page.tsx]
autonomous: true
requirements: [69-02]
must_haves:
  truths:
    - "Step 2 UI is extracted into a dedicated component"
    - "Employee selection state is managed via usePayrollWizard hook"
    - "Min-wage validation tooltip and filtering remain functional"
  artifacts:
    - path: "src/frontend/src/app/pages/payroll/wizard/steps/Step2Employees.tsx"
      provides: "Employee selection step component"
---

<objective>
Decompose Step 2 (Employee Selection) from the monolithic Wizard page.tsx into a modular component and migrate its state to the usePayrollWizard hook.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/frontend/src/app/pages/payroll/wizard/page.tsx
@src/frontend/src/hooks/usePayrollWizard.ts
@src/frontend/src/app/pages/payroll/wizard/steps/Step1Period.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Update usePayrollWizard hook for Step 2 state</name>
  <files>src/frontend/src/hooks/usePayrollWizard.ts</files>
  <action>
    Add the following state and handlers to the hook (migrated from page.tsx):
    - employees (Employee[])
    - loadingEmployees (boolean)
    - checkedIds (Set<string>) - Note: checkedIds might need conversion to number if consistent with hook.
    - filterText (string)
    - Handlers: setEmployees, setLoadingEmployees, setCheckedIds, setFilterText, toggleAll, toggleEmployee.
    Ensure 'reset' clears these.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p src/frontend/tsconfig.json</automated>
  </verify>
  <done>Hook updated with Step 2 state management</done>
</task>

<task type="auto">
  <name>Task 2.2: Create Step2Employees component</name>
  <files>src/frontend/src/app/pages/payroll/wizard/steps/Step2Employees.tsx</files>
  <action>
    Extract the Step 2 JSX and related logic from page.tsx.
    - Component should consume hook for state.
    - Move the useEffect that loads employees when Step 2 is active into this component (or hook).
    - Preserve the Tooltip for min-wage and the search filter.
    - Ensure 'handleNextStep2' correctly sets 'selectedEmployeeIds' in the hook and moves to Step 3.
  </action>
  <verify>
    <automated>test -f src/frontend/src/app/pages/payroll/wizard/steps/Step2Employees.tsx</automated>
  </verify>
  <done>Step2Employees.tsx created</done>
</task>

<task type="auto">
  <name>Task 2.3: Wire Step2Employees into page.tsx</name>
  <files>src/frontend/src/app/pages/payroll/wizard/page.tsx</files>
  <action>
    - Import Step2Employees from ./steps/Step2Employees.
    - Remove local Step 2 state, handlers, and useEffect.
    - Replace Step 2 rendering block with <Step2Employees />.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p src/frontend/tsconfig.json</automated>
  </verify>
  <done>page.tsx refactored to use Step2Employees</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-02 | Information Disclosure | Employee List | accept | Employee names/IDs are necessary for selection; access controlled by AuthMiddleware. |
</threat_model>

<verification>
Check that the employee list loads, search works, and "Select All" functions correctly.
</verification>

<success_criteria>
- Step 2 is rendered by the new component.
- Employee loading and selection works as before.
- Type checking passes.
</success_criteria>

<output>
After completion, create .planning/phases/69-wizard-refactor/69-02-SUMMARY.md
</output>
