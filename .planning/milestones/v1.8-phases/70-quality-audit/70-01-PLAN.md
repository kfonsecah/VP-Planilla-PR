---
phase: 70-quality-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/backend/src/types/payroll.types.ts",
  "src/backend/src/utils/payrollUtils.ts",
  "src/backend/src/service/LegalParamService.ts",
  "src/backend/src/service/NomineeService.ts",
  "src/backend/src/service/AguinaldoService.ts",
  "src/backend/src/service/LaborEventsService.ts",
  "src/backend/src/__tests__/unit/payrollUtils.test.ts"
]
autonomous: true
requirements: [STAB-01, LEGAL-AUDIT]
must_haves:
  truths:
    - "payrollUtils.ts literals (6, 8, 104, 2, 12) are replaced by LegalParamSet fields"
    - "AguinaldoService.ts uses LegalParamSet.aguinaldoDivisor instead of hardcoded 12"
    - "LegalParamService.getParamSetAtDate provides safe defaults for new parameters"
    - "Core services have complete and accurate JSDoc for all public methods"
  artifacts:
    - path: "src/backend/src/types/payroll.types.ts"
      provides: "Extended LegalParamSet"
    - path: "src/backend/src/utils/payrollUtils.ts"
      provides: "Parameterized payroll math"
    - path: "src/backend/src/service/AguinaldoService.ts"
      provides: "Refactored Aguinaldo calculation"
  key_links:
    - from: "src/backend/src/utils/payrollUtils.ts"
      to: "LegalParamSet"
      via: "Interface consumption"
    - from: "src/backend/src/service/AguinaldoService.ts"
      to: "LegalParamService"
      via: "Parameter retrieval"
---

<objective>
Audit and refactor the Payroll Engine, Legal Engine, and Aguinaldo Service to eliminate hardcoded literals and ensure full JSDoc coverage.

Purpose: Improve system maintainability and ensure Costa Rica labor law math is fully parameterizable.
Output: Refactored payrollUtils.ts, AguinaldoService.ts, and updated LegalParam infrastructure.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-quality-audit/RESEARCH.md
@PHASE_CONTRACT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Legal Parameter & Aguinaldo Refactor</name>
  <files>src/backend/src/types/payroll.types.ts, src/backend/src/utils/payrollUtils.ts, src/backend/src/service/LegalParamService.ts, src/backend/src/service/AguinaldoService.ts, src/backend/src/__tests__/unit/payrollUtils.test.ts</files>
  <action>
    1. Update `LegalParamSet` in `payroll.types.ts` to include:
       - `workingDaysPerWeek: number`
       - `weeklyRestNumerator: number`
       - `weeklyRestDenominator: number`
       - `weeklyRestMultiplier: number`
       - `aguinaldoDivisor: number`
    2. Update `DEFAULT_LEGAL_PARAMS` in `payrollUtils.ts` with values: 6, 8, 104, 2, 12 respectively.
    3. Refactor `payrollUtils.ts`:
       - `getWeeklyRestDays`: replace `WORKING_DAYS_PER_WEEK` with `params.workingDaysPerWeek`.
       - `calculateWeeklyRestHours`: replace literals `8`, `104`, `2` with `params.weeklyRestNumerator`, `params.weeklyRestDenominator`, `params.weeklyRestMultiplier`.
       - `averageOfSalaries`: Update signature to `(salaries: number[], params: LegalParamSet = DEFAULT_LEGAL_PARAMS)` and replace `/ 12` with `/ params.aguinaldoDivisor`. (Blocker-02)
    4. Refactor `AguinaldoService.ts`:
       - Replace all hardcoded `/ 12` instances (found at lines 138, 147, 272, 275, 375, 376, 377) with `params.aguinaldoDivisor` or by calling `PayrollUtils.averageOfSalaries(total, params)`. (Blocker-01)
       - Ensure the `params` set is retrieved using `LegalParamService.getParamSetAtDate()` or passed from the caller.
    5. Update `src/backend/src/__tests__/unit/payrollUtils.test.ts`: Update all calls to `averageOfSalaries` to match the new signature (passing default params or empty object if applicable). (Blocker-02)
    6. Update `LegalParamService.getParamSetAtDate`:
       - Map new fields from `rawParams`.
       - Use safe defaults if keys are missing from DB (WORKING_DAYS_PER_WEEK: 6, etc.) to prevent breakage until a DB migration adds them.
  </action>
  <verify>
    <automated>cd src/backend && npx tsc --noEmit && npm test src/backend/src/__tests__/unit/payrollUtils.test.ts</automated>
  </verify>
  <done>Literals parameterized, AguinaldoService refactored, and all payroll unit tests passing.</done>
</task>

<task type="auto">
  <name>Task 2: JSDoc Accuracy Audit</name>
  <files>src/backend/src/service/NomineeService.ts, src/backend/src/service/AguinaldoService.ts, src/backend/src/service/LegalParamService.ts, src/backend/src/service/LaborEventsService.ts</files>
  <action>
    1. Systematically audit all public methods in the 4 services.
    2. Ensure `@param` types match implementation (especially distinguishing between `number` and `Decimal`).
    3. Ensure `@returns` descriptions are accurate.
    4. Add missing `@throws` for methods that can fail (per PHASE_CONTRACT.md).
    5. Clean up any stale comments or debugging logs found during audit.
  </action>
  <verify>
    <automated>grep -r "@param" src/backend/src/service/NomineeService.ts</automated>
  </verify>
  <done>All methods have valid JSDoc. No console.logs or stale comments remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DB → Legal Engine | Legal parameters are trusted but must be validated before use in math |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Tampering | LegalParamSet | mitigate | Ensure defaults are applied in LegalParamService if DB records are corrupted or missing |
</threat_model>

<verification>
Run unit tests for payrollUtils.ts and LegalParamService.ts.
</verification>

<success_criteria>
- Zero hardcoded literals in payrollUtils.ts math.
- AguinaldoService.ts refactored to use LegalParamSet.
- LegalParamSet expanded.
- Comprehensive JSDoc coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/70-quality-audit/70-01-SUMMARY.md`
</output>
