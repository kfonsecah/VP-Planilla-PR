---
phase: 65-proyeccion-aguinaldo-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 65-01
files_modified:
  - src/backend/src/controller/PayrollController.ts
  - src/backend/src/routes/PayrollRoutes.ts
  - src/backend/src/routes/EmployeeRoute.ts
autonomous: true
requirements:
  - PAY-30

must_haves:
  truths:
    - "GET /api/employees/:id/aguinaldo returns 200 with AguinaldoAccrual JSON"
    - "GET /api/payroll/:id/aguinaldo-summary returns 200 with AguinaldoSummaryRow[] JSON"
    - "Both routes are protected by AuthMiddleware (applied at router level in existing code)"
    - "Endpoints return 401 if no valid JWT is provided"
    - "npx tsc --noEmit passes in src/backend"
  artifacts:
    - path: "src/backend/src/controller/PayrollController.ts"
      provides: "getEmployeeAguinaldo and getAguinaldoSummary methods"
    - path: "src/backend/src/routes/PayrollRoutes.ts"
      provides: "GET /payroll/:id/aguinaldo-summary route"
    - path: "src/backend/src/routes/EmployeeRoute.ts"
      provides: "GET /employees/:id/aguinaldo route"
---

<objective>
Wire AguinaldoService to authenticated HTTP endpoints.

Purpose: Expose aguinaldo data to the frontend via two authenticated REST endpoints, ensuring secure access to sensitive salary information.
Output: GET /api/employees/:id/aguinaldo and GET /api/payroll/:id/aguinaldo-summary live and protected by AuthMiddleware.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-proyeccion-aguinaldo-ui/65-CONTEXT.md
@.planning/phases/65-proyeccion-aguinaldo-ui/65-01-SUMMARY.md

<interfaces>
<!-- AguinaldoService (from Plan 01) -->
```typescript
export class AguinaldoService {
  static async calculateAccruedAguinaldo(employeeId: number, asOfDate?: Date): Promise<AguinaldoAccrual>;
  static async getAguinaldoSummaryForPayroll(payrollId: number): Promise<AguinaldoSummaryRow[]>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add controller methods to PayrollController</name>
  <files>
    src/backend/src/controller/PayrollController.ts
  </files>
  <action>
    Add `getEmployeeAguinaldo` and `getAguinaldoSummary` to `PayrollController.ts`. 
    Ensure they parse params correctly and call the corresponding `AguinaldoService` methods.
    The response format must be `{ success: true, data: ... }`.
  </action>
  <verify>
    <automated>cd src/backend && npx tsc --noEmit</automated>
  </verify>
  <done>Methods added to controller, type check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Register routes and verify AuthMiddleware</name>
  <files>
    src/backend/src/routes/PayrollRoutes.ts,
    src/backend/src/routes/EmployeeRoute.ts
  </files>
  <action>
    Register the routes:
    - `GET /payroll/:id/aguinaldo-summary` in `PayrollRoutes.ts`.
    - `GET /employees/:id/aguinaldo` in `EmployeeRoute.ts`.
    
    CRITICAL: Verify that both router files use `router.use(AuthMiddleware.verifyToken)` or have it applied to these specific paths. In this project, routes are generally protected at the router level. Ensure the new routes are NOT placed before the middleware declaration.
  </action>
  <verify>
    <automated>grep -E "router\.use\(AuthMiddleware\.verifyToken\)|router\.get\(\".*aguinaldo\"" src/backend/src/routes/EmployeeRoute.ts</automated>
  </verify>
  <done>Routes registered and verified as protected.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-03 | Disclosure| Unauth access | mitigate | AuthMiddleware required for all new routes |
</threat_model>

<verification>
- npx tsc --noEmit in src/backend passes
</verification>

<success_criteria>
- Endpoints return 200 for valid requests
- Endpoints return 401 for requests without JWT
</success_criteria>

<output>
After completion, create `.planning/phases/65-proyeccion-aguinaldo-ui/65-02-SUMMARY.md`
</output>
