---
phase: 58-redondeo-minutos-motor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/backend/src/types/payroll.types.ts, src/backend/src/utils/payrollUtils.ts, src/backend/src/service/LegalParamService.ts]
autonomous: true
requirements: [PAY-23]
---

<objective>
Implementar la base técnica para el redondeo de minutos: definición de tipos, función de redondeo pura y carga de la política desde la configuración de empresa.

Propósito: Proveer la lógica matemática de redondeo (EXACT, ALWAYS_UP, NEAREST_QUARTER) y asegurar que el motor de cálculo conozca qué política aplicar.
Salida: Función applyMinuteRounding implementada y LegalParamSet actualizado con la política activa.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/backend/src/types/payroll.types.ts
@src/backend/src/utils/payrollUtils.ts
@src/backend/src/service/LegalParamService.ts
@Payroll.md
@PHASE_CONTRACT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Actualizar tipos de nómina</name>
  <files>src/backend/src/types/payroll.types.ts</files>
  <behavior>
    - Importar `MinuteRoundingPolicy` desde `@prisma/client`.
    - Actualizar la interfaz `LegalParamSet` para incluir `minuteRoundingPolicy: MinuteRoundingPolicy`.
  </behavior>
  <action>
    - Añadir el import: `import { MinuteRoundingPolicy } from '@prisma/client';`.
    - Modificar `LegalParamSet` para usar el enum de Prisma en lugar de `string` opcional (per D-01 de Fase 57).
  </action>
  <verify>
    <automated>cd src/backend && npx tsc --noEmit</automated>
  </verify>
  <done>Tipos actualizados y sin errores de compilación.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar applyMinuteRounding</name>
  <files>src/backend/src/utils/payrollUtils.ts</files>
  <behavior>
    - Sanitizar input: Asegurar que totalMinutes sea un entero (Math.round) para evitar drift de punto flotante de timestamps.
    - EXACT: Retorna proporcional exacto (minutos / 60).
    - ALWAYS_UP: Redondea al cuarto de hora superior (ceil(minutos/15) * 15 / 60).
    - NEAREST_QUARTER: Redondea al cuarto más cercano (round(minutos/15) * 15 / 60).
    - Fallback: EXACT si la política es null o no reconocida.
  </behavior>
  <action>
    - Importar `MinuteRoundingPolicy` desde `@prisma/client`.
    - Implementar la función `applyMinuteRounding(totalMinutes: number, policy: MinuteRoundingPolicy = MinuteRoundingPolicy.EXACT): number` como función pura.
    - El primer paso de la función debe ser `const sanitizedMinutes = Math.round(totalMinutes);`.
    - Usar `sanitizedMinutes` para toda la lógica subsiguiente.
    - Manejar el switch con un `default` que retorne el cálculo EXACT.
  </action>
  <verify>
    <automated>cd src/backend && npx tsc --noEmit</automated>
  </verify>
  <done>Función de redondeo implementada con sanitización y fallback robusto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cargar política de redondeo en LegalParamService</name>
  <files>src/backend/src/service/LegalParamService.ts</files>
  <behavior>
    - `getParamSetAtDate` debe consultar `vpg_enterprise` para obtener la política actual.
    - Retornar la política dentro del `LegalParamSet`.
  </behavior>
  <action>
    - Importar `MinuteRoundingPolicy` de `@prisma/client`.
    - En `getParamSetAtDate`, realizar una consulta a `prisma.vpg_enterprise.findFirst({ select: { enterprise_minute_rounding_policy: true } })`.
    - Asignar el valor encontrado al campo `minuteRoundingPolicy` del objeto de retorno.
    - Usar `MinuteRoundingPolicy.EXACT` como valor por defecto si no hay registros o el campo es nulo.
  </action>
  <verify>
    <automated>cd src/backend && npx tsc --noEmit</automated>
  </verify>
  <done>LegalParamService integra la política de redondeo de la empresa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DB -> Service | Los datos de configuración de empresa se cargan desde la base de datos. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering | vpg_enterprise | mitigate | La política de redondeo está restringida por tipos (Enum) y se valida en la carga. |
| T-58-02 | Denial of Service | applyMinuteRounding | mitigate | Función pura matemática sin recursión ni loops externos. Entrada limitada a números razonables (minutos diarios). |
</threat_model>

<verification>
- Compilación de TypeScript exitosa en el backend.
- Revisión visual del código de applyMinuteRounding contra las fórmulas de Payroll.md.
</verification>

<success_criteria>
- `LegalParamSet` contiene la propiedad `minuteRoundingPolicy`.
- `applyMinuteRounding` está implementado según los requisitos de ley laboral de Costa Rica descritos en §4 de Payroll.md.
</success_criteria>

<output>
Después de completar, crear `.planning/phases/58-redondeo-minutos-motor/58-01-SUMMARY.md`
</output>
