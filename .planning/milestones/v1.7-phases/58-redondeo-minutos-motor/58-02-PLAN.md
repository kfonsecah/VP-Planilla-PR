---
phase: 58-redondeo-minutos-motor
plan: 02
type: execute
wave: 2
depends_on: [58-01]
files_modified: [src/backend/src/service/NomineeService.ts, src/backend/src/__tests__/unit/payrollUtils.test.ts]
autonomous: true
requirements: [PAY-23]
---

<objective>
Integrar el redondeo de minutos en el motor de cálculo de planilla y verificar exhaustivamente con pruebas unitarias que cubren los casos legales requeridos.

Propósito: Asegurar que el redondeo se aplique a la suma diaria de minutos antes de convertir a horas, eliminando errores flotantes acumulativos.
Salida: NomineeService actualizado y tests pasando al 100%.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/backend/src/service/NomineeService.ts
@src/backend/src/__tests__/unit/payrollUtils.test.ts
@src/backend/src/utils/payrollUtils.ts
@Payroll.md
@PHASE_CONTRACT.md
@.planning/phases/58-redondeo-minutos-motor/58-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrar redondeo en NomineeService</name>
  <files>src/backend/src/service/NomineeService.ts</files>
  <behavior>
    - El redondeo debe aplicarse al **TOTAL de minutos trabajados en el día** (Jornada Total) antes de cualquier división de horas extra.
    - Confirmar que `calculateDailyHours` es privado (ya verificado) y actualizar su firma.
    - La conversión a horas decimales redondeadas ocurre antes de calcular el diferencial de OT.
  </behavior>
  <action>
    - Localizar el método privado `calculateDailyHours` en `NomineeService`.
    - Modificar su firma para aceptar `policy: MinuteRoundingPolicy`.
    - Cambiar la lógica interna: en lugar de sumar `pair.durationHours`, sumar minutos calculados a partir de los timestamps: `Math.round((out.getTime() - in.getTime()) / (1000 * 60))`.
    - Al final del loop de pares, llamar a `PayrollUtils.applyMinuteRounding(totalMinutes, policy)`.
    - El valor retornado (horas redondeadas) es el que se comparará contra el límite de la jornada (8h, 48h, etc.) en `processDailyWork`.
    - Actualizar el llamado a `calculateDailyHours` en `processDailyWork`, pasándole `params.minuteRoundingPolicy`.
  </action>
  <verify>
    <automated>cd src/backend && npx tsc --noEmit</automated>
  </verify>
  <done>El motor de cálculo aplica el redondeo sobre la jornada total diaria antes de segmentar horas extra.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Añadir pruebas unitarias de redondeo y casos borde</name>
  <files>src/backend/src/__tests__/unit/payrollUtils.test.ts</files>
  <behavior>
    - Probar EXACT: 431 min -> 7.1833 (7.18 rounded to money).
    - Probar ALWAYS_UP: 431 min -> 7.25.
    - Probar NEAREST_QUARTER: 424 min -> 7.00.
    - Probar NEAREST_QUARTER: 438 min -> 7.25.
    - Casos borde: 7.5 min (debe ser 15 min / 0.25h en NEAREST_QUARTER), 7 min (0.00h), 8 min (0.25h).
    - Probar inputs sucios: 431.2 min (debe redondearse internamente a 431 antes de la política).
  </behavior>
  <action>
    - Añadir un nuevo bloque `describe("applyMinuteRounding", ...)` en `payrollUtils.test.ts`.
    - Implementar los casos de prueba definidos en `Payroll.md §4` y los casos borde sugeridos en la revisión.
    - Verificar que el fallback a EXACT funciona cuando no se pasa política.
  </action>
  <verify>
    <automated>cd src/backend && npm test src/backend/src/__tests__/unit/payrollUtils.test.ts</automated>
  </verify>
  <done>Tests exhaustivos implementados cubriendo casos legales y de precisión técnica.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Service -> Utils | El motor de cálculo confía en la precisión de la utilidad de redondeo. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-03 | Business Logic | NomineeService | mitigate | Uso de lógica de enteros (minutos) antes de la división final para prevenir errores de punto flotante de IEEE 754. |
| T-58-04 | Business Logic | Payroll.md Compliance | mitigate | Pruebas unitarias exhaustivas con los casos exactos del documento de requerimientos. |
</threat_model>

<verification>
- Ejecución de `npm test` en el backend.
- Verificación de que no hay regresiones en cálculos de horas ordinarias/extra.
</verification>

<success_criteria>
- `applyMinuteRounding(431, 'ALWAYS_UP')` retorna 7.25.
- `applyMinuteRounding(424, 'NEAREST_QUARTER')` retorna 7.00.
- El motor de planilla usa la política de la empresa correctamente.
</success_criteria>

<output>
Después de completar, crear `.planning/phases/58-redondeo-minutos-motor/58-02-SUMMARY.md`
</output>
