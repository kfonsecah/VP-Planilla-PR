---
phase: 57-enterprise-config
plan: 03
type: execute
wave: 3
depends_on: [57-02]
files_modified:
  - src/frontend/src/services/enterpriseService.ts
  - src/frontend/src/app/pages/configuracion/empresa/page.tsx
  - src/frontend/src/app/pages/configuracion/page.tsx
  - src/frontend/src/components/LegalRoundingModal.tsx
autonomous: false
requirements: [PAY-22]

must_haves:
  truths:
    - "User can view and edit enterprise configuration in the UI"
    - "Selecting NEAREST_QUARTER triggers a legal disclaimer modal with verbatim Spanish text"
    - "Cancelling the legal modal reverts the rounding policy dropdown to its previous value"
    - "Configuración Dashboard contains a 'Configuración Laboral' card pointing to /pages/configuracion/empresa"
    - "Frontend passes npx next lint"
  artifacts:
    - path: "src/frontend/src/app/pages/configuracion/empresa/page.tsx"
      provides: "Enterprise configuration form"
    - path: "src/frontend/src/components/LegalRoundingModal.tsx"
      provides: "High-stakes compliance warning"
  key_links: []
---

<objective>
Implement the frontend user interface for managing enterprise configuration, including legal compliance safeguards for rounding policies and dashboard integration.

Purpose: Allow administrators to configure payroll rules with a clear understanding of legal implications.
Output: New configuration page, service wrapper, and compliance modal.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-enterprise-config/57-CONTEXT.md
@src/frontend/src/services/http.ts
@src/frontend/src/app/pages/configuracion/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Service and Compliance Modal</name>
  <files>src/frontend/src/services/enterpriseService.ts, src/frontend/src/components/LegalRoundingModal.tsx</files>
  <action>
    1. Create `src/frontend/src/services/enterpriseService.ts` using `http.ts` to call the backend endpoints.
    2. Create `src/frontend/src/components/LegalRoundingModal.tsx` following the Shadcn/UI dialog pattern.
    3. **Legal Compliance**: Implement the EXACT legal disclaimer text and buttons from CONTEXT.md:
       - Header: **⚠️ Advertencia legal — Redondeo bidireccional**
       - Body: "Esta modalidad puede descartar minutos trabajados por el empleado cuando la fracción es menor a 8 minutos. Según el artículo 17 del Código de Trabajo (principio in dubio pro operario), esta práctica puede ser objetada por el Ministerio de Trabajo si no está respaldada en el reglamento interno de trabajo de su empresa.\n\nAl confirmar, usted declara que esta política está documentada en su reglamento interno y es del conocimiento de todos sus trabajadores."
       - Buttons: "Cancelar" and "Confirmo, activar Modalidad C".
  </action>
  <verify>
    <automated>npx next lint --dir src/frontend/src/components</automated>
  </verify>
  <done>Frontend service and compliance modal implemented with exact legal verbatim and passing lint.</done>
</task>

<task type="auto">
  <name>Task 2: Create Enterprise Config Page</name>
  <files>src/frontend/src/app/pages/configuracion/empresa/page.tsx</files>
  <action>
    Create the configuration page with a form:
    - Fields (use `enterprise_` prefix in payload): `enterprise_minute_rounding_policy` (Select), `enterprise_is_commercial_activity` (Switch), `enterprise_ordinary_shift_type` (Select).
    - If user selects `NEAREST_QUARTER`, intercept the save process to show `LegalRoundingModal`.
    - **State Sync**: If the user clicks "Cancelar" in the modal, the `enterprise_minute_rounding_policy` form field MUST revert to its previous value to avoid UI desynchronization.
    - If user clicks "Confirmo, activar Modalidad C", set `enterprise_rounding_policy_acknowledged: true` in the PATCH payload.
    - If user selects any other policy, the reset of the acknowledgment flag is handled by the backend.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project src/frontend/tsconfig.json</automated>
  </verify>
  <done>Configuration form page implemented with conditional modal logic, state sync on cancel, and correct field names.</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard Integration</name>
  <files>src/frontend/src/app/pages/configuracion/page.tsx</files>
  <action>
    Update `ConfiguracionDashboard` in `src/frontend/src/app/pages/configuracion/page.tsx`:
    - Add a card named **"Configuración Laboral"**.
    - Link: `/pages/configuracion/empresa`.
    - Icon: `BriefcaseIcon` or `Cog6ToothIcon`.
    - Ensure it matches the style of existing cards.
  </action>
  <verify>
    <automated>npx next lint --dir src/frontend/src/app/pages/configuracion</automated>
  </verify>
  <done>Enterprise config linked from the configuration center with the correct Spanish name.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Visual Verification: Modal, Sync and Card</name>
  <what-built>Enterprise Configuration UI with Legal Compliance Modal and Dashboard Card</what-built>
  <how-to-verify>
    1. Run `npm run dev` in frontend.
    2. Go to /pages/configuracion. Verify there is a card named "Configuración Laboral".
    3. Click it to navigate to /pages/configuracion/empresa.
    4. Change Rounding Policy to NEAREST_QUARTER and click Save.
    5. Verify the modal appears with the header "**⚠️ Advertencia legal — Redondeo bidireccional**".
    6. Verify the body text matches CONTEXT.md exactly.
    7. Click "Cancelar" in the modal. Verify that the rounding policy dropdown reverts to its original value.
    8. Change back to NEAREST_QUARTER, click Save, and click "Confirmo, activar Modalidad C".
    9. Verify success toast and data persistence.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI→User | Displaying sensitive legal information correctly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-03-01 | Information Disclosure | LegalRoundingModal | mitigate | Ensure the legal text is clearly visible and requires active acknowledgment |
</threat_model>

<verification>
Run `npx next lint` and `npx tsc --noEmit` across the frontend.
</verification>

<success_criteria>
The configuration page is functional, the "Configuración Laboral" card exists, and the NEAREST_QUARTER policy is gated by the verbatim Spanish legal modal with correct state synchronization on cancellation.
</success_criteria>

<output>
After completion, create `.planning/phases/57-enterprise-config/57-03-SUMMARY.md`
</output>
