---
phase: 57-enterprise-config
plan: 02
type: execute
wave: 2
depends_on: [57-01]
files_modified:
  - src/backend/src/__tests__/unit/services/EnterpriseService.test.ts
  - src/backend/src/service/EnterpriseService.ts
  - src/backend/src/controller/EnterpriseController.ts
  - src/backend/src/routes/EnterpriseRoute.ts
  - src/backend/src/index.ts
autonomous: true
requirements: [PAY-22]

must_haves:
  truths:
    - "GET /api/enterprise/config returns the current enterprise settings"
    - "PATCH /api/enterprise/config updates the configuration and creates an audit log"
    - "Audit entity name is exactly 'enterprise_config' in vpg_audit_logs"
    - "Changing policy away from NEAREST_QUARTER resets the acknowledgment flag to false"
    - "Partial updates (PATCH) do not modify unprovided fields like enterprise_image"
  artifacts:
    - path: "src/backend/src/service/EnterpriseService.ts"
      provides: "Singleton-style configuration management"
    - path: "src/backend/src/__tests__/unit/services/EnterpriseService.test.ts"
      provides: "TDD coverage for enterprise rules"
  key_links:
    - from: "EnterpriseService"
      to: "AuditLogsService"
      via: "Static method call"
---

<objective>
Implement the backend service, controller, and routes for managing enterprise configuration with TDD assurance, strict audit logging, and support for partial updates.

Purpose: Provide a secure and auditable API for updating critical payroll rules.
Output: New service, controller, and route files registered in the main application, plus unit tests.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/backend/src/index.ts
@src/backend/src/service/AuditLogsService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create EnterpriseService unit tests (Wave 0)</name>
  <files>src/backend/src/__tests__/unit/services/EnterpriseService.test.ts</files>
  <action>
    Create the test scaffold in `src/backend/src/__tests__/unit/services/EnterpriseService.test.ts` following the project's testing pattern (Jest + ts-jest).
    
    The tests must cover:
    1. `getConfig()` returns the singleton enterprise record.
    2. `updateConfig()` persists fields using `enterprise_` prefix.
    3. **Partial Updates**: Verify that if a field (e.g., `enterprise_image`) is NOT in the update payload, its value in the DB remains unchanged.
    4. **Business Logic**: If `enterprise_minute_rounding_policy` is updated and the new value is NOT `NEAREST_QUARTER`, `enterprise_rounding_policy_acknowledged` MUST be forced to `false`.
    5. **Audit Audit**: Verify `AuditLogsService` is called with:
       - `entity_name`: 'enterprise_config'
       - `action`: 'NEAREST_QUARTER_ACKNOWLEDGED' (when applicable) or 'UPDATE_CONFIG'.
    6. Updates are wrapped in a transaction.
  </action>
  <verify>
    <automated>ls src/backend/src/__tests__/unit/services/EnterpriseService.test.ts</automated>
  </verify>
  <done>Test file created with failing stubs for EnterpriseService logic including partial update verification.</done>
</task>

<task type="auto">
  <name>Task 1: Implement EnterpriseService</name>
  <files>src/backend/src/service/EnterpriseService.ts</files>
  <action>
    Create `src/backend/src/service/EnterpriseService.ts` with static methods:
    - `getConfig()`: Use `prisma.vpg_enterprise.findFirst()` to retrieve the singleton.
    - `updateConfig(data, userId)`:
      - **Partial Updates**: Ensure `prisma.vpg_enterprise.update` only receives fields present in the `data` object to avoid unintended overrides (especially for `enterprise_image`).
      - **Logic Enforcement**: If `data.enterprise_minute_rounding_policy` is present AND its value is NOT `NEAREST_QUARTER`, set `data.enterprise_rounding_policy_acknowledged = false` in the update payload.
      - **Audit Log**: Every update must create a log in `vpg_audit_logs`.
      - **Audit Entity**: Use `enterprise_config` as the entity name for all configuration updates.
      - **Action String**: Use `NEAREST_QUARTER_ACKNOWLEDGED` if `enterprise_rounding_policy_acknowledged` is being set to `true`.
    - Use `prisma.$transaction`.
  </action>
  <verify>
    <automated>npm test -- src/backend/src/__tests__/unit/services/EnterpriseService.test.ts</automated>
  </verify>
  <done>EnterpriseService implemented, passing all unit tests with correct entity names, logic resets, and partial update safety.</done>
</task>

<task type="auto">
  <name>Task 2: Create Controller and Routes</name>
  <files>src/backend/src/controller/EnterpriseController.ts, src/backend/src/routes/EnterpriseRoute.ts</files>
  <action>
    1. Create `src/backend/src/controller/EnterpriseController.ts`:
       - `getConfig`: Call `EnterpriseService.getConfig`.
       - `updateConfig`: Extract `userId`, call `EnterpriseService.updateConfig`.
    2. Create `src/backend/src/routes/EnterpriseRoute.ts`:
       - `GET /enterprise/config`
       - `PATCH /enterprise/config` (Admin/Payroll Manager only).
  </action>
  <verify>
    <automated>npx tsc --noEmit --project src/backend/tsconfig.json</automated>
  </verify>
  <done>REST interface for enterprise configuration created.</done>
</task>

<task type="auto">
  <name>Task 3: Register routes and implement validation</name>
  <files>src/backend/src/index.ts</files>
  <action>
    1. Register `EnterpriseRoute` in `src/backend/src/index.ts`.
    2. Apply Zod validation to the PATCH payload, ensuring `enterprise_` prefix is used in all field names and validating against DB Enums.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project src/backend/tsconfig.json</automated>
  </verify>
  <done>API routes exposed and input validation enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API | Request body is untrusted and must be validated |
| API→DB | Queries use Prisma (parameterized) to prevent SQLi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-02-01 | Spoofing | EnterpriseRoute | mitigate | Enforce AuthMiddleware and role checks for PATCH |
| T-57-02-02 | Tampering | EnterpriseService | mitigate | Validate inputs using Zod, record all changes in vpg_audit_logs with entity 'enterprise_config', and ensure partial updates don't wipe data |
</threat_model>

<verification>
Check that the new endpoints are reachable and correctly enforce authorization.
</verification>

<success_criteria>
PATCH /api/enterprise/config successfully updates the DB, resets acknowledgment flag if needed, and creates an audit log entry with entity 'enterprise_config'. Unprovided fields are preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/57-enterprise-config/57-02-SUMMARY.md`
</output>
