---
phase: 59
plan: 02
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - src/backend/src/types/payroll.types.ts
  - src/backend/src/service/LegalParamService.ts
  - src/backend/src/__tests__/unit/services/LegalParamService.test.ts
autonomous: true
requirements:
  - PAY-24
must_haves:
  truths:
    - "Backend can retrieve the global minimum wage for a specific date."
    - "Backend defaults to 1529.62 if the wage is not configured."
    - "LegalParamSet includes GLOBAL_MIN_WAGE_RATE."
  artifacts:
    - path: "src/backend/src/service/LegalParamService.ts"
      provides: "getGlobalMinWageRate and getParamSetAtDate updates"
      contains: "getGlobalMinWageRate"
    - path: "src/backend/src/types/payroll.types.ts"
      provides: "LegalParamSet interface update"
      contains: "GLOBAL_MIN_WAGE_RATE?: number"
    - path: "src/backend/src/__tests__/unit/services/LegalParamService.test.ts"
      provides: "Unit tests for getGlobalMinWageRate"
      contains: "getGlobalMinWageRate"
  key_links:
    - from: "src/backend/src/service/LegalParamService.ts"
      to: "vpg_legal_params"
      via: "getParamAtDate('GLOBAL_MIN_WAGE_RATE')"
      pattern: "getParamAtDate\\('GLOBAL_MIN_WAGE_RATE'"
---

<objective>
Implement the `getGlobalMinWageRate` method, update `LegalParamSet` type, and update `getParamSetAtDate` in the `LegalParamService`, plus unit tests.

Purpose: Provides a programmatic way to get the reference minimum wage with a safe default, and makes it available to the Motor via `LegalParamSet`.
Output: Updated `payroll.types.ts`, `LegalParamService.ts` and `LegalParamService.test.ts`.
</objective>

<context>
@.planning/phases/59-tarifa-minima-global/59-CONTEXT.md
@.planning/phases/59-tarifa-minima-global/59-RESEARCH.md
@.planning/phases/59-tarifa-minima-global/59-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update LegalParamSet type</name>
  <files>src/backend/src/types/payroll.types.ts</files>
  <read_first>
    - src/backend/src/types/payroll.types.ts
  </read_first>
  <action>
    In `src/backend/src/types/payroll.types.ts`, update the `LegalParamSet` interface to include `GLOBAL_MIN_WAGE_RATE?: number;` (or whatever the precise key/type should be). This resolves the open question from RESEARCH.md ensuring Motor access.
  </action>
  <acceptance_criteria>
    - src/backend/src/types/payroll.types.ts contains GLOBAL_MIN_WAGE_RATE
    - Command `cd src/backend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd src/backend && grep -q "GLOBAL_MIN_WAGE_RATE" src/backend/src/types/payroll.types.ts</automated>
  </verify>
  <done>Interface is updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement getGlobalMinWageRate, getParamSetAtDate update, and tests</name>
  <files>src/backend/src/service/LegalParamService.ts, src/backend/src/__tests__/unit/services/LegalParamService.test.ts</files>
  <read_first>
    - src/backend/src/service/LegalParamService.ts
    - src/backend/src/__tests__/unit/services/LegalParamService.test.ts
  </read_first>
  <action>
    1. In `src/backend/src/__tests__/unit/services/LegalParamService.test.ts`, follow the "Service method test pattern" and "Mock object factory pattern" from 59-PATTERNS.md. Add tests for `LegalParamService.getGlobalMinWageRate(date)`.
       - Test that it returns the decimal value converted to a number when the parameter exists.
       - Test that it returns the fallback value `1529.62` when the parameter is missing or `getParamAtDate` returns null.
       
    2. In `src/backend/src/service/LegalParamService.ts`, follow the "Date-Effective Parameter Access" pattern from 59-PATTERNS.md.
       - Implement: `static async getGlobalMinWageRate(date: Date = new Date()): Promise<number>`
       - CRITICAL: Per GEMINI.md rules, you MUST include a JSDoc block for this new public method. Provide `@param date` and `@returns` documentation.
       - The method must call `await this.getParamAtDate('GLOBAL_MIN_WAGE_RATE', date)`
       - If a parameter is returned, return `Number(param.value)`
       - Otherwise, return `1529.62` (which is the verified 2025 MTSS base rate for unskilled workers).
       - Also, update the existing `getParamSetAtDate` method in this file to fetch `GLOBAL_MIN_WAGE_RATE` using the newly created `getGlobalMinWageRate` or similar logic, and include it in the returned `LegalParamSet` object.
  </action>
  <acceptance_criteria>
    - src/backend/src/service/LegalParamService.ts contains static async getGlobalMinWageRate with proper JSDoc (@param, @returns)
    - src/backend/src/service/LegalParamService.ts updates getParamSetAtDate
    - src/backend/src/__tests__/unit/services/LegalParamService.test.ts contains getGlobalMinWageRate tests following analog patterns
    - Command `cd src/backend && npm test -- LegalParamService.test.ts && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd src/backend && npm test -- LegalParamService.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Method exists, includes JSDoc, uses the correct fallback, updates param set, and is fully unit-tested using analog patterns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → DB | Service relies on database for legal parameters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-02-01 | Spoofing | LegalParamService | accept | Database is trusted; values are fetched server-side only. |
</threat_model>

<verification>
- `npm test -- LegalParamService.test.ts` passes.
- `npx tsc --noEmit` passes.
</verification>

<success_criteria>
- Method `getGlobalMinWageRate` returns the correct rate or the 1529.62 fallback.
- `LegalParamSet` interface includes the new rate.
</success_criteria>

<output>
After completion, create `.planning/phases/59-tarifa-minima-global/59-02-SUMMARY.md`
</output>