---
phase: 60
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/frontend/src/services/legalParamService.ts"
  - "src/frontend/src/hooks/usePayrollWizard.ts"
  - "src/backend/src/service/PayrollService.ts"
autonomous: true
requirements:
  - PAY-25
must_haves:
  truths:
    - "El frontend puede obtener la configuración MIN_WAGE_CHECK_ENABLED y la tarifa GLOBAL_MIN_WAGE_RATE."
    - "El backend registra en auditoría si se aprueba una planilla con salarios bajo el mínimo cuando el toggle está activo."
  artifacts:
    - path: "src/frontend/src/services/legalParamService.ts"
      provides: "Funciones para consumir los endpoints de legal-params (lectura y actualización)."
    - path: "src/frontend/src/hooks/usePayrollWizard.ts"
      provides: "Estado y funciones para almacenar los parámetros legales en el Wizard."
    - path: "src/backend/src/service/PayrollService.ts"
      provides: "Lógica de auditoría al aprobar planilla con salarios bajos."
  key_links:
    - from: "src/frontend/src/hooks/usePayrollWizard.ts"
      to: "src/frontend/src/services/legalParamService.ts"
      via: "Llamada a API para obtener los parámetros durante el flujo de planilla."
    - from: "src/backend/src/service/PayrollService.ts"
      to: "src/backend/src/service/AuditLogsService.ts"
      via: "Llamada a createAuditLog si se detectan salarios bajos."
---

<objective>
Implementar la infraestructura de detección de salarios bajos: el servicio frontend para parámetros legales, la extensión del estado del Wizard y el registro de auditoría en el backend al aprobar planillas con salarios bajos.

Purpose: Asegurar que el sistema tenga los medios para consultar configuraciones y dejar rastro de cumplimiento (auditoría).
Output: Nuevo `legalParamService.ts`, hook `usePayrollWizard.ts` extendido y `PayrollService.ts` con lógica de auditoría.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/60-advertencia-tarifa-minima/60-CONTEXT.md
@.planning/phases/60-advertencia-tarifa-minima/60-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear legalParamService en el frontend</name>
  <read_first>
    - src/frontend/src/services/enterpriseService.ts
  </read_first>
  <files>src/frontend/src/services/legalParamService.ts</files>
  <action>
    Basado en el patrón de `enterpriseService.ts`, crear `legalParamService.ts` que exporte `LegalParamService`.
    Debe implementar los siguientes métodos de comunicación con el backend:
    - `getParam(key: string): Promise<{ value: number | string }>`: GET `/legal-params/${key}`.
    - `updateParam(key: string, value: any): Promise<{ success: boolean }>`: PATCH `/legal-params/${key}` enviando `{ value }` en el body.
    Asegurar que el servicio use el cliente central `http.ts`.
  </action>
  <acceptance_criteria>
    - Archivo `src/frontend/src/services/legalParamService.ts` existe.
    - Exporta un objeto u clase con métodos `getParam` y `updateParam`.
  </acceptance_criteria>
  <verify>
    <automated>cd src/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Servicio creado y tipado correctamente con capacidades de lectura y escritura.</done>
</task>

<task type="auto">
  <name>Task 2: Extender usePayrollWizard hook</name>
  <read_first>
    - src/frontend/src/hooks/usePayrollWizard.ts
  </read_first>
  <files>src/frontend/src/hooks/usePayrollWizard.ts</files>
  <action>
    Modificar `usePayrollWizard.ts` para agregar dos nuevas piezas de estado:
    `minWageCheckEnabled: number | null` (o boolean)
    `globalMinWageRate: number | null`
    
    Añadir los setters `setMinWageCheckEnabled` y `setGlobalMinWageRate` al objeto retornado.
    Agregar ambos estados en la función `reset()` para que se limpien a `null` cuando se reinicia el wizard.
  </action>
  <acceptance_criteria>
    - El hook retorna `minWageCheckEnabled`, `globalMinWageRate` y sus respectivos setters.
    - `reset()` limpia estos valores.
  </acceptance_criteria>
  <verify>
    <automated>cd src/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>El estado del Wizard puede almacenar la configuración de validación de salario mínimo.</done>
</task>

<task type="auto">
  <name>Task 3: Implementar Auditoría de Salario Mínimo en Backend</name>
  <read_first>
    - src/backend/src/service/PayrollService.ts
    - src/backend/src/service/LegalParamService.ts
    - src/backend/src/service/AuditLogsService.ts
  </read_first>
  <files>src/backend/src/service/PayrollService.ts</files>
  <action>
    Modificar el método `approvePayroll` en `PayrollService.ts`.
    Antes o después de la actualización del estado de la planilla:
    1. Obtener el valor de `MIN_WAGE_CHECK_ENABLED` usando `LegalParamService.getParam`.
    2. Si es `1` (true):
       - Obtener `GLOBAL_MIN_WAGE_RATE` de `LegalParamService`.
       - Consultar los empleados/detalles de la planilla para identificar si alguno tiene un salario base inferior al mínimo.
       - Si existen tales empleados, registrar un log en `vpg_audit_logs` usando `AuditLogsService.createAuditLog`.
       - El log debe tener: `action: "APPROVE_WITH_MIN_WAGE_WARNING"`, `entity: "vpg_payrolls"`, `entityId: payrollId`, y detalles listando los IDs de empleados afectados o un resumen.
  </action>
  <acceptance_criteria>
    - La lógica de auditoría solo se ejecuta si el toggle está activo.
    - Se usa el `AuditLogsService` existente.
    - No se bloquea la aprobación si hay salarios bajos.
  </acceptance_criteria>
  <verify>
    <automated>cd src/backend && npx tsc --noEmit && npm test -- --passWithNoTests</automated>
  </verify>
  <done>El backend registra auditoría preventiva al aprobar planillas con inconsistencias salariales.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | Frontend requests/updates configuration values. |
| Backend → DB | Service writes audit logs based on business rules. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Information Disclosure | legalParamService | accept | Legal params are global company configs, not sensitive PII. |
| T-60-01b | Repudiation | Audit Logging | mitigate | Mandatory audit log entry when approving payrolls with warnings ensures traceability. |
</threat_model>

<verification>
- Hook y servicio compilan sin errores.
- Backend compila y los tests pasan.
</verification>

<success_criteria>
- Lógica de obtención de datos estructurada e inyectada en el estado global del Wizard.
- Registro de auditoría funcional en el backend.
</success_criteria>

<output>
After completion, create `.planning/phases/60-advertencia-tarifa-minima/60-01-SUMMARY.md`
</output>
