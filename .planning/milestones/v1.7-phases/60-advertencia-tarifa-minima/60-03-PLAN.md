---
phase: 60
plan: 03
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - "src/frontend/src/hooks/useLegalParamConfig.ts"
  - "src/frontend/src/app/pages/configuracion/empresa/page.tsx"
autonomous: true
requirements:
  - PAY-25
must_haves:
  truths:
    - "El administrador puede activar o desactivar la verificación de salario mínimo desde la UI de configuración."
    - "La lógica de datos está separada de la UI mediante un custom hook."
  artifacts:
    - path: "src/frontend/src/hooks/useLegalParamConfig.ts"
      provides: "Lógica de gestión de parámetros legales con react-hook-form."
    - path: "src/frontend/src/app/pages/configuracion/empresa/page.tsx"
      provides: "UI toggle para MIN_WAGE_CHECK_ENABLED consumiendo useLegalParamConfig."
  key_links:
    - from: "src/frontend/src/app/pages/configuracion/empresa/page.tsx"
      to: "src/frontend/src/hooks/useLegalParamConfig.ts"
      via: "Consumo de hook para estado y acciones."
    - from: "src/frontend/src/hooks/useLegalParamConfig.ts"
      to: "src/frontend/src/services/legalParamService.ts"
      via: "Llamadas de lectura y escritura para el parámetro legal."
---

<objective>
Separar la lógica de configuración de parámetros legales en un custom hook y proveer la interfaz visual en la página de Configuración de Empresa.

Purpose: Cumplir con la arquitectura (PHASE_CONTRACT 4.2) y permitir al cliente controlar la verificación de salario mínimo.
Output: Nuevo hook `useLegalParamConfig.ts` y refactorización de `page.tsx` para consumirlo.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/60-advertencia-tarifa-minima/60-CONTEXT.md
@.planning/phases/60-advertencia-tarifa-minima/60-RESEARCH.md
@src/frontend/src/app/pages/configuracion/empresa/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear custom hook useLegalParamConfig</name>
  <read_first>
    - src/frontend/src/services/legalParamService.ts
  </read_first>
  <files>src/frontend/src/hooks/useLegalParamConfig.ts</files>
  <action>
    Crear el hook `useLegalParamConfig.ts`.
    Debe encapsular:
    1. Esquema de Zod: `legalConfigSchema` con `minWageCheckEnabled: z.boolean()`.
    2. Inicialización de `useForm` con `zodResolver`.
    3. Lógica de carga (`useEffect`) que llame a `LegalParamService.getParam('MIN_WAGE_CHECK_ENABLED')` y haga `reset()` del formulario.
    4. Función `saveConfig` que llame a `LegalParamService.updateParam` y maneje Toasts de éxito/error.
    5. Retornar: `{ form, isSubmitting, saveConfig }`.
  </action>
  <acceptance_criteria>
    - El hook existe y está tipado.
    - Encapsula toda la lógica de `react-hook-form` relacionada con los parámetros legales.
  </acceptance_criteria>
  <verify>
    <automated>cd src/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Lógica de negocio y formularios extraída a un hook reutilizable.</done>
</task>

<task type="auto">
  <name>Task 2: Refactorizar Page y Renderizar Toggle</name>
  <read_first>
    - src/frontend/src/app/pages/configuracion/empresa/page.tsx
    - src/frontend/src/hooks/useLegalParamConfig.ts
  </read_first>
  <files>src/frontend/src/app/pages/configuracion/empresa/page.tsx</files>
  <action>
    En `page.tsx`:
    1. Consumir el hook `useLegalParamConfig`.
    2. Añadir una nueva tarjeta en el grid de la vista.
       Título: "Validación de Salario Mínimo".
       Icono: `CurrencyDollarIcon` (Heroicons).
       Descripción: "Habilitar advertencias en la planilla si un empleado tiene un salario base inferior al mínimo global.".
    3. El control debe ser un switch/toggle conectado al `register` del formulario del hook.
    4. Ejecutar `saveConfig` automáticamente en el `onChange` del switch o añadir un botón de "Guardar" específico si el patrón de la página lo requiere.
  </action>
  <acceptance_criteria>
    - La Page queda limpia de lógica de `useForm` para parámetros legales.
    - El switch refleja el estado real y permite su actualización.
    - Estilo consistente con el resto de la página (Zinc colors).
  </acceptance_criteria>
  <verify>
    <automated>cd src/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>UI clara e integrada que consume la lógica desacoplada del hook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | Enforcing user role authorization on configuration endpoints. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-03 | Elevation of Privilege | config update | mitigate | Relies on backend AuthMiddleware to ensure only admins can modify legal parameters. |
</threat_model>

<verification>
- Compilación limpia con `npx tsc --noEmit`.
</verification>

<success_criteria>
- Administrador puede habilitar o deshabilitar la regla de salario mínimo mediante la interfaz cumpliendo reglas de arquitectura.
</success_criteria>

<output>
After completion, create `.planning/phases/60-advertencia-tarifa-minima/60-03-SUMMARY.md`
</output>
