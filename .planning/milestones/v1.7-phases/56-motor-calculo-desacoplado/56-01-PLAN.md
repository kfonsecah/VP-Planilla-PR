---
phase: 56-motor-calculo-desacoplado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/src/types/payroll.types.ts
  - src/backend/src/utils/payrollUtils.ts
autonomous: true
requirements:
  - PAY-21

must_haves:
  truths:
    - "LegalParamSet interface is exported in payroll.types.ts"
    - "DEFAULT_LEGAL_PARAMS is exported in payrollUtils.ts and contains fallback values matching current literals"
    - "Hardcoded literals inside payrollUtils.ts (8, 1.5, 48) are removed from the top of the file"
  artifacts:
    - path: "src/backend/src/types/payroll.types.ts"
      provides: "LegalParamSet interface definition"
      contains: "export interface LegalParamSet"
    - path: "src/backend/src/utils/payrollUtils.ts"
      provides: "DEFAULT_LEGAL_PARAMS fallback configuration"
      contains: "export const DEFAULT_LEGAL_PARAMS: LegalParamSet ="
---

<objective>
Introduce the `LegalParamSet` interface in the payroll types and define `DEFAULT_LEGAL_PARAMS` in `payrollUtils.ts`. Remove the standalone top-level const literals (REGULAR_HOURS_PER_DAY, OVERTIME_MULTIPLIER, WORKING_DAYS_PER_WEEK).

Purpose: Establish the structural types needed for injecting payroll configuration dynamically, while providing a safe fallback that mimics existing behavior.
Output: `LegalParamSet` interface and `DEFAULT_LEGAL_PARAMS` constant.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/56-motor-calculo-desacoplado/56-CONTEXT.md
@.planning/phases/56-motor-calculo-desacoplado/56-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LegalParamSet to payroll.types.ts</name>
  <files>
    src/backend/src/types/payroll.types.ts
  </files>
  <action>
Append the `LegalParamSet` interface to `src/backend/src/types/payroll.types.ts`.

```typescript
export interface LegalParamSet {
  regularHoursPerDay: number; // TODO: Phase 66 (Jornadas)
  regularHoursPerWeek: number; // TODO: Phase 66
  otFactor: number;
  holidayMandatoryFactor: number;
  holidayTripleFactor: number;
  ccssObreroSalud: number;
  ccssObrerosPension: number;
  ccssObreroBP: number;
  minuteRoundingPolicy?: string; // TODO: Phase 58
}
```
  </action>
  <verify>
    <automated>cd src/backend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `LegalParamSet` interface is exported in `payroll.types.ts`.
    - Type check passes.
  </acceptance_criteria>
  <done>Interface added to types.</done>
</task>

<task type="auto">
  <name>Task 2: Add DEFAULT_LEGAL_PARAMS and remove literals in payrollUtils.ts</name>
  <files>
    src/backend/src/utils/payrollUtils.ts
  </files>
  <action>
1. Import `LegalParamSet` from `../types/payroll.types`.
2. Define `DEFAULT_LEGAL_PARAMS` at the top of the file using the values of the previous literals.
3. Remove the standalone constant declarations (`REGULAR_HOURS_PER_DAY`, `OVERTIME_MULTIPLIER`, `WORKING_DAYS_PER_WEEK`). Since removing them will cause TS errors in the file, temporarly redefine them below the new constant or just leave them failing for the next wave, but preferably update the local usages in this task to use `DEFAULT_LEGAL_PARAMS` directly to maintain compile-ability, OR just define the constant and keep the old constants until Wave 2.
Wait, let's just define `DEFAULT_LEGAL_PARAMS` and keep the old constants aliased to it for now so `tsc` doesn't break, then remove them entirely in Plan 02.

```typescript
export const DEFAULT_LEGAL_PARAMS: LegalParamSet = {
  regularHoursPerDay: 8,
  regularHoursPerWeek: 48,
  otFactor: 1.5,
  holidayMandatoryFactor: 2.0,
  holidayTripleFactor: 3.0,
  ccssObreroSalud: 5.5,
  ccssObrerosPension: 4.33,
  ccssObreroBP: 1.0
};

// Temporarily alias old constants to maintain compilation for this wave
const REGULAR_HOURS_PER_DAY = DEFAULT_LEGAL_PARAMS.regularHoursPerDay;
const OVERTIME_MULTIPLIER = DEFAULT_LEGAL_PARAMS.otFactor;
const WORKING_DAYS_PER_WEEK = 6; // Used for weekly rest divisor
```
  </action>
  <verify>
    <automated>cd src/backend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `DEFAULT_LEGAL_PARAMS` is exported.
    - `tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Fallback parameters defined.</done>
</task>

</tasks>

<success_criteria>
1. `LegalParamSet` is in `payroll.types.ts`.
2. `DEFAULT_LEGAL_PARAMS` is in `payrollUtils.ts`.
3. Compilation succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/56-motor-calculo-desacoplado/56-01-SUMMARY.md`
</output>
