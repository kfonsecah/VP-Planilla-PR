---
phase: 66-jornadas-mixtas-nocturnas
plan: 03
type: test
wave: 2
depends_on:
  - 66-02
files_modified:
  - src/backend/src/__tests__/unit/services/NomineeService.test.ts
autonomous: true
requirements:
  - PAY-31

must_haves:
  truths:
    - "Test case 1: Employee with NOCTURNA shift and 7h worked produces 6h regular + 1h OT"
    - "Test case 2: Employee with MIXTA shift and 7h worked produces 7h regular + 0h OT"
    - "Test case 3: Employee with DIURNA shift and 7h worked produces 7h regular + 0h OT"
    - "Test case 4: Employee with DIURNA shift and 9h worked produces 8h regular + 1h OT"
    - "Test case 5: USE_ENTERPRISE_DEFAULT with enterprise MIXTA and 7h worked produces 7h regular + 0h OT"
    - "Test case 6: Regression - DIURNA (default) produces same results as before"   
  artifacts:
    - path: "src/backend/src/__tests__/unit/services/NomineeService.test.ts"
      provides: "Unit tests for shift type resolution and its impact on payroll calculation"
      contains: "resolveEffectiveShiftType"
---

<objective>
Verify the shift type resolution logic and its impact on payroll calculations through comprehensive unit tests.
This plan covers the 6 scenarios defined in 66-CONTEXT.md and ensures naming standardization.

Purpose: Ensure the backend correctly applies different hour caps (6/7/8h) based on the resolved shift type.
Output: New test file src/backend/src/__tests__/unit/services/NomineeService.test.ts.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/66-jornadas-mixtas-nocturnas/66-CONTEXT.md
@.planning/phases/66-jornadas-mixtas-nocturnas/66-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create NomineeService.test.ts with shift resolution and calculation tests</name>
  <files>src/backend/src/__tests__/unit/services/NomineeService.test.ts</files>      
  <action>
Create a new test file `src/backend/src/__tests__/unit/services/NomineeService.test.ts`.
Use standardized `shift_type` property name in employee objects.

The test suite should include:

1. **Tests for `resolveEffectiveShiftType`**:
   - `USE_ENTERPRISE_DEFAULT` + `DIURNA` -> `DIURNA`
   - `USE_ENTERPRISE_DEFAULT` + `MIXTA` -> `MIXTA`
   - `USE_ENTERPRISE_DEFAULT` + `NOCTURNA` -> `NOCTURNA`
   - `DIURNA` + `MIXTA` -> `DIURNA` (Employee override)
   - `MIXTA` + `DIURNA` -> `MIXTA` (Employee override)
   - `NOCTURNA` + `DIURNA` -> `NOCTURNA` (Employee override)

2. **Tests for `calculatePayrollForPeriod` (Mocked)**:
   - Verify that `LegalParamService.getParamSetAtDate` is called exactly 3 times (once per shift type) to ensure N+1 optimization.
   - Verify that the correct `LegalParamSet` is used based on the employee's `shift_type`.

3. **Verify the 6 scenarios from CONTEXT.md**:
   - Scenario 1: NOCTURNA, 7h -> 6h regular, 1h OT.
   - Scenario 2: MIXTA, 7h -> 7h regular, 0h OT.
   - Scenario 3: DIURNA, 7h -> 7h regular, 0h OT.
   - Scenario 4: DIURNA, 9h -> 8h regular, 1h OT.
   - Scenario 5: USE_ENTERPRISE_DEFAULT + Enterprise MIXTA, 7h -> 7h regular, 0h OT. 
   - Scenario 6: Regression check.

Use `jest-mock-extended` for Prisma.
  </action>
  <verify>
    <automated>cd src/backend && npm test -- src/backend/src/__tests__/unit/services/NomineeService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 6 scenarios pass.
    - `resolveEffectiveShiftType` passes all cases.
    - Tests verify the pre-fetching logic (calls `getParamSetAtDate` once per type, not once per employee).
  </acceptance_criteria>
  <done>NomineeService.test.ts created and all shift-related tests pass.</done>      
</task>

</tasks>

<success_criteria>
- New test file exists at src/backend/src/__tests__/unit/services/NomineeService.test.ts.
- Tests cover `resolveEffectiveShiftType`.
- Tests cover the 6 specific scenarios from Phase 66 Context.
- `npm test` for the new file exits 0.
</success_criteria>
