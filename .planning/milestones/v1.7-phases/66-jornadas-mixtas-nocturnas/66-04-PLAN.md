---
phase: 66-jornadas-mixtas-nocturnas
plan: 04
type: execute
wave: 3
depends_on:
  - 66-01
  - 66-02
files_modified:
  - src/frontend/src/schemas/employee.ts
  - src/frontend/src/types/employee.ts
  - src/frontend/src/components/AddEmployeeModal.tsx
  - src/frontend/src/components/EditEmployeeModal.tsx
  - src/frontend/src/components/PayrollWizardStep3.tsx
autonomous: true
requirements:
  - PAY-31

must_haves:
  truths:
    - "AddEmployeeModal and EditEmployeeModal include a shift_type dropdown"
    - "Zod schema and Employee interface use shift_type (standardized name)"
    - "PayrollWizardStep3 shows a tooltip for employees with shift overrides"        
  artifacts:
    - path: "src/frontend/src/components/AddEmployeeModal.tsx"
      provides: "New dropdown for shift_type"
      contains: "shift_type"
    - path: "src/frontend/src/components/PayrollWizardStep3.tsx"
      provides: "Tooltip showing resolved shift per employee"
      contains: "shift_type"
---

<objective>
Implement the frontend changes for Phase 66, allowing users to assign shift types to employees and see them reflected in the payroll wizard.
Standardize on the `shift_type` property name.

Purpose: Provide UI controls for the new per-employee shift type feature.
Output: Updated employee modals, schema, types, and payroll wizard.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/66-jornadas-mixtas-nocturnas/66-CONTEXT.md
@.planning/phases/66-jornadas-mixtas-nocturnas/66-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update Frontend Types and Schema (Standardize on shift_type)</name>
  <files>
    src/frontend/src/types/employee.ts
    src/frontend/src/schemas/employee.ts
  </files>
  <action>
1. In `src/frontend/src/types/employee.ts`:
   - Add `shift_type?: string;` to `Employee` and `EmployeeFormData`. (Standardized per REVIEWS.md).

2. In `src/frontend/src/schemas/employee.ts`:
   - Add `shift_type: z.enum(['USE_ENTERPRISE_DEFAULT', 'DIURNA', 'MIXTA', 'NOCTURNA']).default('USE_ENTERPRISE_DEFAULT')` to `employeeSchema`.
  </action>
  <verify>
    <automated>cd src/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `Employee` and `EmployeeFormData` interfaces include `shift_type`.    
    - `employeeSchema` includes `shift_type` as an enum with correct default.
  </acceptance_criteria>
  <done>Frontend types and schema updated and standardized.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update Employee Modals</name>
  <files>
    src/frontend/src/components/AddEmployeeModal.tsx
    src/frontend/src/components/EditEmployeeModal.tsx
  </files>
  <action>
Add the "Jornada de trabajo" dropdown to both `AddEmployeeModal.tsx` and `EditEmployeeModal.tsx`.

Follow the UI spec from `66-UI-SPEC.md`:
- Label: "Jornada de trabajo"
- Options:
  - "Default de empresa" (USE_ENTERPRISE_DEFAULT)
  - "Jornada diurna (8h/día)" (DIURNA)
  - "Jornada mixta (7h/día)" (MIXTA)
  - "Jornada nocturna (6h/día)" (NOCTURNA)

Ensure the field name used is `shift_type`.
Ensure the field is correctly registered with `react-hook-form`.
  </action>
  <verify>
    <automated>cd src/frontend && npx next lint</automated>
  </verify>
  <acceptance_criteria>
    - Dropdown exists in both modals with correct options and labels.
    - Uses `shift_type` as field name.
  </acceptance_criteria>
  <done>Employee modals updated with shift_type dropdown.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update Payroll Wizard Tooltip</name>
  <files>src/frontend/src/components/PayrollWizardStep3.tsx</files>
  <action>
In `PayrollWizardStep3.tsx`, find where employee names or hours are rendered in the table.
Add a tooltip that appears when `employee.shift_type` is NOT `USE_ENTERPRISE_DEFAULT`.

Text: `Jornada: ${shiftName} (${hours}h/día)`
- DIURNA -> 8h
- MIXTA -> 7h
- NOCTURNA -> 6h

Use the `Tooltip` component from the UI library if available, or a simple `title` attribute.
  </action>
  <verify>
    <automated>cd src/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Tooltip logic implemented.
    - Tooltip displays correct hours for each shift type.
  </acceptance_criteria>
  <done>Payroll wizard updated with shift type tooltip.</done>
</task>

</tasks>

<success_criteria>
- Frontend types and schema include shift_type.
- Add/Edit employee modals have the shift_type dropdown.
- Payroll wizard step 3 shows the effective shift for overrides.
- Frontend builds without errors.
</success_criteria>
