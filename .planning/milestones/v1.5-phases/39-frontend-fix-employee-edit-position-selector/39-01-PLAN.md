---
gsd_plan_version: 1.0
phase: 39
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/src/components/AddEmployeeModal.tsx
  - src/frontend/src/components/EditEmployeeModal.tsx
  - src/frontend/src/app/pages/employee/edit/[id]/page.tsx
autonomous: true
requirements:
  - UX-03
must_haves:
  - AddEmployeeModal select component includes selectedLabel
  - EditEmployeePage select component includes selectedLabel
  - EditEmployeeModal select component includes selectedLabel
  - All selectors use String(id) === String(value) pattern
---

<objective>
Fix the visual display of the position selector in all employee forms by adding the missing `selectedLabel` prop and ensuring type-safe ID comparisons using String() coercion.
</objective>

<context>
@.planning/phases/39-frontend-fix-employee-edit-position-selector/39-RESEARCH.md
@src/frontend/src/components/AddEmployeeModal.tsx
@src/frontend/src/components/EditEmployeeModal.tsx
@src/frontend/src/app/pages/employee/edit/[id]/page.tsx
@src/frontend/src/components/ui/Select.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix AddEmployeeModal selector display</name>
  <files>src/frontend/src/components/AddEmployeeModal.tsx</files>
  <read_first>
    - src/frontend/src/components/AddEmployeeModal.tsx (lines 160-180 for Select usage)
    - src/frontend/src/components/ui/Select.tsx (to confirm selectedLabel prop)
  </read_first>
  <action>
    Update the `Select` component in `AddEmployeeModal.tsx` (around line 164) to include:
    - Add `selectedLabel` prop: `selectedLabel={positionOptions.find(p => String(p.id) === String(field.value))?.name}`
    - The positionOptions already converts id to String (line 32: `id: String(position.id)`)
  </action>
  <verify>
    grep -n "selectedLabel.*positionOptions.find" src/frontend/src/components/AddEmployeeModal.tsx
  </verify>
  <done>
    AddEmployeeModal now displays the selected position name instead of the ID.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix EditEmployeePage selector display and type safety</name>
  <files>src/frontend/src/app/pages/employee/edit/[id]/page.tsx</files>
  <read_first>
    - src/frontend/src/app/pages/employee/edit/[id]/page.tsx (lines 365-385 for Select usage)
  </read_first>
  <action>
    Update the `Select` component in `EditEmployeePage` around line 370 to use the robust `String()` comparison:
    - Change: `selectedLabel={positionOptions.find(p => p.id === field.value)?.name}`
    - To: `selectedLabel={positionOptions.find(p => String(p.id) === String(field.value))?.name}`
  </action>
  <verify>
    grep -n "String(p.id).*String(field.value)" src/frontend/src/app/pages/employee/edit/[id]/page.tsx
  </verify>
  <done>
    EditEmployeePage selector is robust against ID type mismatches.
  </done>
</task>

<task type="auto">
  <name>Task 3: Audit and fix EditEmployeeModal type safety</name>
  <files>src/frontend/src/components/EditEmployeeModal.tsx</files>
  <read_first>
    - src/frontend/src/components/EditEmployeeModal.tsx (lines 240-260 for Select usage)
  </read_first>
  <action>
    Update the `Select` component in `EditEmployeeModal.tsx` around line 245 to use the robust `String()` comparison:
    - Change: `selectedLabel={positionOptions.find(p => p.id === field.value)?.name}`
    - To: `selectedLabel={positionOptions.find(p => String(p.id) === String(field.value))?.name}`
  </action>
  <verify>
    grep -n "String(p.id).*String(field.value)" src/frontend/src/components/EditEmployeeModal.tsx
  </verify>
  <done>
    EditEmployeeModal selector is robust against ID type mismatches.
  </done>
</task>

</tasks>

<verification>
- All three Select components have selectedLabel prop
- All use String() comparison pattern for ID matching
- No console warnings or errors when opening employee forms
</verification>

<success_criteria>
- AddEmployeeModal shows position name in selector when editing
- EditEmployeePage shows position name in selector when editing  
- EditEmployeeModal shows position name in selector when editing
- Selectors handle both string and number ID types correctly
</success_criteria>

<output>
After completion, create `.planning/phases/39-frontend-fix-employee-edit-position-selector/39-01-SUMMARY.md`
</output>
