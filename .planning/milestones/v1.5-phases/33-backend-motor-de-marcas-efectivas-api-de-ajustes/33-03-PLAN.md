---
phase: 33-backend-motor-de-marcas-efectivas-api-de-ajustes
plan: 03
type: execute
wave: 3
depends_on: ["33-02"]
files_modified: [src/backend/src/controller/ClockLogAdjustmentController.ts, src/backend/src/service/ClockLogEffectiveService.ts, src/backend/src/routes/ClockLogsRoute.ts, src/backend/src/service/NomineeService.ts]
autonomous: true
requirements: [MARCAS-01, MARCAS-02, MARCAS-06]
must_haves:
  truths:
    - "The API provides paired IN/OUT entries with durations"
    - "Endpoints handle justification-based adjustments"
    - "New manual logs are created directly in vpg_clock_logs (source: manual)"
  artifacts:
    - path: "src/backend/src/controller/ClockLogAdjustmentController.ts"
      provides: "HTTP entry points for adjustment management"
  key_links:
    - from: "ClockLogAdjustmentController"
      to: "ClockLogEffectiveService"
      via: "getPairedEffectiveMarks"
---

<objective>
Expose the effective marks and adjustment functionality via API endpoints. This includes the pairing logic that groups raw logs into IN/OUT sessions for payroll calculation.

Purpose: Provide the frontend with processed, payroll-ready attendance data.
Output: ClockLogAdjustmentController.ts and updated route definitions.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/backend/src/service/ClockLogEffectiveService.ts
@src/backend/src/service/ClockLogAdjustmentService.ts

<interfaces>
From src/backend/src/service/ClockLogAnalysisService.ts (Existing pairing logic):
```typescript
// Reuse/Refactor logic that pairs IN and OUT logs.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Pairing Logic to Effective Service</name>
  <files>src/backend/src/service/ClockLogEffectiveService.ts</files>
  <action>
    Implement `getPairedEffectiveMarks(employeeId, startDate, endDate)`:
    - Fetch effective marks using `getEffectiveLogs`.
    - Apply pairing algorithm:
      1. Sort by `effectiveTimestamp` ASC.
      2. Iterate through logs:
         - IN log: Look for the next OUT log for the same employee within 24 hours.
         - Pair: If found, create a pair object with duration.
         - Orphan: If no OUT found, mark as orphan.
         - Anomaly: If next is another IN, mark current as anomaly.
    - Return structure: `[{ date, employeeId, pairs: [{ in, out, durationHours, status }] }]`.
  </action>
  <verify>
    <automated>npm test src/backend/src/service/ClockLogEffectiveService.test.ts</automated>
  </verify>
  <done>Pairing logic implemented using effective marks.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ClockLogAdjustmentController</name>
  <files>src/backend/src/controller/ClockLogAdjustmentController.ts</files>
  <action>
    Create the controller:
    - `POST /api/clock-logs/adjust`: Calls `ClockLogAdjustmentService.createAdjustment`.
    - `POST /api/clock-logs/manual`: Calls `ClockLogsService.createManualLog` (MARCAS-02).
    - `GET /api/clock-logs/effective`: Calls `ClockLogEffectiveService.getPairedEffectiveMarks`.
    - Handle errors: Return 400 for validation errors, 403 for Payroll Lock.
  </action>
  <verify>
    <automated>Check file exists and exports Controller class</automated>
  </verify>
  <done>Controller implemented with all required endpoints.</done>
</task>

<task type="auto">
  <name>Task 3: Register API Routes</name>
  <files>src/backend/src/routes/ClockLogsRoute.ts</files>
  <action>
    Register the new controller methods in ClockLogsRoute.ts (NOT clockLog.routes.ts — that file does not exist).
    - POST /api/clock-logs/adjust → ClockLogAdjustmentController.createAdjustment
    - GET  /api/clock-logs/effective → ClockLogAdjustmentController.getEffectiveMarks
    - POST /api/clock-logs/manual already exists (Phase 21) — do not duplicate.
    Wrap all with asyncHandler. Apply AuthMiddleware.verifyToken.
    Add @swagger JSDoc annotations on each route.
  </action>
  <verify>
    <automated>npx tsc --noEmit in src/backend</automated>
  </verify>
  <done>Routes registered in ClockLogsRoute.ts and secured with auth middleware.</done>
</task>

<task type="auto">
  <name>Task 4: REWIRE CRÍTICO — NomineeService.preloadClockLogs</name>
  <files>src/backend/src/service/NomineeService.ts</files>
  <action>
    This is the critical wire that connects mark corrections to payroll calculation.
    Without this change, correcting marks has ZERO effect on payroll output.

    Current code (line ~890):
    ```typescript
    private static async preloadClockLogs(startDate: Date, endDate: Date): Promise<Map<number, any[]>> {
      const logs = await prisma.vpg_clock_logs.findMany({ ... });
      // maps raw logs
    }
    ```

    Replace with:
    ```typescript
    private static async preloadClockLogs(startDate: Date, endDate: Date): Promise<Map<number, any[]>> {
      // Use effective marks so EDIT/VOID adjustments are reflected in payroll
      return ClockLogEffectiveService.getEffectiveMarksForAllEmployees(startDate, endDate);
    }
    ```

    The returned Map<employeeId, EffectiveMark[]> must maintain the same shape the rest of
    NomineeService expects: objects with `id`, `employee_id`, `timestamp`, `log_type`.
    Map EffectiveMark fields: effectiveTimestamp → timestamp, logType → log_type.
    VOIDed marks are already excluded by the engine — no additional filtering needed.
  </action>
  <verify>
    <automated>npm test src/backend/src/service/NomineeService.test.ts (existing tests must still pass)</automated>
  </verify>
  <done>preloadClockLogs delegates to ClockLogEffectiveService. Existing NomineeService tests pass.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-05 | Information Disclosure | GET /effective | mitigate | Enforce user session and employee ownership checks |
| T-33-06 | Tampering | POST /adjust | mitigate | CSRF protection (standard) and Auth middleware |
</threat_model>

<verification>
1. Run integration tests for the new endpoints.
2. Verify that `POST /adjust` creates adjustment records.
3. Verify that `GET /effective` returns paired entries with correct durations.
</verification>

<success_criteria>
- Endpoints are operational and return expected data.
- Pairing logic correctly handles effective (adjusted) timestamps.
- Manual additions are created as primary logs.
</success_criteria>

<output>
After completion, create `.planning/phases/33-backend-motor-de-marcas-efectivas-api-de-ajustes/33-03-SUMMARY.md`
</output>
