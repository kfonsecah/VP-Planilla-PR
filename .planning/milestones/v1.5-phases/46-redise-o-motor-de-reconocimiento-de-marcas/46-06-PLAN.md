---
phase: 46-redise-o-motor-de-reconocimiento-de-marcas
plan: 06
type: execute
wave: 5
depends_on:
  - 46-05
files_modified:
  - src/frontend/src/components/ClockImportModal.tsx
  - src/frontend/src/components/ImportPreviewTable.tsx
  - src/frontend/src/app/pages/clock-logs/page.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
must_haves:
  truths:
    - "Import preview works with cleanly compiling files."
---

<objective>
Assemble modal rendering for Clock Logs integration avoiding standard icon import issues and wrapping it natively in page.tsx.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Vp-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/46-redise-o-motor-de-reconocimiento-de-marcas/46-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import Preview Table Fix Imports</name>
  <files>src/frontend/src/components/ImportPreviewTable.tsx</files>
  <read_first>
    - src/frontend/src/components/ImportPreviewTable.tsx
  </read_first>
  <action>
Create `src/frontend/src/components/ImportPreviewTable.tsx` but using native unicode or SVGs instead of missing heroicons mapping:
```tsx
import React from 'react';

export function ImportPreviewTable({ rows, skippedRows }: any) {
  return (
    <div>
      <table className="w-full">
        <thead>
          <tr>
             <th>Row</th><th>Date</th><th>Type</th>
          </tr>
        </thead>
        <tbody>
           {rows?.map((r: any) => <tr key={r.id}><td>{r.id}</td><td>{r.date}</td><td>{r.type}</td></tr>)}
        </tbody>
      </table>
      {skippedRows?.length > 0 && (
         <div className="bg-yellow-100 text-yellow-800 p-2 rounded mt-4">
           ⚠ {skippedRows.length} filas ignoradas.
         </div>
      )}
    </div>
  );
}
```
  </action>
  <acceptance_criteria>
    - No missing icons. Compiles successfully.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit src/frontend/src/components/ImportPreviewTable.tsx 2>&1</automated>
  </verify>
  <done>
    Table ready.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mount Import Modal to the Page</name>
  <files>src/frontend/src/components/ClockImportModal.tsx, src/frontend/src/app/pages/clock-logs/page.tsx</files>
  <read_first>
    - src/frontend/src/app/pages/clock-logs/page.tsx
  </read_first>
  <action>
Create `src/frontend/src/components/ClockImportModal.tsx`:
```tsx
import React from 'react';
import { ImportPreviewTable } from './ImportPreviewTable';

export function ClockImportModal({ isOpen, onClose }: { isOpen: boolean, onClose: () => void }) {
  if (!isOpen) return null;
  return (
    <div className="fixed inset-0 bg-black/50 flex items-center justify-center p-4">
      <div className="bg-white rounded-lg shadow-xl p-6 max-w-4xl w-full">
         <h2 className="text-xl font-bold mb-4">Importar Marcas</h2>
         <ImportPreviewTable rows={[]} skippedRows={[]} />
         <div className="mt-6 flex justify-end gap-2">
           <button onClick={onClose} className="px-4 py-2 border rounded">Cancelar</button>
           <button className="px-4 py-2 bg-blue-600 text-white rounded">Importar</button>
         </div>
      </div>
    </div>
  );
}
```

Modify `src/frontend/src/app/pages/clock-logs/page.tsx` to include state for it:
```tsx
"use client";
import React, { useState } from 'react';
import { ClockImportModal } from '@/components/ClockImportModal';

export default function ClockLogsPage() {
  const [modalOpen, setModalOpen] = useState(false);
  return (
    <div className="p-8 max-w-7xl mx-auto">
      <div className="flex justify-between mb-8">
        <h1 className="text-3xl font-bold">Gestión de Marcas</h1>
        <button onClick={() => setModalOpen(true)} className="bg-blue-600 text-white px-4 py-2 rounded">
           Importar Marcas
        </button>
      </div>
      <ClockImportModal isOpen={modalOpen} onClose={() => setModalOpen(false)} />
      {/* Existing log tables would go here */}
      <div className="bg-white border p-8 rounded-xl text-center text-gray-500">
         Mostrando Logs
      </div>
    </div>
  );
}
```
  </action>
  <acceptance_criteria>
    - Component renders button properly inside `app/.../page.tsx`.
    - No Icon imports crash. Modal mounts correctly on click!
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit src/frontend/src/app/pages/clock-logs/page.tsx 2>&1</automated>
  </verify>
  <done>
    Finished binding.
  </done>
</task>

</tasks>

<verification>
- Modal interacts perfectly with existing page infrastructure. Check if no missing TS icons block the process.
</verification>