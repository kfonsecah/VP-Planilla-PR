---
phase: 42-frontend-gestion-aliases-marcas
plan: 02
type: execute
wave: 2
depends_on:
  - 42-01
files_modified:
  - src/frontend/src/components/EditEmployeeModal.tsx
autonomous: true
requirements:
  - REQ-42-01
  - REQ-42-02
  - REQ-42-03
  - REQ-42-04

must_haves:
  truths:
    - "User can see aliases as chips in EditEmployeeModal"
    - "User can add new alias via inline input"
    - "User can delete alias via X button"
  artifacts:
    - path: "src/frontend/src/components/EditEmployeeModal.tsx"
      provides: "Alias section integrated before action buttons"
      insertion_point: "before line 339 (border-t div with buttons)"
  key_links:
    - from: "EditEmployeeModal.tsx"
      to: "useClockAliases.ts"
      via: "import and use hook"
      pattern: "useClockAliases\\(employee\\.id\\)"
---

<objective>
Integrate the alias management section into EditEmployeeModal.tsx. Insert chips display, inline input, and delete buttons - all before the action buttons at line 339.
</objective>

<context>
@.planning/phases/42-frontend-gestion-aliases-marcas/42-CONTEXT.md
@.planning/phases/42-frontend-gestion-aliases-marcas/42-RESEARCH.md
@src/frontend/src/components/EditEmployeeModal.tsx
@.planning/phases/42-frontend-gestion-aliases-marcas/42-01-PLAN.md
</context>

<interfaces>
From Plan 01 (useClockAliases.ts):
```typescript
export interface UseClockAliasesReturn {
  aliases: ClockAlias[];
  isLoading: boolean;
  error: string | null;
  fetchAliases: () => Promise<void>;
  addAlias: (name: string) => Promise<boolean>;
  removeAlias: (aliasId: number) => Promise<boolean>;
}
```

From EditEmployeeModal.tsx line 337-339 (insertion point):
```typescript
                    </div>
                    </div>
                    </div>

                    <div className="border-t border-zinc-200 dark:border-zinc-800 pt-4">
                      <div className="flex flex-col sm:flex-row gap-3">
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add imports and hook wiring into component</name>
  <files>src/frontend/src/components/EditEmployeeModal.tsx</files>
  <read_first>
    - src/frontend/src/components/EditEmployeeModal.tsx
  </read_first>
  <action>
Add import for useClockAliases hook at the top imports section of EditEmployeeModal.tsx:

```typescript
import { useClockAliases } from '@/hooks/useClockAliases';
```

Then inside the component body (after the useForm call), add the alias state and handler:

```typescript
  // Alias management
  const [newAlias, setNewAlias] = useState('');
  const [isAdding, setIsAdding] = useState(false);
  const { aliases, isLoading, error, addAlias, removeAlias } = useClockAliases(employee?.id ?? '');

  const handleAddAlias = useCallback(async () => {
    if (!newAlias.trim()) return;
    setIsAdding(true);
    const success = await addAlias(newAlias);
    setIsAdding(false);
    if (success) setNewAlias('');
  }, [newAlias, addAlias]);
```

Ensure `useState` and `useCallback` are already imported (they should be — verify before adding).
  </action>
  <verify>Import exists for useClockAliases; newAlias, isAdding, handleAddAlias defined in component</verify>
  <done>useClockAliases imported and wired up with state and handler in EditEmployeeModal</done>
</task>

<task type="auto">
  <name>Task 2: Add alias section before action buttons</name>
  <files>src/frontend/src/components/EditEmployeeModal.tsx</files>
  <read_first>
    - src/frontend/src/components/EditEmployeeModal.tsx
  </read_first>
  <action>
Insert the alias management section BEFORE the action buttons div (line 339).

The insertion point is: before `<div className="border-t border-zinc-200 dark:border-zinc-800 pt-4">` that contains the Cancelar/Guardar buttons.

Add this section AFTER the closing </div> of the last field group (Género) but BEFORE the border-t div:

```typescript
                    {/* Aliases Section - D-01, D-02 */}
                    {employee?.id && (
                      <div className="border-t border-zinc-200 dark:border-zinc-800 pt-4 mt-4">
                        <h3 className="text-base font-medium text-zinc-700 dark:text-zinc-100 mb-3 pb-2 border-b border-zinc-200 dark:border-zinc-700">
                          Aliases de Reloj
                        </h3>

                        {/* Aliases list as chips - D-03 */}
                        <div className="flex flex-wrap gap-2 mb-3">
                          {isLoading ? (
                            <span className="text-sm text-zinc-500">Cargando...</span>
                          ) : aliases.length === 0 ? (
                            <span className="text-sm text-zinc-500">Sin aliases configurados</span>
                          ) : (
                            aliases.map((alias) => (
                              <span
                                key={alias.id}
                                className="inline-flex items-center gap-1 px-2.5 py-1 bg-zinc-100 dark:bg-zinc-800 rounded-full text-sm text-zinc-700 dark:text-zinc-300"
                              >
                                <span>{alias.name}</span>
                                <button
                                  type="button"
                                  onClick={() => removeAlias(alias.id)}
                                  className="ml-1 text-zinc-400 hover:text-red-500 transition-colors"
                                  aria-label={`Eliminar alias ${alias.name}`}
                                >
                                  ×
                                </button>
                              </span>
                            ))
                          )}
                        </div>

                        {/* Inline input + button - D-05 */}
                        <div className="flex gap-2">
                          <input
                            type="text"
                            value={newAlias}
                            onChange={(e) => setNewAlias(e.target.value)}
                            onKeyDown={(e) => {
                              if (e.key === 'Enter') {
                                e.preventDefault();
                                handleAddAlias();
                              }
                            }}
                            placeholder="Nuevo alias..."
                            className="flex-1 px-3 py-2 text-sm border border-zinc-300 dark:border-zinc-600 rounded-lg bg-white dark:bg-zinc-900 text-zinc-900 dark:text-zinc-100 placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent"
                          />
                          <button
                            type="button"
                            onClick={handleAddAlias}
                            disabled={!newAlias.trim() || isAdding}
                            className="px-4 py-2 bg-green-600 hover:bg-green-500 disabled:bg-zinc-400 text-white text-sm font-medium rounded-lg transition-colors"
                          >
                            {isAdding ? 'Agregando...' : 'Agregar'}
                          </button>
                        </div>

                        {/* Error message - D-04 */}
                        {error && (
                          <p className="text-sm text-red-600 mt-2">{error}</p>
                        )}
                      </div>
                    )}
```

Place exactly before line 339: `<div className="border-t border-zinc-200 dark:border-zinc-800 pt-4">`.
  </action>
  <verify>Alias section exists in modal, shows chips and input</verify>
  <done>Alias section integrated in EditEmployeeModal before action buttons</done>
</task>

</tasks>

<verification>
- [ ] useClockAliases import added
- [ ] Alias section renders before action buttons (line 339)
- [ ] Chips display with delete button (×)
- [ ] Inline input + Agregar button present
- [ ] Error message displayed inline (D-04)
- [ ] Optimistic delete works (from hook)
- [ ] newAlias cleared after successful add
</verification>

<success_criteria>
EditEmployeeModal shows alias section with chips, inline input, and delete functionality - matching all D- decisions from CONTEXT.md.
</success_criteria>

<output>
After completion, create `.planning/phases/42-frontend-gestion-aliases-marcas/42-02-SUMMARY.md`
</output>