---
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/backend/src/routes/index.ts
  - src/backend/src/routes/companyHolidayRoutes.ts
  - src/backend/src/controller/companyHolidayController.ts
  - src/backend/src/service/companyHolidayService.ts
  - src/backend/src/model/companyHolidayModel.ts
autonomous: false
---

# Phase 44 - Plan 01: Schema & Backend CRUD

## 1. Mapeo de Schema Prisma
<read_first>
- prisma/schema.prisma
</read_first>
<action>
Agrega el modelo `CompanyHoliday` al schema de Prisma bajo el contexto existente de Planillas. Debe incluir:
- `id` Int @id @default(autoincrement())
- `name` String
- `date` DateTime
- `is_mandatory_pay` Boolean @default(false)
- `allow_triple_overtime` Boolean @default(false)
- `status` String @default("active")
- `created_at` DateTime @default(now())
- `updated_at` DateTime @updatedAt
</action>
<schema_push_requirement>
**[BLOCKING] Schema Push Required**
The planner MUST include a `[BLOCKING]` task that runs the database schema push command AFTER all schema file modifications are complete.
</schema_push_requirement>
<acceptance_criteria>
- `prisma/schema.prisma` contains exactly `model CompanyHoliday {`
</acceptance_criteria>

## 2. Aplicar DB Push
<read_first>
- prisma/schema.prisma
</read_first>
<action>
Run `npx prisma db push --accept-data-loss` in the `src/backend` directory to apply the new schema. Set autonomous to false if there's data loss prompt.
</action>
<acceptance_criteria>
- Command runs successfully and tables are created.
</acceptance_criteria>

## 3. Modelo y Servicio
<read_first>
- src/backend/src/service/companyHolidayService.ts
- src/backend/src/model/companyHolidayModel.ts
</read_first>
<action>
Implementa el `CompanyHolidayModel` interface (representando la DB). Luego, construye `CompanyHolidayService` con los métodos CRUD estándar (create, getByYear, update, delete). Usar singletons de prisma importados de `../lib/prisma`.
</action>
<acceptance_criteria>
- `CompanyHolidayService.ts` exports `CompanyHolidayService` class.
- Methods `create`, `getAll(year)`, `update`, `delete` exist.
</acceptance_criteria>

## 4. Controlador y Rutas
<read_first>
- src/backend/src/routes/index.ts
- src/backend/src/routes/companyHolidayRoutes.ts
- src/backend/src/controller/companyHolidayController.ts
</read_first>
<action>
Implementa el controlador `CompanyHolidayController` usando el patrón estándar con `asyncHandler`.
Crea `companyHolidayRoutes.ts` con middleware JWT (`AuthMiddleware.verifyToken`).
Registra este router en `routes/index.ts` bajo `/api/company-holidays`.
</action>
<acceptance_criteria>
- `routes/index.ts` contains `app.use("/api/company-holidays", companyHolidayRoutes)`
- `companyHolidayRoutes.ts` implements CRUD endpoints.
</acceptance_criteria>
