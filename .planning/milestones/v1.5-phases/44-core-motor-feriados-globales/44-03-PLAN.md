---
wave: 2
depends_on: [".planning/phases/44-core-motor-feriados-globales/44-01-PLAN.md"]
files_modified:
  - src/frontend/src/services/holidaysService.ts
  - src/frontend/src/app/pages/configuracion/feriados/page.tsx
  - src/frontend/src/components/LaborEventsCalendar.tsx
autonomous: false
---

# Phase 44 - Plan 03: Frontend Admin y Calendario

## 1. Crear el Frontend Service
<read_first>
- src/frontend/src/services/http.ts
</read_first>
<action>
Crear `src/frontend/src/services/holidaysService.ts` que exporta un hook o métodos asincrónicos para interactuar con `/api/company-holidays`. Debe incluir tipos TypeScript representativos del `CompanyHoliday`.
</action>
<acceptance_criteria>
- `holidaysService.ts` extends standard API HTTP client correctly.
</acceptance_criteria>

## 2. Construir la UI Administrativa
<read_first>
- src/frontend/src/components/ui/Modal.tsx
</read_first>
<action>
Crear un nuevo modulo/página en `app/pages/configuracion/feriados/page.tsx`.
Debe mostrar una tabla o lista interactiva de los `CompanyHolidays`.
Debe permitir presionar botones rápidos de un Switch (On/Off) visual en Tailwind/ReactHookForm para las props `is_mandatory_pay` y `allow_triple_overtime`.
Al presionar 'Guardar', debe parchear la DB.
Incluir un botón para generar automáticamente la "Semana Santa 202X" si faltan, usando la lógica actual de `holidays.ts`, pero inyectándola a la base de datos vía POST al array, ahorrando trabajo manual al de RRHH.
</action>
<acceptance_criteria>
- File `feriados/page.tsx` exists and uses table component syntax.
- Toggle buttons for mandatory pay exist.
</acceptance_criteria>

## 3. Sustituir holidays.ts estático del Calendario
<read_first>
- src/frontend/src/components/LaborEventsCalendar.tsx
- src/frontend/src/utils/holidays.ts
</read_first>
<action>
En el `LaborEventsCalendar.tsx`:
1. Borrar la dependencia de la tabla dura quemada en `utils/holidays.ts`.
2. Consumir el `holidaysService.ts` / el store para levantar los días desde el backend para el año actual mostrado.
3. El mapeo `toCalendarEvent` (en el caso especial de feriados) se lee dinámicamente de esto en lugar del forEach quemado local. 
</action>
<acceptance_criteria>
- `LaborEventsCalendar.tsx` calls an async API fetch (via SWR or React Query or standard useEffect fetch) to load array of DB holidays.
- Code from Phase 43 is updated smoothly.
</acceptance_criteria>
