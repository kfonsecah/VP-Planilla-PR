---
wave: 2
depends_on: [".planning/phases/44-core-motor-feriados-globales/44-01-PLAN.md"]
files_modified:
  - src/backend/src/utils/payrollUtils.ts
  - src/backend/src/service/payrollService.ts
autonomous: false
---

# Phase 44 - Plan 02: Motor Matemático (payrollUtils)

## 1. Modificar payrollUtils.ts para soporte dinámico de Feriados
<read_first>
- src/backend/src/utils/payrollUtils.ts
</read_first>
<action>
Actualizar las funciones en `payrollUtils.ts` que validan feriados. Eliminar la constante harcodeada `FERIADOS_CR`.
Actualiza estas definiciones:
- Agrega un `type CurrentHoliday = { date: string, is_mandatory_pay: boolean, allow_triple_overtime: boolean }`
- Cambiar firmas como `countWorkingDaysInPeriod` para que reciba `Array<CurrentHoliday>` en vez de consultar localmente.
- Implementar funciones para identificar qué días del periodo procesado chocan con `is_mandatory_pay`.
</action>
<acceptance_criteria>
- `FERIADOS_CR` is deleted from `payrollUtils.ts`.
- `isCRHoliday` function expects `holidayList` as a parameter.
</acceptance_criteria>

## 2. Inyectar feriados a calculateGrossSalary
<read_first>
- src/backend/src/utils/payrollUtils.ts
</read_first>
<action>
En `calculateGrossSalary` y `calculateRegularHours`, pasar el `companyHolidays`.
Implementa las reglas:
- Si el empleado trabajó en un feriado `is_mandatory_pay`: su pago se computa x2 (o x3 las horas extras si tiene `allow_triple_overtime`).
- Si el empleado no laboró, pero es un día de pago obligatorio y tocaba laborar (o si se le aplica pago plano mensual/quincenal general, aunque aquí nos basamos en `DayWork[]` derivado de los logs), insertar horas base generadas para ese día feriado, asumiendo su jornada de 8h o el cálculo base programado.
</action>
<acceptance_criteria>
- `calculateGrossSalary` in `payrollUtils.ts` has parameters for holidays.
- Overtime multiplier considers triple overtime if flag present.
</acceptance_criteria>

## 3. Modificar el Servicio de Generación de Planilla
<read_first>
- src/backend/src/service/payrollService.ts
</read_first>
<action>
Dentro del método general que orquesta la generación (donde se construye el periodo entre `startDate` y `endDate`):
1. Hacer un fetch de la BD `prisma.companyHoliday.findMany` para listar los feriados activos que caen en ese rango o año.
2. Formatear la lista recabada.
3. Pasársela por parámetro a todas las utilidades delegadas (e.g. `calculateGrossSalary`, `countWorkingDaysInPeriod`, etc.).
</action>
<acceptance_criteria>
- `payrollService.ts` contains `prisma.companyHoliday.findMany` logic when preparing calculations.
</acceptance_criteria>
