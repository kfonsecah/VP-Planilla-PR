---
phase: 43-frontend-rediseno-calendario-eventos
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/src/components/LaborEventsCalendar.tsx
  - src/frontend/src/styles/calendar.css
  - src/frontend/src/components/EventPopover.tsx
autonomous: true
requirements: ["D-02", "D-03", "D-05", "D-06", "D-08"]
must_haves:
  truths:
    - "Calendar uses FullCalendar with completely restyled CSS matching zinc-950 theme"
    - "Events colored by type (vacaciones=blue, incapacidad=orange, permiso=purple, etc.)"
    - "Events show status badge indicator (active/completed/cancelled)"
    - "Clicking an event opens a popover with Ver/Editar/Eliminar actions"
    - "Right-click context menu is removed"
    - "Only month view is available (week/day views removed)"
    - "Drag & drop still works with improved visual feedback"
  artifacts:
    - path: "src/frontend/src/components/EventPopover.tsx"
      provides: "Click-based action popover for calendar events"
      min_lines: 60
    - path: "src/frontend/src/styles/calendar.css"
      provides: "Complete FullCalendar restyling for zinc-950 theme"
      min_lines: 80
    - path: "src/frontend/src/components/LaborEventsCalendar.tsx"
      provides: "Refactored FullCalendar wrapper with new interactions"
      min_lines: 150
  key_links:
    - from: "src/frontend/src/components/LaborEventsCalendar.tsx"
      to: "src/frontend/src/components/EventPopover.tsx"
      via: "renders popover on event click"
      pattern: "EventPopover"
    - from: "src/frontend/src/components/LaborEventsCalendar.tsx"
      to: "src/frontend/src/styles/calendar.css"
      via: "imports calendar styles"
      pattern: "calendar.css"
---

<objective>
Restyle the FullCalendar component completely, replace context menu with click popover, simplify to month-only view, and implement event type coloring with status badges.

Purpose: Transform the calendar visual appearance and interaction model from generic FullCalendar defaults with right-click menus to a polished zinc-950 themed calendar with intuitive click-based actions.

Output: Restyled calendar.css, new EventPopover component, refactored LaborEventsCalendar.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-frontend-rediseno-calendario-eventos/43-CONTEXT.md

@src/frontend/src/components/LaborEventsCalendar.tsx
@src/frontend/src/styles/calendar.css
@src/frontend/src/types/laborEvent.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete calendar.css restyle for zinc-950 theme</name>
  <files>src/frontend/src/styles/calendar.css</files>
  <read_first>src/frontend/src/styles/calendar.css, src/frontend/src/components/LaborEventsCalendar.tsx</read_first>
  <action>
Rewrite calendar.css from scratch for full zinc-950 dark theme integration. Target selectors:

1. **Root container:**
   ```css
   .fc {
     --fc-border-color: theme('colors.zinc.200');
     --fc-page-bg-color: transparent;
     --fc-neutral-bg-color: theme('colors.zinc.50');
     --fc-today-bg-color: rgba(34, 197, 94, 0.06);
     font-family: inherit;
   }
   .dark .fc {
     --fc-border-color: theme('colors.zinc.800');
     --fc-neutral-bg-color: theme('colors.zinc.800');
     --fc-today-bg-color: rgba(34, 197, 94, 0.08);
   }
   ```

2. **Header toolbar:**
   - `.fc .fc-toolbar-title` — `text-lg font-bold text-zinc-700 dark:text-white`
   - `.fc .fc-button` — `bg-zinc-100 dark:bg-zinc-800 border-zinc-200 dark:border-zinc-700 text-zinc-700 dark:text-white rounded-lg px-3 py-1.5 text-sm font-medium`
   - `.fc .fc-button-active` / `.fc .fc-button:hover` — `bg-green-600 text-white border-green-600`
   - Remove button outlines and box-shadows on focus

3. **Day grid:**
   - `.fc .fc-daygrid-day-number` — `text-sm text-zinc-600 dark:text-zinc-400 p-2`
   - `.fc .fc-daygrid-day.fc-day-today .fc-daygrid-day-number` — `bg-green-600 text-white w-7 h-7 rounded-full flex items-center justify-center`
   - `.fc .fc-daygrid-day:hover` — `bg-zinc-50 dark:bg-zinc-800/50`
   - `.fc .fc-col-header-cell` — `text-xs font-semibold text-zinc-500 dark:text-zinc-400 uppercase py-3`

4. **Event type colors (new classes):**
   ```css
   .event-type-vacaciones { background-color: #3B82F6; border-left: 3px solid #2563EB; }
   .event-type-incapacidad { background-color: #F97316; border-left: 3px solid #EA580C; }
   .event-type-permiso { background-color: #A855F7; border-left: 3px solid #9333EA; }
   .event-type-dia-libre { background-color: #14B8A6; border-left: 3px solid #0D9488; }
   .event-type-suspension { background-color: #EF4444; border-left: 3px solid #DC2626; }
   .event-type-otro { background-color: #71717A; border-left: 3px solid #52525B; }
   ```
   Dark variants slightly muted:
   ```css
   .dark .event-type-vacaciones { background-color: #2563EB; }
   .dark .event-type-incapacidad { background-color: #EA580C; }
   /* ... etc */
   ```

5. **Event rendering:**
   - `.fc-event` — `rounded-md px-2 py-1 text-xs font-medium shadow-sm cursor-pointer transition-all duration-150`
   - `.fc-event:hover` — `shadow-md transform scale-[1.02]`
   - `.fc-event .fc-event-title` — `truncate`

6. **Status badges (via pseudo-elements or added elements):**
   ```css
   .status-badge-active::after { content: ''; width: 6px; height: 6px; background: #22C55E; border-radius: 50%; position: absolute; top: 4px; right: 4px; }
   .status-badge-completed::after { content: ''; width: 6px; height: 6px; background: #60A5FA; border-radius: 50%; ... }
   .status-badge-cancelled::after { content: ''; width: 6px; height: 6px; background: #F87171; border-radius: 50%; ... opacity: 0.7; }
   ```

7. **Drag feedback:**
   ```css
   .fc-event-dragging { opacity: 0.7; box-shadow: 0 8px 25px rgba(0,0,0,0.15); transform: scale(1.05); }
   .fc-event-resizing { border: 2px dashed currentColor; }
   ```

8. **"More" link:**
   - `.fc .fc-more-link` — `text-xs text-green-600 dark:text-green-400 font-medium hover:underline`

9. **Remove old status classes:**
   - Delete `.status-active`, `.status-completed`, `.status-inactive` (replaced by event-type + status-badge)

10. **Preview state (for ghost events):**
    ```css
    .status-preview {
      background: repeating-linear-gradient(45deg, transparent, transparent 3px, rgba(255,255,255,0.1) 3px, rgba(255,255,255,0.1) 6px) !important;
      border: 2px dashed #D97706 !important;
      animation: pulse 2s infinite;
    }
    ```
  </action>
  <verify>npm run dev (visual check calendar renders correctly in dark mode)</verify>
  <acceptance_criteria>
    - calendar.css contains "--fc-border-color" CSS variable with zinc theme
    - calendar.css contains ".event-type-vacaciones" class with "#3B82F6"
    - calendar.css contains ".event-type-incapacidad" class with "#F97316"
    - calendar.css contains ".event-type-permiso" class with "#A855F7"
    - calendar.css contains ".status-badge-active" class
    - calendar.css contains ".fc-event-dragging" with improved visual feedback
    - calendar.css contains ".status-preview" with dashed border styling
    - calendar.css does NOT contain old ".status-active" class (replaced)
  </acceptance_criteria>
  <done>Calendar fully restyled with zinc-950 theme, event type colors, status badges, and drag feedback</done>
</task>

<task type="auto">
  <name>Task 2: Create EventPopover component</name>
  <files>src/frontend/src/components/EventPopover.tsx</files>
  <read_first>src/frontend/src/components/LaborEventsCalendar.tsx, src/frontend/src/components/ui/DropdownMenu.tsx, src/frontend/src/types/laborEvent.ts</read_first>
  <action>
Create click-based action popover replacing right-click context menu. Requirements:

1. **Props interface:**
   ```typescript
   interface EventPopoverProps {
     event: EmployeeLaborEvent;
     anchor: { x: number; y: number };
     employeeName: string;
     onView: () => void;
     onEdit: () => void;
     onDelete: () => void;
     onClose: () => void;
   }
   ```

2. **Layout:**
   - Header section showing event name + employee name + date range
   - Status badge (colored pill: Activo/Completado/Cancelado)
   - Event type indicator (colored left border matching type color)
   - Divider
   - Action buttons: Ver Detalles (EyeIcon), Editar (PencilIcon), Eliminar (TrashIcon)
   - Eliminar button in red with hover state

3. **Positioning:**
   - Position at anchor coordinates using `position: fixed`
   - Add collision detection: if popover would overflow viewport bottom, position above anchor
   - If would overflow right, flip to left side
   - Use `useEffect` with `useRef` to measure popover dimensions after render
   - Add 8px padding from viewport edges (same pattern as DropdownMenu)

4. **Interactions:**
   - Click outside closes popover (event listener on document)
   - Escape key closes popover
   - Animate in with framer-motion: `initial={{ opacity: 0, scale: 0.95 }}` `animate={{ opacity: 1, scale: 1 }}`
   - 200ms debounce to prevent instant close on the click that opened it

5. **Styling:**
   - `bg-white dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-700`
   - `rounded-xl shadow-xl` — elevated popover look
   - `w-64` fixed width
   - Action buttons: full width, left-aligned with icon + text, `hover:bg-zinc-100 dark:hover:bg-zinc-800`
  </action>
  <verify>npx tsc --noEmit (from src/frontend/)</verify>
  <acceptance_criteria>
    - EventPopover.tsx exists and exports default component
    - EventPopover.tsx contains "onView", "onEdit", "onDelete", "onClose" props
    - EventPopover.tsx contains collision detection logic (viewport boundary check)
    - EventPopover.tsx contains "EyeIcon", "PencilIcon", "TrashIcon" imports
    - EventPopover.tsx contains framer-motion animation for enter/exit
    - EventPopover.tsx contains Escape key handler
    - npx tsc --noEmit passes
  </acceptance_criteria>
  <done>EventPopover component provides click-based action menu with collision detection and animations</done>
</task>

<task type="auto">
  <name>Task 3: Refactor LaborEventsCalendar — new interactions + colors + month-only</name>
  <files>src/frontend/src/components/LaborEventsCalendar.tsx</files>
  <read_first>src/frontend/src/components/LaborEventsCalendar.tsx, src/frontend/src/components/EventPopover.tsx, src/frontend/src/styles/calendar.css, src/frontend/src/types/laborEvent.ts</read_first>
  <action>
Refactor LaborEventsCalendar.tsx for the new interaction model. Changes:

1. **Remove:**
   - `timeGridPlugin` import and from plugins array (D-08: month only)
   - `timeGridWeek` and `timeGridDay` from headerToolbar right section
   - Entire context menu system: `anchor` state, `selectedEvent` state, `menuOpenedAtRef`, `openMenuForEvent()`, `closeMenu()`, the click-outside useEffect for context menu, the `contextmenu` event listener in `eventDidMount`
   - The JSX block rendering the options popover div (lines 322-351)

2. **Add EventPopover integration:**
   ```typescript
   const [popoverEvent, setPopoverEvent] = useState<{
     event: EmployeeLaborEvent;
     anchor: { x: number; y: number };
   } | null>(null);
   ```

3. **Change eventClick handler:**
   Instead of calling `onEventClick` directly, open EventPopover:
   ```typescript
   const handleEventClick = (info: EventClickArg) => {
     const jsEvent = info.jsEvent;
     jsEvent?.preventDefault();
     jsEvent?.stopPropagation();
     
     const event = events.find(e => String(e.id) === info.event.id);
     if (event) {
       const rect = info.el.getBoundingClientRect();
       setPopoverEvent({
         event,
         anchor: { x: rect.right + 8, y: rect.top }
       });
     }
   };
   ```

4. **Update toCalendarEvent function — event type coloring:**
   Replace `getStatusClass` with event-type based class:
   ```typescript
   function getEventTypeClass(eventName?: string): string {
     const name = (eventName || '').toLowerCase();
     if (name.includes('vacacion')) return 'event-type-vacaciones';
     if (name.includes('incapacidad')) return 'event-type-incapacidad';
     if (name.includes('permiso')) return 'event-type-permiso';
     if (name.includes('libre')) return 'event-type-dia-libre';
     if (name.includes('suspensi')) return 'event-type-suspension';
     return 'event-type-otro';
   }
   
   function getStatusBadgeClass(status?: string): string {
     if (status === 'completed') return 'status-badge-completed';
     if (status === 'cancelled') return 'status-badge-cancelled';
     return 'status-badge-active';
   }
   ```
   In `toCalendarEvent`: `classNames: [getEventTypeClass(event.labor_event_name), getStatusBadgeClass(event.status)]`

5. **Add new prop for navigating to date from mini-calendar:**
   ```typescript
   navigateToDate?: Date;  // Add to Props interface
   ```
   Use `calendarRef` to call `calendarRef.current?.getApi().gotoDate(navigateToDate)` when it changes.

6. **Update headerToolbar:**
   ```typescript
   headerToolbar={{
     left: 'prev,next today',
     center: 'title',
     right: ''  // Remove view switchers — month only
   }}
   ```

7. **Render EventPopover in component JSX:**
   ```tsx
   {popoverEvent && (
     <EventPopover
       event={popoverEvent.event}
       anchor={popoverEvent.anchor}
       employeeName={employees.find(e => String(e.id) === String(popoverEvent.event.employee_id))?.name || 'Empleado'}
       onView={() => { onEventClick?.(popoverEvent.event); setPopoverEvent(null); }}
       onEdit={() => { onEventClick?.(popoverEvent.event); setPopoverEvent(null); }}
       onDelete={async () => {
         await deleteAssignment(popoverEvent.event.id);
         await refreshEvents();
         setPopoverEvent(null);
       }}
       onClose={() => setPopoverEvent(null)}
     />
   )}
   ```

8. **Keep:** drag & drop (eventDrop, eventResize), date selection, datesSet, preview event system
  </action>
  <verify>npx tsc --noEmit (from src/frontend/)</verify>
  <acceptance_criteria>
    - LaborEventsCalendar.tsx does NOT import "timeGridPlugin"
    - LaborEventsCalendar.tsx does NOT contain "contextmenu" event listener
    - LaborEventsCalendar.tsx contains "EventPopover" import
    - LaborEventsCalendar.tsx contains "getEventTypeClass" function
    - LaborEventsCalendar.tsx contains "getStatusBadgeClass" function
    - LaborEventsCalendar.tsx headerToolbar right is empty string ''
    - LaborEventsCalendar.tsx contains "popoverEvent" state
    - npx tsc --noEmit passes
  </acceptance_criteria>
  <done>Calendar refactored: month-only, click popover, event type colors + status badges, no context menu</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes from src/frontend/
- [ ] Calendar renders with new event type colors
- [ ] Clicking event opens popover (not context menu)
- [ ] Right-click no longer opens custom menu
- [ ] Only month view available (no week/day switcher)
- [ ] Drag & drop still functional
- [ ] Events show status badge dots
</verification>

<success_criteria>
- Calendar completely restyled for zinc-950 dark theme
- Event type color system implemented (6 types)
- Status badge system working (3 states)
- Click popover replaces right-click context menu
- Month-only view (week/day views removed)
- Drag & drop preserved with improved feedback
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/43-frontend-rediseno-calendario-eventos/43-02-SUMMARY.md`
</output>
