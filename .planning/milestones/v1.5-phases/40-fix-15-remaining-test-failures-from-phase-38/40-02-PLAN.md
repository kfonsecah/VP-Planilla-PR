---
phase: 40-fix-15-remaining-test-failures-from-phase-38
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/__tests__/pages/clock-logs/page.test.tsx
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "All frontend tests pass with 0 failures"
    - "clock-logs/page.test.tsx 10 failures are resolved"
    - "Tests match current grouped UI structure"
  artifacts:
    - path: "src/frontend/__tests__/pages/clock-logs/page.test.tsx"
      provides: "Test pass for clock-logs page"
  key_links:
    - from: "src/frontend/__tests__/pages/clock-logs/page.test.tsx"
      to: "src/frontend/src/features/clock-logs/page.tsx"
      via: "UI element queries"
---

<objective>
Fix the 10 frontend test failures in clock-logs/page.test.tsx (UI element mismatches).
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/40-fix-15-remaining-test-failures-from-phase-38/40-CONTEXT.md
@.planning/phases/38-tests-unitarios-verificacion-integracion/38-VERIFICATION.md

**Gap analysis from Phase 38:**
- clock-logs/page.test.tsx: Tests look for old UI elements:
  - "siguiente" button (doesn't exist in grouped view)
  - "buscar empleado" placeholder (replaced with search in header)
  - "hoy" button (replaced with period selector)
- Current UI is a hierarchical grouped view (Branch > Employee > Day > Pair)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run clock-logs page tests to see specific failures</name>
  <files>src/frontend/__tests__/pages/clock-logs/page.test.tsx</files>
  <action>
Run the frontend clock-logs page tests to see the specific failures:
  cd src/frontend && npm test -- --testPathPattern="clock-logs/page.test.tsx" 2>&1
  
Capture the exact errors to understand what UI elements are being queried.
  </action>
  <verify>
    <automated>cd src/frontend && npm test -- --testPathPattern="clock-logs/page.test.tsx" 2>&1 | tail -40</automated>
  </verify>
  <done>Test output shows specific failure messages</done>
</task>

<task type="auto">
  <name>Task 2: Update test queries to match current UI structure</name>
  <files>src/frontend/__tests__/pages/clock-logs/page.test.tsx</files>
  <action>
Read the current clock-logs page (src/frontend/src/features/clock-logs/page.tsx) to understand the current UI structure:

The new grouped UI shows:
- BranchGroup components (grouped by branch)
- EmployeeCard components (grouped by employee)  
- DailyRow components (grouped by day)
- Search in header area (not "buscar empleado" input)
- Pagination or infinite scroll (not "siguiente" button)
- Period selector (not "hoy" button)

Update the test queries to:
1. Remove queries for non-existent elements ("siguiente", "buscar empleado", "hoy")
2. Add queries for new elements (BranchGroup, EmployeeCard, period selector)
3. Update assertions to match the grouped view structure
  </action>
  <verify>
    <automated>cd src/frontend && npm test -- --testPathPattern="clock-logs/page.test.tsx" 2>&1 | tail -20</automated>
  </verify>
  <done>All 10 previously failing tests now pass</done>
</task>

<task type="auto">
  <name>Task 3: Verify all frontend tests pass</name>
  <files>src/frontend</files>
  <action>
Run full frontend test suite to confirm all tests pass:
  cd src/frontend && npm test
  </action>
  <verify>
    <automated>cd src/frontend && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>Frontend test suite shows 0 failures</done>
</task>

</tasks>

<verification>
Run: cd src/frontend && npm test
Expected: All tests pass, 0 failures
</verification>

<success_criteria>
- [ ] clock-logs/page.test.tsx tests pass (was 10 failures, now 0)
- [ ] Full frontend test suite passes with 0 failures
- [ ] Tests correctly verify the grouped clock-logs UI
</success_criteria>

<output>
After completion, create `.planning/phases/40-fix-15-remaining-test-failures-from-phase-38/40-02-SUMMARY.md`
</output>