---
phase: 35-frontend-clock-log-corrections
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/src/components/VoidClockLogModal.tsx
autonomous: true
requirements:
  - MARCAS-04
  - MARCAS-05
  - UX-02
context_references:
  - .planning/phases/35-frontend-clock-log-corrections/35-CONTEXT.md
  - .planning/phases/35-frontend-clock-log-corrections/35-UI-SPEC.md

must_haves:
  truths:
    - User can void/annul a mark with soft delete
    - Justification required and validated
    - Confirmation requires typing "ANULAR" per UX-02
    - Soft delete preserves original, audit trails the void
  artifacts:
    - path: src/frontend/src/components/VoidClockLogModal.tsx
      provides: Modal for soft-deleting marks with typed confirmation
      min_lines: 80
  key_links:
    - from: VoidClockLogModal
      to: clockLogAdjustmentService.voidClockLog
      via: onConfirm handler with text validation
      pattern: onConfirm.*voidClockLog.*text === 'ANULAR'
---

<objective>
Create Void Clock Log Modal with destructive action confirmation requiring typed "ANULAR".
</objective>

<interfaces>
Reuse from Plan 01:
```typescript
interface ClockLog {
  id: string;
  employeeId: string;
  timestamp: string;
  type: 'IN' | 'OUT';
  // ...
}
```
</interfaces>

<read_first>
- src/frontend/src/components/ClockLogDetailModal.tsx — Existing void/warning pattern
- src/frontend/src/components/EditClockLogModal.tsx — From Plan 02
- src/frontend/src/components/ui/Modal.tsx — Base modal
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Create VoidClockLogModal Component</name>
  <files>src/frontend/src/components/VoidClockLogModal.tsx</files>
  <action>
Create void modal with destructive confirmation per UX-02:

**Props:**
```typescript
interface VoidClockLogModalProps {
  isOpen: boolean;
  onClose: () => void;
  clockLog: ClockLog;
  onConfirm?: (voidedMark: ClockLog) => void;
}
```

**Display (read-only):**
- Show the mark being voided:
  - "Marca a anular: {tipo} el {fecha} a las {hora}"
  - Employee name

**Warning section:**
- Red styling (#dc2626) for destructive context
- Text: "Esta acción es IRREVERSIBLE. La marca no se eliminará físicamente pero no se incluirá en cálculos."

**Form fields:**
1. Justification textarea (min 10 chars)
   - Character counter

2. Confirmation text input (D-07, UX-02):
   - Label: "Confirmar anulación"
   - Placeholder: 'Escriba "ANULAR" para confirmar'
   - Validation: `value.toUpperCase() === 'ANULAR'`
   - Error: "Debe escribir ANULAR para confirmar"

**Buttons:**
- Cancel (secondary/gray): Closes modal
- Confirm (destructive/red): Calls voidClockLog, enabled only when confirmation passes

**Submission:**
- Call `clockLogAdjustmentService.voidClockLog(clockLog.id, justification)`
- Soft delete: original preserved, flagged as annulled
- On success: `onConfirm()` then close

**Styling:**
- Red/destructive color theme
- Red warning banner at top
- Disabled confirm until "ANULAR" typed
  </action>
  <verify>
    <automated>test -f src/frontend/src/components/VoidClockLogModal.tsx && echo "File exists"</automated>
  </verify>
  <done>Modal with confirmation requiring "ANULAR" text, justification, soft delete via service</done>
</task>

</tasks>

<verification>
- [ ] VoidClockLogModal shows red warning about irreversible action
- [ ] Confirmation input requires typing "ANULAR" (case-insensitive)
- [ ] Justification field with character counter
- [ ] Confirm button disabled until "ANULAR" typed
- [ ] Calls voidClockLog service on confirm
</verification>

<success_criteria>
User can void mark with confirmation requiring "ANULAR" text, justification recorded in audit.
</success_criteria>

<output>
After completion, create `.planning/phases/35-frontend-clock-log-corrections/35-03-SUMMARY.md`
</output>