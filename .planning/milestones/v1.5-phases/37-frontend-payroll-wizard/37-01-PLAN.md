---
phase: 37-frontend-payroll-wizard
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/src/components/PayrollWizard.tsx
  - src/frontend/src/hooks/usePayrollWizard.ts
  - src/frontend/src/components/PayrollPeriodCard.tsx
autonomous: true
requirements:
  - PLANILLA-01
  - PLANILLA-03

must_haves:
  truths:
    - User can select a biweekly period from pre-calculated period cards
    - Selected period shows green highlight and triggers calculation
    - Wizard state persists across step navigation
  artifacts:
    - path: src/frontend/src/components/PayrollWizard.tsx
      provides: Parent wizard container with step management
      min_lines: 80
    - path: src/frontend/src/hooks/usePayrollWizard.ts
      provides: Wizard state management hook
      min_lines: 50
    - path: src/frontend/src/components/PayrollPeriodCard.tsx
      provides: Individual period card component
      min_lines: 40
  key_links:
    - from: src/frontend/src/components/PayrollWizard.tsx
      to: src/frontend/src/hooks/usePayrollWizard.ts
      via: usePayrollWizard hook import
      pattern: import.*usePayrollWizard
    - from: src/frontend/src/components/PayrollPeriodCard.tsx
      to: src/frontend/src/hooks/usePayrollWizard.ts
      via: Period type import
      pattern: BiweeklyPeriod
---

<objective>
**Phase 37 Plan 01:** Core wizard infrastructure — create parent wizard component, state hook, and period card component.

Purpose: Establish the foundation for the 3-step wizard. This enables Step 1 (period selection) functionality.

Output:
- PayrollWizard.tsx (parent container)
- usePayrollWizard.ts (state hook)
- PayrollPeriodCard.tsx (period card component)
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-frontend-payroll-wizard/37-RESEARCH.md
@.planning/phases/37-frontend-payroll-wizard/37-UI-SPEC.md
@.planning/milestones/v1.5-ROADMAP.md

<interfaces>
<!-- Key types from 37-RESEARCH.md -->
```typescript
interface WizardState {
  currentStep: 1 | 2 | 3;
  selectedPeriod: {
    start: string;
    end: string;
    label: string;
    preCalculated?: boolean;
  } | null;
  calculationData: CalculationResult | null;
  payrollId: number | null;
}

interface BiweeklyPeriod {
  start: Date;
  end: Date;
  label: string;
  isCurrent: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create usePayrollWizard hook</name>
  <files>src/frontend/src/hooks/usePayrollWizard.ts</files>
  <read_first>src/frontend/src/hooks/useEffectiveMarks.ts (existing hook pattern)</read_first>
  <action>
Create usePayrollWizard.ts hook with:
- WizardState interface matching RESEARCH.md patterns
- State: currentStep (1|2|3), selectedPeriod, calculationData, payrollId
- Functions: selectPeriod, goToStep, setCalculationData, setPayrollId, reset
- Export BiweeklyPeriod interface for period card consumption

Use React useState + useCallback. Match patterns from useEffectiveMarks.ts.
  </action>
  <verify>
grep -l "usePayrollWizard" src/frontend/src/hooks/usePayrollWizard.ts && \
grep -l "currentStep" src/frontend/src/hooks/usePayrollWizard.ts && \
grep -l "selectedPeriod" src/frontend/src/hooks/usePayrollWizard.ts
  </verify>
  <acceptance_criteria>
- File exists at src/frontend/src/hooks/usePayrollWizard.ts
- Exports usePayrollWizard hook function
- WizardState interface with currentStep, selectedPeriod, calculationData, payrollId
- selectPeriod, goToStep, setCalculationData, setPayrollId, reset functions exported
  </acceptance_criteria>
  <done>usePayrollWizard hook created with full state management functions</done>
</task>

<task type="auto">
  <name>Task 2: Create PayrollPeriodCard component</name>
  <files>src/frontend/src/components/PayrollPeriodCard.tsx</files>
  <read_first>src/frontend/src/components/ui/Badge.tsx (existing card patterns)</read_first>
  <action>
Create PayrollPeriodCard.tsx with:
- Props: period (BiweeklyPeriod), isSelected (boolean), onSelect (function)
- Display: CalendarIcon (@heroicons/react), label, date range, "Actual" badge if isCurrent
- Styling: Selected = green border (#16a34a) + green bg, unselected = zinc border
- Click handler calls onSelect with period

Based on UI-SPEC.md: "Selected card: green border (#16a34a), green subtle background"
  </action>
  <verify>
grep -l "PayrollPeriodCard" src/frontend/src/components/PayrollPeriodCard.tsx && \
grep -l "green" src/frontend/src/components/PayrollPeriodCard.tsx
  </verify>
  <acceptance_criteria>
- File exists at src/frontend/src/components/PayrollPeriodCard.tsx
- Props interface matches BiweeklyPeriod from usePayrollWizard
- isSelected prop triggers green styling (border-zinc-200 unselected, green-600 selected)
- "Actual" badge displayed when period.isCurrent is true
  </acceptance_criteria>
  <done>PayrollPeriodCard component with green selection state</done>
</task>

<task type="auto">
  <name>Task 3: Create PayrollWizard parent component</name>
  <files>src/frontend/src/components/PayrollWizard.tsx</files>
  <read_first>src/frontend/src/components/PayrollWizardStep1.tsx (if exists in codebase)</read_first>
  <action>
Create PayrollWizard.tsx (main container) with:
- Import usePayrollWizard hook
- Render step indicator at top (1 → 2 → 3 with current step highlighted)
- Conditional rendering based on state.currentStep:
  - Step 1: Period selection grid (PayrollPeriodCard array)
  - Step 2: Review calculation placeholder
  - Step 3: Executive summary placeholder
- Primary CTA navigation ("Calcular Planilla" → Step 2, "Revisar y continuar" → Step 3)

Step indicator per UI-SPEC.md: "Green accent on selected card and primary CTA"
  </action>
  <verify>
grep -l "PayrollWizard" src/frontend/src/components/PayrollWizard.tsx && \
grep -l "currentStep" src/frontend/src/components/PayrollWizard.tsx
  </verify>
  <acceptance_criteria>
- File exists at src/frontend/src/components/PayrollWizard.tsx
- Imports and uses usePayrollWizard hook
- Renders step indicator (1, 2, 3) with current step highlighted
- Conditional rendering for Step 1, 2, 3
- Primary CTAs: "Calcular Planilla", "Revisar y continuar"
  </acceptance_criteria>
  <done>PayrollWizard container with step management and navigation</done>
</task>

</tasks>

<verification>
Check that all three files exist and contain the expected exports:
- usePayrollWizard.ts exports hook
- PayrollPeriodCard.tsx renders with green selection
- PayrollWizard.tsx manages step navigation
</verification>

<success_criteria>
- [ ] usePayrollWizard.ts created with full state management
- [ ] PayrollPeriodCard.tsx created with green selection styling
- [ ] PayrollWizard.tsx created with step navigation
- [ ] Files follow UI-SPEC.md color and typography rules
</success_criteria>

<output>
After completion, create `.planning/phases/37-frontend-payroll-wizard/37-01-SUMMARY.md`
</output>