---
phase: 37-frontend-payroll-wizard
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/src/components/PayrollWizard.tsx
  - src/frontend/src/app/pages/payroll/list/page.tsx
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - Step 2 Review displays actual calculation data from backend
    - Step 3 Approve displays actual summary from calculation data
    - Status badges use correct colors (PAGADA = blue)
    - Contextual action buttons appear based on status
  artifacts:
    - path: src/frontend/src/components/PayrollWizard.tsx
      provides: Integration with calculation service
    - path: src/frontend/src/app/pages/payroll/list/page.tsx
      provides: Status badge color fix + contextual actions
  key_links:
    - from: PayrollWizard.tsx
      to: PayrollService
      via: API call to fetch calculation data
---

<objective>
**Gap Closure Plan 05:** Fix UAT verification failures from 37-UAT.md.

This plan addresses:
1. Step 2 & 3 showing placeholder text instead of actual calculation data
2. Status badge colors wrong (PAGADA vs PAGADO mismatch)
3. Contextual action buttons not appearing

Root cause: Backend returns `PAGADA` but frontend checks for `PAGADO`. Also, wizard doesn't fetch calculation data.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-frontend-payroll-wizard/37-UAT.md (gap source)
@.planning/phases/37-frontend-payroll-wizard/37-01-SUMMARY.md
@.planning/phases/37-frontend-payroll-wizard/37-02-SUMMARY.md
@.planning/phases/37-frontend-payroll-wizard/37-03-SUMMARY.md
@.planning/phases/37-frontend-payroll-wizard/37-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix status badge colors (PAGADA vs PAGADO)</name>
  <files>src/frontend/src/app/pages/payroll/list/page.tsx</files>
  <read_first>src/frontend/src/app/pages/payroll/list/page.tsx (line 47-92)</read_first>
  <action>
Fix the getStatusBadge function to check for both 'PAGADA' (from backend) and 'PAGADO' (legacy):

Current buggy code (line 67):
```
if (status === 'PAGADO' || status === 'paid' || status === 'completed') {
```

Fix to:
```
if (status === 'PAGADA' || status === 'PAGADO' || status === 'paid' || status === 'completed') {
```

This fixes the color mismatch: PAGADA = blue badge, BORRADOR = gray badge.
  </action>
  <verify>
grep -n "PAGADA.*PAGADO" src/frontend/src/app/pages/payroll/list/page.tsx
  </verify>
  <acceptance_criteria>
- getStatusBadge checks for 'PAGADA' (backend value) and 'PAGADO' (legacy)
- PAGADA shows blue badge (bg-blue-600)
- BORRADOR shows gray badge (bg-zinc-100)
  </acceptance_criteria>
  <done>Status badge colors correct for all statuses</done>
</task>

<task type="auto">
  <name>Task 2: Fix contextual action buttons (status check)</name>
  <files>src/frontend/src/app/pages/payroll/list/page.tsx</files>
  <read_first>src/frontend/src/app/pages/payroll/list/page.tsx (lines 402-470)</read_first>
  <action>
Fix the status checks in contextual action buttons to include 'PAGADA':

Current (lines 402, 422, 452):
```
p.status === 'BORRADOR' && ...
p.status === 'APROBADA' && ...
p.status === 'PAGADO' && ...
```

Fix to:
```
(p.status === 'BORRADOR' || status === 'draft' || ...) && ...
(p.status === 'APROBADA' || status === 'approved') && ...
(p.status === 'PAGADA' || p.status === 'PAGADO' || ...) && ...
```

This ensures buttons appear when backend returns the correct enum values.
  </action>
  <verify>
grep -n "PAGADA.*&&" src/frontend/src/app/pages/payroll/list/page.tsx | head -5
  </verify>
  <acceptance_criteria>
- BORRADOR rows show "Aprobar" button
- APROBADA rows show "Marcar como Pagada" + "Reabrir" buttons  
- PAGADA rows show "Reabrir" button
  </acceptance_criteria>
  <done>Contextual action buttons appear based on status</done>
</task>

<task type="auto">
  <name>Task 3: Integrate calculation data into wizard steps</name>
  <files>src/frontend/src/components/PayrollWizard.tsx</files>
  <read_first>src/frontend/src/components/PayrollWizard.tsx (lines 1-159)</read_first>
  <action>
The wizard needs to fetch calculation data when a period is selected. Current code shows placeholders in Steps 2 and 3.

Add integration with calculation service:

1. Update usePayrollWizard hook to include:
   - calculationData state
   - fetchCalculation(periodStart, periodEnd) function that calls the calculation API

2. In PayrollWizard.tsx handleCalculate:
   - Call fetchCalculation with selected period dates
   - Store result in calculationData
   - Pass calculationData to PayrollWizardStep2 and Step3

3. Wire PayrollWizardStep2 and PayrollWizardStep3 to use calculationData prop:
   - Step 2: Display PayrollResults with actual employee data
   - Step 3: Display executive summary with totals from calculationData

Reference the backend API at /api/payroll/calculate or similar endpoint that returns CalculationResult.
  </action>
  <verify>
grep -n "calculationData" src/frontend/src/components/PayrollWizard.tsx
  </verify>
  <acceptance_criteria>
- Step 2 shows employee breakdown table (not placeholder)
- Step 3 shows executive summary with totals (not placeholder)
- Data fetched when user clicks "Calcular Planilla"
  </acceptance_criteria>
  <done>Wizard steps display actual calculation data</done>
</task>

</tasks>

<verification>
Run UAT tests again:
1. Test 3 - Step 2 Review should show actual data
2. Test 5 - Step 3 Approve should show actual summary  
3. Test 6 - Status badges: PAGADA=blue, BORRADOR=gray
4. Test 7 - Contextual buttons appear based on status
</verification>

<success_criteria>
- [ ] Status badge colors fixed (PAGADA = blue)
- [ ] Contextual action buttons appear based on correct status
- [ ] Step 2 and 3 display actual calculation data
</success_criteria>

<output>
After completion, create `.planning/phases/37-frontend-payroll-wizard/37-05-SUMMARY.md`
</output>
