---
phase: 37-frontend-payroll-wizard
plan: "03"
type: execute
wave: 3
depends_on:
  - 37-02
files_modified:
  - src/frontend/src/components/PayrollWizardStep3.tsx
  - src/frontend/src/services/payrollService.ts
autonomous: true
requirements:
  - PLANILLA-04
  - UX-02

must_haves:
  truths:
    - User sees executive summary with totals before approval
    - User must type "APROBAR" to confirm (UX-02 requirement)
    - Approval triggers Phase 36 state machine transition
  artifacts:
    - path: src/frontend/src/components/PayrollWizardStep3.tsx
      provides: Step 3 summary + confirmation dialog
      min_lines: 80
  key_links:
    - from: src/frontend/src/components/PayrollWizardStep3.tsx
      to: src/frontend/src/services/payrollService.ts
      via: approvePayroll API call
      pattern: PayrollService.approvePayroll
    - from: src/frontend/src/components/PayrollWizardStep3.tsx
      to: src/frontend/src/components/ui/ConfirmDialog.tsx
      via: Component import
      pattern: ConfirmDialog
---

<objective>
**Phase 37 Plan 03:** Step 3 — Executive Summary + Confirmation — integrate with Phase 36 state machine.

Purpose: Final step with executive summary and approval confirmation. Triggers Phase 36 backend state machine for approval workflow.

Output:
- PayrollWizardStep3.tsx (Step 3 with confirmation)
- Update payrollService.ts with approvePayroll, markAsPaid, reopenPayroll
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-frontend-payroll-wizard/37-RESEARCH.md
@.planning/phases/37-frontend-payroll-wizard/37-UI-SPEC.md

<interfaces>
<!-- From 37-RESEARCH.md backend integration -->
```typescript
export const PayrollService = {
  async approvePayroll(payrollId: number): Promise<Payroll>
  async markAsPaid(payrollId: number): Promise<Payroll>
  async reopenPayroll(payrollId: number, reason: string): Promise<Payroll>
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PayrollService with state machine methods</name>
  <files>src/frontend/src/services/payrollService.ts</files>
  <read_first>src/frontend/src/services/payrollService.ts (existing service)</read_first>
  <action>
Update payrollService.ts to add:
- approvePayroll(payrollId): POST /payroll/${payrollId}/approve
- markAsPaid(payrollId): POST /payroll/${payrollId}/pay
- reopenPayroll(payrollId, reason): POST /payroll/${payrollId}/reopen

These endpoints correspond to Phase 36 state machine transitions per 37-RESEARCH.md.
  </action>
  <verify>
grep -l "approvePayroll" src/frontend/src/services/payrollService.ts && \
grep -l "markAsPaid" src/frontend/src/services/payrollService.ts
  </verify>
  <acceptance_criteria>
- File updated at src/frontend/src/services/payrollService.ts
- approvePayroll calls POST /payroll/${id}/approve
- markAsPaid calls POST /payroll/${id}/pay
- reopenPayroll calls POST /payroll/${id}/reopen
  </acceptance_criteria>
  <done>PayrollService updated with state machine methods</done>
</task>

<task type="auto">
  <name>Task 2: Create PayrollWizardStep3 with confirmation</name>
  <files>src/frontend/src/components/PayrollWizardStep3.tsx</files>
  <read_first>src/frontend/src/components/ui/ConfirmDialog.tsx (existing dialog)</read_first>
  <action>
Create PayrollWizardStep3.tsx with:
- Props: payrollId, calculationData, onApprove
- Executive Summary card (green bg #16a34a):
  - Period label, employee count, total bruto, total neto
- "Aprobar Planilla" primary CTA → opens ConfirmDialog
- ConfirmDialog requires user to type "APROBAR" (UX-02 requirement)
- On confirm: call PayrollService.approvePayroll(payrollId)
- On success: toast "Planilla aprobada exitosamente"
- On back: return to Step 2

Per UI-SPEC.md: "ConfirmDialog requires user to type APROBAR"
  </action>
  <verify>
grep -l "PayrollWizardStep3" src/frontend/src/components/PayrollWizardStep3.tsx && \
grep -l "ConfirmDialog" src/frontend/src/components/PayrollWizardStep3.tsx
  </verify>
  <acceptance_criteria>
- File exists at src/frontend/src/components/PayrollWizardStep3.tsx
- Imports ConfirmDialog component
- Shows executive summary grid (period, employees, bruto, neto)
- "Aprobar Planilla" button opens confirmation dialog
- Requires "APROBAR" text input to enable confirm
- Calls PayrollService.approvePayroll on confirm
  </acceptance_criteria>
  <done>PayrollWizardStep3 with confirmation requiring "APROBAR"</done>
</task>

</tasks>

<verification>
Check that both files exist with correct integrations.
</verification>

<success_criteria>
- [ ] PayrollService updated with state machine methods
- [ ] PayrollWizardStep3 requires text input "APROBAR"
- [ ] Approve triggers Phase 36 state machine
- [ ] Success toast on approval
</success_criteria>

<output>
After completion, create `.planning/phases/37-frontend-payroll-wizard/37-03-SUMMARY.md`
</output>