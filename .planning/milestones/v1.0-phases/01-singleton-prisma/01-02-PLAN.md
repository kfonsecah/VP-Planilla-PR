---
phase: 01-singleton-prisma
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/src/service/AuditLogsService.ts
  - src/backend/src/service/BonusesService.ts
  - src/backend/src/service/ClockLogsService.ts
  - src/backend/src/service/DeductionsService.ts
  - src/backend/src/service/EmployeeDeductions.ts
autonomous: true
requirements: [1.1, 1.2, 1.3]

must_haves:
  truths:
    - "AuditLogsService, BonusesService, ClockLogsService, DeductionsService, EmployeeDeductions all import prisma from the singleton"
    - "Zero instances of new PrismaClient() remain in these 5 files"
    - "TypeScript compiler passes after edits"
  artifacts:
    - path: "src/backend/src/service/AuditLogsService.ts"
      provides: "Singleton prisma import"
      contains: "from '../lib/prisma'"
    - path: "src/backend/src/service/BonusesService.ts"
      provides: "Singleton prisma import"
      contains: "from '../lib/prisma'"
    - path: "src/backend/src/service/ClockLogsService.ts"
      provides: "Singleton prisma import"
      contains: "from '../lib/prisma'"
    - path: "src/backend/src/service/DeductionsService.ts"
      provides: "Singleton prisma import"
      contains: "from '../lib/prisma'"
    - path: "src/backend/src/service/EmployeeDeductions.ts"
      provides: "Singleton prisma import"
      contains: "from '../lib/prisma'"
  key_links:
    - from: "src/backend/src/service/AuditLogsService.ts"
      to: "src/backend/src/lib/prisma.ts"
      via: "import { prisma }"
      pattern: "import.*prisma.*from.*lib/prisma"
---

<objective>
Migrate 5 backend service files (AuditLogsService, BonusesService, ClockLogsService, DeductionsService, EmployeeDeductions) from local `new PrismaClient()` instances to the shared singleton.

Purpose: Continue eliminating connection pool fragmentation across the service layer.
Output: 5 more service files using the singleton import.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-singleton-prisma/01-RESEARCH.md

<interfaces>
<!-- The singleton export that all services must use -->
From src/backend/src/lib/prisma.ts:
```typescript
export const prisma: PrismaClient;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate AuditLogsService, BonusesService, ClockLogsService, DeductionsService, EmployeeDeductions to singleton</name>
  <files>
    src/backend/src/service/AuditLogsService.ts,
    src/backend/src/service/BonusesService.ts,
    src/backend/src/service/ClockLogsService.ts,
    src/backend/src/service/DeductionsService.ts,
    src/backend/src/service/EmployeeDeductions.ts
  </files>

  <read_first>
    CLAUDE.md,
    src/backend/src/lib/prisma.ts,
    src/backend/src/service/AuditLogsService.ts,
    src/backend/src/service/BonusesService.ts,
    src/backend/src/service/ClockLogsService.ts,
    src/backend/src/service/DeductionsService.ts,
    src/backend/src/service/EmployeeDeductions.ts
  </read_first>

  <action>
For each of the 5 files, apply the standard two-edit pattern. Read each file first to confirm exact line numbers before editing.

**AuditLogsService.ts:**
1. Remove the line `import { PrismaClient } from "@prisma/client"` (line 1 area)
2. Remove the line `const prisma = new PrismaClient()` (line 4)
3. Add: `import { prisma } from '../lib/prisma';`

**BonusesService.ts:**
1. Remove the line `import { PrismaClient } from "@prisma/client"` (line 1 area)
2. Remove the line `const prisma = new PrismaClient()` (line 4)
3. Add: `import { prisma } from '../lib/prisma';`

**ClockLogsService.ts:**
1. Remove the line `import { PrismaClient } from "@prisma/client"` (line 1 area)
2. Remove the line `const prisma = new PrismaClient()` (line 4)
3. Add: `import { prisma } from '../lib/prisma';`

**DeductionsService.ts:**
1. Remove the line `import { PrismaClient } from "@prisma/client"` (line 1 area)
2. Remove the line `const prisma = new PrismaClient()` (line 5)
3. Add: `import { prisma } from '../lib/prisma';`

**EmployeeDeductions.ts:**
1. Remove the line `import { PrismaClient } from "@prisma/client"` (line 1 area)
2. Remove the line `const prisma = new PrismaClient()` (line 4)
3. Add: `import { prisma } from '../lib/prisma';`

All 5 files follow the standard pattern — no special cases in this batch.
  </action>

  <verify>
    <automated>cd src/backend && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
- `grep "new PrismaClient()" src/backend/src/service/AuditLogsService.ts` returns 0 results
- `grep "new PrismaClient()" src/backend/src/service/BonusesService.ts` returns 0 results
- `grep "new PrismaClient()" src/backend/src/service/ClockLogsService.ts` returns 0 results
- `grep "new PrismaClient()" src/backend/src/service/DeductionsService.ts` returns 0 results
- `grep "new PrismaClient()" src/backend/src/service/EmployeeDeductions.ts` returns 0 results
- `grep "from '../lib/prisma'" src/backend/src/service/AuditLogsService.ts` returns 1 result
- `grep "from '../lib/prisma'" src/backend/src/service/BonusesService.ts` returns 1 result
- `grep "from '../lib/prisma'" src/backend/src/service/ClockLogsService.ts` returns 1 result
- `grep "from '../lib/prisma'" src/backend/src/service/DeductionsService.ts` returns 1 result
- `grep "from '../lib/prisma'" src/backend/src/service/EmployeeDeductions.ts` returns 1 result
- `cd src/backend && npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>All 5 service files use the singleton import. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
After task completion:
1. `grep -c "new PrismaClient()" src/backend/src/service/AuditLogsService.ts src/backend/src/service/BonusesService.ts src/backend/src/service/ClockLogsService.ts src/backend/src/service/DeductionsService.ts src/backend/src/service/EmployeeDeductions.ts` — all lines show :0
2. `cd src/backend && npx tsc --noEmit` — exits 0
</verification>

<success_criteria>
- Zero `new PrismaClient()` in the 5 service files
- All 5 files import from `'../lib/prisma'`
- `npx tsc --noEmit` passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-singleton-prisma/01-02-SUMMARY.md`
</output>
