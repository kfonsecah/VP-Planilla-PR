---
phase: 01-singleton-prisma
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/src/service/AuthService.ts
  - src/backend/src/service/UserService.ts
  - src/backend/src/service/EmployeeService.ts
  - src/backend/src/service/PayrollService.ts
  - src/backend/src/service/PayrollTypeService.ts
autonomous: true
requirements: [1.1, 1.2, 1.3]

must_haves:
  truths:
    - "AuthService, UserService, EmployeeService, PayrollService, PayrollTypeService all import prisma from the singleton"
    - "Zero instances of new PrismaClient() remain in these 5 files"
    - "TypeScript compiler passes after edits"
  artifacts:
    - path: "src/backend/src/service/AuthService.ts"
      provides: "Singleton prisma import"
      contains: "from '../lib/prisma'"
    - path: "src/backend/src/service/UserService.ts"
      provides: "Singleton prisma import + preserved vpg_users type"
      contains: "from '../lib/prisma'"
    - path: "src/backend/src/service/EmployeeService.ts"
      provides: "Singleton prisma import"
      contains: "from '../lib/prisma'"
    - path: "src/backend/src/service/PayrollService.ts"
      provides: "Singleton prisma import"
      contains: "from '../lib/prisma'"
    - path: "src/backend/src/service/PayrollTypeService.ts"
      provides: "Singleton prisma import"
      contains: "from '../lib/prisma'"
  key_links:
    - from: "src/backend/src/service/AuthService.ts"
      to: "src/backend/src/lib/prisma.ts"
      via: "import { prisma }"
      pattern: "import.*prisma.*from.*lib/prisma"
    - from: "src/backend/src/service/UserService.ts"
      to: "src/backend/src/lib/prisma.ts"
      via: "import { prisma }"
      pattern: "import.*prisma.*from.*lib/prisma"
---

<objective>
Migrate 5 backend service files (AuthService, UserService, EmployeeService, PayrollService, PayrollTypeService) from local `new PrismaClient()` instances to the shared singleton at `src/backend/src/lib/prisma.ts`.

Purpose: Eliminate connection pool fragmentation — each `new PrismaClient()` opens its own connection pool to PostgreSQL, wasting resources and risking connection exhaustion.
Output: 5 service files using the singleton import, zero `new PrismaClient()` in these files.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-singleton-prisma/01-RESEARCH.md

<interfaces>
<!-- The singleton export that all services must use -->
From src/backend/src/lib/prisma.ts:
```typescript
export const prisma: PrismaClient;
```

<!-- Reference: NomineeService.ts already uses the correct pattern -->
From src/backend/src/service/NomineeService.ts line 3:
```typescript
import { prisma } from "../lib/prisma";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate AuthService, UserService (special case), EmployeeService, PayrollService, PayrollTypeService to singleton</name>
  <files>
    src/backend/src/service/AuthService.ts,
    src/backend/src/service/UserService.ts,
    src/backend/src/service/EmployeeService.ts,
    src/backend/src/service/PayrollService.ts,
    src/backend/src/service/PayrollTypeService.ts
  </files>

  <read_first>
    CLAUDE.md,
    src/backend/src/lib/prisma.ts,
    src/backend/src/service/AuthService.ts,
    src/backend/src/service/UserService.ts,
    src/backend/src/service/EmployeeService.ts,
    src/backend/src/service/PayrollService.ts,
    src/backend/src/service/PayrollTypeService.ts
  </read_first>

  <action>
For each of the 5 files, apply the two-edit pattern. Read each file first to confirm exact line numbers before editing.

**AuthService.ts:**
1. Remove the line `import { PrismaClient } from '@prisma/client'`
2. Remove the line `const prisma = new PrismaClient()`
3. Add at the top imports: `import { prisma } from '../lib/prisma';`

**UserService.ts — SPECIAL CASE:**
1. The current import line is: `import { PrismaClient, vpg_users } from "@prisma/client";`
2. Replace it with TWO lines:
   - `import { prisma } from '../lib/prisma';`
   - `import type { vpg_users } from "@prisma/client";`
3. Remove the line `const prisma = new PrismaClient()` (line 3)
4. DO NOT remove or modify the `vpg_users` type — it is used throughout UserService

**EmployeeService.ts:**
1. Remove the line `import {PrismaClient} from '@prisma/client'`
2. Remove the line `const prisma = new PrismaClient()`
3. Add at the top imports: `import { prisma } from '../lib/prisma';`

**PayrollService.ts:**
1. Remove the line `import { PrismaClient } from "@prisma/client"`
2. Remove the line `const prisma = new PrismaClient()` (line 5)
3. Add at the top imports: `import { prisma } from '../lib/prisma';`
4. DO NOT touch `import { error } from "console"` on line 3 — that is Phase 2 scope (requirement 2.6)

**PayrollTypeService.ts:**
1. Remove the line `import { PrismaClient } from "@prisma/client"`
2. Remove the line `const prisma = new PrismaClient()`
3. Add at the top imports: `import { prisma } from '../lib/prisma';`
  </action>

  <verify>
    <automated>cd src/backend && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
- `grep "new PrismaClient()" src/backend/src/service/AuthService.ts` returns 0 results
- `grep "new PrismaClient()" src/backend/src/service/UserService.ts` returns 0 results
- `grep "new PrismaClient()" src/backend/src/service/EmployeeService.ts` returns 0 results
- `grep "new PrismaClient()" src/backend/src/service/PayrollService.ts` returns 0 results
- `grep "new PrismaClient()" src/backend/src/service/PayrollTypeService.ts` returns 0 results
- `grep "from '../lib/prisma'" src/backend/src/service/AuthService.ts` returns 1 result
- `grep "from '../lib/prisma'" src/backend/src/service/UserService.ts` returns 1 result
- `grep "from '../lib/prisma'" src/backend/src/service/EmployeeService.ts` returns 1 result
- `grep "from '../lib/prisma'" src/backend/src/service/PayrollService.ts` returns 1 result
- `grep "from '../lib/prisma'" src/backend/src/service/PayrollTypeService.ts` returns 1 result
- `grep "vpg_users" src/backend/src/service/UserService.ts` returns at least 1 result (type preserved)
- `grep "import { error }" src/backend/src/service/PayrollService.ts` returns 1 result (untouched)
- `cd src/backend && npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>All 5 service files use the singleton import. UserService.ts preserves its vpg_users type import. PayrollService.ts console error import is untouched. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
After task completion:
1. `grep -c "new PrismaClient()" src/backend/src/service/AuthService.ts src/backend/src/service/UserService.ts src/backend/src/service/EmployeeService.ts src/backend/src/service/PayrollService.ts src/backend/src/service/PayrollTypeService.ts` — all lines show :0
2. `cd src/backend && npx tsc --noEmit` — exits 0
</verification>

<success_criteria>
- Zero `new PrismaClient()` in the 5 service files
- All 5 files import from `'../lib/prisma'`
- UserService.ts still has `vpg_users` type import
- PayrollService.ts still has `import { error } from "console"`
- `npx tsc --noEmit` passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-singleton-prisma/01-01-SUMMARY.md`
</output>
