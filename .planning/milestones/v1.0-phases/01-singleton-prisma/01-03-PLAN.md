---
phase: 01-singleton-prisma
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/src/service/LaborEventsService.ts
  - src/backend/src/service/PaymentReceiptService.ts
  - src/backend/src/service/PositionService.ts
  - src/backend/src/service/ReportsService.ts
  - src/backend/src/service/VacationService.ts
autonomous: true
requirements: [1.1, 1.2, 1.3]

must_haves:
  truths:
    - "All 15 service files (across plans 01, 02, 03) import prisma from the singleton"
    - "Zero instances of new PrismaClient() exist in src/backend/src/service/"
    - "npx tsc --noEmit passes with zero errors"
    - "npm test passes (regression guard)"
  artifacts:
    - path: "src/backend/src/service/LaborEventsService.ts"
      provides: "Singleton prisma import"
      contains: "from '../lib/prisma'"
    - path: "src/backend/src/service/PaymentReceiptService.ts"
      provides: "Singleton prisma import"
      contains: "from '../lib/prisma'"
    - path: "src/backend/src/service/PositionService.ts"
      provides: "Singleton prisma import"
      contains: "from '../lib/prisma'"
    - path: "src/backend/src/service/ReportsService.ts"
      provides: "Singleton prisma import"
      contains: "from '../lib/prisma'"
    - path: "src/backend/src/service/VacationService.ts"
      provides: "Singleton prisma import"
      contains: "from '../lib/prisma'"
  key_links:
    - from: "all 16 service files"
      to: "src/backend/src/lib/prisma.ts"
      via: "import { prisma }"
      pattern: "import.*prisma.*from.*lib/prisma"
---

<objective>
Migrate the final 5 backend service files (LaborEventsService, PaymentReceiptService, PositionService, ReportsService, VacationService) to the singleton, then run full-phase verification to confirm all 15 files are migrated and the codebase compiles and tests pass.

Purpose: Complete the singleton migration and verify the entire phase is done.
Output: All 15 service files using the singleton. Phase 1 requirements 1.1, 1.2, 1.3 fully satisfied.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-singleton-prisma/01-RESEARCH.md

<interfaces>
<!-- The singleton export that all services must use -->
From src/backend/src/lib/prisma.ts:
```typescript
export const prisma: PrismaClient;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate LaborEventsService, PaymentReceiptService, PositionService, ReportsService, VacationService to singleton</name>
  <files>
    src/backend/src/service/LaborEventsService.ts,
    src/backend/src/service/PaymentReceiptService.ts,
    src/backend/src/service/PositionService.ts,
    src/backend/src/service/ReportsService.ts,
    src/backend/src/service/VacationService.ts
  </files>

  <read_first>
    CLAUDE.md,
    src/backend/src/lib/prisma.ts,
    src/backend/src/service/LaborEventsService.ts,
    src/backend/src/service/PaymentReceiptService.ts,
    src/backend/src/service/PositionService.ts,
    src/backend/src/service/ReportsService.ts,
    src/backend/src/service/VacationService.ts
  </read_first>

  <action>
For each of the 5 files, apply the standard two-edit pattern. Read each file first to confirm exact line numbers before editing.

**LaborEventsService.ts:**
1. Remove the line `import { PrismaClient } from "@prisma/client"` (line 1 area)
2. Remove the line `const prisma = new PrismaClient()` (line 5)
3. Add: `import { prisma } from '../lib/prisma';`

**PaymentReceiptService.ts:**
1. Remove the line `import { PrismaClient } from '@prisma/client'` (line 1 area)
2. Remove the line `const prisma = new PrismaClient()` (line 8)
3. Add: `import { prisma } from '../lib/prisma';`

**PositionService.ts:**
1. Remove the line `import { PrismaClient } from "@prisma/client"` (line 1 area)
2. Remove the line `const prisma = new PrismaClient()` (line 5)
3. Add: `import { prisma } from '../lib/prisma';`

**ReportsService.ts:**
1. Remove the line `import { PrismaClient } from "@prisma/client"` (line 1 area)
2. Remove the line `const prisma = new PrismaClient()` (line 7)
3. Add: `import { prisma } from '../lib/prisma';`

**VacationService.ts:**
1. Remove the line `import { PrismaClient } from "@prisma/client"` (line 1 area)
2. Remove the line `const prisma = new PrismaClient()` (line 4)
3. Add: `import { prisma } from '../lib/prisma';`

All 5 files follow the standard pattern — no special cases in this batch.
  </action>

  <verify>
    <automated>cd src/backend && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
- `grep "new PrismaClient()" src/backend/src/service/LaborEventsService.ts` returns 0 results
- `grep "new PrismaClient()" src/backend/src/service/PaymentReceiptService.ts` returns 0 results
- `grep "new PrismaClient()" src/backend/src/service/PositionService.ts` returns 0 results
- `grep "new PrismaClient()" src/backend/src/service/ReportsService.ts` returns 0 results
- `grep "new PrismaClient()" src/backend/src/service/VacationService.ts` returns 0 results
- `grep "from '../lib/prisma'" src/backend/src/service/LaborEventsService.ts` returns 1 result
- `grep "from '../lib/prisma'" src/backend/src/service/PaymentReceiptService.ts` returns 1 result
- `grep "from '../lib/prisma'" src/backend/src/service/PositionService.ts` returns 1 result
- `grep "from '../lib/prisma'" src/backend/src/service/ReportsService.ts` returns 1 result
- `grep "from '../lib/prisma'" src/backend/src/service/VacationService.ts` returns 1 result
- `cd src/backend && npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>All 5 service files use the singleton import. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Full-phase verification — confirm all 15 files migrated, run tests</name>
  <files>None (verification only)</files>

  <read_first>
    .planning/phases/01-singleton-prisma/01-RESEARCH.md
  </read_first>

  <action>
Run the three phase-level verification commands that prove requirements 1.1, 1.2, and 1.3 are satisfied:

1. **Req 1.2 — Zero new PrismaClient():**
   `grep -r "new PrismaClient()" src/backend/src/service/`
   Expected: zero output lines (exit code 1 = no matches = correct)

2. **Req 1.1 — All services use singleton:**
   `grep -rL "from '../lib/prisma'\|from \"../lib/prisma\"" src/backend/src/service/`
   Expected: zero output lines (every file matches the singleton import pattern).
   Note: NomineeService.ts uses double quotes `"../lib/prisma"` — the grep pattern accounts for both quote styles.

3. **Req 1.3 — TypeScript compiles:**
   `cd src/backend && npx tsc --noEmit`
   Expected: exit code 0, no errors

4. **Regression guard — existing tests pass:**
   `cd src/backend && npm test`
   Expected: all tests pass (0 failures)

If any command fails, diagnose and fix before marking done.
  </action>

  <verify>
    <automated>cd src/backend && grep -r "new PrismaClient()" src/service/ ; echo "EXIT:$?" && npx tsc --noEmit && npm test</automated>
  </verify>

  <acceptance_criteria>
- `grep -r "new PrismaClient()" src/backend/src/service/` returns 0 results (exit code 1)
- `grep -rL "from '../lib/prisma'\|from \"../lib/prisma\"" src/backend/src/service/` returns 0 results (every service file has the singleton import)
- `cd src/backend && npx tsc --noEmit` exits 0
- `cd src/backend && npm test` exits 0 with 0 failures
  </acceptance_criteria>

  <done>All 15 service files confirmed migrated to singleton. Zero new PrismaClient() instances remain. TypeScript compiles. Tests pass. Phase 1 requirements 1.1, 1.2, 1.3 are fully satisfied.</done>
</task>

</tasks>

<verification>
Full phase verification — all three requirements:
1. REQ 1.1: `grep -rL "from '../lib/prisma'\|from \"../lib/prisma\"" src/backend/src/service/` returns empty
2. REQ 1.2: `grep -r "new PrismaClient()" src/backend/src/service/` returns empty
3. REQ 1.3: `cd src/backend && npx tsc --noEmit` exits 0
4. Regression: `cd src/backend && npm test` passes
</verification>

<success_criteria>
- All 16 service files use the singleton (15 migrated + 1 already correct)
- Zero `new PrismaClient()` in the entire service directory
- TypeScript compiles without errors
- Existing tests pass without regressions
- Phase 1 is complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-singleton-prisma/01-03-SUMMARY.md`
</output>
