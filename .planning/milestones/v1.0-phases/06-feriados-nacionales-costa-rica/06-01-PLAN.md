---
wave: 1
depends_on: []
files_modified:
  - src/backend/src/utils/payrollUtils.ts
autonomous: true
---

# Plan 06-01: Agregar feriados de Costa Rica a payrollUtils.ts

## Requisitos
- REQ 6.1: Lista de feriados CR en payrollUtils.ts (estática, actualizable por año)
- REQ 6.2: countWorkingDaysInPeriod() excluye feriados del conteo

## Tareas

### Tarea 1: Agregar FERIADOS_CR array
<read_first>
src/backend/src/utils/payrollUtils.ts
</read_first>

<action>
Agregar al inicio del archivo (después de los constantes), después de la línea 10:
```typescript
// ── Costa Rica National Holidays (Feriados Nacionales) ───────────────────────
export const FERIADOS_CR: Record<number, string[]> = {
  2026: ['01-01', '04-02', '04-03', '04-11', '05-01', '07-25', '08-15', '09-15', '10-12', '12-25'],
  2027: ['01-01', '03-25', '03-26', '04-11', '05-01', '07-25', '08-15', '09-15', '10-12', '12-25'],
};
```

Agregar después de la línea 11 (después del WORKING_DAYS_PER_WEEK):
```typescript
/**
 * Check if a date is a Costa Rica national holiday
 * @param date - Date to check
 * @param year - Year for holiday lookup (defaults to date's year)
 * @returns True if date is a CR national holiday
 */
export function isCRHoliday(date: Date, year?: number): boolean {
  const yr = year ?? date.getFullYear();
  const mmdd = `${String(date.getMonth() + 1).padStart(2, '0')}-${String(date.getDate()).padStart(2, '0')}`;
  return FERIADOS_CR[yr]?.includes(mmdd) ?? false;
}

/**
 * Get all holiday dates for a given year
 * @param year - Year to get holidays for
 * @returns Array of Date objects for each holiday
 */
export function getCRHolidays(year: number): Date[] {
  const holidays = FERIADOS_CR[year] ?? [];
  return holidays.map((mmdd) => {
    const [month, day] = mmdd.split('-').map(Number);
    const date = new Date(year, month - 1, day);
    return date;
  });
}
```
</action>

<acceptance_criteria>
- payrollUtils.ts contains `export const FERIADOS_CR: Record<number, string[]>`
- payrollUtils.ts contains 2026 holidays: '01-01', '04-02', '04-03', '04-11', '05-01', '07-25', '08-15', '09-15', '10-12', '12-25'
- payrollUtils.ts contains `export function isCRHoliday(date: Date, year?: number): boolean`
- payrollUtils.ts contains `export function getCRHolidays(year: number): Date[]`
</acceptance_criteria>

---

### Tarea 2: Modificar countWorkingDaysInPeriod()
<read_first>
src/backend/src/utils/payrollUtils.ts (líneas 302-321)
</read_first>

<action>
Reemplazar la función countWorkingDaysInPeriod (líneas 306-314):

```typescript
export function countWorkingDaysInPeriod(startDate: Date, endDate: Date, year?: number): number {
  const yr = year ?? startDate.getFullYear();
  let count = 0;
  const current = new Date(startDate);
  while (current <= endDate) {
    if (current.getDay() !== 0 && !isCRHoliday(current, yr)) {
      count++;
    }
    current.setDate(current.getDate() + 1);
  }
  return count;
}
```
</action>

<acceptance_criteria>
- countWorkingDaysInPeriod accepts optional third parameter `year?: number`
- countWorkingDaysInPeriod calls `isCRHoliday(current, yr)` in the condition
- `npx tsc --noEmit` passes in src/backend/
</acceptance_criteria>

---

## Verificación

```bash
cd src/backend && npx tsc --noEmit
```

## Dependencias
Ninguna - es el primer plan de la fase
