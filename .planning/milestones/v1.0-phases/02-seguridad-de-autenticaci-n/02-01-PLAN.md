---
phase: 02-seguridad-de-autenticaci-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/src/routes/EmployeeRoute.ts
  - src/backend/src/routes/PayrollRoutes.ts
  - src/backend/src/routes/BonusesRoute.ts
  - src/backend/src/routes/ClockLogsRoute.ts
  - src/backend/src/routes/DeductionsRoute.ts
  - src/backend/src/routes/EmployeeDeductionsRoute.ts
  - src/backend/src/routes/LaborEventsRoute.ts
  - src/backend/src/routes/NomineeRoute.ts
  - src/backend/src/routes/PaymentReceiptRoute.ts
  - src/backend/src/routes/PayrollTypeRoute.ts
  - src/backend/src/routes/PositionRoute.ts
  - src/backend/src/routes/VacationRoute.ts
  - src/backend/src/routes/AuditLogsRoute.ts
autonomous: true
requirements: [2.1, 2.2]

must_haves:
  truths:
    - "Any HTTP request without a valid JWT to /api/employees returns 401"
    - "Any HTTP request without a valid JWT to /api/payrolls returns 401"
    - "Any HTTP request without a valid JWT to /api/vacations returns 401"
    - "Any HTTP request without a valid JWT to /api/audit-logs returns 401"
    - "Public endpoints /api/login, /api/validate, /api/refresh still work without a token"
  artifacts:
    - path: "src/backend/src/routes/EmployeeRoute.ts"
      provides: "Auth-protected employee routes"
      contains: "AuthMiddleware.verifyToken"
    - path: "src/backend/src/routes/PayrollRoutes.ts"
      provides: "Auth-protected payroll routes"
      contains: "AuthMiddleware.verifyToken"
    - path: "src/backend/src/routes/NomineeRoute.ts"
      provides: "Auth-protected nominee routes"
      contains: "AuthMiddleware.verifyToken"
  key_links:
    - from: "src/backend/src/routes/EmployeeRoute.ts"
      to: "src/backend/src/middleware/AuthMiddleware.ts"
      via: "router.use(AuthMiddleware.verifyToken)"
      pattern: "router\\.use\\(AuthMiddleware\\.verifyToken\\)"
---

<objective>
Add AuthMiddleware.verifyToken to all 13 unprotected route files so every non-public API endpoint requires a valid JWT.

Purpose: Close the biggest security gap — 13 of 16 route files currently allow unauthenticated access to all endpoints including employee data, payroll, and deductions.
Output: All 13 route files protected with router-level auth middleware; TypeScript compiles cleanly.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-seguridad-de-autenticaci-n/02-RESEARCH.md

<interfaces>
<!-- Source of truth: ReportsRoute.ts — the existing protected route pattern -->
<!-- File: src/backend/src/routes/ReportsRoute.ts -->
```typescript
import { Router } from "express";
import { asyncHandler } from "../utils/asyncHandler";
import { AuthMiddleware } from "../middleware/AuthMiddleware";

const router = Router();
router.use(AuthMiddleware.verifyToken);  // Pattern A — covers all routes below
```

<!-- AuthMiddleware signature: src/backend/src/middleware/AuthMiddleware.ts -->
```typescript
static async verifyToken(req: Request, res: Response, next: NextFunction): Promise<Response | void>
// Returns 401 { success: false, message: 'Token de acceso requerido' } if no Authorization header
// Returns 401 { success: false, message: 'Token invalido' } if JWT fails
// Calls next() on success
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AuthMiddleware.verifyToken to all 13 unprotected route files</name>
  <files>
    src/backend/src/routes/EmployeeRoute.ts,
    src/backend/src/routes/PayrollRoutes.ts,
    src/backend/src/routes/BonusesRoute.ts,
    src/backend/src/routes/ClockLogsRoute.ts,
    src/backend/src/routes/DeductionsRoute.ts,
    src/backend/src/routes/EmployeeDeductionsRoute.ts,
    src/backend/src/routes/LaborEventsRoute.ts,
    src/backend/src/routes/NomineeRoute.ts,
    src/backend/src/routes/PaymentReceiptRoute.ts,
    src/backend/src/routes/PayrollTypeRoute.ts,
    src/backend/src/routes/PositionRoute.ts,
    src/backend/src/routes/VacationRoute.ts,
    src/backend/src/routes/AuditLogsRoute.ts
  </files>
  <read_first>
    CLAUDE.md,
    src/backend/src/middleware/AuthMiddleware.ts,
    src/backend/src/routes/ReportsRoute.ts,
    src/backend/src/routes/EmployeeRoute.ts,
    src/backend/src/routes/PayrollRoutes.ts,
    src/backend/src/routes/BonusesRoute.ts,
    src/backend/src/routes/ClockLogsRoute.ts,
    src/backend/src/routes/DeductionsRoute.ts,
    src/backend/src/routes/EmployeeDeductionsRoute.ts,
    src/backend/src/routes/LaborEventsRoute.ts,
    src/backend/src/routes/NomineeRoute.ts,
    src/backend/src/routes/PaymentReceiptRoute.ts,
    src/backend/src/routes/PayrollTypeRoute.ts,
    src/backend/src/routes/PositionRoute.ts,
    src/backend/src/routes/VacationRoute.ts,
    src/backend/src/routes/AuditLogsRoute.ts
  </read_first>
  <action>
    For each of the 13 route files listed below, apply the EXACT same pattern used by ReportsRoute.ts (Pattern A — router-level auth). For EACH file:

    1. Add this import line near the top (after existing imports):
       `import { AuthMiddleware } from "../middleware/AuthMiddleware";`

    2. Add this line IMMEDIATELY AFTER `const router = Router();` (before the first route definition):
       `router.use(AuthMiddleware.verifyToken);`

    3. Do NOT wrap with asyncHandler — ReportsRoute does not wrap router.use() and that is the correct pattern for Express 5.

    **Standard 11 files** (straightforward — add import + router.use):
    - EmployeeRoute.ts — already imports asyncHandler, just add AuthMiddleware import + router.use
    - PayrollRoutes.ts — same pattern
    - BonusesRoute.ts — same pattern
    - ClockLogsRoute.ts — same pattern
    - DeductionsRoute.ts — same pattern
    - LaborEventsRoute.ts — same pattern
    - NomineeRoute.ts — same pattern
    - PayrollTypeRoute.ts — same pattern
    - PositionRoute.ts — same pattern
    - VacationRoute.ts — same pattern
    - AuditLogsRoute.ts — same pattern

    **Special case: EmployeeDeductionsRoute.ts**
    This file has a LOCAL asyncHandler definition on lines 8-10 (inline function). Do the following:
    - REMOVE the local asyncHandler definition (the `const asyncHandler = (fn: Function) => ...` block)
    - ADD import: `import { asyncHandler } from "../utils/asyncHandler";`
    - ADD import: `import { AuthMiddleware } from "../middleware/AuthMiddleware";`
    - ADD `router.use(AuthMiddleware.verifyToken);` after `const router = Router();`

    **Special case: PaymentReceiptRoute.ts**
    This file uses `import express from 'express'` and `express.Router()` instead of `import { Router } from "express"`. Do the following:
    - ADD import: `import { AuthMiddleware } from "../middleware/AuthMiddleware";`
    - ADD import: `import { asyncHandler } from "../utils/asyncHandler";`
    - ADD `router.use(AuthMiddleware.verifyToken);` after `const router = express.Router();`
    - ALSO wrap all controller method calls with asyncHandler (they are currently unwrapped):
      e.g., change `router.get('/path', PaymentReceiptController.method)` to `router.get('/path', asyncHandler(PaymentReceiptController.method))`

    Do NOT touch AuthRoute.ts, UserRoute.ts, or ReportsRoute.ts — they are already correctly protected.

    Per REQ 2.1: AuthMiddleware.verifyToken applied in all 13 unprotected route files.
    Per REQ 2.2: After this change, curl without token to /api/employees returns 401.
  </action>
  <verify>
    <automated>cd "C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/src/backend" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AuthMiddleware.verifyToken" src/backend/src/routes/EmployeeRoute.ts` returns 1
    - `grep -c "AuthMiddleware.verifyToken" src/backend/src/routes/PayrollRoutes.ts` returns 1
    - `grep -c "AuthMiddleware.verifyToken" src/backend/src/routes/BonusesRoute.ts` returns 1
    - `grep -c "AuthMiddleware.verifyToken" src/backend/src/routes/ClockLogsRoute.ts` returns 1
    - `grep -c "AuthMiddleware.verifyToken" src/backend/src/routes/DeductionsRoute.ts` returns 1
    - `grep -c "AuthMiddleware.verifyToken" src/backend/src/routes/EmployeeDeductionsRoute.ts` returns 1
    - `grep -c "AuthMiddleware.verifyToken" src/backend/src/routes/LaborEventsRoute.ts` returns 1
    - `grep -c "AuthMiddleware.verifyToken" src/backend/src/routes/NomineeRoute.ts` returns 1
    - `grep -c "AuthMiddleware.verifyToken" src/backend/src/routes/PaymentReceiptRoute.ts` returns 1
    - `grep -c "AuthMiddleware.verifyToken" src/backend/src/routes/PayrollTypeRoute.ts` returns 1
    - `grep -c "AuthMiddleware.verifyToken" src/backend/src/routes/PositionRoute.ts` returns 1
    - `grep -c "AuthMiddleware.verifyToken" src/backend/src/routes/VacationRoute.ts` returns 1
    - `grep -c "AuthMiddleware.verifyToken" src/backend/src/routes/AuditLogsRoute.ts` returns 1
    - `grep -rL "AuthMiddleware" src/backend/src/routes/ | grep -v "AuthRoute.ts"` returns empty (all non-auth routes import AuthMiddleware)
    - `grep "const asyncHandler = " src/backend/src/routes/EmployeeDeductionsRoute.ts` returns 0 results (local definition removed)
    - `cd src/backend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All 13 route files have router-level AuthMiddleware.verifyToken applied. TypeScript compiles without errors. EmployeeDeductionsRoute uses shared asyncHandler. PaymentReceiptRoute has asyncHandler wrapping on all controller calls.</done>
</task>

</tasks>

<verification>
1. `grep -rL "AuthMiddleware" src/backend/src/routes/ | grep -v "AuthRoute.ts"` — must return empty output (all route files except AuthRoute import AuthMiddleware)
2. `cd src/backend && npx tsc --noEmit` — 0 errors
3. `cd src/backend && npm test` — existing tests still pass
</verification>

<success_criteria>
- All 13 previously unprotected route files now have `router.use(AuthMiddleware.verifyToken)`
- TypeScript compiles cleanly
- Existing tests pass unchanged
- Pattern matches ReportsRoute.ts exactly (no asyncHandler wrapping on router.use)
</success_criteria>

<output>
After completion, create `.planning/phases/02-seguridad-de-autenticaci-n/02-01-SUMMARY.md`
</output>
