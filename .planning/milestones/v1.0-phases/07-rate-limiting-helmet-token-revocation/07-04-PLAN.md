---
wave: 2
depends_on:
  - 07-01-PLAN.md
  - 07-02-PLAN.md
  - 07-03-PLAN.md
files_modified:
  - src/backend/prisma/schema.prisma
  - src/backend/src/middleware/AuthMiddleware.ts
  - src/backend/src/service/AuthService.ts
autonomous: true
---

# Plan 07-04: Token Blocklist para Logout Real

## Requisitos
- REQ 7.5: `POST /api/logout` invalida el token en DB blocklist
- REQ 7.6: Token usado post-logout retorna 401

## Tareas

### Tarea 1: Agregar tabla vpg_token_blocklist
<read_first>
src/backend/prisma/schema.prisma (líneas 1-30)
</read_first>

<action>
Agregar al final del schema.prisma (antes del último modelo):
```prisma
model vpg_token_blocklist {
  blocklist_id       Int       @id @default(autoincrement())
  blocklist_token    String    @db.VarChar(500)
  blocklist_expires  DateTime  @db.Timestamp(6)
  blocklist_created  DateTime  @default(now()) @db.Timestamp(6)

  @@index([blocklist_token], map: "idx_token_blocklist_token")
}
```

Luego ejecutar:
```bash
cd src/backend && npx prisma db push && npx prisma generate
```
</action>

<acceptance_criteria>
- schema.prisma contains `model vpg_token_blocklist {`
- schema.prisma contains `blocklist_token String`
- schema.prisma contains `blocklist_expires DateTime`
- npx prisma generate succeeds
</acceptance_criteria>

---

### Tarea 2: Agregar métodos de blocklist a AuthService
<read_first>
src/backend/src/service/AuthService.ts (líneas 1-50)
</read_first>

<action>
Agregar al final de AuthService.ts (después del último método, antes del cierre de clase):
```typescript
static async addTokenToBlocklist(token: string, expiresAt: Date): Promise<void> {
  await prisma.vpg_token_blocklist.create({
    data: {
      blocklist_token: token,
      blocklist_expires: expiresAt,
    },
  });
}

static async isTokenBlocklisted(token: string): Promise<boolean> {
  const found = await prisma.vpg_token_blocklist.findFirst({
    where: {
      blocklist_token: token,
      blocklist_expires: {
        gt: new Date(),
      },
    },
  });
  return !!found;
}

static async cleanupExpiredTokens(): Promise<void> {
  await prisma.vpg_token_blocklist.deleteMany({
    where: {
      blocklist_expires: {
        lt: new Date(),
      },
    },
  });
}
```
</action>

<acceptance_criteria>
- AuthService.ts contains `addTokenToBlocklist`
- AuthService.ts contains `isTokenBlocklisted`
- AuthService.ts contains `cleanupExpiredTokens`
- npx tsc --noEmit passes in src/backend/
</acceptance_criteria>

---

### Tarea 3: Modificar AuthMiddleware para verificar blocklist
<read_first>
src/backend/src/middleware/AuthMiddleware.ts (líneas 39-55)
</read_first>

<action>
En el método `verifyToken`, después de `const decoded = AuthService.verifyToken(token);` (línea 40), agregar:

```typescript
// Check if token is blocklisted
const isBlocklisted = await AuthService.isTokenBlocklisted(token);
if (isBlocklisted) {
  return res.status(401).json({
    success: false,
    message: 'Token ha sido invalidado'
  });
}
```
</action>

<acceptance_criteria>
- AuthMiddleware.ts contains `isTokenBlocklisted(token)`
- AuthMiddleware.ts contains "Token ha sido invalidado"
- npx tsc --noEmit passes in src/backend/
</acceptance_criteria>

---

### Tarea 4: Modificar logout para agregar token a blocklist
<read_first>
src/backend/src/controller/AuthController.ts (líneas 125-145)
</read_first>

<action>
En el método `logout`, reemplazar el comentario "// En un sistema JWT stateless, el logout se maneja del lado del cliente" con la lógica real:

```typescript
static async logout(req: Request, res: Response): Promise<Response> {
  try {
    const authHeader = req.headers.authorization;
    
    if (authHeader) {
      const token = authHeader.split(' ')[1];
      
      if (token) {
        // Decode token to get expiration
        const decoded = AuthService.verifyToken(token);
        const expiresAt = new Date(decoded.exp * 1000);
        
        // Add token to blocklist
        await AuthService.addTokenToBlocklist(token, expiresAt);
      }
    }
    
    return res.status(200).json({
      success: true,
      message: 'Sesión cerrada exitosamente'
    });
  } catch (error) {
    console.error('Error en logout:', error);
    return res.status(500).json({ 
      success: false,
      error: 'Error al cerrar sesión' 
    });
  }
}
```
</action>

<acceptance_criteria>
- AuthController.ts logout contains `addTokenToBlocklist`
- AuthController.ts logout contains 'Sesión cerrada exitosamente'
- npx tsc --noEmit passes in src/backend/
</acceptance_criteria>

---

## Verificación

```bash
grep "addTokenToBlocklist" src/backend/src/service/AuthService.ts  # → exists
grep "isTokenBlocklisted" src/backend/src/middleware/AuthMiddleware.ts  # → exists
grep "vpg_token_blocklist" prisma/schema.prisma  # → exists
cd src/backend && npx tsc --noEmit  # → 0 new errors
```

## Dependencias
- Plan 07-01 (helmet + express-rate-limit instalados)
- Plan 07-02 (login limiter configurado)
- Plan 07-03 (helmet global activo)
