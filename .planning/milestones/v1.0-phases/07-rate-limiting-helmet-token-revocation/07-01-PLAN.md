---
wave: 1
depends_on: []
files_modified:
  - src/backend/package.json
autonomous: true
---

# Plan 07-01: Instalar dependencias

## Requisitos
- REQ 7.1: `express-rate-limit` instalado
- REQ 7.3: `helmet` instalado

## Tareas

### Tarea 1: Instalar express-rate-limit y helmet
<read_first>
src/backend/package.json
</read_first>

<action>
```bash
cd src/backend && npm install express-rate-limit helmet
```
</action>

<acceptance_criteria>
- package.json contains `"express-rate-limit":`
- package.json contains `"helmet":`
- node_modules/.package-lock.json updated
- No npm install errors
</acceptance_criteria>

---

## Verificación

```bash
cd src/backend && npm install express-rate-limit helmet
grep "express-rate-limit" package.json  # → exists
grep "helmet" package.json  # → exists
```

## Dependencias
Ninguna - primer plan de la fase
