---
wave: 1
depends_on:
  - 07-01-PLAN.md
files_modified:
  - src/backend/src/index.ts
autonomous: true
---

# Plan 07-03: Helmet Global Middleware

## Requisitos
- REQ 7.3: `helmet()` aplicado globalmente en `src/backend/src/index.ts`
- REQ 7.4: Headers `X-Frame-Options` y `X-Content-Type-Options` presentes

## Tareas

### Tarea 1: Agregar helmet() global
<read_first>
src/backend/src/index.ts (líneas 1-35)
</read_first>

<action>
Agregar import de helmet al inicio del archivo (después de línea 3):
```typescript
import helmet from 'helmet';
```

Agregar helmet después de cors (línea 33):
```typescript
// After cors middleware
app.use(helmet());
```

La línea debería quedar así:
```typescript
app.use(cors({ origin: process.env.ALLOWED_ORIGINS?.split(',') }));
app.use(helmet());
app.use(express.json());
```
</action>

<acceptance_criteria>
- index.ts contains `import helmet from 'helmet';`
- index.ts contains `app.use(helmet());`
- helmet() is placed AFTER cors() and BEFORE express.json()
- npx tsc --noEmit passes in src/backend/
</acceptance_criteria>

---

## Verificación

```bash
grep "helmet" src/backend/src/index.ts  # → exists
grep "helmet()" src/backend/src/index.ts  # → exists
cd src/backend && npx tsc --noEmit  # → 0 new errors
```

## Dependencias
- Plan 07-01 (helmet debe estar instalado)
