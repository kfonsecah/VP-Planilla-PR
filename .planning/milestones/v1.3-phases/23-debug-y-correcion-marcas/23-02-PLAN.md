---
phase: 23
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - src/frontend/src/hooks/useClockLogs.ts
  - src/frontend/src/services/clockLogsService.ts
autonomous: true
requirements:
  - BUG-01
  - BUG-02
---

<objective>
Corrección del frontend — Arreglar el hook de datos y la visualización de sesiones de importación.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-debug-y-correcion-marcas/23-CONTEXT.md
@.planning/phases/23-debug-y-correcion-marcas/23-RESEARCH.md

## Phase 23 Requirement IDs (MUST appear in this plan's frontmatter)
- BUG-01: Marcas importadas persisten y se muestran al recargar
- BUG-02: Sesión de importación aparece en historial de trazabilidad

## Dependencies
- 23-01 (backend fix) debe completarse primero porque el frontend depende de los endpoints corregidos
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar hook useClockLogs y fechas por defecto</name>
  <files>src/frontend/src/hooks/useClockLogs.ts</files>
  <read_first>
    - src/frontend/src/hooks/useClockLogs.ts (métodos fetchLogs, fetchImportSessions)
    - src/frontend/src/services/clockLogsService.ts (métodos getClockLogs, getImportSessions)
  </read_first>
  <action>
1. Revisar las fechas por defecto del hook:
   - getDefaultInitDate() y getDefaultEndDate()
   - ¿Incluyen la fecha 6/4/26? Si endDate es "hoy" (9/4) e initDate es primer día del mes (1/4), sí debería incluirla
   - Si initDate es "hace 7 días", podría no incluir el 6/4 si estamos a 9/4

2. Verificar el query params que se envían al backend:
   - Los parámetros initDate y endDate deben seguir el formato YYYY-MM-DD
   - El backend espera este formato para parseLocalDate()

3. Verificar manejo de errores:
   - Si el endpoint retorna 500 o error, ¿el hook hace retry?
   - ¿maneja el caso de respuesta vacía gracefully?

4. Revisar el sessionStorage cache:
   - El hook usa el patrón de cache mencionado en RESEARCH
   - Verificar que la key del cache incluye las fechas del query
   - Si las fechas cambian entre cargas, el cache sería diferente
  </action>
  <verify>
    <automated>npx tsc --noEmit --project src/frontend/tsconfig.json</automated>
  </verify>
  <done>
    Fechas por defecto del hook incluyen rango correcto. Query params se envían en formato YYYY-MM-DD.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verificar endpoint de sesiones de importación</name>
  <files>src/frontend/src/services/clockLogsService.ts</files>
  <read_first>
    - src/frontend/src/services/clockLogsService.ts (getImportSessions)
    - src/backend/src/controller/ClockLogsController.ts (endpoints de import-sessions)
  </read_first>
  <action>
1. Verificar que el frontend llama al endpoint correcto:
   - GET /clock-logs/import-sessions (no es /import-sessions directa)
   - Verificar ruta exacta en el controller

2. Revisar el endpoint GET /clock-logs/import-sessions:
   - ¿Existe?
   - ¿Requiere parámetros de paginación?
   - ¿Retorna el formato esperado por el frontend?

3. Verificar que el servicio parsea la respuesta correctamente:
   - El tipo de retorno debe coincidir con lo que el frontend espera
   - Verificar campos: id, started_at, status, source, total_created, etc.

4. Si el endpoint no existe o tiene problemas, crear/corregir en controller:
   - GET /api/clock-logs/import-sessions?page=0&size=10
  </action>
  <verify>
    <automated>npx tsc --noEmit --project src/frontend/tsconfig.json && curl -s http://localhost:3001/api/clock-logs/import-sessions 2>/dev/null | head -20</automated>
  </verify>
  <done>
    Endpoint GET /clock-logs/import-sessions existe y retorna sesiones correctamente.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verificar componentes de visualización de sesiones</name>
  <files>src/frontend/src/components/attendance/</files>
  <read_first>
    - src/frontend/src/components/attendance/ImportSessionsPanel.tsx (si existe)
    - src/frontend/src/components/attendance/ClockLogDetailModal.tsx (si existe)
  </read_first>
  <action>
1. Verificar que el ImportSessionsPanel:
   - Llama a fetchImportSessions() correctamente
   - Mapea los campos de la respuesta a la UI
   - Muestra el estado (status) de cada sesión

2. Revisar los campos que muestra la UI:
   - ID de sesión
   - Fecha/hora de inicio
   - Origen (source)
   - Conteo de creados
   - Estado (success/partial/failed)

3. Verificar que el modal de detalle de marca:
   - Muestra el import_session_id si existe
   - Permite ver la sesión vinculada

4. Testear la visualización con datos reales:
   - Importar attendance_sample_valid.xlsx
   - Verificar que la sesión aparece en el panel
  </action>
  <verify>
    <automated>npx tsc --noEmit --project src/frontend/tsconfig.json</automated>
  </verify>
  <done>
    ImportSessionsPanel muestra las sesiones correctamente. Detalle de marca muestra vínculo a sesión.
  </done>
</task>

</tasks>

<verification>
- [ ] useClockLogs usa fechas por defecto correctas
- [ ] Query params en formato YYYY-MM-DD
- [ ] Endpoint GET /clock-logs/import-sessions funciona
- [ ] ImportSessionsPanel muestra sesiones correctamente
- [ ] TypeScript compila sin errores
- [ ] UI muestra sesión importada tras prueba manual
</verification>

<success_criteria>
Tras ejecutar plan 23-01 (backend corregido):
1. Al recargar la página, las marcas del 6/4/26 aparecen correctamente
2. La sesión de importación de attendance_sample_valid.xlsx aparece en el panel de sesiones
3. Los badges de estado muestran correctamente el status de cada marca
</success_criteria>

<output>
After completion, create `.planning/phases/23-debug-y-correcion-marcas/23-02-SUMMARY.md`
</output>