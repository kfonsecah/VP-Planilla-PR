---
phase: 25-http-client-layer-enforcement
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified: [
  src/frontend/src/services/externalHttp.ts,
  src/frontend/src/utils/weather.ts
]
autonomous: true
requirements: [HTTP-01, HTTP-03]
must_haves:
  truths:
    - "weather.ts uses externalHttp.get instead of direct fetch"
    - "No unauthorized fetch() calls exist in the services directory"
    - "Internal JWT is NOT sent to OpenWeatherMap"
  artifacts:
    - path: "src/frontend/src/services/externalHttp.ts"
      provides: "Secure external API client without internal tokens"
    - path: "src/frontend/src/utils/weather.ts"
      provides: "Hook using externalHttp.ts"
  key_links:
    - from: "src/frontend/src/utils/weather.ts"
      to: "src/frontend/src/services/externalHttp.ts"
      via: "import { externalHttp }"
---

<objective>
Handle external API calls through a dedicated client to avoid token leakage, and perform final codebase audit to ensure total enforcement of the HTTP client layer.

Purpose: Prevent security leaks and verify compliance with architectural standards.
Output: New 'externalHttp.ts' service, refactored 'weather.ts' hook, and clean audit report.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/frontend/src/services/http.ts
@src/frontend/src/utils/weather.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create externalHttp.ts and refactor weather.ts</name>
  <files>src/frontend/src/services/externalHttp.ts, src/frontend/src/utils/weather.ts</files>
  <action>
    - Create 'src/frontend/src/services/externalHttp.ts'.
    - Implement a simple wrapper around fetch() that does NOT include Authorization headers.
    - Provide an 'externalHttp.get' method that takes a URL, performs fetch, and returns JSON.
    - Refactor 'src/frontend/src/utils/weather.ts' to use 'externalHttp.get'.
    - Ensure 'weather.ts' no longer calls fetch() directly.
  </action>
  <verify>
    <automated>node -e "if(!require('fs').existsSync('src/frontend/src/services/externalHttp.ts') || require('fs').readFileSync('src/frontend/src/utils/weather.ts', 'utf8').includes('fetch(')) throw new Error('Task 1 failed')"</automated>
  </verify>
  <done>External HTTP client created and weather hook refactored.</done>
</task>

<task type="auto">
  <name>Task 2: Final automated verification</name>
  <files>src/frontend/src/services/index.ts</files>
  <action>
    - Run an audit across src/frontend/src/services and src/frontend/src/utils.
    - Use grep_search (or the bash equivalent) to ensure zero unauthorized matches of fetch().
    - Authorized matches are ONLY allowed in services/http.ts and services/externalHttp.ts.
    - If any other match is found, refactor it immediately or document why it's a valid exception.
  </action>
  <verify>
    <automated>node -e "const cp=require('child_process'); try { const out=cp.execSync('grep -r \"fetch(\" src/frontend/src/services src/frontend/src/utils').toString(); const lines=out.split('\\n').filter(l=>l.trim() && !l.includes('http.ts') && !l.includes('externalHttp.ts') && !l.includes('refetch(')); if(lines.length > 0) throw new Error('Found unauthorized fetch calls:\\n' + lines.join('\\n')); console.log('Audit passed'); } catch(e) { if(e.message.includes('grep')) { console.log('grep not found, skipping automated check - manual check required'); } else throw e; }"</automated>
  </verify>
  <done>Zero unauthorized direct fetch calls remain in the services or utilities directories.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → OpenWeatherMap | External third-party boundary. MUST NOT receive internal tokens. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Information Disclosure | `weather.ts` / OpenWeatherMap | mitigate | Use `externalHttp.ts` to ensure no internal JWT is sent to third-party APIs. |
</threat_model>

<verification>
Automated grep audit across services and utils.
</verification>

<success_criteria>
- weather.ts hook uses externalHttp.get.
- Zero unauthorized direct fetch calls remain in services/utils.
- Audit report (or grep output) confirms compliance.
</success_criteria>

<output>
After completion, create `.planning/phases/25-http-client-layer-enforcement/25-02-SUMMARY.md`
</output>
