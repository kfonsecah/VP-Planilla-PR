---
phase: 25-http-client-layer-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  src/frontend/src/services/auditLogsService.ts,
  src/frontend/src/services/branchService.ts,
  src/frontend/src/services/payrollEmployeesService.ts
]
autonomous: true
requirements: [HTTP-01, HTTP-02]
must_haves:
  truths:
    - "AuditLogsService uses http.get/post for all backend calls"
    - "BranchService uses http.get/post/put/delete for all backend calls"
    - "PayrollEmployeesService uses http.get/post for all backend calls"
  artifacts:
    - path: "src/frontend/src/services/auditLogsService.ts"
      provides: "Internal service using http.ts"
    - path: "src/frontend/src/services/branchService.ts"
      provides: "Internal service using http.ts"
    - path: "src/frontend/src/services/payrollEmployeesService.ts"
      provides: "Internal service using http.ts"
  key_links:
    - from: "src/frontend/src/services/auditLogsService.ts"
      to: "src/frontend/src/services/http.ts"
      via: "import { http }"
---

<objective>
Refactor internal frontend services that currently bypass the central HTTP client to use 'http.ts', ensuring consistent authentication, token refresh, and error normalization.

Purpose: Standardize API communication and leverage the centralized auth lifecycle management.
Output: Refactored service files without direct 'fetch' usage.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/frontend/src/services/http.ts

<interfaces>
From src/frontend/src/services/http.ts:
```typescript
export const http = {
  get: (path: string) => Promise<any>,
  post: (path: string, body?: unknown) => Promise<any>,
  put: (path: string, body?: unknown) => Promise<any>,
  patch: (path: string, body?: unknown) => Promise<any>,
  delete: (path: string, body?: unknown) => Promise<any>,
  raw: (path: string, options?: RequestInit, retry?: boolean) => Promise<Response>,
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor AuditLogsService and BranchService</name>
  <files>src/frontend/src/services/auditLogsService.ts, src/frontend/src/services/branchService.ts</files>
  <action>
    - Import { http } from './http'.
    - Remove direct BASE_URL and API_CONFIG usage in these files.
    - Replace all fetch() calls with http.get(), http.post(), http.put(), or http.delete().
    - Remove manual response.ok checks and error parsing (http.ts handles this).
    - Ensure URLSearchParams are used for query strings where appropriate.
    - Note: http.get automatically unwraps { data } or { success, data } based on backend pattern.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); ['src/frontend/src/services/auditLogsService.ts', 'src/frontend/src/services/branchService.ts'].forEach(f => { if(fs.readFileSync(f, 'utf8').includes('fetch(')) throw new Error('fetch still present in ' + f) })"</automated>
  </verify>
  <done>Direct fetch calls and manual error handling removed from both services.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor PayrollEmployeesService</name>
  <files>src/frontend/src/services/payrollEmployeesService.ts</files>
  <action>
    - Import { http } from './http'.
    - Remove direct BASE_URL and API_CONFIG usage.
    - Replace fetch() in getPayrollEmployees with http.get().
    - Path should be `/payroll/${payrollId}/employees`.
    - Return the unwrapped data directly.
  </action>
  <verify>
    <automated>node -e "if(require('fs').readFileSync('src/frontend/src/services/payrollEmployeesService.ts', 'utf8').includes('fetch(')) throw new Error('fetch still present')"</automated>
  </verify>
  <done>PayrollEmployeesService migrated to http.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend Service → http.ts | High-trust internal boundary. |
| http.ts → Backend API | Untrusted boundary crossing via HTTP. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02 | Information Disclosure | `http.ts` / Internal API | mitigate | Centralizing all calls in `http.ts` ensures consistent auth header attachment and refresh handling, preventing session failures or token leakage through inconsistent implementations. |
</threat_model>

<verification>
Standard check: 'npm run build' in src/frontend (if environment allows) or ensure no 'fetch(' exists in services folder except in http.ts.
</verification>

<success_criteria>
- AuditLogsService, BranchService, and PayrollEmployeesService use http.ts for all communication.
- Manual error parsing and URL building are removed from these services.
</success_criteria>

<output>
After completion, create `.planning/phases/25-http-client-layer-enforcement/25-01-SUMMARY.md`
</output>
