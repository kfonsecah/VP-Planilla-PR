---
phase: 24-auth-token-lifecycle-end-to-end
plan: "02"
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - src/backend/src/service/AuthService.ts
  - src/backend/src/controller/AuthController.ts
  - src/backend/src/routes/AuthRoute.ts
  - src/backend/src/__tests__/integration/auth.lifecycle.test.ts
autonomous: true
requirements:
  - AUTH-07
  - AUTH-06
must_haves:
  truths:
    - "Logout invalida sesión de manera completa en servidor y cliente no puede reutilizar token previo"
    - "Refresh endpoint deja de ser placeholder y devuelve token nuevo válido"
    - "Un token invalidado no recupera acceso tras logout"
  artifacts:
    - path: "src/backend/src/controller/AuthController.ts"
      provides: "refreshToken/logout implementados con lógica real"
      contains: "static async refreshToken"
    - path: "src/backend/src/service/AuthService.ts"
      provides: "helpers para emisión y revocación de tokens"
      contains: "addTokenToBlocklist"
    - path: "src/backend/src/__tests__/integration/auth.lifecycle.test.ts"
      provides: "flujo login→refresh→logout→reuse denied"
      contains: "logout"
  key_links:
    - from: "src/backend/src/routes/AuthRoute.ts"
      to: "src/backend/src/controller/AuthController.ts"
      via: "POST /refresh y POST /logout"
      pattern: "router\.post\(\"/(refresh|logout)\""
    - from: "src/backend/src/controller/AuthController.ts"
      to: "src/backend/src/service/AuthService.ts"
      via: "verify/add blocklist/generate token"
      pattern: "AuthService\.(verifyToken|addTokenToBlocklist|authenticate)"
---

<objective>
Implementar ciclo backend completo de refresh y logout con invalidación real para impedir reutilización de tokens.

Purpose: AUTH-07 exige cierre de sesión total y AUTH-06 requiere enforcement real de token invalidado más allá del cliente.
Output: Endpoint `/api/refresh` funcional, logout reforzado, y prueba de ciclo auth end-to-end en backend.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-auth-token-lifecycle-end-to-end/24-RESEARCH.md
@.planning/phases/07-rate-limiting-helmet-token-revocation/07-04-SUMMARY.md
@.planning/phases/24-auth-token-lifecycle-end-to-end/24-01-SUMMARY.md

<interfaces>
From src/backend/src/controller/AuthController.ts:
```typescript
static async login(req: Request, res: Response): Promise<Response>
static async logout(req: Request, res: Response): Promise<Response>
static async refreshToken(req: Request, res: Response): Promise<Response>
```

From src/backend/src/routes/AuthRoute.ts:
```typescript
router.post('/login', ...)
router.post('/logout', asyncHandler(AuthMiddleware.verifyToken), asyncHandler(AuthController.logout))
router.post('/refresh', asyncHandler(AuthController.refreshToken))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Definir pruebas de ciclo auth lifecycle (AUTH-07)</name>
  <files>src/backend/src/__tests__/integration/auth.lifecycle.test.ts</files>
  <read_first>
    src/backend/src/controller/AuthController.ts,
    src/backend/src/routes/AuthRoute.ts,
    src/backend/src/__tests__/unit/services/AuthService.test.ts,
    src/backend/jest.config.js
  </read_first>
  <behavior>
    - Caso 1: login válido -> obtiene access token.
    - Caso 2: refresh con `refresh_token` válido -> devuelve nuevo token de acceso.
    - Caso 3: logout con bearer token válido -> token queda blocklisted.
    - Caso 4: token antiguo tras logout recibe 401 al revalidar en middleware.
  </behavior>
  <action>
    Crear `auth.lifecycle.test.ts` con mocks de Prisma + jwt para simular login/refresh/logout/reuse. La prueba debe verificar explícitamente status codes y codes canónicos definidos en Plan 24-01.

    Incluir caso idempotente: segundo logout sobre token ya invalidado no debe devolver 500; debe responder éxito o 401 controlado, nunca crash.
  </action>
  <verify>
    <automated>cd src/backend && npm test -- auth.lifecycle.test.ts --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - src/backend/src/__tests__/integration/auth.lifecycle.test.ts existe
    - auth.lifecycle.test.ts contiene "login"
    - auth.lifecycle.test.ts contiene "refresh"
    - auth.lifecycle.test.ts contiene "logout"
    - auth.lifecycle.test.ts contiene "401"
  </acceptance_criteria>
  <done>
    Existe test de integración del ciclo auth que captura regresiones de logout incompleto y refresh inexistente.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar refresh real y reforzar logout server-side (AUTH-07, AUTH-06)</name>
  <files>
    src/backend/src/service/AuthService.ts,
    src/backend/src/controller/AuthController.ts,
    src/backend/src/routes/AuthRoute.ts
  </files>
  <read_first>
    src/backend/src/service/AuthService.ts,
    src/backend/src/controller/AuthController.ts,
    src/backend/src/routes/AuthRoute.ts,
    src/backend/src/middleware/AuthMiddleware.ts,
    src/backend/src/__tests__/integration/auth.lifecycle.test.ts
  </read_first>
  <behavior>
    - Caso 1: `/api/refresh` deja de responder placeholder y entrega token nuevo válido.
    - Caso 2: `/api/logout` invalida token actual siempre que pueda parsearse y evita reutilización posterior.
    - Caso 3: errores de refresh/logout usan envelope auth canónico (coherente con D-Plan 24-01).
  </behavior>
  <action>
    En `AuthService.ts`, exponer helper público de emisión de access token (sin duplicar lógica) reutilizando secret/expiración actuales. Mantener `jsonwebtoken` existente (sin nuevas libs).

    En `AuthController.refreshToken`, reemplazar el placeholder por implementación concreta:
    1) leer `refresh_token` del body,
    2) validar presencia (400 si falta),
    3) verificar token con `AuthService.verifyToken`,
    4) buscar usuario por id,
    5) emitir access token nuevo,
    6) responder `{ success: true, token: <new_access_token> }`.

    En `AuthController.logout`, reforzar invalidación:
    - Extraer bearer token,
    - si token válido, calcular `expiresAt` y registrar en blocklist,
    - si token ya expirado/invalidado, responder controlado (no 500).

    En `AuthRoute.ts`, mantener `/refresh` público (sin `verifyToken`) pero asegurar docs Swagger alineados al payload real; `/logout` se mantiene protegido por middleware.
  </action>
  <verify>
    <automated>cd src/backend && npm test -- auth.lifecycle.test.ts AuthService.test.ts --runInBand && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - AuthController.ts no contiene "pendiente implementar" en refreshToken
    - AuthController.ts contiene "refresh_token"
    - AuthController.ts contiene "addTokenToBlocklist"
    - AuthRoute.ts contiene "/refresh"
    - auth.lifecycle.test.ts pasa
    - npx tsc --noEmit finaliza con código 0 en src/backend
  </acceptance_criteria>
  <done>
    Refresh entrega token de acceso utilizable y logout invalida de forma durable tokens previos para impedir reutilización.
  </done>
</task>

</tasks>

<verification>
- `cd src/backend && npm test -- auth.lifecycle.test.ts AuthService.test.ts --runInBand` pasa
- `cd src/backend && npx tsc --noEmit` pasa
- `/api/refresh` devuelve token nuevo (no mensaje placeholder)
</verification>

<success_criteria>
- AUTH-07 cubierto: logout invalida sesión de extremo a extremo y token previo no vuelve a servir
- AUTH-06 reforzado: revocación efectiva en middleware + flujo logout
- Flujo login→refresh→logout→reuse denied queda automatizado en tests
</success_criteria>

<output>
After completion, create `.planning/phases/24-auth-token-lifecycle-end-to-end/24-02-SUMMARY.md`
</output>
