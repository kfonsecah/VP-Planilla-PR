---
phase: 24-auth-token-lifecycle-end-to-end
plan: "03"
type: execute
wave: 3
depends_on:
  - 24-01
  - 24-02
files_modified:
  - src/frontend/src/services/authService.ts
  - src/frontend/src/services/http.ts
  - src/frontend/src/hooks/useAuth.tsx
  - src/frontend/src/__tests__/services/http.auth.test.ts
  - src/frontend/src/__tests__/hooks/useAuth.logout.test.tsx
autonomous: true
requirements:
  - AUTH-05
  - AUTH-07
  - AUTH-08
must_haves:
  truths:
    - "El frontend refresca access token de forma consistente en una sola ruta de transporte"
    - "Logout limpia credenciales cliente y redirige a reautenticación de forma determinista"
    - "Errores auth del backend se interpretan por `error.code` y no por strings frágiles"
  artifacts:
    - path: "src/frontend/src/services/http.ts"
      provides: "Single-flight refresh + retry-once + auth failure callback"
      contains: "tryRefreshToken"
    - path: "src/frontend/src/services/authService.ts"
      provides: "Auth API calls delegando vía http.ts"
      contains: "http.post('/login'"
    - path: "src/frontend/src/hooks/useAuth.tsx"
      provides: "Inicialización de sesión + logout cliente coherente"
      contains: "setOnAuthFailure"
    - path: "src/frontend/src/__tests__/services/http.auth.test.ts"
      provides: "Pruebas refresh-once y forced logout"
      contains: "401"
  key_links:
    - from: "src/frontend/src/services/authService.ts"
      to: "src/frontend/src/services/http.ts"
      via: "auth calls through shared transport"
      pattern: "import .*http"
    - from: "src/frontend/src/hooks/useAuth.tsx"
      to: "src/frontend/src/services/http.ts"
      via: "setOnAuthFailure + setTokens/clearTokens"
      pattern: "http\.(setOnAuthFailure|setTokens|clearTokens)"
---

<objective>
Cerrar el lifecycle de auth en frontend eliminando bypasses y garantizando refresh/logout robustos con manejo uniforme de errores.

Purpose: AUTH-05 requiere refresh consistente sin romper sesiones; AUTH-07 y AUTH-08 exigen logout completo y lectura determinista de errores auth.
Output: authService alineado a http.ts, transporte con refresh single-flight y pruebas frontend para refresh/logout.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-auth-token-lifecycle-end-to-end/24-RESEARCH.md
@.planning/phases/24-auth-token-lifecycle-end-to-end/24-01-SUMMARY.md
@.planning/phases/24-auth-token-lifecycle-end-to-end/24-02-SUMMARY.md

<interfaces>
From src/frontend/src/services/http.ts:
```typescript
export class ApiError extends Error {
  statusCode: number;
  fieldErrors: Record<string, string> | null;
  isNetworkError: boolean;
}

export function setStoredTokens(accessToken: string | null, refreshToken: string | null): void;
export function clearStoredTokens(): void;
export function setOnAuthFailure(cb: () => void): void;

export const http: {
  get(path: string): Promise<any>;
  post(path: string, body?: unknown): Promise<any>;
  put(path: string, body?: unknown): Promise<any>;
  patch(path: string, body?: unknown): Promise<any>;
  delete(path: string, body?: unknown): Promise<any>;
}
```

From src/frontend/src/hooks/useAuth.tsx:
```typescript
const login: (username: string, password: string) => Promise<void>;
const logout: () => Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Escribir pruebas frontend para refresh single-flight y logout completo (AUTH-05, AUTH-07)</name>
  <files>
    src/frontend/src/__tests__/services/http.auth.test.ts,
    src/frontend/src/__tests__/hooks/useAuth.logout.test.tsx
  </files>
  <read_first>
    src/frontend/src/services/http.ts,
    src/frontend/src/services/authService.ts,
    src/frontend/src/hooks/useAuth.tsx,
    src/frontend/src/__tests__/hooks/useClockLogs.test.ts,
    src/frontend/jest.config.js
  </read_first>
  <behavior>
    - Test 1: dos respuestas 401 concurrentes disparan un único refresh request (single-flight).
    - Test 2: refresh exitoso reintenta request original una sola vez y devuelve data.
    - Test 3: refresh fallido limpia `vp_access_token`/`vp_refresh_token` y ejecuta callback de auth failure.
    - Test 4: logout en hook limpia estado local, limpia tokens y navega a pantalla de login.
  </behavior>
  <action>
    Crear tests dedicados para auth lifecycle en frontend:
    - `http.auth.test.ts` para orquestación de `http.raw/requestJson` con mocks de fetch/localStorage.
    - `useAuth.logout.test.tsx` para validar secuencia de logout en hook (incluyendo llamada a `AuthService.logout` cuando hay token).

    Las aserciones deben verificar llaves `vp_access_token` y `vp_refresh_token` explícitamente (decisión de proyecto no negociable).
  </action>
  <verify>
    <automated>cd src/frontend && npm test -- http.auth.test.ts useAuth.logout.test.tsx --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - src/frontend/src/__tests__/services/http.auth.test.ts existe
    - http.auth.test.ts contiene "single-flight" o validación equivalente de 1 refresh
    - src/frontend/src/__tests__/hooks/useAuth.logout.test.tsx existe
    - useAuth.logout.test.tsx contiene "vp_access_token"
    - useAuth.logout.test.tsx contiene "vp_refresh_token"
  </acceptance_criteria>
  <done>
    Hay suite RED/GREEN que protege refresh consistente y logout cliente completo frente a regresiones.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Eliminar bypass fetch en authService y endurecer http/useAuth para contrato auth (AUTH-05, AUTH-08)</name>
  <files>
    src/frontend/src/services/authService.ts,
    src/frontend/src/services/http.ts,
    src/frontend/src/hooks/useAuth.tsx
  </files>
  <read_first>
    src/frontend/src/services/authService.ts,
    src/frontend/src/services/http.ts,
    src/frontend/src/hooks/useAuth.tsx,
    src/frontend/src/services/index.ts,
    src/frontend/src/__tests__/services/http.auth.test.ts,
    src/frontend/src/__tests__/hooks/useAuth.logout.test.tsx
  </read_first>
  <behavior>
    - Caso 1: authService no usa `fetch` directo para login/me/logout/validate/refresh/change-password.
    - Caso 2: http.ts maneja 401 por `error.code` canónico sin depender de mensajes en español.
    - Caso 3: useAuth conserva inicialización de tokens y callback de fallo auth sin loops ni estado inconsistente.
  </behavior>
  <action>
    Refactorizar `authService.ts` para delegar todas las llamadas a `http.get/post` (sin `fetchWithTimeout` local). Mantener misma API pública de métodos para no romper consumidores.

    Endurecer `http.ts`:
    - agregar lock single-flight (Promise compartida) en `tryRefreshToken()` para evitar stampede,
    - preservar retry único por request (`retry` boolean),
    - al recibir 401 final, limpiar tokens y disparar `onAuthFailureCallback` una vez por flujo fallido.

    Ajustar parseo de errores auth para priorizar `error.code` cuando backend responda envelope canónico del Plan 24-01; mantener backward compatibility con `message` mientras conviva código legacy.

    En `useAuth.tsx`, validar que `logout()` siempre limpie estado local incluso si llamada server falla y que navegación a auth ocurra tras limpieza.
  </action>
  <verify>
    <automated>cd src/frontend && npm test -- http.auth.test.ts useAuth.logout.test.tsx --runInBand && npx tsc --noEmit && npx next lint</automated>
  </verify>
  <acceptance_criteria>
    - src/frontend/src/services/authService.ts no contiene "fetch(" 
    - src/frontend/src/services/authService.ts contiene "http.post('/login'"
    - src/frontend/src/services/authService.ts contiene "http.post('/refresh'"
    - src/frontend/src/services/http.ts contiene lock/promise compartida para refresh (variable dedicada)
    - src/frontend/src/services/http.ts contiene referencia a `error.code` en parseo auth
    - useAuth.tsx mantiene `http.setOnAuthFailure(`
    - npx tsc --noEmit finaliza con código 0 en src/frontend
    - npx next lint finaliza con código 0 en src/frontend
  </acceptance_criteria>
  <done>
    Auth frontend queda centralizado en http.ts, con refresh consistente, logout determinista y manejo uniforme de errores auth.
  </done>
</task>

</tasks>

<verification>
- `cd src/frontend && npm test -- http.auth.test.ts useAuth.logout.test.tsx --runInBand` pasa
- `cd src/frontend && npx tsc --noEmit` pasa
- `cd src/frontend && npx next lint` pasa
- `grep -R "fetch(" src/frontend/src/services/authService.ts` no devuelve resultados
</verification>

<success_criteria>
- AUTH-05: refresh consistente centralizado y sin carreras por concurrencia
- AUTH-07: logout cliente limpia sesión y no deja tokens reutilizables localmente
- AUTH-08: frontend interpreta errores auth por contrato estable (`error.code`) y status 401/403
</success_criteria>

<output>
After completion, create `.planning/phases/24-auth-token-lifecycle-end-to-end/24-03-SUMMARY.md`
</output>
