---
phase: 24-auth-token-lifecycle-end-to-end
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/src/middleware/AuthMiddleware.ts
  - src/backend/src/service/AuthService.ts
  - src/backend/src/__tests__/unit/middleware/AuthMiddleware.test.ts
  - src/backend/src/__tests__/unit/services/AuthService.test.ts
autonomous: true
requirements:
  - AUTH-06
  - AUTH-08
must_haves:
  truths:
    - "A revoked or invalid access token is rejected uniformly with HTTP 401"
    - "A role/scope failure is returned uniformly with HTTP 403"
    - "Protected endpoints share one canonical auth-error payload shape"
  artifacts:
    - path: "src/backend/src/middleware/AuthMiddleware.ts"
      provides: "Canonical 401/403 auth failure mapping"
      contains: "error.code"
    - path: "src/backend/src/service/AuthService.ts"
      provides: "Token verification and revocation helpers used by middleware"
      contains: "isTokenBlocklisted"
    - path: "src/backend/src/__tests__/unit/middleware/AuthMiddleware.test.ts"
      provides: "Regression tests for 401/403 mapping and revoked token behavior"
      contains: "AUTH_TOKEN_REVOKED"
  key_links:
    - from: "src/backend/src/middleware/AuthMiddleware.ts"
      to: "src/backend/src/service/AuthService.ts"
      via: "verifyToken + blocklist checks"
      pattern: "AuthService\.(verifyToken|isTokenBlocklisted)"
    - from: "src/backend/src/routes/*.ts"
      to: "src/backend/src/middleware/AuthMiddleware.ts"
      via: "router.use(AuthMiddleware.verifyToken)"
      pattern: "router\.use\(AuthMiddleware\.verifyToken\)"
---

<objective>
Establecer el contrato canónico de errores de autenticación/autorización y endurecer el gate global de middleware para todos los endpoints protegidos.

Purpose: AUTH-06 y AUTH-08 requieren que cualquier token inválido/revocado/expirado falle igual en toda la API y que 401/403 no se mezclen.
Output: Middleware y servicio de auth alineados a un contrato único + pruebas unitarias que bloqueen regresiones.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-auth-token-lifecycle-end-to-end/24-RESEARCH.md
@.planning/phases/02-seguridad-de-autenticaci-n/02-01-SUMMARY.md
@.planning/phases/07-rate-limiting-helmet-token-revocation/07-04-SUMMARY.md

<interfaces>
From src/backend/src/middleware/AuthMiddleware.ts:
```typescript
static async verifyToken(req: Request, res: Response, next: NextFunction): Promise<Response | void>
static requireRole(allowedRoles: string[])
```

From src/backend/src/service/AuthService.ts:
```typescript
static verifyToken(token: string): any
static async isTokenBlocklisted(token: string): Promise<boolean>
static async addTokenToBlocklist(token: string, expiresAt: Date): Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear pruebas RED para contrato canónico 401/403 (AUTH-08)</name>
  <files>
    src/backend/src/__tests__/unit/middleware/AuthMiddleware.test.ts,
    src/backend/src/__tests__/unit/services/AuthService.test.ts
  </files>
  <read_first>
    src/backend/src/middleware/AuthMiddleware.ts,
    src/backend/src/service/AuthService.ts,
    src/backend/src/__tests__/unit/services/AuthService.test.ts,
    src/backend/src/routes/AuthRoute.ts
  </read_first>
  <behavior>
    - Test 1: verifyToken sin Authorization header responde 401 con `{ success:false, error:{ code:'AUTH_TOKEN_MISSING', status:401, retryable:true } }`.
    - Test 2: verifyToken con token blocklisted responde 401 con `AUTH_TOKEN_REVOKED`.
    - Test 3: requireRole con usuario autenticado sin rol permitido responde 403 con `AUTH_INSUFFICIENT_SCOPE`.
    - Test 4: payload de error mantiene shape consistente (`error.code`, `error.message`, `error.status`, `error.retryable`).
  </behavior>
  <action>
    Agregar `src/backend/src/__tests__/unit/middleware/AuthMiddleware.test.ts` (nuevo) y ampliar `AuthService.test.ts` para cubrir contratos del middleware y helper de token invalidado. Usar mocks de `AuthService.verifyToken`, `AuthService.isTokenBlocklisted`, y `AuthService.getUserById` para forzar cada branch.

    No usar aserciones sobre texto libre en español como criterio principal; asertar `status` + `error.code` para garantizar manejabilidad frontend (AUTH-08).
  </action>
  <verify>
    <automated>cd src/backend && npm test -- AuthMiddleware.test.ts AuthService.test.ts --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - src/backend/src/__tests__/unit/middleware/AuthMiddleware.test.ts existe
    - AuthMiddleware.test.ts contiene AUTH_TOKEN_MISSING
    - AuthMiddleware.test.ts contiene AUTH_TOKEN_REVOKED
    - AuthMiddleware.test.ts contiene AUTH_INSUFFICIENT_SCOPE
    - AuthService.test.ts cubre token blocklisted true/false
  </acceptance_criteria>
  <done>
    Existe red suite que define explícitamente el contrato 401/403 y falla si el middleware vuelve a payloads inconsistentes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar helper de error auth + normalizar AuthMiddleware (AUTH-06, AUTH-08)</name>
  <files>
    src/backend/src/middleware/AuthMiddleware.ts,
    src/backend/src/service/AuthService.ts
  </files>
  <read_first>
    src/backend/src/middleware/AuthMiddleware.ts,
    src/backend/src/service/AuthService.ts,
    src/backend/src/routes/UserRoute.ts,
    src/backend/src/routes/ClockLogsRoute.ts,
    src/backend/src/__tests__/unit/middleware/AuthMiddleware.test.ts
  </read_first>
  <behavior>
    - Test 1: cualquier token faltante/inválido/expirado/revocado retorna 401 con envelope canónico.
    - Test 2: requireRole mantiene 403 canónico y no usa payload legacy `message` suelto.
  </behavior>
  <action>
    En `AuthMiddleware.ts`, definir un helper interno concreto:
    `buildAuthError(status: 401 | 403, code: 'AUTH_TOKEN_MISSING' | 'AUTH_TOKEN_INVALID' | 'AUTH_TOKEN_REVOKED' | 'AUTH_TOKEN_EXPIRED' | 'AUTH_INSUFFICIENT_SCOPE', message: string)` que devuelva:
    `{ success:false, error:{ code, message, status, retryable: status===401 } }`.

    Reemplazar TODAS las respuestas auth actuales (`message` plano) por este helper en `verifyToken` y `requireRole`. Mantener `router.use(AuthMiddleware.verifyToken)` sin cambios estructurales (patrón validado en fases previas).

    En `AuthService.verifyToken`, preservar firma actual pero diferenciar error expirado (`TokenExpiredError`) de inválido para mapear `AUTH_TOKEN_EXPIRED` vs `AUTH_TOKEN_INVALID` en middleware. No introducir nuevas dependencias.
  </action>
  <verify>
    <automated>cd src/backend && npm test -- AuthMiddleware.test.ts AuthService.test.ts --runInBand && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - AuthMiddleware.ts contiene buildAuthError(
    - AuthMiddleware.ts contiene AUTH_TOKEN_EXPIRED
    - AuthMiddleware.ts contiene AUTH_TOKEN_INVALID
    - AuthMiddleware.ts contiene AUTH_INSUFFICIENT_SCOPE
    - AuthMiddleware.ts no contiene `message: 'Token inválido'`
    - npx tsc --noEmit finaliza con código 0 en src/backend
  </acceptance_criteria>
  <done>
    Middleware protegido retorna errores auth uniformes 401/403 con códigos estables y revocación aplicada centralmente.
  </done>
</task>

</tasks>

<verification>
- `cd src/backend && npm test -- AuthMiddleware.test.ts AuthService.test.ts --runInBand` pasa
- `cd src/backend && npx tsc --noEmit` pasa
- `grep -R "router.use(AuthMiddleware.verifyToken)" src/backend/src/routes` muestra protección vigente en routers privados
</verification>

<success_criteria>
- AUTH-06: token revocado se rechaza consistentemente por middleware
- AUTH-08: payload 401/403 tiene shape único (`success:false`, `error.code`, `error.status`)
- Existe cobertura unitaria explícita para missing/invalid/expired/revoked/insufficient-scope
</success_criteria>

<output>
After completion, create `.planning/phases/24-auth-token-lifecycle-end-to-end/24-01-SUMMARY.md`
</output>
