---
phase: 27
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified: [src/frontend/src/app/pages/clock-logs/page.tsx, src/frontend/src/features/clock-logs/presenters/clockLogPresenter.ts, src/backend/src/controller/ClockLogsController.ts, src/backend/src/service/ClockLogsImportService.ts, src/backend/src/utils/dateUtils.ts]
autonomous: true
requirements: [MOD-01, MOD-02]

must_haves:
  truths:
    - "ClockLogsDashboardPage no contiene lógica de formateo de fechas o mapeo de colores"
    - "ClockLogsController delega la resolución de empleados e importación a un servicio"
  artifacts:
    - path: "src/frontend/src/features/clock-logs/presenters/clockLogPresenter.ts"
      provides: "Funciones puras de presentación para UI"
    - path: "src/backend/src/service/ClockLogsImportService.ts"
      provides: "Lógica de negocio desacoplada para importación"
---

<objective>
Aplicar el "Layered Feature Pattern" para desacoplar la lógica de negocio y transformación de datos de los componentes de UI (Frontend) y controladores (Backend).

Propósito: Reducir la complejidad cognitiva de los archivos monolíticos y facilitar su mantenimiento y testeo.
Salida: Presenter en frontend y Servicio especializado en backend.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-monolith-decomposition-and-maintainability/27-01-SUMMARY.md
@.planning/phases/27-monolith-decomposition-and-maintainability/27-AUDIT-REPORT.md
@src/frontend/src/app/pages/clock-logs/page.tsx
@src/backend/src/controller/ClockLogsController.ts
</context>

<tasks>

<task type="auto">
  <name>Frontend: Extraer ClockLogPresenter</name>
  <files>src/frontend/src/app/pages/clock-logs/page.tsx, src/frontend/src/features/clock-logs/presenters/clockLogPresenter.ts</files>
  <action>
    1. Crear `src/frontend/src/features/clock-logs/presenters/clockLogPresenter.ts`.
    2. Mover las funciones helper `isoToDisplay` y `parseDisplayToISO` de `page.tsx` al presenter.
    3. Mover las constantes de configuración visual (`SOURCE_LABELS`, `STATUS_CARD_COLORS`, `STATUS_NAMES`, `STATUS_TOGGLE_COLORS`) al presenter.
    4. Crear una función `getClockLogViewModel(log: any)` que encapsule la lógica de badges y colores.
    5. Refactorizar `page.tsx` para importar y usar estas funciones/constantes, eliminando las definiciones locales.
  </action>
  <verify>
    <automated>cd src/frontend && npx eslint src/app/pages/clock-logs/page.tsx --rule "sonarjs/cognitive-complexity: [error, 15]"</automated>
  </verify>
  <done>La lógica de presentación está desacoplada del componente de página.</done>
</task>

<task type="auto">
  <name>Backend: Extraer ClockLogsImportService y DateUtils</name>
  <files>src/backend/src/controller/ClockLogsController.ts, src/backend/src/service/ClockLogsImportService.ts, src/backend/src/utils/dateUtils.ts</files>
  <action>
    1. Crear `src/backend/src/utils/dateUtils.ts` y mover `parseLocalDate` y `parseLocalDateEnd`.
    2. Crear `src/backend/src/service/ClockLogsImportService.ts`.
    3. Mover `normalizeName` y `resolveEmployeeId` de `ClockLogsController.ts` al nuevo servicio.
    4. Crear un método `processImport(logs: any[], userId: number)` en `ClockLogsImportService` que orqueste la importación (actualmente en el controller).
    5. Refactorizar `ClockLogsController.ts` para usar el servicio y los utils, reduciendo su tamaño y complejidad.
  </action>
  <verify>
    <automated>cd src/backend && npx eslint src/controller/ClockLogsController.ts --rule "sonarjs/cognitive-complexity: [error, 25]"</automated>
  </verify>
  <done>La lógica de importación y utilitarios está desacoplada del controlador.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Logic Transfer | Moving sensitive employee resolution logic between files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Information Disclosure | resolveEmployeeId | mitigate | Ensure the logic still respects `employee_fired` and other security filters during extraction. |
</threat_model>

<verification>
Ejecutar el lint de complejidad en los archivos refactorizados y confirmar que la complejidad ha disminuido significativamente.
</verification>

<success_criteria>
- `ClockLogPresenter` creado y usado en frontend.
- `ClockLogsImportService` creado y usado en backend.
- Complejidad cognitiva de `page.tsx` y `ClockLogsController.ts` reducida.
</success_criteria>

<output>
After completion, create `.planning/phases/27-monolith-decomposition-and-maintainability/27-02-SUMMARY.md`
</output>
