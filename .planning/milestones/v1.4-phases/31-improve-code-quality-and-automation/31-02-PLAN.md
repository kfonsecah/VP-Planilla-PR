---
phase: 31-improve-code-quality-and-automation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/Java/clocklogs/pom.xml, src/Java/clocklogs/src/test/java/com/verde/pradera/controller/ClockLogProcessorTest.java]
autonomous: true
requirements: [QUAL-02]
user_setup: []

must_haves:
  truths:
    - "Java clocklogs utility can run automated unit tests"
    - "Core processing logic (IN/OUT matching) is verified by tests"
  artifacts:
    - path: "src/Java/clocklogs/src/test/java/com/verde/pradera/controller/ClockLogProcessorTest.java"
      provides: "Automated verification of log processing"
  key_links:
    - from: "src/Java/clocklogs/pom.xml"
      to: "junit-jupiter"
      via: "dependency management"
---

<objective>
Close the manual execution gap for the Java clocklogs utility by introducing JUnit 5 and creating automated unit tests for its core processing logic.

Purpose: Ensure reliability of clock log normalization and matching without manual spot-checking.
Output: Maven-integrated test suite for the Java utility.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@src/Java/clocklogs/pom.xml
@src/Java/clocklogs/src/main/java/com/verde/pradera/controller/ClockLogProcessor.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Maven for automated testing</name>
  <files>src/Java/clocklogs/pom.xml</files>
  <action>
    Update src/Java/clocklogs/pom.xml:
    1. Add junit-jupiter-api, junit-jupiter-engine (v5.10.2) and mockito-core (v5.11.0) dependencies.
    2. Add maven-surefire-plugin to the build/plugins section to enable test execution.
    3. Ensure the project structure supports src/test/java.
  </action>
  <verify>
    <automated>mvn test (expected: Build Success, but 0 tests run)</automated>
  </verify>
  <done>Maven is configured to find and run JUnit 5 tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement core unit tests for ClockLogProcessor</name>
  <files>src/Java/clocklogs/src/test/java/com/verde/pradera/controller/ClockLogProcessorTest.java</files>
  <behavior>
    - Test that IN and OUT marks are correctly matched into sessions.
    - Test that missing OUT marks are handled correctly (orphans).
    - Test that normalization logic (e.g., date formats) works as expected.
  </behavior>
  <action>
    Create src/Java/clocklogs/src/test/java/com/verde/pradera/controller/ClockLogProcessorTest.java:
    1. Create a test class using JUnit 5.
    2. Mock dbConnector and QueryManager if necessary to isolate processing logic.
    3. Write tests covering successful matching and edge cases (empty input, mismatched logs).
  </action>
  <verify>
    <automated>mvn test</automated>
  </verify>
  <done>Core logic is verified by automated tests with passing status</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Data -> Logic | In-memory test data ensures logic correctness without affecting production database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Tampering | pom.xml | mitigate | Dependency version pinning prevents supply chain issues during build |
</threat_model>

<verification>
Run `mvn test` in `src/Java/clocklogs` and verify that tests are executed and passed.
</verification>

<success_criteria>
1. `mvn test` command executes without errors.
2. At least 5 unit tests are defined covering key logic in `ClockLogProcessor`.
3. Build artifacts (`target/`) are correctly ignored (verified by .gitignore, which should already be done in Phase 30).
</success_criteria>

<output>
After completion, create `.planning/phases/31-improve-code-quality-and-automation/31-02-SUMMARY.md`
</output>
