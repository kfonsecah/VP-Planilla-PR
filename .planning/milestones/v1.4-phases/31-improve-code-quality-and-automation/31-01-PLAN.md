---
phase: 31-improve-code-quality-and-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/backend/src/config/env.ts, src/backend/src/index.ts, src/backend/src/service/AuthService.ts, src/backend/src/service/ReportsService.ts, src/backend/src/utils/docs.ts, src/backend/src/config/emailConfig.ts]
autonomous: true
requirements: [QUAL-01]
user_setup: []

must_haves:
  truths:
    - "Application fails fast at startup if critical environment variables are missing"
    - "Direct process.env calls are eliminated from core services"
    - "Environment variables are type-safe (e.g., PORT is a number)"
  artifacts:
    - path: "src/backend/src/config/env.ts"
      provides: "Centralized environment configuration"
  key_links:
    - from: "src/backend/src/index.ts"
      to: "src/backend/src/config/env.ts"
      via: "import { env } from './config/env'"
---

<objective>
Centralize and validate all backend environment variables using Zod to ensure type safety and early failure if configuration is missing.

Purpose: Eliminate "undefined" runtime errors and improve maintainability.
Output: Validated `env` configuration object and refactored backend services.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@src/backend/src/index.ts
@src/backend/src/service/AuthService.ts
@src/backend/src/config/emailConfig.ts

<interfaces>
From src/backend/src/config/emailConfig.ts:
```typescript
export const emailConfigSchema = z.object({
  RESEND_API_KEY: z.string().min(1, 'RESEND_API_KEY is required'),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create centralized env configuration</name>
  <files>src/backend/src/config/env.ts</files>
  <behavior>
    - Should load .env using dotenv
    - Should validate PORT (coerce to number), JWT_SECRET (string), DATABASE_URL (string/url), RESEND_API_KEY (string), ALLOWED_ORIGINS (string), JWT_EXPIRES_IN (coerce to number)
    - Should throw error if critical variables (JWT_SECRET, DATABASE_URL) are missing
  </behavior>
  <action>
    Create src/backend/src/config/env.ts:
    1. Import { z } from 'zod' and dotenv from 'dotenv'.
    2. Call dotenv.config() at the top.
    3. Define envSchema including all variables found in src/backend/src/index.ts and services.
    4. Export 'env' as the result of envSchema.parse(process.env).
    5. Handle comma-separated list for ALLOWED_ORIGINS by using z.string().transform(s => s.split(',')).
  </action>
  <verify>
    <automated>node -e "require('ts-node/register'); console.log(require('./src/backend/src/config/env').env.PORT)"</automated>
  </verify>
  <done>env.ts exists and correctly parses environment variables from .env</done>
</task>

<task type="auto">
  <name>Task 2: Refactor backend startup and docs to use env.ts</name>
  <files>src/backend/src/index.ts, src/backend/src/utils/docs.ts</files>
  <action>
    1. In index.ts:
       - Replace direct dotenv.config() with import { env } from './config/env'.
       - Remove manual JWT_SECRET check (env.ts handles it).
       - Replace process.env.PORT with env.PORT.
       - Replace process.env.ALLOWED_ORIGINS with env.ALLOWED_ORIGINS.
    2. In utils/docs.ts:
       - Replace process.env.PORT with env.PORT.
  </action>
  <verify>
    <automated>npm run dev (check if server starts without errors)</automated>
  </verify>
  <done>index.ts and docs.ts no longer use process.env directly</done>
</task>

<task type="auto">
  <name>Task 3: Refactor services and email config</name>
  <files>src/backend/src/service/AuthService.ts, src/backend/src/service/ReportsService.ts, src/backend/src/config/emailConfig.ts</files>
  <action>
    1. In AuthService.ts:
       - Replace process.env.JWT_SECRET and process.env.JWT_EXPIRES_IN with env.JWT_SECRET and env.JWT_EXPIRES_IN.
    2. In ReportsService.ts:
       - Replace any process.env calls with env equivalents.
    3. In emailConfig.ts:
       - Refactor getEmailConfig to use env.RESEND_API_KEY instead of manual process.env access.
  </action>
  <verify>
    <automated>npm test src/backend/src/__tests__/unit/services/AuthService.test.ts</automated>
  </verify>
  <done>Core services use type-safe env configuration</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process Env -> App | Untrusted environment variables are validated at entry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Information Disclosure | env.ts | mitigate | Ensure env variables are never logged directly to console in production |
| T-31-02 | Denial of Service | env.ts | mitigate | Strict validation prevents app from starting in inconsistent state |
</threat_model>

<verification>
Run `npm run lint` and `npm test` in the backend directory to ensure no regressions and all process.env calls are replaced (manually check grep if needed).
</verification>

<success_criteria>
1. All process.env calls in src/backend/src are replaced with env object references (except in env.ts itself).
2. The server starts and passes existing tests.
3. env.ts provides IDE autocompletion for all configuration keys.
</success_criteria>

<output>
After completion, create `.planning/phases/31-improve-code-quality-and-automation/31-01-SUMMARY.md`
</output>
