---
phase: 26-repository-hygiene-and-build-artifacts-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements:
  - HYG-02

must_haves:
  truths:
    - "The root .gitignore has no global *.json rule"
    - "The root .gitignore has a Backend section covering src/backend/dist/ and *.tsbuildinfo"
    - "The root .gitignore has a Frontend section covering src/frontend/.next/ and src/frontend/out/"
    - "The root .gitignore has a Java section covering src/Java/clocklogs/target/"
    - "The .claude/ and .planning/ directories are still explicitly re-included"
    - "Running git status after committing .gitignore shows no unexpected untracked or ignored files"
  artifacts:
    - path: ".gitignore"
      provides: "Multi-stack per-section ignore rules"
      contains: "src/backend/dist/"
  key_links:
    - from: ".gitignore"
      to: "src/backend/dist/"
      via: "gitignore rule"
      pattern: "src/backend/dist/"
    - from: ".gitignore"
      to: "src/Java/clocklogs/target/"
      via: "gitignore rule"
      pattern: "src/Java/clocklogs/target/"
---

<objective>
Replace the current root-level `.gitignore` with a well-structured, per-stack file that covers Backend (Node/TypeScript), Frontend (Next.js), Java (Maven), and General entries. Remove the broad `*.json` global ignore rule and its associated `!` re-include hacks — these silently suppress any new JSON file outside `.claude/` and `.planning/`.

Purpose: HYG-02 — `.gitignore` must cover all build artifacts across all three stacks with clear, maintainable rules rather than a global wildcard hack.
Output: An updated `.gitignore` committed to git, with no global `*.json` rule and explicit per-stack sections.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/26-repository-hygiene-and-build-artifacts-cleanup/26-CONTEXT.md
@.planning/phases/26-repository-hygiene-and-build-artifacts-cleanup/26-RESEARCH.md

<interfaces>
<!-- Current .gitignore content (the file being replaced). Read it first before writing. -->

Current .gitignore (47 lines) contains:
- LaTeX auxiliary file patterns (*.aux, *.bbl, *.blg, etc.)
- A global `*.json` rule that appears TWICE (lines 9 and 24)
- `!.claude/` and `!.claude/**` and `!.claude/**/*.json` re-includes
- `.claude/settings.local.json` and `.claude/worktrees/` explicit excludes
- `!.planning/` and `!.planning/**` and `!.planning/**/*.json` re-includes
- `coverage/` and `*.lcov` (test coverage reports)
- Temp/debug script names (parse_tmp.js, temp_script.py, etc.)
- `*.vscode` and `*.vscode/*` (IDE)
- `*.DS_Store` and `.DS_Store` (OS)

The new .gitignore MUST preserve:
- `.claude/settings.local.json` exclusion (local Claude settings, not tracked)
- `.claude/worktrees/` exclusion
- `coverage/` and `*.lcov` (test coverage is generated output)
- Temp/debug script list (parse_tmp.js, temp_script.py, test_hours.js, check_employee.ts, query_emp.mjs, *.tmp.js, *.tmp.ts)
- `!.claude/` and `!.claude/**` re-includes (still needed)
- `!.planning/` and `!.planning/**` re-includes (still needed)

The new .gitignore MUST remove:
- ALL `*.json` lines (both occurrences — lines 9 and 24)
- ALL `!.claude/**/*.json` and `!.planning/**/*.json` re-include hacks (no longer needed once *.json global rule is gone)

The new .gitignore MUST add:
- `src/backend/dist/` under Backend section
- `src/backend/*.tsbuildinfo` under Backend section
- `src/backend/node_modules/` under Backend section
- `package-lock.json` (specific lock file ignore, replacing global *.json)
- `src/frontend/.next/` under Frontend section
- `src/frontend/out/` under Frontend section
- `src/frontend/node_modules/` under Frontend section
- `src/Java/clocklogs/target/` under Java section
- `src/Java/clocklogs/*.class` under Java section
- `src/Java/clocklogs/*.jar` under Java section
- Maven metadata patterns under Java section
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace .gitignore with per-stack structured version</name>
  <files>.gitignore</files>
  <read_first>
    - `.gitignore` — read the current file completely before overwriting; preserve content that must be kept (see interfaces block above)
    - `.planning/phases/26-repository-hygiene-and-build-artifacts-cleanup/26-CONTEXT.md` — locked decisions for structure and *.json removal
    - `.planning/phases/26-repository-hygiene-and-build-artifacts-cleanup/26-RESEARCH.md` — the recommended .gitignore content is in the "Code Examples" section
  </read_first>
  <action>
Overwrite `.gitignore` with the following exact content. Do NOT add anything not listed here. Do NOT keep any `*.json` line.

```
# ============================================================================
# VP-Planilla Repository — .gitignore
# Organized per stack: Backend (Node/TypeScript), Frontend (Next.js), Java
# utility, General (OS/IDE/temp). Generated files are never tracked.
# ============================================================================

# ===== BACKEND (Node.js / TypeScript) =====
# Compiled TypeScript output — regenerate with: cd src/backend && npm run build
src/backend/dist/
src/backend/*.tsbuildinfo

# Dependencies
src/backend/node_modules/

# Package manager lock files (regenerable from package.json)
package-lock.json

# ===== FRONTEND (Next.js) =====
# Next.js build output — regenerate with: cd src/frontend && npm run build
src/frontend/.next/
src/frontend/out/

# Dependencies
src/frontend/node_modules/

# ===== JAVA UTILITY (Maven) =====
# Maven build output — regenerate with: mvn clean package -f src/Java/clocklogs/pom.xml
src/Java/clocklogs/target/

# Compiled classes and archives
src/Java/clocklogs/*.class
src/Java/clocklogs/*.jar

# Maven metadata
src/Java/clocklogs/pom.xml.tag
src/Java/clocklogs/pom.xml.releaseBackup
src/Java/clocklogs/pom.xml.versionsBackup
src/Java/clocklogs/pom.xml.next
src/Java/clocklogs/release.properties
src/Java/clocklogs/dependency-reduced-pom.xml

# ===== GENERAL (IDE, OS, Temp Files) =====
# IDE configurations
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Log files
*.log
logs/

# LaTeX auxiliary files
*.aux
*.bbl
*.blg
*.lof
*.lot
*.out
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk
*.synctex(busy)
*.txt
/docs/Formal/TFM.pdf
/docs/Formal/out/

# Test coverage reports — generated, do not commit
coverage/
*.lcov

# Temp/debug scripts — do not commit
parse_tmp.js
temp_script.py
test_hours.js
check_employee.ts
query_emp.mjs
*.tmp.js
*.tmp.ts

# ===== TRACKED CONFIGURATION DIRECTORIES =====
# .claude/ directory is tracked (project configuration)
!.claude/
!.claude/**

# Except local/personal settings
.claude/settings.local.json
.claude/worktrees/

# .planning/ directory is tracked (milestone & phase planning)
!.planning/
!.planning/**
```

After writing the file, run these verification commands:
1. `grep "^\*.json" .gitignore` — must return NOTHING (confirms global *.json rule is gone)
2. `grep "src/backend/dist/" .gitignore` — must return the line
3. `grep "src/Java/clocklogs/target/" .gitignore` — must return the line
4. `grep "src/frontend/.next/" .gitignore` — must return the line
5. `git add .gitignore && git status` — review output to confirm no unexpected files are hidden or newly untracked

Then commit: `git commit -m "refactor(gitignore): organize per-stack sections, remove global *.json rule"`

Note: Per user decision D-03 (CONTEXT.md): single root-level .gitignore, organized by Backend/Frontend/Java/General sections.
  </action>
  <verify>
    <automated>
      cd "C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla" && grep "^\*.json" .gitignore; echo "exit:$?"
    </automated>
    The grep for `^\*.json` must return NOTHING (empty output). If it returns any line, the global *.json rule was not removed.

    Also verify coverage:
    - `grep "src/backend/dist/" .gitignore` returns a match
    - `grep "src/Java/clocklogs/target/" .gitignore` returns a match
    - `grep "src/frontend/.next/" .gitignore` returns a match
    - `git log --oneline -1` shows the gitignore refactor commit
  </verify>
  <acceptance_criteria>
    - `grep "^\*.json" .gitignore` returns empty (exit 1 is acceptable — means no match found, which is correct)
    - `grep -c "src/backend/dist/" .gitignore` returns 1
    - `grep -c "src/Java/clocklogs/target/" .gitignore` returns 1
    - `grep -c "src/frontend/.next/" .gitignore` returns 1
    - `grep -c "!.claude/" .gitignore` returns at least 1 (re-include still present)
    - `grep -c "!.planning/" .gitignore` returns at least 1 (re-include still present)
    - `grep -c "\.claude/settings\.local\.json" .gitignore` returns 1 (local settings still excluded)
    - `grep -c "coverage/" .gitignore` returns 1 (test coverage still excluded)
    - `grep -c "parse_tmp\.js" .gitignore` returns 1 (temp scripts still excluded)
    - `git log --oneline -1` output contains "refactor(gitignore)"
    - `git status` shows clean working tree (no unexpected files surfaced or hidden)
  </acceptance_criteria>
  <done>
    .gitignore is committed with per-stack sections. No global *.json rule. All three build output directories (dist/, .next/, target/) explicitly covered. .claude/ and .planning/ re-includes preserved. Commit visible in git log.
  </done>
</task>

</tasks>

<verification>
After Plan 01 completes:

1. `grep "^\*.json" .gitignore` — returns nothing (HYG-02 partial: global rule gone)
2. `grep -E "src/backend/dist/|src/Java/clocklogs/target/|src/frontend/\.next/" .gitignore` — returns 3 matches (HYG-02: all stacks covered)
3. `git log --oneline -3` — shows refactor(gitignore) commit as most recent
4. `git status` — clean working tree, no unexpected changes

Note: HYG-01 and HYG-03 are NOT verified here — those are addressed in Plan 02 (untracking and build verification).
</verification>

<success_criteria>
- .gitignore file committed with per-stack organized sections
- No global `*.json` rule present anywhere in the file
- All three build outputs (dist/, .next/, target/) covered by explicit rules
- .claude/ and .planning/ still trackable (re-includes present)
- Local settings (.claude/settings.local.json, .claude/worktrees/) still excluded
- git log shows the refactor commit
</success_criteria>

<output>
After completion, create `.planning/phases/26-repository-hygiene-and-build-artifacts-cleanup/26-01-SUMMARY.md`
</output>
