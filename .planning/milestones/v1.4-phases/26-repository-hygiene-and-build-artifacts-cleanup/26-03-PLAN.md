---
phase: 26-repository-hygiene-and-build-artifacts-cleanup
plan: 03
type: execute
wave: 3
depends_on: [26-02]
files_modified:
  - .gitignore
  - .opencode/skills/ui-ux-pro-max/scripts/__pycache__/core.cpython-314.pyc
  - .opencode/skills/ui-ux-pro-max/scripts/__pycache__/design_system.cpython-314.pyc
  - .opencode/skills/ui-ux-pro-max/scripts/__pycache__/search.cpython-314.pyc
  - src/backend/package-lock.json
  - src/frontend/package-lock.json
autonomous: true
gap_closure: true
requirements: [HYG-01, HYG-02]
must_haves:
  truths:
    - "No .pyc files are tracked in the repository"
    - "package-lock.json files in subdirectories are not tracked"
    - ".gitignore contains explicit rules for Python artifacts and recursive lock files"
  artifacts:
    - path: ".gitignore"
      provides: "Exclusion rules for all build-time artifacts"
  key_links:
    - from: ".gitignore"
      to: "git index"
      via: "git rm --cached"
      pattern: "git ls-files.*grep"
---

<objective>
Close hygiene gaps identified in 26-VERIFICATION.md by untracking residual Python build artifacts and ensuring lock files in subdirectories are consistently ignored. Reinforce .gitignore with recursive patterns.

Purpose: Maintain repository cleanliness and prevent accidental bloat from build-time generated files.
Output: Clean git index and robust .gitignore rules.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-repository-hygiene-and-build-artifacts-cleanup/26-VERIFICATION.md
@.planning/phases/26-repository-hygiene-and-build-artifacts-cleanup/26-02-SUMMARY.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .gitignore for Python and Recursive Locks</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore
    - .planning/phases/26-repository-hygiene-and-build-artifacts-cleanup/26-VERIFICATION.md
  </read_first>
  <action>
    Update .gitignore to fix the reported gaps:
    1. Replace 'package-lock.json' (line 17) with '**/package-lock.json' to ensure it matches recursively across all directories.
    2. Add a new 'Python' section under 'GENERAL' (before 'IDE configurations' or after 'OS files') with the following rules:
    
    ```gitignore
    # Python artifacts
    **/__pycache__/
    *.py[cod]
    *$py.class
    ```
  </action>
  <acceptance_criteria>
    - .gitignore contains '**/package-lock.json'
    - .gitignore contains '**/__pycache__/'
    - .gitignore contains '*.py[cod]'
    - grep "package-lock.json" .gitignore | grep "\*\*/" exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -E "\*\*/package-lock.json|\*\*/__pycache__/|\*\.py\[cod\]" .gitignore</automated>
  </verify>
  <done>.gitignore updated to support recursive lock file exclusion and Python build artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Untrack Residual Python Artifacts and Lock Files</name>
  <files>.gitignore, .opencode/skills/ui-ux-pro-max/scripts/__pycache__/*.pyc, src/backend/package-lock.json, src/frontend/package-lock.json</files>
  <read_first>
    - .gitignore (ensure Task 1 changes are present)
  </read_first>
  <action>
    Use git command line to remove tracked artifacts from the index without deleting local files.
    
    Commands to run:
    1. git rm --cached .opencode/skills/ui-ux-pro-max/scripts/__pycache__/core.cpython-314.pyc
    2. git rm --cached .opencode/skills/ui-ux-pro-max/scripts/__pycache__/design_system.cpython-314.pyc
    3. git rm --cached .opencode/skills/ui-ux-pro-max/scripts/__pycache__/search.cpython-314.pyc
    4. git rm --cached src/backend/package-lock.json
    5. git rm --cached src/frontend/package-lock.json
    
    Verify that git status shows these files as 'deleted' (from index) and that they still exist locally.
  </action>
  <acceptance_criteria>
    - git ls-files | grep "\.pyc$" returns empty
    - git ls-files | grep "src/backend/package-lock.json" returns empty
    - git ls-files | grep "src/frontend/package-lock.json" returns empty
    - git status shows 5 deletions in the staged changes area
  </acceptance_criteria>
  <verify>
    <automated>git ls-files | grep -E "\.pyc$|package-lock\.json" | grep -v "^\.gitignore$" || echo "Success: No tracked artifacts found"</automated>
  </verify>
  <done>Offending build artifacts untracked from git index while preserved locally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo index | Files entering the repository history from local environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03-01 | Information Disclosure | .gitignore | mitigate | Ensure sensitive environment files (.env) are explicitly ignored if present. |
| T-26-03-02 | Tampering | git index | mitigate | Use git rm --cached to remove unintended files without affecting developer local environments. |
</threat_model>

<verification>
After completion, verify that running 'git status' on a clean workspace shows no new untracked files from the directories where we just untracked files, proving that .gitignore is working.
</verification>

<success_criteria>
1. All .pyc files identified in gaps are untracked.
2. src/backend/package-lock.json and src/frontend/package-lock.json are untracked.
3. .gitignore contains recursive rules to prevent re-tracking.
4. Local files remain on disk.
</success_criteria>

<output>
After completion, create .planning/phases/26-repository-hygiene-and-build-artifacts-cleanup/26-03-SUMMARY.md
</output>
